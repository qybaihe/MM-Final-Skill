# 病根台账（活文件：每个病根一行，未闭环的不许删）

> 规则：病根 = 「换个地方还会再犯」的原因，不是某次事故本身。每条必须有 ①证据（可回查）②归属（链路 / 提示词 / 契约 / 环境）③修法 ④**检测**（回归用例或机械判据——没有检测的病根等于没记住）⑤状态。
> 事故编号（E/L/S）见 `对抗性重构方案-v2.md` 第 1 节；工单号见 `施工日志.md`。

## 一、收敛类（生成→评审→修订 循环为什么不收敛）

| # | 病根 | 证据 | 归属 | 修法 | 检测 | 状态 |
|---|---|---|---|---|---|---|
| R1 | 意见没有身份，每轮从零审，同一毛病换措辞成新问题 | 评委模拟 轮1/轮4「卡住」同一件事；四轮同一意见（M4-7）；修订单跨轮文本相似度≈0 | 链路 | 台账（`回路.台账`）：编号、状态、跨轮存活；评审引用编号 | `回路单测` ★显式对应用例；上线后指标：重开次数 | S3/S4/S5 已接线（干跑通过）；真实腿待验 |
| R2 | 修订动作粒度错：整章重写把好的一起推倒 | 章评 6.50→6.40，低分 4→5 章（运行日志 12:46/13:13） | 链路 + 提示词 | 定向修改协议 + 变化守卫（>40% 回退） | `回路单测` ★整章推倒用例；上线后：回退次数 | S4/S5/G5 已接线（守卫 45%/70%）；真实腿待验 |
| R3 | 绝对分当收敛信号，噪声 ±0.5 之下「平台期 0.15」无意义 | 审稿 6.15→7.15→6.05→6.30；同轮 A=6.9/B=5.2 | 链路（主）+ 提示词 | 退出判据改「台账阻塞级清零」；相对判断先于打分；锚点 6/8/9 | `回路单测` ★收敛看阻塞级别；上线后：同稿两次打分分差 ≤0.5 | S3/S4/S5 已接线；锚点已进角色文件 |
| R4 | 退步照单全收 | 章评变差；美化 85→88→126 页；G5 返工 126→27 页 | 链路 | 最优保留：快照 + 相对判断「更差」即回退 | `回路单测` ★更差→回退；C3 张力回放 16:47/16:57 必须被标 | S3 逐图 / S4 / S5 已接线；美化页数守卫接线中 |
| R5 | 修不掉的没有出口：轮数用完即丢弃，再被门逼出最便宜解 | 「剩余问题记入运行日志」→ G5 返工腿自造 `硬伤_轮5.json`（L0） | 链路（所有权） | 熔断：两次未消解 → 升格或搁置留痕；门只读驱动持有的台账视图 | `回路单测` ★熔断；G5 夹具：伪造硬伤轮 → 门不认 | S5 熔断 + G5 视图已接线；门检单测 G5伪造 通过 |
| R6 | 提示词住在两处（角色文件 + 驱动内联）必然漂移 | 驱动给硬伤猎手的内联 schema 抄丢 `目标/修改指令` → 修订单 32 条全「文」、指令「清除该硬伤：逻辑幻觉」 | 提示词管理 / 契约 | 驱动只引用角色 schema 不重述（M4-6 已做一处）；核对器禁止驱动字符串里出现 `[{`/`{"` 形式的 schema | 契约核对新增判据：驱动内联 schema = 0 处 | 硬伤 schema 漂移已修（驱动只引用）；核对器判据待做 |
| R7 | 修订单被静默截断 | 审稿员 `[:6]`、审计 `[:8]`、文腿 `[:24]`、美化 `[:5000]` 字符 → 每轮丢弃十几条 | 链路 | 台账不截断，分批派完；美化按严重度取前 N 而不是按字符截 | 上线后：台账「未派出」计数必须为 0 | S5 已不截断；美化按严重度取前 15 接线中 |
| R8 | 摘要在 S5 之前定稿，回炉后数字漂移无人重写 | 评委模拟轮4：问题一在摘要里没有结论句 | 链路（顺序） | S5 收敛后重生成摘要 + 评委模拟 30 秒复述作验收 | 摘要四要素判据（每问有数值+单位） | 摘要定稿节点接线中 |
| R9 | 评审者没有锚点、修改者只有禁令 | 图评师/章评师各 13 行只有维度；撰稿师 8 条全禁令，「写得别扭」 | 提示词 | 锚点（6/8/9 长什么样）；正向示范（`优秀论文标准.md` §5 已开头） | 同稿两次打分分差；术语密度/页数余量联动 | 部分完成（M4-7），待系统化 |

## 二、判据与契约类

| # | 病根 | 证据 | 归属 | 修法 | 检测 | 状态 |
|---|---|---|---|---|---|---|
| R10 | 门只量过程不量产出 → 「不发布」成最优策略 | 三问无答案 G2 全绿（M4-4.11） | 链路（激励） | 答案门 + 必须作答 | `答案门单测` 7/7；生产已验证一次放行 | 已闭环 |
| R11 | 每条判据都有最便宜逃生路径，只封了一条 | L0：自造空轮、删引用、塞附录、自行销号 | 链路 | 逃生路径封堵表（方案 B2） | 每条封堵一个作弊夹具 | 待做 |
| R12 | 门读的文件谁都能写 | L0；仲裁台账双写；红队报告驱动也能写 | 链路（所有权） | 可写范围 + 驱动戳 + 末轮由驱动指定（方案 B1） | 夹具：写 硬伤_轮99 → 丢弃记越权 | 待做 |
| R13 | 驱动与门检口径打架（字段级已治，值域/行为级未治） | 已解释 vs 已消解；红队缺失不阻塞 vs 一票否决 | 契约 | 契约核对扩到值域（方案 B6） | 单测：门检比较未声明值 → 报错 | 待做 |
| R14 | 合规要求互相打架 | `.json` 禁用词 vs 附录必须含代码；术语密度 vs 页数 vs 骨架（正文 20/20） | 链路（跨门） | 判据上线前跑真稿分布；C3 张力监测 | 张力事件计数 | 待做 |
| R15 | 裁决类节点无判据锚点，同输入结论相反 | 4 次仲裁全是 0 处超容差；两次运行仲裁相反 | 提示词 + 链路 | 不齐分三类；仲裁/裁决加显式判据清单（方案 B5） | 回放两次真实仲裁输入结论一致 | 待做 |

## 三、环境与自身类（摘要，详见方案 v2）

| # | 病根 | 归属 | 修法 | 检测 | 状态 |
|---|---|---|---|---|---|
| R16 | 维护环境不给的不变量（容器持久） | 环境/链路 | 腿即事务（A1） | 故障注入换实例无半态 | **已闭环（M5-0 迁移本地）**：本地文件系统免费给出不变量，450 行韧性代码随容器删除 |
| R17 | 环境常数无实测出处（560s 默认、6MB 兜底 155 分钟、T_播种=300） | 自身 | 常数账本（A2）；回传上限 6→20MB | AST 0 处默认超时、0 个无出处常数 | **已闭环（M5-0）**：超时分级随沙箱退役，本地只剩 T_轻 一档；h.exec 全部显式 timeout_s |
| R18 | 隐式依赖（词表、字体、模板）静默失效 | 自身 | 环境依赖清单 + 模板自审（A3） | 删词表 → 判据报缺失而非空过 | 部分闭环：venv+PATH 依赖显式化、S0 环境要件验齐；模板自审待做 |
| R19 | 韧性只在健康态验证 | 自身 | 故障注入成强制门禁 | `本地故障注入.py` 11/11（M5-0 本地化） | 已闭环 |
| R20 | 单次观测当判据 | 自身 | 三态探活、连续确认 | `探活三态单测` 12/12 | **已闭环（M5-0）**：探活机制删除，判死改 kill -0 铁证 + ps 僵尸位双保险 |

## 四、表达类（2026-09-08 登记，来源 `表达优化方案-v1.md` 第 2 节；证据均为骨架版终稿实测）

| # | 病根 | 证据 | 归属 | 修法 | 检测 | 状态 |
|---|---|---|---|---|---|---|
| R21 | 表达没有正向目标：无范文、评审无实物锚点、撰稿只有禁令与阈值 | R9 部分完成；本机 0 篇范文；负向检查全满分而审稿总分 5.9–6.7 | 资产 + 提示词 | 方案 A1 范文卡片库 / A2 锚点集 / A3 分布基线 | 同稿两次表达判定一致率；阈值全部有分布出处 | 已闭环（20260908 M5-1）：用户许可后下载 GitHub zhanwen/MathModel 10 篇研究生数模一等奖稿到 `运行时/范文/`（PDF 不入库，`文本/*.txt` 入库）；`验证/范文分布.py` 改散文过滤口径（首版把目录当正文，数字密度 300+）后写回 `表达阈值.json`：数字密度 正文 41.9/摘要 29.0，对冲密度 1.0（真稿 ≈0，留置信声明余量），句均 55.1/69.2，摘要数字 31、对冲 1，段内数字 硬线 10/软线 6（先验 4/2 会把真稿本身也拦下）；`表达锚点.md` 首表与 6/8/9 档、`范文卡片库.md` 九卡全部改指真稿篇号与小节。残留：样本是研究生赛稿（40–104 页），本科国赛（高教社杯）稿到位后按维护指南替换 |
| R22 | 撰稿输入全是数字事实清单，没有故事层 | 脊柱/结果解读/实验记录三源皆为带数字的事实条目，无一份白话讲"这题在干什么" | 链路 | B1 叙事底稿节点 | 叙事底稿零公式零缩写（机械检） | 已闭环（20260908 M5-1）：驱动新节点 S4:叙事底稿 + G叙事（`门检.py 叙事`：五段标记/≥150 汉字/零公式命令文件名缩写流程词内部术语），撰稿与脊柱腿以底稿为第一输入；门检单测 叙事正/反 |
| R23 | 过程感素材源被流程噪声污染 | 实验记录 327 条中 318 条（97%）含流程词；撰稿师军规第 6 条强制引用 | 契约 | B2 记录分流 科学尝试/流程事件 | 进论文的记录流程词占比 = 0 | 已闭环（20260908 M5-1）：实验记录 schema 加 `类别`=科学尝试|流程事件（建模师/解读师围栏，契约表登记双生产方），G2 判 本问≥1 科学尝试/未分类=0/科学尝试不含流程词，审计第 15 节全局账；撰稿师只许引科学尝试 |
| R24 | 溯源规则与可读性打架（逐数 `% src` + "数字必须进论证句"） | 71 数字/千汉字，第 4 章 79；第 4 章 69 条 src | 提示词 + 契约 | C1 段级溯源 + 每段 ≤2 数字预算 | 数字密度落入范文分布；溯源核验通过数不降 | 已闭环（20260908 M5-1）：`% src` 改段级（审计第 7 节段级溯源，键路径递归回退），段内数字硬线 4（G4 阻塞）/软线 2（报告标密集段），段首数字 0；量尺 `表达画像.py`（上标/幂/命令名不计）；审计单测覆盖 |
| R25 | 正确性体制外溢成对冲通胀 | "降低置信表述"条款无上限；摘要 26 对冲词/千字；"置信"进句首 Top15 | 链路（结果声明无置信字段）+ 提示词 | B4 置信预算：结果声明唯一 `置信` 字段，全文一处声明其余正常语气 | 对冲密度门（摘要 ≤8、正文 ≤6/千字，待 A3 校准） | 已闭环（20260908 M5-1）：结果声明加 `置信{等级,理由}` 字段（唯一母本），撰稿师表达四律第 2 条「每问一处」，统稿师收口，图题零对冲（审计第 14 节 + G4），对冲密度门（摘要 8/首读 6/正文 6 先验，待 A3 校准） |
| R26 | 命名激励造词 | 规划师"定制改造必须命名"→ 标题含 3 个自造词，正文 DOP--VP/CM--DN--EG/证据门 | 提示词 | B3 命名政策（描述性中文短语 ≤12 字）+ 缩写白名单 | 审计"自造缩写" = 0；标题无自造词 | 已闭环（20260908 M5-1）：规划师命名政策（≤12 字中文短语、白名单外不缩写、不用内部术语）；审计第 13 节自造缩写 + 标题违规（含内部术语）→ G4 阻塞；`缩写白名单.txt` 155 项 |
| R27 | 反同质化过矫：问题重述被删、禁"对于问题N"后无替代范式 | 1.1 无"给定/要求"两要素；摘要首句无白话对象 | 提示词 | C5 恢复问题重述/总体分析（反句式不反内容）+ C4 摘要首句规则 | 1.1 含给定/要求两要素；摘要首句含研究对象白话 | 已闭环（20260908 M5-1）：审计第 16 节问题重述五要素（有章/给定/要求/总体分析/思路图）→ G4；规划师图表规划必含全文思路图；摘要腿首句白话规则 + 画像门 + 读者复述 |
| R28 | 分章并行无统稿，全程无一条腿通读全文 | 第 3 章 s / 第 4 章 u 符号漂移（章评 R2 点名）；S5 回炉定向、S6 只改版式 | 链路 | C3 统稿腿（单腿全文、只改句不改数） | 数字多重集合/引用集合/公式数前后相等，否则回退 | 已闭环（20260908 M5-1）：新角色 `统稿师.md` + 驱动节点 S4:统稿（章评后、摘要前），`运行时/统稿守卫.py` 机械比对数字多重集合/label-ref-cite/公式数/插图数/标题/src，动了整份回退（单测 11/11），再叠 45% 变化守卫 |
| R29 | 表达评审无稳定判据、且全员开卷审（评委闭卷） | 章评 R2 0 条可读性意见；绝对分噪声 ±0.5（R3） | 链路 + 提示词 | D1 配对比较 + D2 闭卷读者腿 | 同稿两次一致率 ≥80%；读者腿"读不懂"数逐轮下降 | 已闭环（20260908 M5-1）：章评师/审稿员语言维改 范文对照（更像|一样|更远 + 理由）；新角色 `读者.md` 闭卷腿进章评循环（`读者R{轮}_{k}`，卡住/自造词进台账 来源=读者）与摘要复述；读者腿真跑（20260908 冒烟）：14 章读者分 5–7、卡住 36 条逐条指原句、摘要复述三问可复述/置信声明 6 次——产物可用；一致率指标待对照跑后统计 |
| R30 | 模型换代未重标（gpt-5.6-sol → gpt-6-astra） | 提示词/锚点/阈值全对旧模型校准 | 环境 | D3 骨架版稿子三次评审重标 | 三次打分方差入档；阈值重定 | 已闭环（20260908 M5-1）：重标完成——章评 6.0/6.5/6.0 均 6.17（offset −0.13），审稿 A 总分 6.5（印象 6.0） / B 总分 6.2（印象 6） / C 总分 6.1（印象 6） 均 6.27；建议 章评阈值 6.9 / 审稿达标 8.5（现行 7.0/8.6），只给建议、改 配置 由维护者定；范文校准后锚点已换真稿，下轮对照跑重新读数 |
| R31 | 角色腿超时硬编码 1200s、不随任务重量变化；且超时只杀得到 node 包装进程，原生 codex 子进程成孤儿继续跑 | 20260908 重标真跑：全文审稿腿在新锚点下 >20 分钟，`重标审稿A.done` 17:14 被写成 `AUTO(rc=142)`（perl alarm 杀了 `codex.js` 包装），但原生 `codex-darwin-arm64` 子进程 ppid=1 继续跑：C 在判死 8 分钟后（17:22）自己写出 done 与 25KB 审稿意见，A 孤儿跑了 30 分钟以上仍在写日志——判死后重派会与孤儿双写同一批文件 | 链路（`运行时/codex公共.sh` + `role.sh` + `本地蜂巢.leg` + `蜂群驾驶.wave`） | ①腿上限按波次下发：`role.sh` 读 `LEG_TIMEOUT`（默认 1200）；`本地蜂巢.leg(leg_timeout=)` 经环境变量交给 `_spawn(env=)`；`wave()` 下发 `max(600, timeout−60)`，重活波次放宽（章评+读者/审稿场/统稿 2700，脊柱/撰稿/章修订/G4 返工 1800，摘评/摘要定稿 1200）；冒烟与重标脚本 leg_timeout=2700。②`with_timeout` 改为 fork+setsid 后 exec，到点对整个子进程组 TERM→10s→KILL（退出码仍 142）；收到外部 TERM/INT/HUP 也转发给子组，驱动 `kill -- -pgid` 整组回收不失效 | wave 失败分支识别 `rc=142` 记「超过腿上限」；包装单测：`with_timeout 3 bash -c "sleep 300 & sleep 300 & wait"` 后 `pgrep sleep` 为空且 rc=142、外部 TERM 后无残留；每次真跑后 `grep -l "AUTO(rc=142)" 日志/*.done` 应为 0，且 `ps -eo ppid,command | grep codex-darwin | awk '$1==1'` 应为空（无孤儿） | 已闭环（20260908 M5-1）：改动 py_compile/bash -n 过，包装三项单测过，干跑全链 31 节点、韧性 A+B PASS；真腿验证：冒烟 3 腿 + 补跑 1 腿在新包装下 rc=142 计数 0、孤儿 codex 0、腿上限 2700 确认进了子进程环境 |
| R32 | 并发腿数不看上游配额，6 腿并发即触发 429 限流且被当普通失败 | 20260908 重标真跑：`日志/重标审稿B.log` 末行 `ERROR: exceeded retry limit, last status: 429 Too Many Requests`，done 为 rc=1，与超时/崩溃混在一起无法区分 | 链路（`蜂群驾驶.wave`）+ 环境（codex 端点配额） | 第一步先让限流在日志里可见：wave 失败分支 grep 腿日志 `429 Too Many Requests` 记「命中上游限流」并重试一次；重标补跑改 ≤3 并发。并发上限自适应（按 429 计数动态收缩波次宽度）待真跑数据后再定 | 每次真跑后 `grep -l "429 Too Many Requests" 日志/*.log` 计数入施工日志；连续两次真跑为 0 才算闭环 | 部分闭环（20260908 M5-1）：识别与日志已落地；并发自适应未做，等对照跑数据；本晚 ≤3 并发 4 条真腿 429 计数 0 |
| R33 | 建模改数后红队不重跑：仲裁返工 / G2 返工 / 升格裁决改的都是建模侧文件，`交接/红队_问题N.json` 永远是改前那份，解读师与门检据旧报告判决 | 20260909 对照跑问1：红队缺冻结合成输入 8 项全 null→仲裁 8 项判建模→仲裁返工导出冻结包→`解读_问题1_仲裁后.done` 仍据旧报告判"0/8 可比"FAIL→G2 返工只能改建模侧，红队报告不可能翻新（归档 `真题测试/蜂巢镜像/_归档_问1首跑_1701/`） | 链路 | 驱动 `红队复算(编号, 标, 深度)`：仲裁返工→执行后派**复核轮**（深度 1：可再仲裁/再返工，不递归），解读_仲裁后只在复核后派一次；G2 `_返工`/`_升格` 后同样复核；红队/仲裁返工/建模任务文本与 `角色/红队.md`、`角色/建模师.md` 加冻结输入包口径（`数据/问题N_冻结合成输入/`+`输入清单.json`） | 干跑 `验证/驱动干跑.py 红队不齐`：先后序 红队→跑→报告→仲裁→仲裁返工→执行_仲裁后→红队_复核→跑→报告_复核→解读_仲裁后；解读恰 1 次、无 复核_复核、复核任务文本含冻结包说明 | **已闭环（20260909 M5-2）**；补①（二跑 18:05 实测）：复核轮拿新脚本撞旧声明——`结果声明` 由解读师产出、返工重跑后不会自动刷新，6 处"不齐"全是假分歧；修法加 `声明刷新` 腿（解读师按最新 结果/ 逐键刷新声明，不裁定）插在 执行 与 红队复核 之间（仲裁返工路径与 G2 返工路径）；另：复核轮红队腿两次 `AUTO(rc=0) 角色未写done标记`（任务文本"重新独立实现并比对"让它以为比对前不算完成），任务文本改为"只写不跑、写完必须写 done"；干跑期望序同步更新 |
| R34 | 升格链路顺序反了：三条变体脚本在裁决腿**之后**才跑，裁决看到的是空结果目录；且升格后不派解读腿，门检只认最新 `解读_问题N*.done`（仍是返工那轮的 FAIL）——升格在数学上不可能通过 | 骨架版 `真题测试/蜂巢镜像.old/日志/升格裁决_G2问2.done`："升格1、升格3结果目录为空，升格2结果目录不存在"；问3 "四条当前脚本均无版本匹配的运行产物"；`驾驶.out` 三问升格后全部 "解读未 PASS：FAIL → 降级放行" | 链路 | `升格蜂群(..., 变体后=)` 在裁决腿之前跑完三条变体脚本；裁决后派红队复核 + `解读_问题N_升格后`（首行 PASS/FAIL） | 干跑 `门升格`：断言 跑升格_1..3 < 升格裁决 < 红队_升格后 < 解读_升格后，且两次 G2 返工各自有红队复核在解读之前 | **已闭环（20260909 M5-2）** |
| R35 | 头条数字选了方法自检量：解读师把"本方法在合成数据上的误差/留出误差/失配误差"写进 `核心指标`，红队用另一套方法算出的误差天然不是同一个量，逐键比对必然"不齐"→ 每问白跑一轮仲裁+返工+复核 | 20260909 三跑问1 复核轮：实测 d·q 双方 19.5376 vs 19.5366（0.0%），其余 6 处"不齐"全是自检误差（建模 0.0008% vs 红队 9.66%）；二跑仲裁亦有 3 项落在合成误差上 | 契约（结果声明口径） | `角色/解读师.md`：`核心指标` 只放独立实现能复现的量（对给定数据的估计值/诊断量/区间），方法自检量放新键 `自检指标`（红队不比对、只核自洽）；`角色/红队.md` 判据只对 核心指标；驱动解读任务内联 schema 同步。角色文件已热同步进运行中的镜像（腿每次派发都重读角色文件） | 复核轮红队分歧数：三跑 6 → 之后各问应以 口径/估计量 分歧为主；契约核对 通过（`自检指标` 仅列为门检不读的声明键） | 已落地（20260909 18:45，提示词/契约级；链路级 R33 已先闭环） |
| R36 | 变化守卫的尺子量错了对象：按**行**比对，而 tex 一段一行、`% src` 注释行占一半——4 段各改一句就是 100% 正文行变化，定向修订被机械判成「重写」整份回退；台账再把这些条记成「修过仍未消解」催腿换改法。附带：修订腿名额按台账字典序取前 6 章，最低分章轮不到 | 20260910 对照跑 S4 章评轮1：6 章修订 5 章回退（61/68/78/58/47%），回执列出的改动行全是点名行；`1.1.问题重述.tex` 18 行=正文 4+注释 9+空 4+标题 1；章评 3 分的 4.1.2.模型建立 未进修订名单 | 链路 | `回路.修订守卫`：正文句子为单位、`%` 行不计、评审引号点名句不计、再配字符丢失比，两尺皆超线才回退；驱动回退前留底新稿 `审稿/回退稿/`；选章按章评分升序、名额 `章修订每轮章数`；`撰稿师.md` 协议措辞对齐 | `回路单测` ★四段各改一句+重排注释（行尺误判/新尺放行）、整章推倒、删半章、统稿式小改 4 用例；上线后：日志 `变化守卫[…]` 两尺明细 + `审稿/回退稿/` 留底可复核每次回退是否真重写 | 已落地（20260910 02:12，S4:章评循环 重做中，待真实腿验证） |
| R36-补② | 点名粒度只到句子：评审说「从A到B压缩为一段」「X起头的两段移出」「整段前移」时，范围/段内其他句都算未点名改动；按序比对又把搬家的句子当丢失 | 20260910 03:02 新尺首跑：8 章 6 过 2 退；留底稿比对——2.数据审查 88%（评审要求压缩 16→6 句，腿照做）、4.1.1 50%（评审要求整段前移）| 链路 | `回路.点名掩码` 三级指法（句/范围/段）+ `_存活` 不看位置；台账再派提示分流（回退项「只动点名句」，未消解项「换改法」） | `回路单测` 从A到B压缩、起头两段移出、搬段不算丢失、再派提示分流 4 用例；上线后：`审稿/回退稿/` 每次回退可复核 | 已生效（20260910 03:44 于 S4:统稿 节点边界续跑；真实腿验证待 S5 回炉） |
| R37 | 审计器把附录代码块当正文、把存在的 Markdown/脚本来源当缺失：禁用词/有效数字/段首重复扫的是去注释后的原始 tex（lstlisting 内容照数），溯源核验只会 `json.load`，`% src:…建模笔记.md:节名` 一律「缺失或非JSON」存疑；「附录不检」只按文件名含「附录」判，8.1–8.3 源码章漏网 | 20260910 04:43 对照跑 G4 第 1 次 FAIL：禁用词 2（代码里 'FAIL'/'不发布'）、过精 8（代码浮点常量）、段首重复 'def re'×10 等 12 项、存疑 20 条全是 .md 来源；返工腿被派去改代码清单 | 链路 | `审计.py`：`_遮代码` 按行遮蔽代码环境；`_附录集`（主控 `\appendix` 之后的 `\input`）+ `_是附录` 统一口径并接入段首重复；非 JSON 来源按文本核对（节名/函数名命中，数字池取全文数字） | 影子根审计前后对比（禁用词 2→0、过精 8→0、段首 13→0、存疑 20→0）；上线后：G4 明细里不应再出现 'def '/'ROOT =' 开头或 .md「缺失或非JSON」 | 已生效（20260910 04:48 热同步 bin/审计.py；G4 第 2 次检查验证） |
| R38 | G4 返工腿无任何守卫（S4/S5/G5 都有变化守卫），任务文本直给「正文≤20页、禁用词清零」，腿就用最省事的办法达标：删附录源码清单、并章改主控、改版式压页 | 20260910 05:00 对照跑 G4 第 2 次 PASS 的回执：G4-1 删三问完整源码（8.1/8.3 无 def）、G4-6 清空 1.1/1.2 并删主控 `\input`、G4-8 改 geometry/行距；标准第 15 条属取消评奖级红线 | 链路 + 提示词 | 驱动 `结构守卫回退`（主控 `\input` 集、章清空、附录 lstlisting 数、整份改动升格线）接 G4/G5 返工；G4 任务加封死条款；稿件手工修复（拆回三文件、源码全文回填 8.1–8.3） | 干跑全链/门升格；上线后：日志 `结构守卫[…]` 行 + `审稿/回退稿/G4返工/` 留底；审计 `_是附录` 让代码字面量不再触发返工 | 已落地（20260910 05:06；真实腿验证待下次 G4/G5 返工） |
| R39 | 版式模板 `沙箱验证/format-sandbox.cls` 按**家族名**加载字体（TeX Gyre Termes/Heros、DejaVu Sans Mono、FandolSong/FandolKai）：沙箱 Linux 的 fontconfig 能解析，本机 XeTeX 走 CoreText——texmf 里的 Fandol 按名找不到（fontspec 报 cannot be found 后 xetex 异常退出、PDF 残缺），未安装的名字（DejaVu Sans Mono，附录 listings 首次用等宽时才查）让查询在 com.apple.FontRegistryUI 里挂死（0% CPU 永不返回，只靠 h.exec 400 s 超时兜底）。叠加：续跑 `本地就绪()` 把 `论文/format.cls` 当基座资产按内容覆盖，把编修腿修好的文件打回模板 | 04:38:29 编译#编0 FAIL → 编修编0 腿自行把 format.cls 改成文件名加载 → 04:43:53 E=0 P=141；05:06:32 续跑「本地基座同步：1 文件更新」= format.cls 回模板；05:13:12 编译#审1前 FAIL（400 s 超时），论文.pdf 损坏（pdfinfo XRef 错）、论文/页 空、评委模拟1 因无页图被静默跳过（驱动沿用 05:02 旧文件）；05:26 镜像手工重编，`sample` 采样 xetex 卡在 `XeTeXFontMgr_Mac::searchForHostPlatformFonts` 11 分钟；05:34 回炉文_轮1_1 腿第二次自行改 format.cls（两条腿各耗 5–10 分钟重新发现同一病） | 环境 + 链路 | ① 模板全部改**文件名**加载（主/无衬线/等宽=texgyre*、CJK 主/黑/仿宋=Fandol*.otf、kai/song 带 Extension=.otf）＝腿两次收敛的版本，模板与镜像已一致；② 驱动 `本地就绪()`：`论文/` 下资产只播种不覆盖（产品目录不归基座同步管）；③ S5 审前编译失败走 `_编译修复循环(审N前修, 2)` 再渲页，不再带病下发；④ 镜像按模板重编：三遍 E=0 P=138、页图 138 | 日志「编译#…」行 E=0 且 P>0（FAIL/E>0 即复发）；续跑不再出现覆盖 论文/ 的「本地基座同步」；卡死用 `sample <pid> 1` 看是否在 FontRegistryUI；干跑 全链/中断续跑 过 | 已落地（20260910 05:40）；待真腿验证：编译#修1x / 编译#审2前 |
| R40 | S5 回炉分路（先算后图后文）靠意见的 `目标` 字段，但审稿员/评委模拟的 schema 根本没有这个字段，`意见条目` 缺省一律填「文」；审稿员 B 的键名（直接执行/验收依据/文件与位置）也没被识别，指令与定位落空。结果算类、图类意见全派给撰稿师：它自己补跑网格搜索、写新结果 JSON、重绘 7 张图并重编，红队复算、G2 门、图评全被绕过 | 20260910 05:30 S5 轮1 台账 20/20 条 目标=文（含「补查网格/起点加密」「汇总图图例移出…重绘」「图1 按三问重排…大字号」）；审稿员 B 5 条 指令/定位 为空；05:48 回执_回炉文_轮1_1：审-1-03 补做 192/384/480 点网格搜索并写 `求解/问题2/结果/搜索补查.json`，审-1-05/09 「实际重绘 7 幅图并重新编译」，审-1-08 正文改称 13.53 μm 触边候选——全由撰稿师完成、无红队/图评 | 链路 + 提示词 | ① 驱动 `推断目标()`：无 目标 字段按关键词分路（算 > 图 > 文）；`意见条目` 键别名（直接执行/修改建议/文件与位置/验收证据/验收依据）；② 驱动 `收回执`：回执 `改动` 以「需转算/需转图」开头 → 改目标、退回待改、下一轮改派；③ S5 轮1 台账非空也渲染给评审并收裁定（续跑从轮1重进时上轮回执有人裁定），轮1 无快照不回退；④ 提示词：撰稿师「算与图不归你」第 5 条；审稿员 schema 加 `目标` 并统一键名（均已热同步）| 日志「S5 轮N 待改 X 条 → 算a/图b/文c」不再 a=b=0 而台账里有算/图类意见；回执行出现「转路 n」；台账 目标 分布；干跑 全链/中断续跑/门升格 过 | 已落地（20260910 05:55）并实盘验证（06:22 重进后 S5 轮1：待改 25 → 算1/图6/文18，算类正确定位到问题3） |
| R41 | G2 门的实验记录分流只认 `科学尝试|流程事件`，而 S3/S4/S5 的绘图、图评、撰稿、审稿腿也往 `交接/实验记录.json` 写（自报 绘图尝试/图评尝试/撰写尝试/工程修复）。S2 时这些条目还不存在，S5 回炉重开 G2 门就把它们当成「建模师没按 schema 写」→ 假 FAIL 派建模返工 | 20260910 07:45 门[G2:问3] FAIL：『问3 实验记录有 7 条未分类』——7 条全是 问题=3 的 绘图尝试/图评尝试（S3 腿写的）；修门检后同一份记录 未分类=0 | 链路（门检）+ 契约（AGENTS 类别词表） | 门检.py 把 绘图尝试|图评尝试|撰写尝试|工程修复 视同流程事件（热同步 bin/门检.py，即刻生效）；AGENTS.md 固定 类别 词表与 问题 填法 | 门检统计 `实验记录_未分类` 只在类别真为空时 >0；干跑 门升格 过 | 已落地（20260910 07:48，热同步生效；G2:问3 第 2 次检查为实盘验证点） |
| R42 | S5 回炉序列先算→后图→后文，解读腿在文/图腿之前跑，却按「配对评审协议」把「正文/附录/图未同步」裁成未消解 → 一票否决 FAIL → G2 门返工/升格/降级；而同步本来就是本轮后面回炉文/回炉图腿的活 | 07:45 解读_问题3_回炉1_仲裁后.done：『八项仲裁核心指标独立对齐，最大相对差 0.0045%；相对上一版更好』却首行 FAIL，理由含『审-1-23：损耗计算已修正，但正文、附录及图仍未同步』| 链路 + 提示词 | 驱动 `_回炉边界句()`：阶段为 S5 时四处解读任务（回炉后/仲裁后/G2 返工后/升格后）附「回炉序列边界」，正文未同步不算 FAIL、裁定写「待文腿同步」；解读师.md 同款条款（热同步，对本轮 G2 返工后的解读腿即刻生效）；真正的算力缺口仍一票否决 | S5 内 解读_*.done 的 FAIL 理由不再含「正文/图未同步」；G2:问N 在 S5 不因同步问题返工 | 已落地（20260910 07:48）；驱动侧待下次 --resume 生效，提示词侧即刻 |
| R43 | S5 审稿场是最长节点（每轮评审+回炉 1–2.5 h，算路一进 G2 门就是 1.5 h+），却只有节点级断点：任何 --resume 都从轮1 重来，评审/回炉/算路全部重跑；30 h 预算护栏又让「跑不完就应急出版（无摘要定稿/美化/G5/复盘）」成为现实风险 | 06:02 为启用 R40 重进 S5，轮1 评审（30 min）重做；09:00 问3 算路 G2 连败进升格，S5 轮1 已耗 2.7 h，按 30 h 护栏推算 S5a/美化/G5/S6 可能被砍 | 链路 | 驱动：`状态.数据["S5已完成轮"]` 每轮修订后落盘，续跑从下一轮起（无上一轮快照则本轮不回退）；节点完成时清除；MAX_HOURS 30→40（用户明示算力不设限） | 续跑日志出现「S5 续跑：前 N 轮已落盘，从轮N+1 继续」且不重跑该轮评审腿；干跑新场景 `驱动干跑.py S5续跑`（轮1 阻塞级→轮2 首腿 kill→续跑）断言：不重做轮1 腿、轮2 评审腿照派、节点完成后清键 | 已落地（20260910 09:07，干跑 S5续跑/全链/中断续跑/门升格 + 回路单测 32/32 过）；下次 --resume 生效 |
| R44 | 门调度器只按本次调用计返工次数（`for 轮 in range(1, 最多返工+2)`），持久化的 门失败计数/降级放行 只用来打日志：一问在 S2 已烧过 返工×2+3 变体升格 并降级放行后，S5 每轮回炉算又把整套重烧一遍（每问每轮 1.5–2 h），而科学缺口（问2 网格敏感、问3 硅侧公平对照输给朴素基线）三条变体都没填上，重复升格只烧钱不改结论 | 对照跑 S5 轮1：问3 回炉算 → G2 三连败 → 升格蜂群 → 09:49 降级放行，耗 2.8 h；轮1 回炉文回执把 审-1-03（问2）/审-1-06（问3）转路到「算」，轮2 起两问都会再走整套 | 链路 | 驱动 `G2门(编号, 只复检=)`：S5 回炉算调用前查 `状态.数据["降级放行"]` 有无该门记录，有则 返工fn=None/升格fn=None 只复检一次（PASS 则重置失败计数，FAIL 再记降级）；调度器降级日志区分「升格后仍不过」与「只复检」 | 干跑 `门升格` 场景轮1 给阻塞级「算」意见指向问1：断言 S5 段有「只复检」行、`升格蜂群[G2问1]` 全程只派 1 次、回炉算_轮1_问1 照跑 | 已落地（20260910 10:20，干跑 门升格/S5续跑/全链/中断续跑 + 回路单测 32/32 过）；下次 --resume 生效 |
| R45 | 驱动 `收裁定` 合并多通道裁定是「任一未消解即未消解」，而评委模拟腿只看前 8 页页图，裁不了工程/全文项时写「未核实…不能销号」也算一票否决：审稿员 A/B+硬伤猎手三路已消解的条目被压成未消解，随即被熔断搁置或升格重写 | 对照跑 S5 轮2（10:50）：审-1-01 三路已消解仅评委「本通道未核实」→ 未消解 → 熔断搁置；审-1-10/-20 同样 → 熔断升格「允许重写该段」 | 链路 | `回路.是弃权票/合并裁定`：评委模拟* 通道且理由命中「未核实/不能核实/未获实物/本通道/不能证明/不能销号…」的未消解记为弃权，不参与否决；实质理由（如「第6页仍写 T=dq」）仍算否决；只有弃权票的 id 不裁走 待复核未裁；驱动日志加「弃权票 N」 | 回路单测 4 条（三路已消解+评委弃权→已消解；审稿员实质未消解仍否决；纯弃权不裁；评委实质否决算数）；轮2 边界切换时按新规则复核轮2 裁定并纠正被误搁置的条目 | 已落地（20260910 10:55，单测 37/37）；下次 --resume 生效 |
| R46 | S5 回炉算按字面「问题N」判断意见涉及哪个问：定位写成 `4.2.3.求解与结果分析.tex` 的算路意见识别不出问2，被并到别问的腿或干脆不派（`条 = … or 组["算"]` 全塞给第一个问） | 对照跑 S5 轮2：算 3 条 → 「回炉S2：重算问[3]」，问2 的 审-1-03（网格加密 10.80→13.53 μm）没有任何回炉算腿接手 | 链路 | `回路.条目涉及问`：认 问题N / 问N / 问题一二三 / 章号 4.N. 与 8.N.（正文第 N 问章与附录源码章文件名）；驱动受影响问与分腿都用它 | 回路单测 1 条（4.2.3./8.3./问题二/问2 认，4.23 与「误差 4.1%」不误判） | 已落地（20260910 10:55）；下次 --resume 生效 |
| R47 | 变化守卫按句子尺量所有章，附录源码章（8.N.问题N源码.tex）随求解器换版整份替换必然「超线」被回退：正式结果已换成升格3 求解器，附录却永远嵌着旧入口旧代码，审稿每轮都判「附录不能复现正式结果」 | 对照跑 S5 轮2（12:02）：8.3.问题三源码.tex 842→177 句、改动 90% > 70% → 回退，新稿留底 审稿/回退稿/S5轮2/ | 链路 | `回路.是代码章`（文件名含 源码/代码，或 lstinputlisting/inputminted，或半数以上行在 lstlisting/verbatim/minted 内）→ 驱动变化守卫跳过该章（结构守卫仍管空章）；切换时把留底新稿回填、对应条目改回待复核 | 回路单测 1 条；日志出现「代码章不设守卫」 | 已落地（20260910 12:08，单测 38/38，干跑 全链/S5续跑 过）；轮3 起生效 |
| R48 | G5 门检「附录代码量」只数文件名含「附录」的 tex 里的内联 lstlisting，不认 `\lstinputlisting` 外链源码，也不看 8.N.问题N源码.tex：源码一旦以外链刊载（正是排版规范鼓励的做法），门就报「代码过少」 | 对照跑 G5 第1次检查（20260910 15:08）：排版执行1 按美化师意见删掉 8.附录.tex 里重复内联刊载的问一/问二完整源码（页数 113→68）后，门检报「附录 lstlisting 代码过少或缺失（107 字符）」，而 8.1/8.2/8.3 源码章有 8 处 `\lstinputlisting` 共 93113 字符 | 链路（门检） | `运行时/门检.py`：附录范围改为文件名含 附录/源码/补充材料；代码量 = 内联 + `_外链清单`/`_行范围` 解析的外链文件（按 firstline/lastline/linerange 截取）；外链文件不存在单列明细 | 门检单测 G5外链（过）/G5外链缺文件（拦）；G5 门检统计多出「附录外链源码数」 | 已落地（20260910 15:20，门检单测 21/21；停跑后对停机镜像实跑 G5 门检：码量 93113、外链 8，只剩台账阻塞级一条明细；--resume 续跑验证中） |
| R49 | G5 返工只派撰稿师（`_G5返工`：撰稿师改文 + 硬伤猎手复核），台账里 目标=图 的阻塞级意见（图内数字/标注与正式结果成套错版）在 G5 永远消不了——撰稿师只能写「需转图」退回待改，G5 必然连败 3 次降级放行，稿子带着错图出版 | 对照跑 G5 第1次检查（20260910 15:12，R48 修后重检）：8 条阻塞级未消解里 4 条 目标=图（审-1-22 图5/图14 缩排后难读、审-1-24、审-2-03 问3 成套错版、审-2-06 图13 仍标 2.58 μm 而正式结果 2.165/1.732 μm）、1 条 目标=算（审-2-04）；S5 四轮到上限退出时这些图条留在台账，G5 只派了撰稿师 G5返工1 | 链路（驱动 G5 返工编排） | `蜂群驾驶._G5返工`：返工前先取 目标=图 的阻塞级 → 绘图师腿 `G5返工图{n}`（定向修改协议改脚本 + 写 `求解/成图回炉G5_{n}.sh`）→ `run_script` 重跑受影响图 → `checkpoint_dir("求解")` → 再派撰稿师改剩下的文/算条 → 硬伤猎手复核一并裁定（与 S5 回炉图同构）；目标=算 仍不在 G5 重算（见 流程与档位 §5 k） | 干跑场景 `驱动干跑.py G5图路`（末轮留一条 目标=图 硬伤 → 断言事件序 G5返工图1→成图回炉G5_1→G5返工1→硬伤G5返工1，第 1 次 FAIL、第 2 次 PASS、不降级）；真跑看日志 `先启 ['G5返工图1']` 与 `脚本 求解/成图回炉G5_1.sh … rc=0` | 已落地（20260910 15:30，验证.sh 全过：py_compile 39、单测 5 组、干跑 7 场景）；15:40 切换生效；15:42 G5 第1次检查 FAIL 后首次走真图路：绘图师 `G5返工图1` 回执受理 3（审-1-22/1-24/2-06），`成图回炉G5_1.sh` 首跑 rc=1 → 修脚本腿 → 第2次 rc=0（15:52），6 张图重绘并带 PNG SHA256/取数核验（图 11–13 改读正式 JSON 20260910_131501）；随后撰稿师 `G5返工1` + 猎手复核 |
| R50 | G5 对无路可派的「算」条空转：G5 没有算路，`_G5返工` 仍把 目标=算 的阻塞级派给撰稿师，撰稿师只能回「需转算」退回待改（转路还把尝试次数 −1，永远熔不断），每次返工白烧 撰稿师 + 猎手复核 ≈ 0.5–1 h，三次检查后照样降级 | 对照跑 G5（20260910 15:40 第2次检查）：4 条文类已消解后剩 审-2-04（算：v3 硅第二折未收敛）与 审-2-06（图），撰稿师 G5返工1 回执里对 审-2-04 只能「需转算」；R49 切换后重检仍是同一条算条会再吃两轮返工 | 链路（驱动 G5 返工编排） | `蜂群驾驶._G5返工`：图路之后，剩余阻塞级里 目标=算 的条目直接记 `搁置` + 搁置理由「G5 无算路，交复盘/人工」（历史记 G5搁置）；无文/其他可改项时不派撰稿师，无图条时连猎手复核也不派，直接回门检；门照判据走（搁置的阻塞级仍让 G5 降级放行并高亮，出版前必须人工看） | 干跑场景 `驱动干跑.py G5算条`（末轮留 图+算 各一条：断言图路照走、无 G5返工N 撰稿师腿、日志有「无路可派 → 搁置」、G5 降级放行后 S6 走完）；真跑看 `G5 返工N：… 无路可派 → 搁置` | 已落地（20260910 15:50，验证.sh 8 场景全过）；布防在续跑段的「门[G5] 第2次检查 FAIL」边界切换；真跑证据：16:25:28 `G5 返工1：1 条 目标=算 的阻塞级无路可派 → 搁置交复盘/人工：['审-2-04']；无其他可改项，不派撰稿师`（切换后新驱动首轮返工，直接进猎手复核） |
| R51 | G5 返工（图路/文路）改完不编译就派硬伤猎手复核，猎手按上一次门检编译的旧 PDF 判 | 对照跑 16:10：`成图回炉G5_1` 15:52 已把图 13 重绘成 全量 2.17 μm（PNG 核验），猎手 16:10 仍判「实际正式PDF物理第18页图13仍标 2.58」→ 审-2-06 硬伤熔断搁置；`_G5返工` 里绘图/撰稿波次之后直接是猎手波次，无 compile_paper | 流水线/蜂群驾驶.py `_G5返工` | 猎手复核前 `_编译修复循环("G5返工N复核前", 1)`，顺带量页数给 R52 | 驱动日志 `编译#G5返工N复核前`；干跑 `驱动干跑.py G5页数` 断言该行存在 | 已修·真跑验证（17:01:39 `编译#G5返工1复核前0: E=0 O=0 P=68`、17:32:52 `编译#G5返工2复核前0: P=68`，随后才派猎手） |
| R52 | 美化后无人再量页数：G5 返工撰稿师按审稿员「补入现有求解入口的引用」把 求解_问题3.py（2566 行）+ 复算.py（757 行）整份 `\lstinputlisting` 进 8.3 源码章；源码章因 R47 豁免变化守卫，G5 门检只查正文 ≤ 20 页与附录代码「不减」，全稿 68→112 页无人拦 | 对照跑：15:12 编译#终编 P=68 → 15:26 撰稿师改 8.3（`日志/G5返工1_修订前/8.3` 无 lst:q3-current，现稿 2 处）→ 15:40 P=112；PDF 问题三源码占 45–104 页 | 流水线/蜂群驾驶.py `_S5美化`/`_G5返工`/`页数守卫_G5` | ① 美化各次编译记 `状态.美化后页数`；② G5 返工复核前编译后 P > 基线+max(2,10%) → 先回退本轮改过的代码章并重编译，仍超再回退其余改动；回退文件上的回执作废（待复核→未消解）、新稿留底 `审稿/回退稿/G5返工N/`；③ 撰稿师任务文本加页数纪律（只许 路径+SHA256+linerange 片段引用源码） | 驱动日志 `!! 页数守卫[G5返工N]`；干跑 `G5页数`（40 页 > 19+2 → 回退代码章、留底、过门）；本炉由切换钩子一次性回退 8.3 到排版执行后版本、记基线 68、并入台账条目 审-6-xx（正确性/文） | 已修·真跑验证（16:37:50 钩子回退 8.3/基线 68/并入 审-7-01；返工 1/2 复核前 P=68 守卫未触发；干跑 G5页数 覆盖回退路径） |
| R53 | S6 终审整改任务把必改清单按 5000 字符截断（`json.dumps(必改)[:5000]`），且回执里「需转图」无人接、无复核即收割 | 复盘官实证：终审 14 条只下发 10 条且第 10 条句中截断（`任务/终审整改.任务.md`、`终审_8/9.json`）；回执 4 项转图、1 项部分完成后直接出版（17:55） | 链路（R7 截断同病复发） | 任务改为只传 `审稿/终审_N.json` 路径与条数，腿逐条读全并写回执 `审稿/回执_终审整改.json`；S6 图路/复核未加（改 S6 结构，用户定） | 干跑 全链 断言「终审整改 任务含 审稿/终审_1.json 与 共 N 条必改」；真跑看 `S6 逐页终审：N 条必改` 与回执条数相等 | 已修·干跑验证（截断部分）；图路/复核 未修 |
| R54 | 输入播种只扫输入目录第一层且只认 pdf/xlsx/csv/docx：附件子目录（2026 A 题 `附件3/result1–4.xlsx` 结果模板）整个被丢，`论文格式规范2026.txt` 也进不了 题目/ | 2026-09-11 准备 A 题：`基建清单()` 对 `国赛2026A/输入` 只列出 A题.pdf + 附件1/2.xlsx，缺 4 个模板与规范 | 链路（播种） | `基建清单()` 改 `rglob`：数据按相对路径播进 `数据/附件3/…`；题目层的 .txt/.md 也播进 题目/ | 播种清单自测（8 项）+ 验证.sh 全链/中断续跑/引擎 干跑；真跑看 S0 日志「题目/… 数据/附件3/…」 | 已修·干跑验证 |
| R55 | codex 腿在 seatbelt 里用 `/bin/bash -lc` 跑命令，macOS 登录 shell 的 path_helper 把 /etc/paths 的 /opt/homebrew/bin 排到驱动放在 PATH 首位的 venv 之前，腿内 `python3` 落到 homebrew 裸 3.14（无 numpy/scipy/pandas/matplotlib/openpyxl）；AGENTS.md 第 12 行承诺「已装齐」与腿实测矛盾，规划师据实测把全炉锁成纯标准库路线（自实现线性代数、zipfile 手写 xlsx、matplotlib 示意图列为「待环境维护」），下游建模/绘图全部跟着降级 | 2026-09-11 A 题：`蜂巢镜像/日志/体检.log:1012` `ModuleNotFoundError: No module named 'openpyxl'`（读题官同）；`日志/规划.log:2677-2681` numpy/scipy/pandas/matplotlib/openpyxl 五个「不可发现」；`交接/计划.json` 交付与执行.环境 段与 数据档案 风险 D07；实验记录 4 条流程事件；本机模拟 `PATH=<venv>/bin:$PATH bash -lc 'command -v python3'` → /opt/homebrew/bin/python3 | 环境 + 链路（`本地蜂巢.LocalHive.__init__` 只靠 PATH 顺序，登录 shell 守不住；2025 B 炉建模师凭 AGENTS 直接写 numpy 没被探针吓退，所以没暴露） | `本地蜂巢.py` 给腿环境加 `PYTHONPATH=<venv>/lib/python3.*/site-packages`（venv 的 home 就是同一个 homebrew 3.14，二进制兼容，谁当 python3 都能导入）；本炉 12:16 停跑（S2 刚开 4 min），一次性 `规划师` 腿（补腿.py，任务：先探针、只改因缺库而降级的口径）修订 计划.json/求解计划.md 后 `--resume` | 真跑后 `grep -l "No module named '\(numpy\|openpyxl\|scipy\|matplotlib\)'" 蜂巢镜像/日志/*.log` 与 `grep -l 不可发现 蜂巢镜像/日志/*.log` 均应为空；`体检.sh` 新增「腿内登录 shell 探针」（用 LocalHive.env 跑 `bash -lc 'python3 -c "import numpy, openpyxl"'`）硬失败项 | 已修·待真跑验证（12:20 本机登录 shell 探针 numpy 2.5.3 / openpyxl 3.1.5 通过；待 计划修订 腿的探针原话与续跑后建模腿日志） |
| R56 | 驱动 `红队复算()` 判「不齐」只看 `分歧明细` 的 相对差 > 容差，不看 P1 新增的 `类型`：P1 让红队把独立口径差异也写进 分歧明细（类型=口径）只作交叉印证并按声明口径判齐，于是一条口径行就把 结论=对齐 的报告派成仲裁腿——P1 提示词侧落地了、驱动侧漏了，每问都会白派一次仲裁（对照跑 7 次仲裁 2 次有效的同类回流） | 2026-09-11 A 题 `驾驶_A题.out` 12:55:00 `!! 问题1 红队不齐（1 处超容差）→ 派仲裁腿`；`交接/红队_问题1.json` 结论=对齐、四项按声明口径最大相对差 5.8e-5，唯一 >1% 的是 类型=口径 的「1800秒表面含水率」4.81%（采样带均值 vs 表面点值）；仲裁腿 12:58 裁「口径差异成立、应改方 无」；干跑 全链 旧代码下 mock 口径行触发 仲裁_问题1/2/3 三次 | 链路（`蜂群驾驶.红队复算` 过滤条件） | `红队复算()`：分歧过滤排除 `类型=口径` 行，日志改打「红队结论=对齐（口径差异 n 条只作交叉印证，不计入不齐）」；`驱动干跑.py` 红队 mock 加一条 4.8% 口径行，全链断言「无 仲裁_问题* 腿 且日志含口径提示」（先对旧驱动失败一次再修，铁律 6）；本炉 13:0x `切换.sh` 在 `先启 ['建模_问题2']` 边界上线 | 日志 `红队结论=对齐（口径差异` 出现 = 生效；`跑后指标.py` 回流账「仲裁」计数应只含真数值分歧；干跑 全链 断言 R56 | 已修·真跑验证（2026-09-11 13:25:11 `问题2 红队结论=对齐（口径差异 6 条只作交叉印证，不计入不齐）`，无 仲裁_问题2 腿，`门[G2:问2] 第1次检查 PASS`；6 条口径行来自红队独立 256 区间有限元/BDF 实现，最大相对差 1.8e-3，与声明口径 6 项 ≤1.7e-3 一致） |
| R57 | `切换.sh` 只把 切换/公共/停跑/启动/验证 五个脚本复制到临时目录执行，而 2026-09-10 双版本打包后 `验证.sh` 新增了对同目录 `打包.sh --核对` 的调用，复制清单没同步：副本目录缺 打包.sh → 停净之后「验证失败，不续跑」，驱动被停在边界上等人工 | 2026-09-11 A 题 12:59:03 切换输出 `bash: /private/var/folders/…/qiehuan.Ycm0k9/打包.sh: No such file or directory` → `!! 验证未通过，不要续跑`（exit 6）；驱动已停净、状态/镜像未动；12:59:52 人工 `启动.sh --resume` 续上（pid 60832），停机 ≈ 1 min；R56 的上线因此没走完自动切换 | 工具（`scripts/切换.sh` 自复制清单 与 `验证.sh` 的同目录依赖失配） | 复制清单加 `打包.sh`（它经 `公共.sh` 用 `$PROJ` 解析核心/发布目录，复制到临时目录照常工作）；`验证.sh` 新增静态核对「切换.sh 复制清单闭合」：解析清单，清单内每个脚本引用的 `dirname "$0"` 同目录脚本都必须在清单内，否则 ✗（先对旧清单失败一次：`验证.sh→打包.sh`，铁律 6）；模拟切换副本（六脚本拷到临时目录跑 验证.sh）→ 发布版一致、验证通过；`打包.sh` 重打两份发布版 | `验证.sh` 每次都跑「切换.sh 复制清单闭合」；下次真切换看输出走到 `切换完成：新驱动 pid=` | 已修·真跑验证（2026-09-11 14:43:23 切换命中标记 → 14:43:26 停净（驱动=0 codex=0 xelatex=0）→ 钩子 → 副本 验证.sh 含 打包.sh --核对 全过 → 14:50:45 `--resume` → 14:52:05 `切换完成：新驱动 pid=1386`） |
| R58 | G2 门与建模师任务文本都不涉及契约要求交的结果模板文件：门只看解读/红队/门检，任务文本没提 resultN.xlsx，写出格式全凭建模师自觉——问1/2 自觉 round，问3 只给单元格设 number_format="0.0000"、存未舍入浮点（2.549999999999922）、角格改成「时间」，门照样 PASS | 2026-09-11 A 题 14:12:50 `门[G2:问3] PASS` 后 `核对结果模板.py 国赛2026A/蜂巢镜像 国赛2026A/输入/附件3` → `✗ result3.xlsx[Sheet1]：存在超过四位小数的值`（3433/3433 行）、`△ 角格 A1='时间'`；`求解_问题3.py` 导出段只设 number_format；契约硬约束「所有结果保留四位小数」 | 链路（G2 之后无结果文件核对）+ 任务文本（建模任务无结果文件条款） | ① 驱动新增 `结果模板(编号)`/`结果文件核对(编号)`：过门后对契约模板文件跑 `核对结果模板.py --只=resultN.xlsx`，只记日志不判门（`!!` 前缀入监督正则；是否进 G2 判据留给用户）；② P11 任务文本条款；③ 本炉：`切换.sh --标记 "先启 \['绘图" --钩子 国赛2026A/钩子_结果文件回炉.py --干跑=全链` 在 S2→S3 边界停净后按核对结果逐文件派 建模师「结果文件回炉」腿（只修写出层、不改数值、从未舍入数表重建、改回 求解脚本导出函数、自检进 导出核验.json），复核仍 ✗ 则不续跑交人工 | 日志 `问N 结果文件核对：… 全部合格` / `!! 问N 结果文件核对不合格`；收尾 `核对结果模板.py <成品> <附件3>` 全 ✓ | 已修·真跑验证（边界回炉：14:43–14:50 钩子派 `回炉_结果文件_问题3`（256 s）、`回炉_结果文件_问题4`（168 s）均 PASS，复核 `结果模板核对：全部合格`；数值未变（result3 末行 205927.3301 s / 中心 0.15、48 h 中心 0.1615；result4 末行 183218.4375 s），结果声明 mtime 未动，各写回 求解脚本导出函数 + 重建结果文件.py + 导出核验.json「回炉自检」；驱动侧 `结果文件核对` 日志行要到下一炉 S2 才可见——本炉 S2 已在旧驱动下跑完） |
| R59 | 审计 16) 问题重述与总体分析（C5）只看**第一个**文件名含「问题重述」的章文件就 break：规划师把第 1 章拆成 1.问题重述与总体分析 / 1.1.问题重述 / 1.2.总体分析 三个文件时，1.2 里的 `\subsection{总体分析}` 与思路图永远看不见 → G4「缺要素 ['有总体分析','有思路图']」无解，返工腿两轮都改不掉，第三次必降级放行 | 2026-09-11 A 题 19:37:03 / 19:52:30 `门[G4]` 两次 FAIL 同一条「缺要素」；`论文/1.2.总体分析.tex` 第 6–11 行有 `\includegraphics{…边界与半径驱动四问答案.png}` 与 `\subsection{总体分析：…}`；旧审计 `report.问题重述.文件 = 论文/1.1.问题重述.tex`；返工腿日志自行复现「第 574 行在第一个问题重述文件后退出」并拒绝为此改稿；新夹具在旧代码下 ✗×2 | 链路（`运行时/审计.py` 第 1 章组判定） | 第 1 章组 =（文件名或 section/subsection 标题含 问题重述/总体分析/分析思路/总体思路 的章文件）合并判定：给定/要求 看所有「问题重述」标题后的段落，总体分析 看组内 `\subsection` 标题或文件名，思路图 看组内任一 `\includegraphics`；判据本身不变。`审计单测.py` 加拆分三文件夹具（有图→五要素 True；无图→有思路图 False），先对旧代码失败再修（铁律 6）；对活镜像副本试跑五要素全 True；19:56 停净 → 改 → `验证.sh --干跑=全链,门升格` 过 → 20:0x `--resume`（G4 节点重跑） | `审计单测` R59 两例；日志 `门[G4]` 明细不再含「缺要素 ['有总体分析','有思路图']」 | 已修·真跑验证（2026-09-11 20:00:55 续跑后 `门[G4] 第1次检查` 明细只剩 2.2 对冲密度一条，「缺要素 ['有总体分析','有思路图']」消失；镜像 bin/审计.py 已同步 R59） |
| R60 | wave() 到时限只把腿判失败（重派或最终失败），不回收还活着的旧进程组；设计依赖「腿上限 LEG_TIMEOUT=波次−60 先于波次超时把腿杀掉」这条不变量，一旦失守（R61）旧副本与重试副本并行双写同一批交接文件，判最终失败的副本还会在下游腿工作时晚写产物 | 2026-09-11 A 题：解读_回炉_轮1_问4 第 1 副本 21:13:25 起、22:18 自然结束（65 min，rc=0），第 2 副本 21:38:42 起、22:29:02 结束（50 min，rc=0），并行 40 min；`交接/结果声明_问题4.json` 22:26:07 在红队复算（21:57 起）窗口内被改写 → 22:23 红队「不齐（0 处超容差）」派仲裁（22:34 仲裁后 G2 PASS）；`日志/图修1.launch` 两副本各记一次 rc=0（15:08 / 15:28 起） | 链路（`流水线/蜂群驾驶.py` wave()） | 新增 `回收超时腿(name)`：判「到时限仍无 done」后、重派/判最终失败前，按 pid 文件收集 role.sh 全部后代进程组（含 perl 子进程另起会话的 codex 组），TERM → ≤10 s → KILL；僵尸/已收割/无 pid 三种情形不误报；驱动日志打「整组回收（R60）」 | 单测：假腿树（role.sh→perl with_timeout→bash→sleep）11 s 内全清 + 三种不误报（现场脚本，先证活腿再回收）；`验证.sh --干跑=韧性` 通过；真跑判据：续跑后每条「到时限仍无 done」紧跟「整组回收（R60）」，`ps` 不再出现同名双副本 | 已修·待真跑验证（22:53 随切换上线；至 2026-09-12 02:52 停炉前未再出现「到时限仍无 done」，回收分支未被触发——补丁生效但真跑判据尚无样本） |
| R61 | 腿硬上限 with_timeout（perl alarm → TERM/KILL 子组）在本炉真实腿上从未生效：全炉 0 条 rc=142；图修1 首副本跑 28 min、解读_回炉_轮1_问4 两副本跑 65/50 min，均远超下发的 LEG_TIMEOUT=1040 s，且都以 rc=0 自然结束 | 2026-09-11 A 题 `日志/图修1.launch`（两条 rc=0）、`日志/解读_回炉_轮1_问4.launch`（rc=0）；ps 组结构正常（perl 在 role.sh 组内、node 自成组）；在跑腿 perl 的 $t 实读为 1040/1240，说明上限已下发；离线复现全部按时 rc=142：同一 codex公共.sh、Homebrew perl 5.44 与系统 perl 5.34、蜂巢 env + start_new_session、忽略 TERM 的子进程、真 `codex exec`（30 s）——差异只剩驱动进程的祖先链；22:50 试验 8 经 后台启动.py→python→LocalHive._spawn→role.sh 拉起真 codex 腿（LEG_TIMEOUT=60）60 s 准时 rc=142，祖先链也排除；1040 s 长闹钟对照 22:57:52 准时 rc=142（1042 s），闹钟长度也排除。七种复现全过、三条真腿全逃，机制未定 | 链路（`运行时/codex公共.sh` with_timeout / 驱动祖先环境） | 待查：观察在跑腿 34787（$t=1240，22:34:24 起）是否在 22:55 到点被杀；1040 s 长闹钟后台试验；若确认闹钟在驱动子进程里失效，改为驱动侧看门狗（按启动时刻 + LEG_TIMEOUT 主动回收，复用 R60 的回收原语），不再依赖 perl 闹钟 | `grep -c rc=142 日志/*.launch` 与「到时限仍无 done」条数对照；每 60 s 采样 perl $t 与 etime（现场采样器） | 待查·活捉（R60 兜底已上线 22:53；采样器每 60 s 记 perl $t 与 etime，下次出现 etime>$t 仍活的腿即刻取证：kill -ALRM 该 perl 看处理器是否响应）；2026-09-12 收尾补记：采样器 22:41–06:29 共 464 次采样、超 $t 存活腿 0（上限 800/900/1040/1140/1240/3540 六档全部自然结束），R60 回收分支全炉未触发——现象未复现、根因未定，保持待查 |
| R62 | 操盘脚本的驱动判定 `pgrep -f "蜂群驾驶.py"` 是子串匹配：操盘手命令行里提到该文件名的 shell（py_compile、grep、heredoc 文本）都被数成「驱动」，停跑.sh 据此判「未停净」、切换.sh 不续跑、状态.sh 报「另有驱动进程」 | 2026-09-11 A 题 22:51:29 切换.sh 停跑结果「驱动=1 codex=2」→「没停净，不续跑」；驱动=1 是滞留工具 shell 41877（argv 含 `流水线/蜂群驾驶.py`），codex=2 是操盘手同时在跑的 R61 试验腿；炉子本身 TERM 腿组 0、停得干净。复现：`bash -c 'sleep 60; echo 流水线/蜂群驾驶.py'` 旧匹配误报、新匹配不误报，真驱动 50250 两者都认 | 操盘脚本（`.claude/skills/paper-foundry/scripts/公共.sh`） | 驱动判定锚定 argv 前缀 `DRV_RE='^[^ ]*[Pp]ython[^ ]* [^ ]*蜂群驾驶\.py( \|$)'`，是驱动/所有驱动/父目录驱动/驱动命令行 四函数统一改；发布版重新打包 | 现场先证误报再证不误报（上文命令）；`验证.sh` 通过（发布版一致、R57 闭合）；操盘纪律补一条：切换窗口内不做会起 codex 进程的诊断实验 | 已修·验证通过 |
| R63 | 腿被整组回收（with_timeout 到点 TERM/KILL 子组，或 R60 回收）后，codex 在沙箱里起的工具子进程不在该进程组，逃出去成孤儿继续跑（长算脚本会继续占 CPU、还可能晚写产物） | 2026-09-11 A 题 22:52 试验 8：LEG_TIMEOUT=60 的真 codex 腿 60 s 准时 rc=142，role.sh/perl/node/codex-darwin 全清，但腿内 shell 工具 `sleep 400`（pid 47299）存活，需手工 pkill；R60 单测的假腿树（bash 子进程同组）则全清 | 链路（`运行时/codex公共.sh` with_timeout 的 reap；`蜂群驾驶.py` 回收超时腿） | 待修：reap 改按会话回收——perl 子进程 setsid 后 sid==$pid，TERM/KILL 组之后再 `pgrep -s $pid` 逐个 KILL；回收超时腿 同理按 role.sh 后代的会话号收尾。先写会让旧代码失败的用例（子进程 os.setpgrp 后 sleep），再改 | 用例：假腿树里加 `python3 -c 'os.setpgrp(); sleep'` 孙进程，旧 reap 留孤儿、新 reap 全清；真跑判据：腿 rc=142 之后 `pgrep -s <腿会话>` 为空 | 已修·待上线（23:05 改 `运行时/codex公共.sh` reap：杀前按 ppid 树收齐后代，组+后代一起 TERM→KILL；失败用例（孙进程 os.setpgrp 后 sleep）旧代码留孤儿、新代码全清；回归：普通/忽略 TERM/透传 rc=7/正常 0 全对；验证.sh 通过。bin/ 在下次 `--resume` 同步生效）→ 已上线·待真跑验证（2026-09-12 02:56 随 R65 切换的 `--resume` 同步进 bin/；真跑判据：腿 rc=142 之后无该腿会话的存活进程） |
| R64 | 红队复算把「脚本自身失败」与「数值不齐」混为一谈：复算脚本读结果声明缺键、或 numpy int64 不能 JSON 序列化而中途崩，红队仍出「不齐」结论 → 驱动派仲裁腿（1 h/次），仲裁两次都裁「对齐」 | 2026-09-11 A 题 问 4：轮 1 回炉 22:26 红队不齐 → 仲裁 → 22:34 G2 PASS（`蜂巢镜像/交接/红队_问题4.json`/`仲裁_问题4.json`：声明缺键）；轮 2 回炉 00:38 同路（int64 dtype）；轮 3 红队脚本正常即直接对齐 02:14 | 链路（红队角色的复算脚本纪律 + `蜂群驾驶.py` 红队结论消费：脚本 rc≠0/异常应走「修脚本」而非「不齐→仲裁」）+ 提示词（红队/建模师：结果声明键集与 JSON 可序列化） | 待修：红队报告增「复算脚本状态」字段，脚本失败 → 派修复腿重跑一次再判；仍失败才进仲裁，且仲裁任务文本注明「脚本失败、非数值分歧」 | 检测：干跑场景「红队脚本失败」；真跑判据：仲裁腿只在 分歧明细 有数值行时出现；每炉「仲裁次数/其中脚本失败」进回流账 | 待查（本炉 2 次仲裁均属此类，1 次直接对齐） |
| R65 | `图片腿()` 直接 spawn、不走 wave() 的并发闸；`_S5美化`/`_S6终审` 按 8 页一腿整份铺开，腿数随总页数线性增长，没有任何并发上限 | 对照跑 66 页 → 9 腿；2026-09-12 A 题 327 页（附录源码 ≈305 页）→ 美化轮 1 会同时起 41 条 codex 腿，10 倍于实测稳定并发 4（6 并发曾整晚 429）；02:51 在美化边界停炉前发现（`驾驶_A题.out` 02:41–02:51、`蜂群驾驶.py` 原 2174–2182/2376–2383 行） | 链路（`蜂群驾驶.py` 美化/终审编排） | 已修：新增 `图片腿群(规格们, 单腿超时, 等待)` 按 `并发上限` 分块起腿、每块 `等腿`；美化与终审两处改为组规格后调用（腿名/任务文本/超时不变），日志「图片腿群 n/N：起 [...]（并发上限 4，R65 分块）」 | 干跑全链：假页 19→40（美化/终审各 5 腿），假 hive 量「同时在跑峰值」，断言 ≤ 并发上限；旧驱动峰值 5 → AssertionError（已复现）。真跑判据：美化轮日志「图片腿群」分块行、`状态.sh` 活腿 ≤ 并发上限 | 已修·真跑验证（2026-09-12 02:56 随 `--resume` 上线；失败用例先红后绿；`验证.sh --干跑=全链` 通过。真跑证据：`驾驶_A题.out` 02:57:02「图片腿群 4/41」→ 03:24:05「41/41」美化轮 1 十一块 28 min、03:44:52 起轮 2 十一块 55 min、05:47:44 起终审「4/37」；`状态.sh` 活腿全程 ≤4，本段 429=0、rc=142=0） |
| R66 | S6 出版收割的列举只收 `论文/*.tex、页/*.png、审稿/*.json、交接/*.json|*.md` 与 `求解/**/*.py|*.json|*.png|*.sh`，不收 `*.xlsx`：题目要求交付的 附件3 模板结果文件（result1–4.xlsx，建模师写在 `求解/问题N/结果/`）留在工作根，成品目录里一个都没有；R58 只在门后核对镜像，没人核对成品 | 2026-09-12 06:18:39「S6 终稿收割 1299 文件 → 国赛2026A/成品_A题」后 `find 成品_A题 -name '*.xlsx'` = 0；`核对结果模板.py 国赛2026A/成品_A题 …` → 4 项「未找到交付文件」（同一命令对镜像全部合格） | 链路（`蜂群驾驶.py` `_S6出版` 收割列举）+ 操盘（收尾 SOP 第一步就是对成品核对，本炉靠它发现） | 已修：列举补 `-o -name '*.xlsx'`；处置：驾驶结束停净后用钩子把 S6:出版 从 已完成节点 摘掉（复盘保持完成）→ `--resume` 只重跑出版节点重新收割 | 干跑全链：假 hive 记下出版列举脚本原文，断言含 `'*.xlsx'`（旧驱动 AssertionError 已复现）；真跑判据：成品目录 `核对结果模板.py` 全部合格 | 已修·真跑验证（2026-09-12 06:25 补丁 + 失败用例先红后绿 + `验证.sh --干跑=全链` 通过；06:29 驾驶结束、停净后钩子摘除 S6:出版 → `--resume` 06:30:18「S6 终稿收割 1307 文件」，成品 `求解/问题N/结果/result1–4.xlsx` 齐，`核对结果模板.py 国赛2026A/成品_A题 …` 全部合格） |
| R67 | S6 逐页终审的整改只派撰稿师（`终审整改`），图内问题（标注挤叠、数值标注压曲线、路线标签重叠）撰稿师只能回「需转图」，成品 PDF 就带着这些图出版；G5 已有图路（R49）、美化已有图路（P6），唯独压轴一关没有 | 2026-09-12 06:05 终审 10 条必改，`审稿/回执_终审整改.json` 3 条 受理=False「需转图：…超出撰稿师权限；图片及脚本指纹未变」（p23 各路线完整性图、p285 环层通量图、p287 长程剖面图），终态 PDF 原图未变 | 链路（`蜂群驾驶.py` `_S6终审`） | 待修：终审必改按 目标=图/文 分路——图条派绘图师按定向修改协议改脚本 + `求解/成图终审.sh` 只重跑受影响图，再编译；文条照旧派撰稿师；两路共用一次 `_编译修复循环` | 干跑全链：终审桩给一条 目标=图 的页问题，断言出现 `终审图` 腿与 `成图终审` 脚本；真跑判据：`回执_终审整改.json` 无「需转图」 | 待修（本炉 3 条留在成品里，见交付报告 §8） |
| R68 | G4/G5 成稿级返工腿为压「正文≤20页」把正文图整体挪进附录（17 张进 8.7.补充验证，正文只剩 2 张图，正文引用全改成「附录图」）；结构守卫 R38 只守 lstlisting/空章/\input，图数不在守卫尺内（R38 ④ 复发）；门检「图数 16–22」把附录文件一起计数，正文图数没有任何尺、任何评审也没报；任务文本硬编码 20 页而契约 `论文格式规范2026.txt` 第四条为「不超过 30 页」 | 2026-09-11 19:37–20:00 `日志/G4返工.log`/`G4返工2.log`；基线快照 `日志/G4返工基线/论文/*.tex` 正文 19 图 vs 成品正文 2 图（8.7 17 图，成品 20 个 \includegraphics 里 17 个在 8.7）；用户 09-12「这图太少了」 | 链路（`蜂群驾驶.py` `结构守卫回退`、`_G4返工` 任务文本）+ 尺（门检 图数尺无正文口径） | 结构守卫 ⑤：正文章（不在 附录集）\includegraphics 减少 → 整份回退且接收挪入图的章一起回退、新稿留底 `审稿/回退稿/<标签>/`；两处调用点传 `附录集=附录章文件`；任务文本禁令见 P13；本炉产品由 5 条撰稿师腿按基线原样归位 + 8.7 清理（不手改论文） | 干跑 `G4返工图`（返工桩挪图改「附录图」→ 断言 `结构守卫[G4返工]…正文插图减少`、正文图回、附录 0 图、留底文件存在；先红后绿）；真跑判据：`结构守卫[G4返工]` 行 0 次 = 腿守规，≥1 次 = 守卫接住 | 已修·干跑验证（2026-09-12；产品修复真跑：5 条腿 PASS，正文 20 图归位、8.7 0 图）；守卫真跑触发待下一炉。待用户拍板：正文图数进门尺、20→30 页参数化（交付报告 §8） |
| R69 | 附录 ≈270 页 listings 源码 + format.cls `breaklines=true`（每个字符挂一个带 \llap 盒子的 \discretionary，≈100 字/字符，是不断行的 8 倍）把 xelatex 主内存顶到 4,996,734/5,000,000（单页峰值 3.63M）；aux 多几十条引用就在 shipout 报 `TeX capacity exceeded [main memory size=5000000]`；两遍编译在「aux 截断→291 页、44 条未定义引用、?? ×46」与「aux 完整→崩」之间振荡；成品能编过纯属运气，补图后正文页一动即触发 | 2026-09-12 `\tracingstats=2` 逐页剖面（scratchpad 编译沙盒：p30 832k、p32 2.56M、p231–240 3.63M 字，末页 untouched 4587）；变体二分：去 listings（V3）或 breaklines=false（Z8）峰值降到 3–6 万字，其它 lstset 选项/hyperref/cleveref/caption 均无关；`成功遍.log` 4,996,734 words of memory | 链路（`本地蜂巢.py` env；`蜂群驾驶.py` compile_paper 无内存尺）+ 模板（format.cls lstset，用户决定） | `本地蜂巢.py` env `extra_mem_bot=5000000`/`extra_mem_top=2000000`（web2c 运行时参数，只进进程环境、不改系统 texmf.cnf；驱动 compile_paper/应急编译与腿内 xelatex 都继承）；compile_paper 输出 `内存=占比(用/总)` | compile_paper R69 尺：占比 ≥85% 预警、`TeX capacity exceeded` 点名；沙盒真编译两遍 E=0、未定义 0、?? 0、291 页、6.31M/10.88M | 已修·真跑验证（2026-09-12 10:51 出版重跑 `编译#出版: E=0 O=0 P=291`，镜像 log 6,309,736/10,883,328 words；此前同稿在 5M 上限下必崩）。附录源码体量与 breakatwhitespace 仍是决策项（交付报告 §8-1/§8-13） |
| R70 | S6 出版收割只增不删：成品页数 292→291 后 `成品/论文/页/` 仍留旧 `p292.png`（镜像侧 `_渲染页` 有 rm -f，成品侧没有），读者会看到一页不存在的页图 | 2026-09-12 10:51 重出版后 成品 页图 292 张 vs PDF 291 页 | 链路（`蜂群驾驶.py` `_S6出版`） | 收割前 `rmtree(产出目录/论文/页)` 再收 | 真跑判据：成品 页图数 == PDF 页数（本次人工删 p292 后 291=291） | 已修·py_compile+验证.sh；真跑待下次出版 |

## 提示词层病根（P 系列，2026-09-10 对照跑复盘；方案见 `提示词收紧方案-v1.md`）

| 编号 | 病根 | 证据 | 归属 | 修法 | 检测 | 状态 |
|---|---|---|---|---|---|---|
| P1 | 红队只按自己口径算，口径不同的数被当数值分歧 → 伪仲裁 | 仲裁 7 次 2 次有效；4 次复核判口径差异；问 2 复核 4 连不齐（驾驶_对照.out） | 角色/红队.md、解读师.md | 两套口径 + 口径说明四段 + 仲裁先对口径 | 红队报告 `口径对照` 字段存在；跑后指标里 仲裁/问 ≤1 | 已落地·冒烟过（18:09 红队报告有 口径说明、结论只填 不齐/对齐、分歧明细不编数；18:22 解读师 口径说明 3 项四段齐全） |
| P2 | 返工单不分实现级/方法级，方法级问题反复返工 | 问 2 解读两轮 FAIL → 返工×2 → 升格 → 降级，1.5 h 答案未变 | 角色/解读师.md、建模师.md | 返工单 `层级` + 方法级收口规则 | 返工单含 `层级`；同一协议项 FAIL ≤2 轮 | 已落地·冒烟过（18:22 解读师返工单 1 条带 层级=实现，六键齐全） |
| P3 | 重算腿不产出换版清单，改文/改图各自猜 | 审稿台账 14 条换版不同步；轮 2 面板 −0.67；图 13 旧数到 G5 | 角色/建模师.md、撰稿师.md、绘图师.md、硬伤猎手.md | 换版清单产出/消费/残留检查 | `交接/换版清单_问题X.json` 在回炉后存在；猎手第 6 项 | 已落地（干跑 全链）；猎手冒烟：无换版清单时如实写「无旧值可检」不臆造；建模师侧待真跑 |
| P4 | 审稿指令无最小改动纪律；撰稿师无体量纪律 | 审-2-01 → 68→112 页（R52）；守卫回退 88%/50% | 角色/审稿员.md、撰稿师.md | 指令写法纪律 + 体量纪律 | R52 页数守卫不再触发 | 已落地·冒烟过（18:28 审稿员 5 条全部 定位到 文件:句 引原句、最小改动、无整章重写；撰稿师体量纪律待真跑） |
| P5 | 评委/审稿员不标版式级别，版式意见占阻塞批次 | 57 条里 30 条版式；评委 21 条 16 条版式 | 角色/评委模拟.md、审稿员.md | 版式类 `级别=版式` | 审稿台账 版式级占比；阻塞级里无字号/留白类 | 已落地·冒烟部分（审稿员 级别 字段齐全；本次样本无版式项，版式=版式 分级未触发；评委待真跑） |
| P6 | 美化师不标目标，图类意见被撰稿师退回 | 排版执行 30 条 14 条转路 | 角色/美化师.md + 驱动 `_S5美化` | `目标` 字段 + 图条直派绘图师 | 美化轮 `转路` 计数 | 已落地（干跑 全链） |
| P7 | 章评不限量不分级，轮 2 重复提 | 221 条消解 19%，轮 2 新 57 条 | 角色/章评师.md + 章评/章修订任务文本 | ≤5 条、严重度、只评改动段 | 章评台账每章 ≤5 条、含 严重度 | 已落地·冒烟过（18:20 章评师两章各 3 条、[严重度N] 3/3、范文对照 有） |
| P8 | 脚本自检写成致命断言 / 腿内用 PATH 外 python / 成图批脚本覆盖 MPLCONFIGDIR 按用时判失败 | 12 次 rc≠0：AssertionError 2、No module named numpy 2、成图回炉G5 首跑 rc=1 两次；17:22 `成图回炉G5_2.sh` 再次首跑 rc=1：脚本第 7 行 `export MPLCONFIGDIR=$ROOT/日志/G5返工图2成图/matplotlib`（新目录重建字体缓存）+ 750 s 总预算 + 「三路线误差按情景分布 失败；旧图不能冒充本轮产物」——与 16:46 同款（旧角色文件未同步，条款尚未生效） | 角色/建模师.md、红队.md、绘图师.md | 自检不致命、只用 python3、成图脚本纪律 | 跑后指标 脚本 rc≠0 次数 | 已落地·冒烟部分（18:09 红队 复算.py 自检落 自检.json 不致命；脚本未执行；绘图师成图脚本纪律待真跑） |
| P9 | 猎手不写明依据版本 | 16:10 按旧 PDF 判已重绘的图（R51） | 角色/硬伤猎手.md | 裁定写 PDF 时间与页码，PDF 旧于源时按源判 | 裁定文件含版本说明 | 已落地·冒烟过（18:15 猎手 done 写明「PDF 修改于 2026-09-10 17:55:54、66 物理页、不早于所核源文件」） |
| P10 | 复盘官不量回流 | 复盘报告只有总账 | 角色/复盘官.md | 回流账 | 复盘报告含 回流账 | 已落地（本炉复盘官仍用旧角色文件——角色只在续跑同步——待下一炉验证） |
| P11 | 建模师任务文本无结果模板文件条款（文件名/表名/网格/步长/小数位/角格/自检） | 问3 result3.xlsx 未舍入存值、角格改名，G2 照过（R58） | 驱动 `跑一问` 建模任务 ⑥（镜像 数据/ 下存在 resultN.xlsx 模板时追加） | 文件名、工作表名、首行网格、A 列步长按模板与契约裁定；所有存入数值（含时间列与补的末行）先按契约小数位 round 再写、只设 number_format 不算；角格 A1 沿用模板；openpyxl 读回自检写进 结果/导出核验.json | `验证.sh --干跑=全链` 通过；边界回炉腿任务用同款措辞，其产物过 `核对结果模板.py` 即冒烟 | 已落地·冒烟过（2026-09-11 两条回炉腿用同款措辞：产物全过 `核对结果模板.py`、角格沿用模板、自检写进 导出核验.json；驱动任务文本条款对下一炉/级联重算生效） |
| P12 | G4 返工腿把门判据当可商量：两轮返工都只改摘要，对 2.2 对冲密度超线（标题「不作坏点」1 词/586 字=1.71>1.0）写了一篇「审计器有误」的意见就不改稿 | 2026-09-11 A 题 `日志/G4返工.log` 5846–5856 行「意见 1/意见 2」；`论文/2.2.异常处置与边界.tex` mtime 18:56 未动；G4 两次 FAIL 明细里 2.2 密度值不变 | 驱动 `_G4返工` 任务文本 | 追加：门判据不由腿裁决——每条超线无论命中正文/标题、是否认同都必须改写措辞压到线下（不改事实/数字/不删内容）；认为审计器判错的另写 审稿/审计异议.md 交操盘手登记病根，不得以此为由不改稿 | 续跑后 G4 返工 → 2.2 密度回到线下；`审稿/审计异议.md` 若出现则进病根评估 | 已落地·冒烟过（真跑证据：20:00 续跑后 G4 首检只剩 2.2 密度一条 → 返工腿 20:02 把副标改为「早期变化原样保留」、密度 1.71→0.0、汉字数 586 不变，并写 `审稿/审计异议.md`（含一条审计器 JSON 指针拆分的疑点，见 R60 候选）→ 20:07:20 G4 二检 PASS） |
| P13 | `_G4返工` 任务文本「正文超 20 页只能把细节退到附录或表格」被腿解读为「图也是细节」→ 挪图（R68） | 同 R68（`日志/G4返工.log` 两轮返工回执、基线 19 图→成品正文 2 图） | 驱动 `_G4返工` 任务文本 | 追加（不删原句）：不许把正文任何一张 \includegraphics 挪进附录或删掉、不许把图引用改「附录图」；图表规划已定每张图的引用章节；守卫整份回退并记越权。20 页字面未动（阈值属用户决定，契约为 30 页） | 结构守卫 R68 日志 0 次触发 = 文本有效；1 次以上 = 守卫接住 | 已落地（2026-09-12；续跑生效，本炉 G4 已过门无触发机会） |
