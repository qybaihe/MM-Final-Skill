# 施工日志 · 论文铸造厂

> 施工方案：`施工方案-论文铸造厂.md`；蓝图：`理想蓝图-论文铸造厂.md`
> 记录格式：`[工单号] 改动文件 / 验收结果 / 遗留`

---

## [M0-0.1] 消灭 scratchpad 依赖

**改动文件**
- `流水线/蜂群驾驶.py`：删除 `SCRATCH` 常量；新增项目内资产路径常量组（`项目根 / 轮子目录 / 模板参考目录 / HMML路径 / 模板CLS`）；`ensure_hive()` 与 P0 播种段的 wheels / 模板参考 / HMML / format.cls 引用全部改指项目内路径。
- `流水线/蜂群驾驶_v3备份.py`：动工前备份（施工守则 §五.1）。
- 新增 `流水线/验证/M0播种验证.py`：独立复现 P0 播种段的验收脚本。

**验收结果（全过）**
- `grep -rn "claude-501" 流水线/ --include=*.py`（排除备份）→ 零命中；`SCRATCH` → 零命中。
- `python3 -m py_compile 蜂群驾驶.py` → 通过。
- 真实播种到容器 `b4d238c355fb`：上传 47 文件，容器内核查
  `wheels=7 模板参考=15 HMML=161895B 角色=10 数据=4 题目=1 cls=25800B 禁用词=35`；
  wheels 离线安装成功、`sklearn/openpyxl` 可导入、pypdf 抽出题目 2 页 1062 字、`xelatex` 在位 → 环境铁则 §二.7/§二.8 复验通过。

**遗留**：无。资产已全部收归项目内，容器轮换重播种不再依赖任何外部临时目录。

---

## [M1-1.1/1.2/1.3/1.4] 骨架：状态机·依赖DAG·门框架·预算（一并闭环）

**改动文件**
- 新增 `流水线/调度器.py`（纯逻辑、可本地单测）：`状态机`（§4.10 状态.json，节点级+问题级粒度、门失败计数、降级放行、级联队列，原子落盘 tmp→replace）、`构建依赖图/检测环/拓扑分层/直接下游/全部下游/上游`、`门`（检查→返工×2→升格蜂群→降级放行，绝不死循环）、`合并修订单/按目标分组`（级别 硬伤>正确性>叙述>版式，同级先算后图后文）。
- 重构 `流水线/蜂群驾驶.py` → v4：`节点(名称, fn, 阶段)` 原子节点框架（已完成即跳过，执行后落盘）；`--resume`；`配置` dict 集中全开/快速两档；`MAX_LEGS=220 / MAX_HOURS=14`；新增 `图片腿/等腿`（铁则§二.4 的 `-i`+`--` 批图，≤8 张）、`门检`、`升格蜂群`、`基建文件`（播种/重播种共用清单）；S2 改为拓扑分层调度 + 每问 `跑一问` 子链（建模→执行→解读⟲→红队→仲裁）+ 独立 `G2门`；新增 `级联重算`。
- 新增 `流水线/验证/驱动干跑.py`：假 Hive 注入，本地秒级跑通整张编排图（零沙箱消耗）。

**验收结果（全过）**
- `py_compile 蜂群驾驶.py 调度器.py` 通过；`python3 调度器.py` 自测全过（DAG/环/自环/分层/闭包/修订单排序/门三分支/状态机落盘重载）；既有 9 个函数签名（log/budget_ok/wave/get_json/run_script/compile_paper/checkpoint/checkpoint_dir/ensure_hive）全部保留。
- **1.2 DAG**：真题式计划（1→2，3 独立）日志实证 `S2 依赖DAG：{1:[],2:[1],3:[]} → 拓扑分层 [[1,3],[2]]`、`第1层启动：问[1,3] 并行`、`第2层启动：问[2]（上游[1]已过G2）`。
- **1.2 级联**：注入一条"算"级硬伤 → `回炉S2：重算问[1]` → 红队复算 → `级联重算(问1) → 作废下游[2] 并按拓扑序重跑` → 问2 子链（建模_问题2重算/解读/红队）真实重跑并重过门 → 受影响章列入下一轮修订。全链贯通。
- **1.1 断点续跑**：在 `建模_问题2` 处模拟 kill（首跑落盘 11 节点）→ `--resume` 重启 → 日志 9 条"已完成，跳过（续跑）"，S0/S1 腿重做数=**0**，运行ID 一致，续跑后完成到 27 节点。
- **1.3 门升格**：门检恒 FAIL 时，G2 逐问 走"返工×2 → 升格蜂群（3 变体腿+1 裁决腿）→ 降级放行并高亮 + 记 状态.json 降级放行"，流水线不阻塞、无死循环；恢复真实门检后正常 PASS。
- **1.4 杂修**：`get_json` 修复腿名改 `修json_{文件名}`（并行不撞名）；`wave` timeout 默认 1300 保持；预算与全部循环阈值集中进 `配置`。
- 全链干跑：27 节点、49 腿（快速档）、S0→S6 走通，状态.json 完整可复盘。

**次级决策（自裁）**
1. **快速档审稿轮数由 1 改 2**：轮1 才产出修订单并走定向回炉，轮数=1 会导致工单4.1"所有新节点各执行一次"无法满足。
2. **同层各问建模腿未合并进单个 wave**：改为逐问推进子链。理由——同层问的瓶颈是脚本执行（容器内 CPU 竞争）而非腿等待，逐问可让红队/解读与下一问建模自然交错，且失败隔离更清晰；DAG 并行语义（跨层等待、层内无序依赖）完整保留。记为对施工方案 §工单1.2 措辞的实现级偏离，不影响蓝图语义。
3. **驱动内不再写 `状态["阶段"]` 全局 dict**，统一走 `运行态` + 状态机落盘，避免双份真相。

**遗留**：门检.py 尚未落地（工单3.3），当前 `门检()` 调用会因脚本缺失返回不通过 → M3 完成后 G0/G2/G3/G4/G5 才真正生效；干跑已用 mock 覆盖其分支逻辑。

---

## [M2-2.1~2.8] 角色扩编 + [M3-3.3] 门检脚本 + 审计扩展（合并闭环）

**新增角色文件**（`流水线/角色/`，全部把 §四 对应 schema 原文贴进提示词）
- `答卷预测官.md`（2.2）：六节结构，前五节预测平庸卷的方法/结构/图表/摘要话术/薄弱环节，第六节"我们必须偏离的清单"给 5-8 条"平庸做法→我们的替代→评分收益"。
- `红队.md`（2.4）：**信息隔离铁律**（只准读 题面契约/数据档案/原始数据/结果声明；严禁读建模脚本、建模笔记、结果解读、结果目录）；两阶段工作流（写 `复算.py` 只写不跑 → 驱动执行 → 读红队结果比对出报告）；结论只能 `对齐`/`不齐`，禁"基本对齐"；复算失败填 null 不许抄对方数。
- `评委模拟.md`（2.7）：**只看页图+摘要 tex**，明令禁止读结果 JSON（评委没有这些）；按评委真实顺序（30秒摘要→2分钟翻图→2分钟找结论→定档）；`卡住的地方` 必须恰好 3 条。
- `硬伤猎手.md`（2.7）：五项猎杀规定动作（抽10数字溯源/抽3处推导核步/核定制改造是否真改/对照答卷预测查同质化/抽5个选择要理由链）；顶层数组，`[]`=清零；`目标` 字段决定回炉路线（算|图|文）；明确"编造硬伤比漏报更严重"。
- `复盘官.md`（2.8）：复盘**系统本身**而非论文；算账→归因（五类根因）→找漏网→提改进；`规则库修改建议` ≥3 条且须指名真实文件+可落笔文本+本炉证据；新增 `不建议改的地方` ≥1 条防军规漂移。

**角色升级**
- `读题官.md`（2.1）→ v2：主产物改 `交接/题面契约.json`（§4.1 schema），需求条目拆到最小可验收粒度、`需求号` 同问内必须 1..n 连续、`评分点推测` 不许空、**歧义必须裁定不许含糊**、附件清单必须覆盖 `数据/` 全部文件；兼容保留 `题面.json`。
- `建模师.md`（2.5）：新增产出 `交接/假设台账_问题X.json`（§4.5 schema），要求每假设有生效位置、依据引真实来源、**灵敏度义务与检验结果必须对应脚本里真实存在的检验并在括号注明结果键**、检验结果禁空/禁"待检验"。
- `规划师.md`（2.2/2.3 接线）：输入加 `题面契约.json` 与 `典型答卷预测.md`（刻意偏离）；计划 schema 补 `叙事主线`/`偏离点`/`锦标赛`；**依赖问题**升为硬性条款（显式、不成环、"仅方法承接不算依赖"以保并行度）。
- `绘图师.md` / `撰稿师.md`（2.2/2.6 接线）：输入加 `典型答卷预测.md` 对应禁区；撰稿师加 `论点脊柱.json` 为写作骨架、`假设台账`。

**新增/改动运行时**
- 新增 `流水线/运行时/门检.py`（工单3.3）：`python3 bin/门检.py G0|G2:X|G3|G4|G5` → `审稿/门检_<门名>.json {通过, 明细, 统计}`。G0 契约 schema/需求号连续性/歧义裁定/附件覆盖/矩阵条目数；G2 结果目录非空+解读PASS+红队对齐或仲裁在案+台账检验非空且引用键真实存在；G3 图数16-22/同型≤3/柱折≤半/示意≥3/图注素材；G4 E=0/摘要恰1页/禁用词0/溯源存疑0(允许"已解释")/句式≤40%/段首重复/溯源覆盖率；G5 矩阵全销号/末轮硬伤空/正文≤20页(用 aux 附录起始页扣除附录)/无目录/附录 lstlisting 非空/cite 键 ⊆ 文献卡片库/图配额/图被引用。
- 改 `流水线/运行时/审计.py`（工单2.5）：新增第 8 项**假设台账核验**——逐条查四字段非空、非"待检验"、括号引用的结果键在该问 `结果/*.json` 真实存在，并检万能假设。
- 新增 `流水线/后台启动.py`：见下方环境补充。

**验收结果（全过）**
- **门检一正一反 10/10 符合期望**（`流水线/验证/门检单测.py`，容器内真实执行，夹具在 `/tmp/门检测试`）：每个反例明细都指到点位，例如 G0 反 →「问1 需求号不连续:[1,3]／歧义裁定第1条缺'裁定'／附件清单未覆盖:['附件2.xlsx']／矩阵条目数(1)≠需求条目数(2)」；G2 反 →「解读未PASS／红队不齐且无仲裁在案／假设A1检验结果为空／假设A2引用的结果键不存在:不存在的键xyz」；G4 反 →「E=2／摘要不是恰好1页／禁用词3处／溯源存疑2条／句式超40%／段首重复」；G5 反 →「矩阵未销号1条['1-2']／末轮硬伤未清零／含\tableofcontents／附录代码0字符／引用键不在卡片库['野键2099']／图数9不在16-22」。
- **审计台账检查一正一反**：正例 违规数=0；反例 逮到「A1 检验结果为空」「A2 引用的结果键不存在:不存在的键xyz」。
- **新角色真实单腿冒烟**（容器 b4d238c355fb，用上次运行的真实交接文件作上游）：
  - `答卷预测官` 148s 完成，产出 6 节完整文档，内容真正咬住本题（点名"常折射率相邻峰间距公式""SG平滑+find_peaks 参数敏感""10°/15°分别算再平均浪费同片双角约束""FFT主频忽略色散展宽""Airy 定性套用"），最高性价比偏离=用 7469 点共轴谱做双角共享厚度约束。**非泛泛而谈，可直接被下游对照。**
  - `硬伤猎手` 完成，输出**合法顶层数组** 3 条，类别/目标/定位/证据/级别齐全，且抽了 10 个真实数字（7.999865 μm 等）逐个尝试溯源、正确指出"结果 JSON 不存在故无法复核"——五项动作真的执行了，不是走过场。
- 干跑回归：全链 27 节点、49 腿依旧走通；`调度器.py` 自测全过；`蜂群驾驶/调度器/门检/审计` 四文件 py_compile 全过。

**环境铁则补充（实测新增，重要）**
- **§二.11 修正**：本机 `nohup ... &` 启动的后台进程**仍会随工具会话被回收**（实测：首次冒烟进程静默消失，`.out` 只留两行），且 macOS **无 `setsid`**。可靠做法是 Python `subprocess.Popen(..., start_new_session=True)`——实测跨多次工具调用存活。已封装为 `流水线/后台启动.py`，**正式全开跑必须用它**，不能用裸 nohup。
- **§二.10 复现**：`/home/sandbox/.codex/skills/xq_*.sh: No such file` 幻影报错确认无害——报错期间文件正常写入（`典型答卷预测.md` 一次 ls 报缺失、隔数秒再看内容完整），按铁则不改架构。
- 容器内 exec 里嵌套 python 单行 `-c` 加中文路径易被引号吃掉，统一改用 `python3 - <<'EOF'` heredoc。

**次级决策（自裁）**
1. **G5 正文页数用 aux 的附录章起始页扣除附录**，而非"总页-1"。理由：附录页数不限，若按总页判会误杀；aux 里 `\contentsline{section}{...附录...}{页}` 可机械取到。
2. **G4 溯源覆盖率阈值定 0.6**（非 1.0）：摘要/结论里派生表述无法逐句标源，1.0 会导致门永不通过；存疑数=0 才是硬条件。
3. **门检报告同时写 `门检_G2:1.json` 与 `门检_G2_1.json`** 两份：冒号在部分 shell 场景下不便引用，双写零成本。
4. `图评师/章评师` 未新建文件（已存在且质量达标），仅在驱动任务文本里收紧为逐图/逐章循环（工单3.1）。

**遗留**：`红队`/`复盘官` 单腿冒烟在跑（后台，产物待验）；工单4.3（a2 对抗泳道）列为可选，视全开跑时间富余再定。

### [M2 补记] 红队 / 复盘官 单腿冒烟验收结果

- **红队**（`交接/红队_问题1_冒烟.json` + `求解/问题1/复算.py` 8354 字节）：
  - schema 六键齐全（问题/复算方式/复算指标/口径说明/结论/分歧明细），结论取值合法（`不齐`），并按要求在分歧明细写明"缺少结果声明，无法比对"——**没有为了好看而谎报对齐**。
  - **信息隔离经实测通过**：日志里 `求解_问题1.py` / `建模笔记_问题1` / `结果解读_问题1` 各命中 1 次，逐条核查全部位于第 62 行（=任务提示词原文回显），无任何实际读取动作；它实际读的是 附件1-4.xlsx（11/11/5/5 次）、数据档案.json（6）、题面.json（3）、题面契约.json（2）——**完全落在白名单内**。
  - 复算脚本独立实现（openpyxl 读谱 + scipy 最小化 + 自建设计矩阵做相位回归），未复用主线代码结构。
- **复盘官**（`审稿/复盘报告_冒烟.json`）：六键齐全，`规则库修改建议` **6 条**（≥3 达标）、`不建议改的地方` **3 条**（≥1 达标）。质量超预期：它正确识别出本次冒烟环境自身的系统性断点——"求解未执行、结果 JSON 缺失，却已写入 10 个精确数字与'核验通过'"，并给出可直接落笔的规则修改（`角色/解读师.md` 增加"结果文件与键逐项存在性硬门，缺失立即 FAIL 且禁止写通过"）。这条建议已择要落实（见 M3 记录）。

**结论**：五个新角色（答卷预测官/红队/评委模拟/硬伤猎手/复盘官）全部完成真实单腿冒烟，输出均为合法 schema 且内容咬住本题，可以接进驱动。

---

## [M3-3.1/3.2] 循环完备接线 + 复盘建议落地 + 真题冒烟发现的真实缺陷修复

**复盘官建议落地（第一条即时采纳）**
- 改 `流水线/角色/解读师.md`：新增**前置硬门（第 0 项）**——解读前先核 `求解/问题X/结果/` 存在且含 JSON、准备引用的每个数值必须能按键在结果 JSON 中找到、执行日志显示脚本正常结束；任一不满足**立即判 FAIL 并写返工单**，明令"绝对禁止编造数值、禁止把预期当结果、禁止写出任何'核验通过'表述"。
- 同时给解读师补齐产出 2 = `交接/结果声明_问题X.json`（§4.3 schema：问题/核心指标/口径说明，只放 3-8 个头条数字），这是红队信息隔离下的**唯一比对入口**，此前缺失会导致红队永远只能给"不齐"。

**蓝图节点覆盖自查**：写脚本对 `蜂群驾驶.py` 逐条比对蓝图 38 个要件（S0-S6 全节点 + G0-G5 + 双轨 + DAG + 级联 + 逐图逐章循环 + 五路并审 + 分级回炉 + 需求销号 + 硬伤清零 + 断点续跑 + 连败升格 + 预算护栏），**覆盖 38/38，无缺失**。

**真题低配冒烟（工单4.1）跑出的真实缺陷 —— 锦标赛被静默跳过**
- 现象：`S1 路线侦察：{}` → "锦标赛未启用或无路线，跳过原型步"，之后 S2 反复报 `锦标赛_问题1.json` 不存在。
- 根因：驱动按 `侦察["问题"]` + `路线/参赛路线` 取数，而规划师**实际写的键是 `问题路线` + `路线名`**。schema 键名漂移导致解析出空表，**锦标赛整段被静默跳过且不报错**——比崩溃更危险的失败模式（论文照样能出，但战略双轨白丢）。
- 修复：新增模块级 `解析路线(侦察, 每问路线数)`——递归遍历任意层级，凡"含 `编号` 且含路线数组"的对象即认作问条目，问键容忍 `问题/问题路线/各问/问题清单/problems` 等、路线键容忍 `路线/参赛路线/候选路线/技术路线/routes` 等，并按配置截断。另加 `路线名(r)` 容忍 `名称/路线名/名字/name`，回退到 `技术范式/简述/核心模型` 前 24 字，避免日志与腿名出现 `?`。
- 验收：用**本次现场真实产出的** `路线侦察.json` 单测 → 正确解析 3 问 × 2 路线；另造 4 种键名变体 + 1 个截断用例，全部通过；干跑回归照旧 27 节点走通。
- **续跑验证同时达成**：改完后 `--resume` 重启，日志显示"已完成，跳过（续跑）"×5（S0三节点+G0+S1:侦察）、运行ID 保持 `20260826-155007`、S0/G0 零重做，随后 `S1 路线侦察：{1:['色散阶次格全局回归','双束全场波形剖面拟合'], 2:['双角连续相位锁定反演','双角峰谷序列图配准'], 3:['相干回程传输矩阵反演','谐波证据门控与基频剥离']}` 并正常派出原型腿。**断点续跑与锦标赛修复在真沙箱一次性双验通过。**

**冒烟中已验证为真的其他环节**
- `G0` 门在真实产物上**第 1 次检查即 PASS**（非空跑）：题面契约 18 条需求（问1:1 / 问2:4 / 问3:13，需求号连续）、8 条硬约束、**3 条歧义裁定均带裁定+理由**（例：问3 条件性任务"必须先明确判断附件1/2 是否存在并影响精度，仅当判定为是才实施消除与重算"）、附件 4 个全覆盖；需求追踪矩阵 18 条与需求条目数一致。
- `S2 依赖DAG：{1:[], 2:[], 3:[2]} → 拓扑分层 [[1,2],[3]]`——规划师在真题上自主判出问3 依赖问2，DAG 与并行分层按蓝图工作。
- 播种 333s 完成（wheels 装完、题目 PDF 2 页 1062 字、xelatex 就绪）。

**次级决策（自裁）**
5. 续跑时**保留 `S1:侦察` 节点为已完成**，只回退 `S1:原型/裁决/定稿/G1`。理由：`路线侦察.json` 本身是合格产物（缺陷在消费侧不在生产侧），重跑白烧一条 6 分钟的腿；这也正好构成一次"部分回退"的续跑真实用例。
6. 键名容错采取**递归+多别名**而非"改角色提示词逼它用固定键"。理由：提示词只能约束倾向不能保证，消费侧容错才是根治；两侧都做（提示词已写明 schema）但以消费侧为准。

### [M3-3.1 验收补记] 禁用词拦截闭环 + 锦标赛问集单一真相

**工单3.1 验收项"故意在一章塞禁用词，确认被逮住并修掉"——已通过**
新建 `流水线/验证/禁用词拦截验证.py`：在容器 `/tmp/禁用词测试` 夹具里造一章 tex，**从项目真实禁用词表（35 条）取前 3 条**注入（`综上所述` / `值得注意的是` / `在此基础上`），三阶段验证：
- 阶段1 干净稿：审计 禁用词数=0（不误报）。
- 阶段2 塞词稿：审计 **逮到 3/3，逐条给出文件+行号+词**（`论文/3.问题1.tex:5 综上所述`、`:5 值得注意的是`、`:6 在此基础上`）；`G4` 门检明细报出 `禁用词命中 3 处` 且 通过=False。
- 阶段3 修掉后：审计 禁用词数=0，G4 该项恢复通过。
结论：**检出 → 门拦 → 修掉归零**三步闭环成立，不是只会报警。

**踩坑（重要，已修）**：`审计.py` 与 `门检.py` 都硬编码 `os.chdir("/tmp/蜂巢")`。首次跑验证脚本时它们**跑去审计了正在进行的正式运行**（报告里出现夹具中根本不存在的 `假设台账_问题1.json` 才发现）。已在验证脚本里把夹具副本的 chdir 用字符串替换改指夹具目录，并加 assert 确保替换成功——这与 `门检单测.py` 的 sed 手法一致，之后所有针对这两个脚本的验证都必须先改 chdir，否则会污染/误读正式运行。

**顺手修掉的第二个真缺陷：锦标赛目标问三处各算一遍**
- 现象：快速档只有最后一问参赛，但建模腿对**每一问**都去 `get_json("交接/锦标赛_问题X.json")`，问1/问2 白等两轮重试 + 刷两条"解析失败"噪声日志（`锦标赛1 解析失败…尝试1/尝试2`）。
- 根因：`目标问` 在原型步、裁决步各算一次（`sorted(路线表) if 全问开 else [-1:]`），消费侧却用 `配置["开锦标赛"]` 判断——**三处口径不一致**，未来改配置极易漂移成"裁决了却不消费"或"没裁决却去读"。
- 修复：`路线表` 解析后**一次性**算出 `锦标赛问集` 作为单一真相，原型/裁决/建模消费三处全部改用它；并新增日志 `S1 锦标赛参赛问：[...]` 让参赛范围可见。
- 验收：干跑显示 `S1 锦标赛参赛问：[3]` 且只派 `原型_问3_1/原型_问3_2/锦标赛_问3`；四场景（全链/中断续跑/门升格/级联）全部回归通过。

**冒烟继续观察到的真实运行证据**
- **锦标赛是真锦标赛，不是走过场**：问3 两条路线各自写脚本并**真跑**（`原型_路线1.py` rc=0 用时 10.0s、`原型_路线2.py` rc=0 用时 16.9s），产出同口径可比指标（8 折连续谱窗留出反射率 NRMSE 中位数：路线1=**1.2027** vs 路线2=**1.7995**；独立零假设不误触发率 96% vs 93%）。裁决 JSON 六键齐全，优胜=`相干回程传输矩阵反演`，**败者被指派了论文用途**（"适合展示为何仅见谐波或尖峰不足以证明多光束，可作频域诊断互补"）——蓝图要的"双轨+败者转化为对比素材"真正落地。
- 路线2 被判败的理由是**它自己脚本里算出的质量门未达标**（不误触发率 93% < 计划的 95%），不是主观偏好——裁决有实测依据。

### [M4-4.1 进行中] 真题低配冒烟 S2 观察记录（S0→G0→S1锦标赛→G1→S2）

**跑到哪**：`S2:第1层`（问1、问2 并行）。已过节点 9 个，累计 15 腿，门失败计数为空、降级放行 0。

**S2 问1 的真实轨迹（这是本次冒烟最有价值的观察）**
1. 建模腿 11 分钟出 `求解_问题1.py`，脚本 rc=0；同时产出 `假设台账_问题1.json` **9 条**，每条都带真实灵敏度检验与结果键，例如
   - A1 双光束截断 →「已编码严格双光束与5%二次回程的同种子压力对比…（求解结果:灵敏度_双光束边界假设）」
   - A2 色散阶数 →「已编码0/1/2阶同口径训练集竞争，测试集不参与选阶…（求解结果:灵敏度_色散阶数假设）」
   - A3 反射相位 →「相位幅度-20%/+20%/+20%但错误固定三分支…（求解结果:灵敏度_反射相位假设）」
   **工单2.5 的台账要求在真题上真正落地了**，不是填空。
2. 解读轮1 **FAIL**。关键：`.done` 首行摘要写着"**硬门通过**：结果JSON存在、键可定位、执行日志正常结束"，随后按五项协议否决——"主方法仅在 MdAPE 上相对改善 12.08%，MAE 反而更差""双角可识别性诊断点误差和覆盖率劣于单角，交叉印证失败""角度与折射率边界扰动使基线优势翻转，灵敏度协议失败"。
   → **我按复盘官建议新加的"第0项硬门"在真题上被真实执行并写进结论**，而且它没有变成橡皮图章：硬门过了，但实质协议照样一票否决。返工单里逐条列出证据键（如 `验证结果_问题1.json → 经典极限核验/相对误差 0.037529%`）。
3. 修复腿 1（18 分钟，日志 2.6MB）→ 改脚本重跑 rc=0 → 解读轮2 **仍 FAIL**（"MdAPE 较基线改善 0.71 个百分点但逐谱配对证据不稳定""全场路线色散模型 MdAPE 劣于常数模型""四个关键扰动全部翻转基线优势，灵敏度一票否决"）。
4. 修复腿 2（日志 3.3MB）完成 → 正在跑修复后的脚本（实测 123% CPU、13 分钟 CPU 时间，是真在算）。

**判断**：这不是系统故障，而是**质量门在如实拦一个确实没打赢基线的方法**。解读师两轮都给出可核验的量化理由，没有为了过门而放水——正是蓝图要的行为。按驱动逻辑，两轮用尽后会走 红队复算 → G2 门，G2 连败两次将触发**升格蜂群**（3 条差异化变体 + 裁决），恰好让工单4.1 覆盖到升格路径。脚本预算 1500s、`repairs=1`，超时会派"降采样但保持口径"的修脚本腿，不会无界卡死。

**环境铁则再补充（§二.10 加强版）**
- 幻影 ENOENT 在长跑期间**显著更频繁**，且**裸 `ls 目录/` 最容易空返**（同一秒 `ls 日志/` 报 No such file，下一秒 `ls 日志/ | wc -l` 返回 42）。
- 应对：一律用 `wc -l` / `test -f && echo` / `wc -c <` 这类**带聚合或判定的探针**，并对同一探针重试 2-3 次取最成功结果。已把这套做法固化成 `流水线/验证/看一眼.py`（一条命令给出：容器 hostname、在跑腿数、交接/日志文件数、done 标记数、每问的 结果JSON数/台账/声明/解读/红队 有无、论文 tex 数、图片数、终稿 PDF 字节数），之后查运行状态统一用它，避免我因一次空返误判"产物没了"。
- 另一坑：`Hive.exec()` 的超时参数名是 `timeout_s` 不是 `timeout`；`h.f_text()` 只构造描述符，必须经 `exec(files=[...])` 才真正上传。

---

## [M4-4.1 关键] 冒烟跑出三个致命韧性缺陷 —— 已全部修复并加专项验证

低配冒烟跑了 **15.3 小时**、31 腿，只完成 10/27 节点后被我手动停止。这次"失败"的冒烟价值极高：它暴露了三个**在干跑里永远测不出来**的真实缺陷，全部与「容器每 2 小时轮换」这一环境事实相关。

### 实测环境事实（新增铁则，必须写进方案 §二）
**容器轮换间隔实测：105 / 144 / 164 / 181 分钟（共 5 次），重播种耗时 6-18 分钟。**
即：**任何单腿或单脚本的挂钟时长若接近或超过 ~100 分钟，就有很大概率被轮换腰斩**。这是本系统最硬的物理约束，所有超时预算都必须在这个窗口内取值。

### 缺陷1：波次中途轮换 → 对着空容器空转到超时
- `wave()` 只在**派腿前**调 `ensure_hive()`，轮询循环内不探活。轮换一旦发生在波次进行中，旧腿随容器消失，而轮询会对着**新的空容器**一直查 done 文件直到 timeout（1300s）耗尽，然后"重试一次"又可能撞上下一次轮换。
- 实证：`建模_问题2` 在 20340s 启动 → 22:30 判"腿最终失败" → 22:47 检测到轮换；`修脚本_执行_问题2` 在 46411s 启动 → 06:40 最终失败 → 06:56 又是轮换。**两条腿连烧约 4 小时零产出**。
- 修复：轮询命令改为 `count + [ -f AGENTS.md ] && echo HIVE_ALIVE` 一次拿回两件事；一旦发现无 `HIVE_ALIVE`，**立刻重播种并当轮重派本波**，且 `attempt -= 1`（轮换不是腿自身的错，不吃掉重试额度），最多重派 2 次防死循环。

### 缺陷2：脚本运行期轮换 → 同样空转，rc 记成 None
- `run_script()` 有一样的病。实证：`00:55 脚本 求解/问题2/求解_问题2.py 第1次 rc=None`，紧接着 `01:11 检测到容器轮换`——脚本随容器没了，白等满 1500s 预算。
- 修复：同样在轮询里带 `HIVE_ALIVE` 探活，轮换则重播种重跑且 `attempt -= 1` 不计入 `repairs` 额度。

### 缺陷3：预算护栏形同虚设（最严重）
- `budget_ok()` 只被用在**可选加项**（多轮审稿、升格、美化、级联）上；主干 `节点()` 完全不查预算。结果 `MAX_HOURS=14` 被突破到 **15.3 小时仍在跑**，全靠我手动 kill。这违背"预算护栏只防失控"的设计意图——它根本没起作用。
- 修复：
  1. `节点()` 入口强制查预算，超限抛新异常 `预算耗尽`；
  2. 顶层 `try` 捕获 `预算耗尽` 视为**正常刹车**而非崩溃，转入新增的 `应急出版()`；
  3. `应急出版()` 尽力编译一次当前稿件 + 把 tex/图/结果/交接/审稿**全量收割**回本地，保证护栏刹车后不是两手空空，人能接手、复盘官有料可查。

### 专项验证：新增干跑场景「韧性」（`python3 流水线/验证/驱动干跑.py 韧性`）
- **场景A 波次中途轮换**：在第一次波次轮询时让假容器"蜂巢消失"两次。结果：日志出现 2 次 `!! 波次中途检测到容器轮换（已等 0s）→ 立刻重播种并重派本波 2 腿`（关键是 **0s** —— 不再等满 1300s），且轮换后**仍走完全链 27/27 节点**，腿没被吞。**PASS**
- **场景B 预算护栏**：把 `MAX_LEGS` 压到 5 逼护栏中途触发。结果：`!! 预算护栏触发：腿数=6/5 用时=0.0/14h，节点[S1:裁决] 不再开工，转收尾` → `预算护栏收尾` → `应急出版：尝试编译…（PDF 存在=True）` → `应急出版：收割 13 文件`，最终阶段落 `预算耗尽收尾`。**PASS**
- 为此还修了干跑桩的一处失真：假 `exec` 原先把 `[ -f AGENTS.md ]` 分支放在轮询分支之前，导致我的组合探活命令只回 `alive` 不回计数，驱动误判轮换并无限重派。现已让假容器与真容器**输出结构一致**（计数 + HIVE_ALIVE），并新增 `轮换控制` 开关精确注入轮换时机。
- 回归：全链 / 中断续跑 / 门升格 / 级联 / 韧性 五场景 + 调度器自测**全部通过**；`蜂群驾驶.py` 已备份为 `蜂群驾驶_v4备份.py`。

### 冒烟正面收获（问1 全程走通，含升格）
问1 从建模到**升格蜂群裁决**在 **3.1 小时内**全部跑完，路径完整可查：
建模 → 脚本 rc=0 → 解读轮1 FAIL → 修复1 → 解读轮2 FAIL → 红队复算 → **红队不齐 → 自动派仲裁腿** → G2 第1次 FAIL → G2返工 → G2 第2次 FAIL（累计2）→ **升格蜂群派 3 条变体 + 1 条裁决** → 变体脚本失败还自动派了 `修脚本_跑升格_问1_3` → 最终 **G2 第3次仍 FAIL → 降级放行并记录到状态**（`降级放行: [{'门':'G2:问1', 时刻:'19:06:15'}]`）。
**这条链路把蓝图的 ⟲ 返工、红队仲裁、连败升格、降级放行四个机制全部真实跑了一遍**，且每次判 FAIL 都有量化依据（如"四个关键扰动全部翻转基线优势，灵敏度一票否决"）。质量门宁可降级放行也不放水——正是要的行为。

### 次级决策（自裁）
7. **护栏触发点选在 `节点()` 而非 `wave()`**。理由：`wave` 粒度太细，超限时可能把一个节点做成半成品（例如图生成一半），状态机会记成"未完成"反而更乱；节点边界是原子的，此处刹车能保证已完成节点全部完整。
8. **轮换重派上限 2 次**（波次与脚本各自独立计数）。理由：连续 3 次轮换命中同一波次说明这波腿本身太长（>5小时），应该由护栏或人来处理，不该无限重播种。
9. **应急出版不判成功失败，只尽力收割**。理由：护栏触发时论文可能根本没成型，编译失败是正常的；这一步的唯一目标是"别丢东西"，因此编译异常也不阻塞收割。

### [M4-4.1 收尾] 角色时间纪律 + 预算对齐轮换窗口

**根因补完**：韧性三修解决了"轮换后不空转"，但没解决"腿为什么会跑到 100 分钟"。冒烟实测腿耗时分布显示——问1 从建模到升格裁决全程只用 **3.1 小时**（单腿多在 10-25 分钟），而 `建模_问题2` 与 `修脚本_执行_问题2` 各自跑到 **99+ 分钟**，正好撞进轮换窗口。查角色提示词发现：**建模师/绘图师/红队/撰稿师/解读师全都没有任何时间约束**，模型自然会无节制地追求完美。

**已落实**：给这 5 个重活角色统一插入「时间纪律（硬约束）」段，明确写出容器每 105-180 分钟轮换、轮换会丢掉未落盘工作，并给出可执行的三条：
- 自身工作时长目标 **≤25 分钟**，先落盘最小可用版本再迭代；
- 要被执行的脚本运行时间 **≤20 分钟**，并列出具体手段（抽样/降采样、限制迭代与网格、缓存中间结果、避免 O(n²) 全谱扫描、bootstrap 组数取 200 而非 5000）；
- **脚本自带时间预算**（记 t0，接近 18 分钟主动收敛并落盘当前最优，结果 JSON 记 `实际用时秒`）；
- **边做边落盘 / 分步 flush**，不攒到最后一次性写——被腰斩也能保住已完成部分。
建模师那份写得最细（它是最容易超时的角色）。

**预算对齐**：`run_script` 默认 budget 由 1500s(25min) 下调到 **1320s(22min)**，与腿超时 1300s 一起压在轮换窗口内；原型/红队/升格/成图等处本来就是 700-900s，无需改动。

**五场景回归全绿**：全链 / 中断续跑 / 门升格 / 级联 / 韧性（A+B 双 PASS）+ 调度器自测。

---

## [M4-4.2] 全开自主运行已启动

- 启动方式：`python3 流水线/后台启动.py 真题测试/驾驶.out python3 流水线/蜂群驾驶.py 真题测试/输入 真题测试/成品`（脱离会话，PID 17264）。**未用裸 nohup**——本机实测 nohup 会随工具会话被回收。
- 档位确认：**全开**（`开锦标赛=True`、`全问开锦标赛=True`、`每问路线数=3`）——即 3 个问各开 3 条路线的战略锦标赛，共 9 条原型腿。
- 起跑健康：播种仅 **55 秒**（冒烟时 333 秒，因容器已热）、wheels 装完、`题目B.pdf 2 页 1062 字` 解析正常、`/usr/bin/xelatex` 就绪、读题+体检双腿已并行启动。
- 运行期间按施工守则**不改代码、不干预**，只做只读观察（用 `流水线/验证/看一眼.py`）。跑完先复盘再决定是否修。

### [M4-4.2 观察1] 全开跑 S0→S1→S2 前半段：显著优于冒烟

**速度对比（同一真题，同样到达 S2 起点）**：全开 **39 分钟 / 19 腿**，冒烟 **46 分钟 / 11 腿**。全开做了 3 倍的锦标赛工作量（9 条原型 vs 2 条）却更快——「时间纪律」注入见效：单条原型脚本从冒烟的 10-17 秒级放大到全开的 ~35 秒仍稳定，建模腿 10.5 分钟（冒烟 11 分钟），播种 55 秒（冒烟 333 秒，容器已热）。

**S0/S1 关键数据**
- G0 首检 PASS，需求追踪矩阵 **17 条**。
- 全问锦标赛真开：3 问 × 3 路线 = **9 条原型腿全部 rc=0**（其中 `原型_路线2`(问2) 第一次 rc=1，自动派 `修脚本_跑原型_问2_2` 后第 2 次 rc=0——脚本级自愈生效）。
- G1 首检 PASS；DAG 再次自主判出 `{1:[], 2:[], 3:[2]} → [[1,2],[3]]`。

**锦标赛裁决质量（超预期，值得记录）**
三份裁决都不是"挑最小指标"的机械行为，而是带口径审计的专业判断：
- **问1**：Q1-B 厚度 MdAPE **0.171%** vs Q1-A 2.632% vs Q1-C 5.177%，数值第一且胜出，干净。
- **问2**：数值排序是 B(7.53) < A(11.98) < C(14.82)，但裁决**否决了数值第一的 B**——理由是"B 的验证指标只从观测极值单向寻找最近预测，不惩罚验证块中过密的额外预测，低值存在由过预测压低的风险"，而 A 用双向最近匹配会同时惩罚漏预测和过预测，口径更严；在数量一致性审计通过的前提下 A 比 C 低 19.14%，故判 A 胜。**这是识别出了"指标被刷"的陷阱。**
- **问3**：数值第一是 C(NRMSE 0.0131)，但裁决指出"C 和 A 最终都只保留 q=1，数值优势实际来自基本波重构，**不能独立支撑题目要求的多光束判定与修正**"，而 B 是三路中**唯一**产出可证伪的多光束触发审计（"硅 1/3 窗口触发 K=4、碳化硅 0/1 窗口触发"），故 B 胜。并且明确写出"B 的分母是 5%-95% 分位距而 A/C 用极差，因此不对三个 NRMSE 作严格比例外推"——**主动声明了口径不可比，没有假装可比**。

**S2 问1：质量门在真实收敛**
解读轮1 FAIL（盲区 NRMSE=4.022、覆盖率仅 **0.71**、灵敏度方向翻转）→ 修复 → 轮2 仍 FAIL 但**指标明显改善**（三厚度最大差 4.02→**3.34 μm**、覆盖率 0.71→**0.87**，逼近 0.90 门槛）。说明返工单是有效的、模型在真的改进，不是原地打转。

**红队 + 仲裁链路完整跑通且判得精准**
- 红队独立复算脚本 rc=0，结论 `不齐`，但分歧明细只有 1 条且 `相对差=null`——原因是 **`交接/结果声明_问题1.json` 不存在**（解读两轮都 FAIL，按规则不许生成声明），红队按纪律判不齐。
- 仲裁腿的裁定**质量很高**：把 1 项判为"建模错"（声明文件确实缺失，属于"交付链缺少约定的比较合同"），另外 3 项全部判为 **"口径差异 / 应改方=无"**，并逐条说明为什么不是真冲突——例如"红队值 19.87 μm 是附件1 真实光谱的**法向光学厚度 q**，建模值 14.31 μm 是冻结半合成样本的**几何厚度 d**，既非同一样本也非同一物理量，不能直接比较"。
  → **仲裁没有被表面数字差骗到，正确区分了"真错"与"伪分歧"**，这正是 §4.4 设仲裁腿的目的。随后驱动按裁定派了 `仲裁返工_问题1` 重跑子链尾部。

### [M4-4.2 观察2] 发现第 4 个真缺陷：G2 门"仲裁未消解"判据读的是**过期文件**（不可满足门）

**现象**：问1 的 G2 门连续两次 FAIL，明细都是同一句 `问1 仲裁裁定建模需修正但未消解：1项`，中间已经跑过 `仲裁返工_问题1`、`G2返工_问1`、脚本 rc=0、`解读_问题1_G2返工`。

**根因（我自己写的门检有洞）**：`运行时/门检.py` 的 G2 第 3 项逻辑是——若红队结论不是"对齐"，则看 `交接/仲裁_问题X.json` 里有没有 `应改方` 以"建模"开头的条目，有就判"未消解"。问题在于：
- 这份仲裁文件是**一次性快照**，返工修完后**没有任何环节会重写它**（既没重跑红队、也没重跑仲裁）；
- 于是 `应改方=建模` 这条**永久留在文件里**，门**永远不可能通过**，只能一路走到降级放行。
- 本次那 1 项的内容恰恰是"`结果声明_问题1.json` 不存在"——而解读一直 FAIL 就不许生成声明，形成 **解读FAIL → 无声明 → 红队不齐 → 仲裁记"建模错" → 门永久失败** 的死环。

**为什么冒烟没暴露**：冒烟档跑到这里时是靠"解读未 PASS"这条明细失败的，仲裁那条被掩盖了；全开档解读在返工后可能已好转，才让这条**唯一的、不可消解的**明细单独浮出来。

**修法（留到本轮跑完再动，遵守"运行期间不改代码"）**：三选一，倾向第 3 种
1. 返工后重跑红队+仲裁（正确但贵，多 2-3 条腿）；
2. 门检改为只看**比仲裁文件更新**的返工产物时间戳（脆弱，依赖 mtime）；
3. **让消解可被显式声明**：G2 返工腿的任务里要求"逐条消解仲裁裁定并把 `交接/仲裁_问题X.json` 的对应条目改为 `应改方=已消解` 并附证据"，门检同时接受 `已消解`。这样文件从"一次性快照"变成"有状态的待办表"，与需求追踪矩阵的销号机制一致，符合蓝图风格。

**同时确认：轮换韧性修复在真环境生效**
`09:50:55 !! 检测到容器轮换，蜂巢丢失——自动重播种` —— 这是全开跑的第 1 次轮换（发生在开跑后 2.5 小时，与实测 105-181 分钟窗口一致）。驱动进程存活并自动重播种，没有像冒烟那样陷入"对着空容器空转到超时"。

**当前进度**：8 节点完成（S0×3 + G0 + S1×4）+ 正在 S2 第 1 层；腿数 33；用时 2.5 小时；门失败 `{G2:问1: 2}`。

### [M4-4.2 观察3] 环境变了：网关开始把同一账号**负载均衡到 2 个容器**（重大发现，必须写进铁则）

**现象**：全开跑到 2.5 小时后，日志开始每 3-4 分钟报一次"检测到容器轮换"，共 6 次，每次重播种 2-4 分钟——照这个速度所有算力都会烧在重播种上。

**诊断过程**（用连续探针而非单次判断）：
1. 单次探针看到 `d4e91fc29c53`、`up 51 min`、`load 0.00`——容器很稳，不像在频繁重启，与"轮换"矛盾。
2. 连续 8 次探针：全是 `e3d2848d0eb3` + `HAS_HIVE`，稳定。
3. 连续 14 次探针（间隔 1s）：**序列 `e3d2848d0eb3 ×3 → d4e91fc29c53 ×11`，出现 2 个不同 hostname**。

**结论**：**同一账号 a1 的 exec 请求被网关分发到 2 个不同容器实例**（本次是 `e3d2848d0eb3` 与 `d4e91fc29c53`），不是容器在轮换。之前的"轮换"实为**在两个实例间来回弹**：请求落到未播种的那个 → `[ -f AGENTS.md ]` 为假 → 驱动判"蜂巢丢失"→ 重播种 → 下次请求又落到另一个 → 再判丢失……形成**重播种风暴**。

**这解释了此前所有反常现象**，也修正了我先前的判断：
- "幻影 ENOENT"（铁则 §二.10）很可能**根本不是幻影**——是请求落到另一个还没有该文件的实例上。之前"重试几次就好了"恰恰因为重试有概率命中正确实例。
- 冒烟阶段那 5 次"轮换间隔 105-181 分钟"，也可能是实例池扩容/缩容造成的，而非固定 TTL。
- `建模_问题2` 连续失败很可能是：腿跑在实例 A，而轮询 done 文件的请求落到实例 B，永远查不到。

**当下如何自愈**：驱动反复重播种的副作用是**两个实例都被播种齐了**（连续 14 次探针全部 `HAS`）。此后 12 分钟内**再无新的轮换日志**，运行恢复正常并推进到升格蜂群（派出 3 条变体 + 1 条裁决）。所以本轮不必中断——环境已被动收敛。

**必须落实的修法（本轮跑完后做，工单4.4 一并处理）**
1. **实例亲和**：`hive_sdk` 若支持会话/亲和参数（如固定 session id 或在 payload 带 sticky key），必须启用；否则在 `ensure_hive()` 里改判据——不只看 `AGENTS.md` 是否存在，而是**记录首次拿到的 hostname 作为主实例**，请求落到别的实例时先播种该实例再重试，而不是把状态判成"丢失"。
2. **幂等播种**：把重播种改成**幂等且廉价**的检查（缺什么补什么），而不是每次全量重装 wheels（现在每次 2-4 分钟）。
3. **关键读写加 hostname 断言**：收割/写状态这类关键操作，返回里带上 hostname，不一致就重试到一致，避免"写到 A、读到 B"。
4. 铁则 §二.10 需**改写**：从"幻影 ENOENT，重试即可"升级为"**同账号可能被路由到多个实例，任何单次文件检查都不可信；必须重试并做实例一致性判断**"。

**当前进度**：9 节点完成，腿数 36，用时 3.3 小时，门失败 `{G2:问1: 3}`，升格蜂群已启动（问1 三条变体在跑）。

### [M4-4.2 观察4] 问1 走完全链后降级放行；问2 复现同一路径；已备好 §七 验收脚本

**问1 最终结果：降级放行（记录在案）**
完整路径（2.8 小时）：建模 → rc=0 → 解读轮1 FAIL（覆盖率 0.71）→ 修复 → 解读轮2 FAIL（覆盖率 0.87，已逼近 0.90）→ 红队复算 rc=0 → 红队不齐 → **仲裁**（1 项建模错 + 3 项判"口径差异/应改方=无"）→ 仲裁返工 → G2 第1次 FAIL → G2返工 → G2 第2次 FAIL → **升格蜂群 3 变体全部 rc=0 + 裁决** → G2 第3次仍 FAIL → **降级放行并高亮，不阻塞流水线**，随即启动问2。
- 状态落盘正确：`问题门状态: {'1': '降级放行'}`、`降级放行: [{'门':'G2:问1', ...}]`。
- **这证明"连败升格→仍不过→降级放行"的最后一道兜底真的能防死锁**，流水线没有被一个问卡住。
- 但根因是我发现的第 4 个缺陷（G2 仲裁判据读过期文件），**它让每个问都必然走完"2次返工+3变体升格"的最贵路径**，每问白烧约 1 小时。这是本轮预算吃紧的主因，必须在 4.4 修掉。

**问2 正在复现同一路径**：建模 14 分钟（冒烟时 99 分钟撞轮换，**时间纪律见效**）→ 脚本 rc=1 → `修脚本` 自愈 → rc=0 → 解读轮1/轮2 均 FAIL → 红队 → 红队不齐 → **仲裁裁定 2 项建模需返工** → 正在 `仲裁返工_问题2`。

**环境已稳定**：自两个实例都被播种齐后，**再无新的"轮换"日志**（累计停在 6 次），运行连续推进了 1.5 小时。印证观察3 的判断。

**预算前瞻（4.4h 已用 / 48 腿）**：按问1 实测 2.8h/问推算，S2 三问约需 7.8h，加 S0/S1 的 1.1h ≈ 8.9h，剩余 S3→S6 约需 3-4h，**合计逼近 14h 上限**。若护栏先触发，会走新加的 `应急出版()` 兜底收割，不会两手空空。

**已就绪：§七 验收脚本**（`流水线/验证/端到端验收.py`，只读、可重复跑）
把方案 §七 的产物齐备 21 项 + 硬指标 20 项全部机械化，每项输出**实测值**而不只给结论。已用两种数据验证脚本本身可用：
- 对当前 `真题测试/成品`（尚未到 S6）→ 正确报出绝大多数缺失，并**真的读出了状态里的 `降级放行: G2:问1`**；
- 对 `真题测试/蜂巢镜像`（有真实中间产物）→ 正确识别 12 个求解脚本、21 个结果 JSON、**3 个锦标赛**、2 份红队报告、17 条需求（全未销号，因 S5 未跑）、2 个门检报告，并正确判定"无 \tableofcontents"通过、"附录无 lstlisting"未过。
说明脚本能区分真有/真无，不是只会打勾。

### [M4-4.4] 修掉两个真缺陷：G2 仲裁死锁 + 多实例路由误判（选项2：停跑→修→重跑）

第一次全开跑在 **7.3 小时 / 80 腿 / S2 第2层** 处按用户决策停止（PID 17264 已 kill，状态与日志归档为
`状态_第一次全开跑_停于问3升格.json`、`运行日志_第一次全开跑.md`）。停跑前拿到了决定性证据。

#### 缺陷A：G2 仲裁死锁（三问全中，是本轮预算吃紧的主因）
**证据**：三个问的 G2 失败明细**一字不差**都是 `仲裁裁定建模需修正但未消解`，且 `门失败计数={'G2:问1':3,'G2:问2':3,'G2:问3':3}`——
全部撞满 3 次、全部走完"2次返工+3变体升格"、问1/问2 已降级放行。真实仲裁文件结构证实：
`逐项[]` 只有 `指标/裁定/理由/应改方`，**没有任何表示"已处理"的字段**；问1 有 1 条 `应改方=建模`、问2 有 2 条、问3 有 4 条，
与各自门检报的条数完全吻合。

**根因**：`门检.py` G2 把 `仲裁_问题X.json` 当**一次性快照**读，只要存在 `应改方=建模` 就判未消解；
而返工腿修完代码后**没有任何环节回写这个文件**，于是这条记录永久留存 → **G2 在数学上不可能通过**。
升格蜂群、降级放行这些兜底反而掩盖了它（流水线没死，只是每问白烧约 1 小时）。

**修法（把快照改成有状态的待办台账，与需求追踪矩阵的销号机制对齐）**
1. `门检.py`：每条 `应改方=建模` 的条目要看 `消解状态`+`消解证据` 才算出清。
   - `已消解/已修正/已解决/已落实` + **有证据** → 出清；
   - `已解释/裁定不成立/复核不成立` + **有证据** → 放行但计入 `统计.仲裁已解释项`（软路径，留痕供人工复核）；
   - **只声明不给证据 → 一律不放过**（防糊弄）；旧格式（无字段）→ 照旧拦住，不破坏既有语义。
2. `蜂群驾驶.py` 三处派单（`仲裁返工`、`G2返工`、`升格裁决`）都明确要求回写台账，且证据必须"具体到文件/位置/新数值，或不成立的理由"。

**验收**：判据单测 **9/10→9/9 全过**（含"已消解无证据要拦"、"已解释+证据要放"，以及照抄问1、问3 的真实条目组合）；
**容器内真实门检双向验证**：旧格式 → `通过=False, 仲裁未消解=1`；补上消解状态+证据 → `通过=True, 仲裁未消解=0`。死锁解除。

#### 缺陷B：多实例路由被误判为"容器轮换"（观察3 的落实）
**新证据**：`account` 参数**不控制路由**——实测 `a1` 与 `a2` 均落到 `d4e91fc29c53`；网关也不提供亲和/会话接口
（`/v1/accounts`、`/v1/status` 均 404，可用路由只有 exec/tasks/env 等）。既然拿不到亲和控制，只能让驱动自身对多实例免疫。
**修法（全部基于"单次探测不可信"这一条原则）**
1. `ensure_hive`：一次探 3 次并回收 hostname，**只要有实例缺蜂巢就幂等播种**，但不再据此宣布"轮换"；
   播种命令加 `[ -d pylibs/sklearn ] ||` 短路，避免每次重装 8 个 wheel（原来每次 2-4 分钟）；维护 `实例池` 供复盘。
2. `wave` 轮询：取 3 次探测中的**最大 done 数**，**只有全部探针都看不到蜂巢**才认定丢失。
   —— 这修掉一个**此前未被发现的严重隐患**：原实现单探读到 0 就重派整波，会把**已经跑完的腿重复派发**（静默翻倍烧算力）。
3. `run_script` 轮询：rc 与蜂巢同样多探，避免把"脚本已跑完"误判成轮换而重跑（每次白烧十几分钟）。
4. `hive_sdk.harvest`：收不到文件时重试 3 次（换一次路由就可能命中真正写了文件的实例）。
   **这大概率就是"幻影 ENOENT"（旧铁则§二.10）的真身**——不是幻影，是落到了另一个实例。密钥与端点未改动。

**回归验收**：`py_compile` 三文件全过；**4 个干跑场景（全链/中断续跑/门升格/级联）全部通过**，G2-G5 全 PASS、
续跑运行ID 保持一致、级联作废下游正确；**门检单测 10/10**；`hive_sdk` 冒烟正常（`蜂巢在线 host=d4e91fc29c53`）。
harvest 重试对照实验当前 6/6 vs 6/6（两实例都已播种、路由稳定时无差异），属零成本保险。

**蓝图偏离：无。** 两处都是实现缺陷修复，未改动流程/门/军规语义。G2 判据从"存在即拦"变为"未消解才拦"是**恢复**
蓝图原意（蓝图写的是"仲裁裁定…**但未消解**"，旧实现根本没实现"消解"这一半）。

### [M4-4.5] 第二次全开跑已落系统后台（补一条环境铁则：macOS 上 nohup & 不足以解耦）

**踩坑**：按方案 §二.11 用 `nohup python3 … &` 起跑，驱动**只活了几十秒**就随包裹它的 shell 一起消失
（日志停在"S0 播种开始"，`pgrep` 查不到进程）。这正是方案里"上次教训：挂在会话上的后台任务随会话死"的同一个坑——
但 `nohup … &` 本身**不够**：它防的是 SIGHUP，而这里的工具 shell 退出时整个进程组被回收。
接着试 `setsid`：**macOS 没有 setsid**（`command not found`）；`timeout` 同样不存在（前面跑干跑套件时已撞到）。

**可用解法（已实测）**：用 Python 自己脱离会话——
`subprocess.Popen([...], stdout=<file>, stderr=STDOUT, stdin=DEVNULL, start_new_session=True)`。
`start_new_session=True` 等价于 setsid，进程被 **reparent 到 init（PPID=1）**，与我的会话彻底解耦。
实测 `PID=10638 PPID=1`，起跑后连续存活并推进（S0 播种完成 → wheels 装完 → 题目 2 页 1062 字 → xelatex 就绪 → 派出 读题/体检 两腿）。

**新增环境铁则（第 14 条）**：macOS 上落长跑后台**必须**用 `start_new_session=True` 的 Popen（或等价的双重 fork），
`nohup &` 不可靠；且 `setsid`、`timeout` 这两个 Linux 常用命令在本机**都不存在**，脚本里不要依赖。

**第二次全开跑**：运行ID `20260827-145354`，全开档，输入 `真题测试/输入`（题目B.pdf + 附件1-4.xlsx 已确认在位），
产出 `真题测试/成品`。起跑前已把第一次跑的全部资产归档到 `真题测试/归档_第一次全开跑/`（状态/日志/成品/蜂巢镜像），
并对容器 `rm -rf /tmp/蜂巢` 连清 4 次（覆盖多个实例）确保无残留污染。按守则，运行期间不改代码、不干预。

### [M4-4.6 跟踪] 第二次全开跑 2.2h：问1 已真过门；但暴露出「裁决不可复现」这一框架级问题

**修复效果确认（干净的 A/B 对照，同输入同提示词）**
| | 第一次跑 | 第二次跑（修后） |
|---|---|---|
| G2:问1 第1次 | FAIL：`仲裁裁定建模需修正但未消解：1项` | FAIL：`解读未 PASS`（仲裁项已不在拦截原因里） |
| G2:问1 第2次 | FAIL：同上 | FAIL：`解读未 PASS` |
| G2:问1 第3次 | FAIL → **升格3变体 → 仍不过 → 降级放行** | **PASS** |
| 问1 耗时 | 约 2.8h | 约 1.9h（4546s→6669s 段内完成） |
| 腿数（到问1过门） | 36 | 30 |

门检报告实证：`仲裁建模项: 0, 仲裁未消解: 0, 解读首行: PASS`，**明细为空**。死锁确实解除，且**没有靠降级放行糊过去**。

**但发现一件更值得注意的事：仲裁裁决在两次运行间不一致。**
- 第一次跑：问1 仲裁判 `应改方=[建模, 无, 无, 无]` → 1 项要建模返工；
- 第二次跑：问1 仲裁判 `应改方=[无, 无, 无, 无]`，总裁定"口径差异成立，未发现同口径数值冲突"。

两次的**输入、角色提示词、容差全部相同**，红队报的都是"**0 处超容差**"。也就是说：数值上本来就没有任何一处超过 1% 容差，
两次分歧完全来自仲裁腿的**主观裁量**——第一次把"结果声明文件缺失"归成"建模错"，第二次认定纯属展示口径差异。
本轮 G2 之所以能过，一部分是我修好了消解机制，**一部分是这次仲裁恰好判得更宽**。这个区分必须诚实记下来，
不能把"这次过了"全算成修复的功劳。

**由此暴露的框架级问题（不改本轮代码，写进复盘建议）**
1. **"红队不齐"的触发判据本身可疑**：两次运行、共 4 次触发仲裁，**每一次都是"0 处超容差"**。
   数值全部对齐却仍判"不齐"，说明"不齐"不是由数值分歧触发的，而是由**键名/字段对不上**（一边有、一边没有）触发的。
   这会把大量"其实没矛盾"的情况推给昂贵的仲裁腿（每次约 4 分钟 + 后续返工链上百分钟）。
   应该把"键缺失"与"同键数值超容差"分成两类：前者只提示补齐口径，不进仲裁；后者才进仲裁。
2. **裁决类节点缺少判据锚点**：仲裁、升格裁决、锦标赛裁决都是"让模型自由裁量"，同样输入可得相反结论。
   应当给裁决腿一份**显式判据清单**（如：仅当同键同口径数值相对误差 >容差 才可判"建模错"；文件缺失一律判"补齐口径"而非"建模错"），
   把主观裁量收窄成可复现的规则匹配。
3. **日志摘要行有误导**：`S2 问1 过门=True 解读=FAIL` 里的 `解读=FAIL` 是上一轮的陈旧变量，
   实际 G2 返工后的解读 done 首行已是 PASS（门检报告 `解读首行: PASS` 可证）。摘要行应取最后一次解读结果，否则复盘会误判。

**当前进度**：2.2h / 35 腿 / 问1 PASS，正在问2（建模已过，脚本自愈 rc=1→rc=0，解读轮1 FAIL 后在修复）。
进度显著快于上轮同期（上轮 2.2h 时还在问1 的第二次 G2 返工）。

### [M4-4.6] 第二次全开跑跟踪：G2 修复被证实有效，但我自己引入了一个"静默死波"回归（已修）

#### 好消息：G2 仲裁死锁修复经真题验证成立
第二次跑（运行ID `20260827-145354`）跑到 5.2 小时、54 腿时的状态：
- **`问题门={'1':'PASS'}`、`门失败计数['G2:问1']=0`、`降级放行=[]`**
- 对比第一次跑：问1 是 `G2 失败3次 → 升格3变体 → 降级放行`，耗时 2.8 小时；
  这次问1 **G2 第3次检查 PASS，真正过门**（16:46:55 `S2 问1 过门=True`），没有降级、没有升格。
- 仲裁台账被正确回写：镜像里 `仲裁_问题1.json` 的 7 条全部是 `应改方=无`（仲裁复核后判定不是建模错），
  因此门检不再拦——**这正是修复想要的行为**：真错必须消解，伪分歧允许判"无"放行。

#### 坏消息：我在 4.4 引入的"多探取最大值"逻辑造成静默死波，白烧 70 分钟
**现象**：`升格_G2问2_*` 三条腿的波次里出现 `!! 波次中途蜂巢丢失（3 次探活均无，已等 4184s）`——
**白等了 4184 秒（70 分钟）**才动作；随后重派又立刻判死（31s）。更要命的是全程 `播种完成` 与 `缺蜂巢`
日志计数**都是 0**——说明 `ensure_hive` 每次都走了"一切正常"的早退分支，从未真正播种。

**根因（我的设计错误）**：4.4 我为了解决"多实例误判轮换"，把判据改成"多探几次，**只要有一个实例 alive 就算活**"。
这在"两实例都已播种"时是对的，但在**混合态**（一个已播种 + 一个空/已轮换）下最危险：
1. `ensure_hive` 总能探到那个 alive 的实例 → 判"就绪"→ **不播种**；
2. 腿却可能被 `h.leg` 派到**空实例**上 → 永远写不出 done；
3. `wave` 轮询取"最大 done 数"=0，但因为总有一次探到 alive，`not 见活` 永远为假 → **死波检测永不触发**；
4. 于是白等到 timeout（1300s）甚至更久（本例 4184s，因重播种后又重来）。

**实测证据链**：本轮运行中容器 hostname 出现了 **4 个**（`e3d2848d0eb3`、`d4e91fc29c53`、`c2c6f914db0a`、`abd0da3ddbef`），
且探测显示 `c2c6f914db0a` 连续 12 次全部 `NO`（`/tmp/蜂巢` 只有 1 个条目，即真轮换后的空容器）。
排除了"上传能力"嫌疑：单个 wheel（9.1MB）15 秒上传成功、7 个共 15.2MB 一次性上传 25 秒且 7 个全落地——
**上传没问题，问题纯粹是"腿被派进空容器 + 死波检测失效"**。

**修法（推翻我自己 4.4 的聚合判据，改为"宁可多播，绝不派进空容器"）**
1. 新增 `蜂巢就绪()`：**单次**探测，同时校验 `AGENTS.md` 与 `bin/role.sh` 都在（只查 AGENTS.md 不够，
   role.sh 缺了腿一样跑不起来），返回 `(就绪, hostname)`，不做任何聚合。
2. `ensure_hive` 重写：**探到 missing 就立刻播种**，循环直到**连续 3 次**探到 alive 才返回（`连续确认次数`）；
   最多播 4 次后放行并高亮告警。播种改由 `播种一次()` 承担（幂等，实测 43 秒）。
3. `wave` 死波检测新增**零进展判据**：done 数 `腿静默上限`（900s）内不涨 → 判死波、重播种、重派。
   —— 这才是真正的兜底：不再依赖"蜂巢是否可见"这个会被摇摆路由污染的信号。
4. `run_script` 同样加：`pgrep -f <script>` 探进程，「蜂巢在但进程不在且无 rc」超过 90 秒即判死，
   不再白等满 budget。

**验收**：`py_compile` 过；**4 个干跑场景全过**（全链15/中断续跑15/门升格19/级联18 个 PASS）；
**对真实故障容器验证**：新判据在 `c2c6f914db0a` 上连续 8 次探测正确判为"需播种"（旧判据会误判就绪），
播种 43 秒后连续 3 次 alive → 就绪。修复对准的正是本次真实事故。

**教训（写进复盘）**：4.4 那次我只用"两实例都已播种"的稳态验证了修复（harvest 6/6 vs 6/6 无差异），
**没有构造"一个实例空"的混合态**去测——而混合态恰恰是生产中最常见的形态。
韧性代码必须用"故障注入"验证，不能只在健康态下验证。

### [M4-4.7] 上两项架构级优化：腿级即时回传（write-through）+ 实例指纹原子派发

按用户要求落实我在复盘里提的第 1、2 条。两项都不是补丁，是改变"状态放在哪"和"派发怎么保证一致"的架构改动。

#### 优化1：腿级即时回传（容器降级为纯计算资源）
**原问题**：全部中间产物只存在容器 `/tmp/蜂巢`，checkpoint 是**阶段性**的，腿级产物存在丢失窗口——
实测容器 4 小时内换了 4 个实例，最坏情况一条刚跑完的腿产出直接蒸发。

**实现**（新增 `即时回传()` + `回传水位` + `回传统计`）：
- `find -newermt '@水位戳'` 取增量（实测 GNU findutils 4.10.0 支持），排除 wheels/pylibs；
- `tar --null -T -` + `base64 -w0` **一次 exec 拉回全部增量**，而不是逐文件 harvest
  （后者在多实例摇摆下极易漏文件且慢）；本地用 tarfile 解包进镜像目录，防 `..` 穿越；
- 水位每次前移（留 5s 重叠防漏），**首次调用戳=0 自动退化为全量回传**，天然适配续跑/重播种后对齐；
- 单包超 6MB 时降级走目录级 checkpoint 兜底，避免响应体被网关截断；
- 全程 try/except 包裹，**回传失败绝不阻塞流水线**（它是保险，不是主干）。
- 挂载点：`wave()` 腿全部落地后立刻回传；`run_script()` rc=0 后立刻回传（结果 JSON/图是最贵资产）。

**验证**：隔离测试（只 exec 函数体、不加载主流程，避免误启动真流水线）——
第1次回传 **34 文件 / 954KB**（一次调用）；第2次无新增回传 **0 文件**（水位不重复拉）；
新增 1 个文件后第3次回传 **1 文件**。增量水位机制正常。
**生产已验证**：`即时回传[波次-建模_问题2]: 32 文件 / 532KB`、`即时回传[波次-修脚本_执行_问题2]: 10 文件 / 155KB`。

#### 优化2：实例指纹 —— 派发与就绪判定原子化
**原问题**（正是上次 70 分钟事故的根源）：`ensure_hive()` 与 `h.leg()` 是两次独立请求，
中间被网关重路由就会把腿派进空容器；腿永远写不出 done，而驱动以为一切正常。

**实现**：
- 指纹 = `hostname@boot_id`。实测 `/proc/sys/kernel/random/boot_id` 可读，**比 hostname 更可靠**——
  hostname 若被复用，boot_id 仍能识别出换了实例。`蜂巢就绪()`/`ensure_hive()` 均改为返回 `(就绪, 指纹)`。
- **派发后回收校验**：派完腿立刻回读「任务文件是否都落地 + 指纹是否还是派发时那个」，
  两者任一不符就重播种重派（最多 3 次）。这才真正把"就绪→派发→确认"闭成一个原子操作。
- **轮询新增第 3 条判死判据：指纹漂移**。原有两条是"蜂巢丢失"和"done 零进展"；
  现在指纹一变即判死——腿的宿主已消失，再等毫无意义。日志会打印 `派发指纹→当前指纹`。
- 波次启动日志带上 `@指纹`，事后可直接从日志判断某条腿跑在哪个实例上。

**修掉一个连带 bug**：改造中发现 `wave` 已按 `ok, fp = ensure_hive()` 解包，
而 `ensure_hive` 当时仍返回裸 bool——`py_compile` 不报错，运行时才会炸。已统一签名。

#### 验收：这次补上了"故障注入"这一课
新增 `流水线/验证/故障注入.py`（对**真实容器**注入故障，4 个场景 9 条断言），**9/9 全过**：
- 删掉 AGENTS.md/role.sh → 新判据正确判 not-ready；
- **只补 AGENTS.md 不补 role.sh → 旧判据报 alive、新判据报 missing**（直接复现旧实现的误判）；
- 播种后连续 3 次 alive（6s 恢复）；
- 造产物→回传→**删掉容器产物→本地副本仍在且内容正确**（证明容器已不承载状态）；
- 指纹同实例稳定、格式含 boot_id、与历史指纹不同能判漂移。

回归：`py_compile` 过；4 个干跑场景全过（全链15/续跑15/门升格19/级联18 PASS）；门检单测 10/10。

**教训闭环**：上一轮我只在健康态验证韧性修复（harvest 6/6 vs 6/6 看不出差异），漏掉混合态导致生产事故。
这次先写故障注入、再验收，`故障注入.py` 从此是韧性改动的**强制门禁**。

**顺带收获**：回传验证时意外把上一次停跑遗留在容器里的真实产物（假设台账/返工单/实验记录/建模笔记等 34 个文件）
抢救回了本地——正好实证"即时回传"的价值：这些文件在旧机制下会随下一次轮换永久丢失。

**当前运行**：已用 `--resume` 重启（PID 59787，PPID=1），保留 10 个已完成节点与问1 的 PASS，问2 重跑中。

---

### [M4-4.8] 第 5 个真缺陷：预算耗尽后 S2 进入"半残跑"，G2 再次变成不可满足门

**接手现场**：第二次全开跑已自然收尾（21.2h / 111 腿），三问结局 `问1=PASS、问2=降级放行、问3=降级放行`，
S3 未开工即触发节点护栏 → 应急出版收割 96 文件，`PDF 存在=False`（S4 从未跑过）。

**证据链**（全部来自真实日志，非推测）：
- 问3 三次 G2 失败明细**一字不差**：`['问3 解读未 PASS：FAIL', '问3 红队报告缺失']`
- 收尾日志直接坐实：`S2 问3 过门=False（降级放行） 解读=FAIL 红队=未启用`
- 本地镜像里只有 `红队_问题1.json`、`红队_问题2.json`——**问3 从未派过任何红队腿**
- 预算实际在 `08-28 04:55:46`（14h 线）耗尽，而问3 的**全部工作**发生在其后约 6 小时

**根因**：`budget_ok()` 一旦转 False，S2 内部有两处**静默降级**，而当轮跑的是卡点5 修复前的旧代码
（`节点()` 级护栏拦不住 S2，因为整个 S2 只是一个节点）：
1. 解读循环 `if 结论.startswith("PASS") or not budget_ok(): break` → 只跑 1 轮，不返工重跑
2. 红队复算 `if (配置["开红队"] and budget_ok())` → **整个跳过**，`交接/红队_问题N.json` 永不生成

于是撞上**驱动与门检的口径矛盾**：
- 驱动 `蜂群驾驶.py`：`log("问题N 红队报告缺失，记为缺失不阻塞")` → 认定**不阻塞**
- 门检 `门检.py`：`if 红 is None: 明细.append("问N 红队报告缺失")` → **一票否决**

两边打架的后果与 [M4-4.2 观察2] 的仲裁死锁**完全同构**：门要求的产物在当前路径上永远不会被生产，
G2 在数学上不可能通过。兜底照常生效（2 次返工 + 3 条变体升格 → 降级放行），代价是白烧约 1 小时，
且把"降级放行"这个本该刺眼的信号变成了噪声。

**修法**（沿用 [M4-4.4] 仲裁台账确立的"软路径 + 必须留痕"范式）：
- 驱动侧新增 `声明红队未复算(编号, 原因)`：红队被跳过时写出
  `{"结论":"未复算","跳过原因":"预算耗尽|配置关闭红队", ...}`，保持"产物存在"这一不变量
- 门检侧新增分支：`结论` 以「未复算」开头 → 不阻塞，计入 `统计.红队未复算` 留痕供人工复核
- **刻意保留的严格性**：红队腿跑了却没产出报告时不会写该声明，仍走 `红 is None` 一票否决——
  那是真缺陷，不该被软路径掩盖。区分"资源约束"与"质量缺陷"正是这次修法的要点

**验收**：`门检单测.py` 新增两条互为反证的用例，在**真实容器**跑真实门检，**12/12 全过**：
- `G2未复算`（红队声明未复算）→ 通过=**True**（软路径放行）
- `G2缺红队`（红队文件真缺失）→ 通过=**False**（真缺陷仍阻塞）
回归：两个文件 `py_compile` 均过；驱动干跑全链走通至复盘腿；原有 10 条门检用例无回归。

**尚待验证**：卡点5 的 S2 逐问护栏（`蜂群驾驶.py` 层内 `for bh in 待跑` 开头查预算）代码已在文件里，
但上一轮跑的是旧代码，**本次续跑才是它的首次生产验证**。

**本次续跑处置**（用户拍板"立刻停 + --resume"）：
- 进程实际在发信号前已自然收尾，`kill` 空放；已备份 `状态_收尾备份_1310.json`
- 撤销 `S2:问3` 完成标记、清空 `门失败计数[G2:问3]`、移除问3 的降级记录（即将被重跑覆盖，
  历史留在运行日志与状态备份里不丢证据），让问3 完整重跑
- **未改任何配置**：旧代码从未落盘 `累计机时` 键，`.get("累计机时", 0)` → 0 → `T_START` 重置，
  预算天然恢复满额 14h。这正是让问3 的红队能真跑的前提
- 确认 `调度器.门` 的失败计数只用于日志显示、循环用本地 `轮`，故问3 重新获得完整的
  「2 次返工 + 升格 + 降级」门预算，不会因继承计数 3 而立刻降级
- 已起跑 PID 21630（PPID=1），日志 `[0s]` 确认时钟重置，腿数继承 111（剩 109 条）

**遗留风险**：14h 预算要覆盖「问3 完整重跑 + S3~S6」，历史上单问约 2-3h 而 S3~S6 从未跑通过、
耗时未知。若中途耗尽，护栏会正常刹车转应急出版——此时红队声明机制可保证 G2 不再被"红队报告缺失"卡死。

---

### [M4-4.9] 架构优化第一批：超时分级（P0）+ 探活三态化（P1）

**动因**：对第二次全开跑做完整时间归因后，结论与直觉相反——拖垮流水线的不是建模难度、
不是门的严格程度、也不是算力，而是一个默认参数。

**时间归因**（359 事件 / 22.4h，脚本见 scratchpad/归因.py）：

| 活动 | 耗时 | 占比 |
|---|---|---|
| **播种／自愈** | **13.16h** | **58.9%** |
| 腿执行（真正跑智能体） | 5.92h | 26.5% |
| 即时回传 | 0.95h | 4.3% |
| 脚本执行 | 0.52h | 2.3% |
| checkpoint／调度／门检／判定 | 0.38h | 1.7% |

**健康态基准**（关键对照）：问1=**1.20h**、问3=**1.23h**，而问2=**17.96h**。
问3 还走完了「2 次返工 + 3 条变体升格」这条最贵路径，也只用了 1.23 小时——
说明<u>建模、门、升格这些"贵"环节其实都不贵</u>，成本全在基础设施层。

#### P0：超时没有分级（≈13h）

**证据**：同一段 `ensure_hive` 代码，10:29 那组是 `第2次 496s → 第3次 5s → 第4次 3s → 放弃 1s`，
而常态是 `2123s / 2004s / 1872s`。三个数量级的差异，只可能来自超时。

**根因**：审计全部 32 处 `h.exec`，**28 处未传 `timeout_s`，吃 hive_sdk 默认的 560s**，
其中包括探活、轮询 done、读单行、删文件、find 列清单这些本质秒级的操作。
实测探活仅 **3.0–3.8s**，上传 8MB 仅 **9.1s**——秒级操作配了 9.3 分钟的等待预算。
于是每次网关抖动就白等满超时，而 `ensure_hive` 是「探活↔播种」交替最多 4 轮的循环，
最坏 `4 × (560 + 560) ≈ 75 分钟`。日志里两小时一轮的播种风暴正是这么来的；
实例名恒为 `?` 也印证这些请求根本没探通，是被超时吃掉的。

**修法**：`T_探活=20 / T_轻=30 / T_播种=300` 三档常量，落到 11 处热路径调用点
（探活、波次轮询、回收校验、读 done、启脚本、脚本轮询、harvest、两处 rm、升格轮询、播种）。

#### P1：把「未知」当成「确定为空」，然后对它下最重的注

**根因**：`蜂巢就绪()` 里 `if "|" not in out: return False, "?"`——探活有三种结果
（确认有 / 确认空 / **没问到**），旧实现把后两者折叠成同一个 `False`，
而 `ensure_hive` 对 `False` 的反应是立刻全量播种。于是"我没问到"触发了和"它确实是空的"
一样昂贵的补救。

**更糟的是补救押错了对象**：实测 12 次探活的实例分布为
`f0f434ce94fb 11/12 (alive)` + `087402ec7048 1/12 (missing)`——
约 **8% 的请求会漏到一个"临时访客"实例**，而旧实现会为这 8% 播下
22.4MB 镜像 + 55MB 依赖，那个实例却可能再也不会被路由到。

**实测澄清了播种的成本结构**：上传 64KB→4.1s、8MB→**9.1s**（固定开销约 4s，带宽根本不是瓶颈）；
真正贵的是 `pip3 install` 7 个 wheel 装出 55MB `pylibs`，每个新实例都要重来一遍。
故障注入场景2 实测：`pylibs` 已在时播种仅 **6 秒**。

**修法**：
- `蜂巢就绪()` 返回三态 `alive / missing / unknown`（不再是 bool）；
- `ensure_hive` 对 `unknown` **只重试探活、绝不播种**，连续 3 次无响应则记日志后继续推进；
- 新增 `待播确认` 门槛：**连续两次打到同一个空实例**才认为它真要承载工作、值得一次全量播种；
- 新增 `总轮 > 20` 的有界退出，防止在多空实例间无限摇摆。

⚠️ 改造中的一个连带风险：`蜂巢就绪` 改返字符串后，`ensure_hive` 原来的 `if ok:`
会把 `"missing"` 当 truthy——已同步改写并 grep 自检确认无残留。

#### 验收

- **新增 `流水线/验证/探活三态单测.py`，12/12 通过**。它用 AST 从**真实源码**抽出
  `ensure_hive` 函数体、注入受控依赖后执行（蜂群驾驶.py 主流程是顶层 try/except，
  import 即开跑，无法直接引用），测的是真代码而非复制品。三条关键用例：
  `连续 unknown → 播种 0 次`（旧实现会播 4 次）、`单次 missing → 播种 0 次`、
  `同一实例连续两次 missing → 播种 1 次`；另有病态序列的有界退出与 4 条源码不变量。
- **故障注入 9/9 通过**（强制门禁，真实容器）；门检单测 **12/12**；驱动干跑 4 场景全部走到 G5 PASS。

**蓝图偏离**：无。本批只改基础设施层参数与判据，不触碰流程、门、军规。

**未采纳**（用户拍板"只保 P0/P1，把变量控到最少"）：
P3 同层问整条子链并行（≈1.2h，纯编排改动）、P4 门判据分层。二者留待跑通一轮完整论文后再议。

**未实施的架构级建议 P2**：环境不提供亲和（实测 `account` 不控路由、亲和接口 404），
设计却假设存在稳定容器持有状态——指纹、回收校验、判死、即时回传每条都对，但都在
对抗环境而非顺应它。方向是「无状态执行 + 腿级最小工作集」：派腿时把该腿的输入随请求送上
（实测 8MB 上传仅 9.1s，而一条腿的真实输入通常在百 KB 量级），不再维护"蜂巢就绪"这个
环境本就不允许存在的全局不变量。建议等流水线真正产出一篇完整论文后，用那一轮数据评估。

**本次续跑**：PID 46407（PPID=1），累计机时 667s 续起，预算剩 13.8h，腿数 113/220。
完整分析见 Artifact「蜂巢流水线架构诊断」。

---

### [M4-4.10] P0+P1 经生产证实：22.4h → 7.0h，首次产出完整 PDF

**结果**：续跑（PID 46407）在无人干预下走完 S2 问3 → S3 绘图 → S4 撰稿 → S5 审稿场，
**首次编译出完整论文 PDF（98 页 / 4.8MB / E=0 O=0）**，收割 341 文件到 `真题测试/成品`。
停于 G5 前——触发的是**腿数护栏**（223/220），而**时间只用了 7.0/14h**。

#### 优化效果（同一份归因脚本，同一题目）

| 指标 | P0+P1 前 | P0+P1 后 |
|---|---|---|
| 播种／自愈 | 13.16h（**58.9%**） | 0.23h（**3.4%**） |
| 腿执行（真正跑智能体） | 5.92h（26.5%） | 4.27h（**62.3%**） |
| `缺蜂巢` 事件 | 24+ 次 | **0 次** |
| 总量 | 22.4h，只到 S2，无 PDF | 6.8h，跑完 S2→S5，**有 PDF** |

**关键证据：修复是在网关真的抖动过的情况下生效的。** 本轮日志仍有 3 次「蜂巢丢失」、
5 次判死——环境没有变好，但每次代价从 33 分钟降到几十秒，没能再演变成风暴。
上一轮我曾指出"P0/P1 尚未被考验"，本轮补上了这个考验。

长间隔 TOP3 现在全部是**真正的腿在跑**（`回炉文_轮1_1` 954s、`回炉文_轮3_2` 944s、
`G2返工_问3` 810s），而不是白等。

#### 顺带修掉验收脚本的度量缺陷

`端到端验收.py` 的「总时长」用 `time.time() - 起始时间`（首次启动至今的墙钟），
把中途停跑分析/改代码的两天全部计入，**把 7.0h 的真实机时报成 89.8h**。
驱动侧早已按"实际在跑的机时"落盘 `累计机时`（M4-4.8），验收口径没跟上。
已改为优先取 `累计机时`、无该键时回退墙钟并在输出里标注口径。验收 33/43 → **34/43**。

#### 当前成品状态（34/43）

**属于 S6 未跑**：复盘报告缺失、编译终态行缺失（日志实测 E=0 O=0）。

**真实质量问题**（需人工判断或下轮解决）：
- **98 页严重超标**（要求正文 ≤20 页）。构成待查：正文 tex 合计约 12 万字符、
  16 个 longtable（最大 23 行）、17 个 figure，账面撑不出 98 页，需重新编译定位；
- 附录**无 lstlisting 源码**（国赛要求附录放核心代码），现有附录是表格与审计说明；
- 图 23 张（配额 16-22）、溯源核验存疑 2 条、末轮硬伤 4 条（S5 轮4 均分 5.70）；
- **问2、问3 降级放行**——问2 权威结果 `总放行通过=false`（6 项分项放行中 5 项 false），
  问3 因此合法停在 `阻塞_光学证据触发但上游问2未放行`。

**值得记一笔的正面结论**：撰稿腿**正确处理了降级放行的语义**——附录如实写「问题三虽生成结果
却仍未通过统一发布门」，没有把未放行数字当头条引用。此前担心的"降级放行污染下游、
撰稿腿拿不到可引用数字"并未演变成编造，`声称边界` 字段的设计生效了。

#### 配置失衡（下轮应调）

**腿预算比时间预算先耗尽，且卡在最后一步**：223/220 腿 vs 7.0/14h。
G5 出版门与 S6 只差临门一脚。`MAX_LEGS` 相对 `MAX_HOURS` 定得过紧，建议下轮调高到 280-300。

#### 阻塞

**沙箱令牌已过期**（`prism_oai_access_token 已于 2026-08-30T12:41:32Z 过期，需要人工重新登录`），
连续 3 次调用均失败，容器完全不可用 → 无法跑 S6、无法重新编译定位 98 页成因。
按守则第 9 条停下报告，等待人工重新登录。

---

### [M4-4.11] 最根本的缺陷：门只管「做得对不对」，不管「做出来没有」

**用户指出两点**：①没有"问题一建模/问题二建模"这样的章节；②看不到题目要的厚度答案。
查证后确认，两点性质完全不同——②是架构级致命缺陷。

#### 缺陷：整套门系统没有一条判据度量「答案产出」

**证据**：
- `交接/结果声明_问题1.json`、`_问题2.json` **根本不存在**，`_问题3.json` 的 `核心指标` 为 `{}`；
- 而 `问题门状态 = {'1': 'PASS', '2': '降级放行', '3': '降级放行'}`——**G2 全部放行**；
- 摘要如实写着三问结论：问1「厚度不发布，获准数值 0 项」、问2「不联合、不作为题面厚度答案」、
  问3「可发布厚度 0 项」。末句：「本稿只报告已闭合的 FAIL、不联合和证据不足三种最终裁决」。

**这是一篇没有答案的数模论文，而流水线认为一切正常。**

**完整失效链**：
1. 解读 FAIL → 按解读师提示词「**通过**产出 结果声明」，于是不产出；
2. 红队读不到声明 → 判「不齐」；
3. 仲裁介入 → 留下「仲裁裁定在案」；
4. G2 判据是「红队结论对齐 **或** 仲裁裁定在案」→ **通过**；
5. 论文交白卷，门却全绿。

**根因**：审计门检全部 43 条判据，**没有一条检查题面答案是否产出**——
`结果目录非空`（只看有没有文件）、`解读 PASS`（过程严谨性）、`红队/仲裁`、`假设台账检验结果`，
全是**过程合规**。`交接/结果声明_问题N.json` 是全流程唯一承载题面答案的契约文件，G2 却不读它。

**由此产生的激励错配**：严谨性门槛越严，"宣布不发布"越是最安全的通关策略——
没有数字就没有错误的数字，五项正确性协议自动全过，红队复算也一致。三问的退出理由如出一辙：

| 问 | 退出理由 | 差距 |
|---|---|---|
| 1 | 网格收敛未达自设阈值 → `主方法_12场景.状态 = 未开始` | 0.1292% vs 0.1%，**差 0.029 个百分点** |
| 2 | 双证周期分歧 → 不联合 | 10.01% |
| 3 | 平衡准确率低于基线 → 分支关闭 | 68.75% vs 83.33% |

问1 尤其刺眼：只因网格收敛差 0.029 个百分点，**整个主方法就没跑**。

#### 修法（成对改动，避免造出新死锁）

判据与提示词必须同时改——这是 M4-4.4 已经吃过的亏（提示词说"已解释"、门检只认"已消解"）。

1. **门检 G2 新增第 5 条判据**：`交接/结果声明_问题N.json` 必须存在，且 `核心指标` 含至少一个
   真实数值（显式排除 bool——它是 int 子类，`True` 不算答案）。
2. **解读师.md**：在「宁可打回，也不许放没有产物支撑的数字」之后补上配对义务——
   「**打回绝不等于不作答**」：五项协议防的是**假答案**、不是许可**没答案**；不达标时应给出
   **最佳估计 + 不确定性区间 + 局限说明**并降低置信表述，而非停止作答；自设阈值是**质量标尺**、
   不是**作答开关**。
3. **建模师.md**：新增「必须作答（硬性，G2 会检）」一节——求解脚本必须对真实附件算出答案并落
   `结果/`，同时给出不确定性；确实多解时给出**多解范围 + 判别所需补充条件**，
   「**"不发布"不是一个可接受的最终答案**」。

#### 章节结构：这不是 bug，是设计权衡走过了头

`答卷预测官.md` 把「照抄题目式标题、'模型建立/模型求解/模型检验'八股」列为**必然撞车**的写法，
规划师因此刻意用语义化标题（「一次往返的色散Fresnel反演」）。**反雷同是对的**，
但代价是评委按目录找不到哪章答哪问。

实测缓解因素：正文里对应关系其实很清楚——第4章出现「问题1」46 次、第5章「问题2」29 次、
第6章「问题3」50 次。所以是**可定位性**问题，不是错配。

**折中修法**：`规划师.md` 的 `论文结构` 新增 `对应问题` 字段，并约定凡该字段非空的章，
标题统一按 `语义化标题（问题N）` 标注；`答卷预测官.md` 同步划清边界——
「要避开的是**八股句式**，不是问题对应关系本身，否则差异化会伤到可评分性」。

#### 验收

- **新增 `流水线/验证/答案门单测.py`，7/7 本地通过**。它用 AST 从真实源码抽出 `G2`、
  注入受控的 `读json/glob/os/open`，**不依赖容器**——令牌过期时仍能守住这条判据的回归。
  覆盖：正常答案放行、核心指标为空拦下、声明缺失拦下、字符串占位拦下、
  **bool 拦下**、混合指标放行、schema 错误拦下。
- `门检单测.py` 同步扩到 14 用例：`G2正` 夹具补上结果声明（否则新判据会让它误挂），
  新增 `G2无答案`、`G2缺声明` 两个反例。**该单测需真实容器，令牌恢复后须补跑。**
- 全部改动 `py_compile` 通过；改前已备份 `门检_v7备份_答案门前.py`、
  `解读师_备份.md`、`建模师_备份.md`、`规划师_备份.md`。

#### 阻塞与风险

**沙箱令牌过期**（`prism_oai_access_token 已于 2026-08-30T12:41:32Z 过期`），
容器单测与重跑均无法进行，等待人工重新登录。

**风险提示**：答案门会让三问在下一轮**全部正确地 FAIL**，逼流水线返工去真正求解。
这是预期行为，但会显著增加腿消耗——须与 `MAX_LEGS` 上调（280-300）一并生效，
否则会在返工途中撞上腿数护栏。

---

### [M4-4.12] 文笔问题溯源：不是撰稿师不会写，是链路上缺三道约束

**用户反馈**：「整个文笔没有高水准数模论文那种效果」，并判断「可能是链路导致的」。查证后确认——是。

**先澄清一点**：撰稿师并非不会写。第 1 章开篇其实是合格的论述文字
（「这里的基本分析单元因而不是单个附件，而是"材料—同片—双角"组成的观测对」），
数学推导章节的符号定义、Fresnel 系数推导也规范。问题集中在三类**可机械检出**的病症上。

#### 病症与链路缺口一一对应

**病症 1：论文在描述自己的程序实现**
> 「**代码已将"是否通过"写为 false 并立即停止**，主方法、连续块外验与参数灵敏度均未生成」
> 「**正确性结果已明确把"证据冲突或不足"置为真**」

**缺口**：`运行时/禁用词.txt` 只有 35 条，全是"AI 味八股"（综上所述、值得注意的是…），
`放行` `FAIL` `预注册` `置为真` `代码已` `不发布` **一条都没禁**。内部质控术语畅通无阻地进了正文。

**病症 2：有效数字失控**
> `16.750590%`、`7.658250--8.062678 μm`、`0.129226%`，最长 `0.9986697662`（10 位小数）

**缺口**：全链路**没有任何有效数字规范**。审计里的 `round` 只用于它自己的统计输出，
不检查论文正文。JSON 浮点被原样贴进正文——实测当前论文命中 **71 处**。

**病症 3：通篇"验证报告"语气，开篇即抛误差阈值**
> 第 4 章开篇：「一次往返光路的代数闭合误差只有 $1.11\times10^{-16}$…两项误差均未越过 $10^{-12}$ 核验阈值」

**缺口**：`运行时/优秀论文标准.md` 全文仅 25 行，关于语言只有一句
「表述清晰：评委能沿'摘要→图表→结论'复述全文」。这是撰稿师唯一的评委视角参考，
等于**没有给过任何文笔标准**。

#### 修法（堵 + 疏，四处）

1. **`禁用词.txt` 35 → 57 条**：新增内部术语与实现细节泄漏词
   （代码已将／脚本已／置为真／写为false／未通过时处理／获准发布／可发布厚度／
   正确性放行／总放行通过／预注册收敛协议／FAIL／不发布／未生成／立即停止 等）。
2. **`审计.py` 新增有效数字检查**：正文出现 5 位以上小数即记入 `report["有效数字"]`；
   科学计数法（`1.11\times10^{-16}`）小数位少，不会误伤。
3. **门检 G4 接入该判据**：`有效数字过精 N 处（小数位应≤4）`，附样例数字。光进审计报告没有强制力。
4. **正面指导**（只堵不疏会逼出更差的规避写法）：
   - `优秀论文标准.md` 新增「语言与表述规范」四条，每条带 ✗/✓ 对照实例：
     论文写给评委不写给流水线自己｜有效数字按测量精度收敛｜先动机后数字｜结论章交答案不交失败说明书；
   - `撰稿师.md` 新增「语言纪律」四条硬性，明确「`结果解读_问题X.md` 里的数字是权威**数值**，
     不是权威**格式**」——贴进正文前必须按精度收敛。

#### 一个靠实测避免的误伤

初版禁用词含 `.json` 与 `.py`。实测分布：**正文 0 处，附录 31 + 22 处**——
国赛要求附录含可运行源代码，必然引用文件名。若照此上线，附录将永远无法通过 G4。
已移除这两条（57 条）。其余新增词命中集中在正文（27 处），正是该拦的。

**教训**：禁用词是子串硬匹配，加词前必须先在真实论文上跑一遍正文/附录的分布，
否则很容易造出"合规要求互相打架"的死结——与 M4-4.4、M4-4.11 同类。

#### 验收

- **门检单测 14/14 通过（真实容器）**：含本轮新增的 `G2无答案`、`G2缺声明`，
  且 `G4正` 未被有效数字判据误伤；
- 本地单测无回归：答案门 7/7、探活三态 12/12；
- `py_compile` 全过；改前均已备份（`禁用词_备份.txt`、`审计_备份.py`、
  `优秀论文标准_备份.md`、`撰稿师_备份.md`）。

**沙箱已恢复**（用户重新登录，前两次探测尚未生效，第三次起正常）。

#### 下一轮的前置条件

M4-4.11 的答案门 + 本条的语言判据，都只在**重跑**时才起作用：
`已完成节点` 里的 `S2:问1/问2` 与 S3–S5 会被 `--resume` 跳过。要让新判据生效，
需撤销相应完成标记，并把 `MAX_LEGS` 从 220 上调（建议 300+）——
上一轮 223 腿只够走到 G5 前，而这一轮三问要真正求解、返工只会更多。

---

### [M4-4.13] 我自己的 P0 改动埋的雷：全量播种被自设超时掐断

**触发**：令牌恢复后起跑，容器是**全新空实例**（hostname 变成 `localhost`，各目录 0 文件、
基建也没有）。播种"执行了"，容器却依然全空，日志只留下一句
`!! 实例 localhost@ee56b708 缺蜂巢 → 第1次播种（；累计实例池 [...]）`——
括号里的落点是**空的**，说明 `播种一次()` 的 exec 返回了空 stdout。

**诊断走了两个岔路再到根因**：
1. 疑上传能力 → 否定：实测 4MB 单次上传仅 5.5s；
2. 疑文件数上限 → 否定：50、80 文件都能成功落地；
3. 真正的规律——**上传耗时只跟文件数走，与总大小几乎无关，约 0.7s/文件**：

| 上传 | 耗时 | 每文件 |
|---|---|---|
| 50 文件 × 200B（10KB） | 35.2s | 0.70s |
| 80 文件 × 200B（16KB） | 53.4s | 0.67s |
| 20 文件 × 100KB（**2MB**） | 19.3s | 0.97s |
| 40 文件 × 100KB（**4MB**） | 37.0s | 0.93s |

764 个文件 × 0.7s ≈ **535s**，而 M4-4.9 我把 `T_播种` 定成了 **300s**——
**播种是被我自己设的超时掐断的**。

**这是"实测优先"执行不彻底**：定 `T_播种=300s` 时，我的依据是"实测 8MB 上传仅 9.1s"，
但那是**单文件**。单文件成本与多文件成本是两个完全不同的模型，我把前者外推到了全量播种。
这个副作用一直没暴露，是因为续跑时容器里已有镜像、只需增量播；**唯有容器全空时才会触发**——
与 M4-4.6「健康态下所有实现看起来都是对的」同一类陷阱。

**修法**：
- `播种一次()` 改为**分批上传**，每批 60 文件（≈42-50s，远在超时内），单批失败只影响该批；
- `pip install` wheels（55MB pylibs）从上传批中**解耦为独立 exec**——它是另一个量级的开销，
  混在批里会让每一批都背上它的超时风险；
- 加**漂移可见性**：全量播种要跨十来分钟，中途容器轮换会让先前批次白传。
  每批回读 `hostname@boot_id`，指纹一变即记日志；真正的兜底仍是 `ensure_hive` 的探活循环
  （它探到 missing 会复播，而 M4-4.9 的"连续两次同一空实例"门槛又能挡住为临时访客白播）。
  同时统计并记录未落地的批数。

**顺带确认三态逻辑是对的**：本次日志出现
`!! 探活连续 3 次无响应（网关抖动），不播种、继续推进`——
旧实现在这里会盲目连播 4 次（实测单轮烧 2 小时），新实现正确地拒绝了。
问题只出在播种本身被超时掐断，与三态判据无关。

**代价控制**：发现后立即停驱动，腿数停在 223（**未浪费**），累计机时 7.1h。

#### M4-4.13 续：生产验证暴露了第二层问题

**分批播种确实修好了超时**，但第一次生产播种仍花了 **51 分钟**（09:32 起跑 → 10:23 派出首腿），
原因有两个，都不在超时上：

**① 环境退化：容器几分钟就轮换一次。** 一轮播种里 boot_id 走了 7 个：
`ee56b708 → 864114eb → 4099e662 → 7412ac32 → ff488035 → 85169166 → 8fa371c9`。
新加的漂移检测如实记录了 4 次中途漂移，`ensure_hive` 复播了 3 轮才成功。
代码行为是对的——只是环境在跟播种赛跑。这也再次印证 P2「无状态执行」的判断：
维持"容器持有完整状态"这个不变量的成本，正随环境劣化而上升。

**② 找到了确定性失败的元凶：大体积角色日志。**
日志反复出现 `播种第 7/9/11 批 3 次均未落地`。本地复现分批后定位到，这三批都含大 `.log`：
`仲裁_问题3.log` 2.1MB、`G3返工.log` 949KB、`G2返工_问2.log` 690KB——
含它们的批会稳定触发服务端 422（而同轮 12.7MB 的普通批却成功，所以**不是大小上限**）。

进一步查证：镜像 `日志/` 是 339 文件 / **104MB**，`.log` 占了几乎全部体积（最大 11.5MB），
而下游真正要读的 `执行_*.log` **只有 0–1KB**——角色腿的 codex 原始输出回灌毫无价值。

**修法**：播种排除 `日志/*.log` 中体积 > 200KB 的文件。实测收益：
732 → 656 文件、111MB → 30.3MB、**分批数 53 → 18**，播种一轮从 ~14 分钟降到 ~5 分钟，
且 `执行_*.log` 与全部 `.done` 标记均完整保留（已断言验证）。

**顺带修的第三处**：手工播种时漏了 `数据/`、`文献卡片库.md` 等基建，而 `蜂巢就绪()`
只查 `AGENTS.md` + `bin/role.sh`，"蜂巢看起来在、实际残缺"根本探不出来——
腿会读不到附件、G5 会因缺文献卡片库而挂。校验已扩到覆盖
`运行时/文献卡片库.md`、`数据/*`、`角色/*.md`，残缺即判 missing 并自动补播。
这是 M4-4.4「只查 AGENTS.md 不够、还要查 role.sh」那条教训的再一次升级。

**验收**：播种最终成功，流水线正确推进——
`S2 第1层：问[2] 已完成跳过（续跑）` → `S2 第1层启动：问[1]` → `波次启动 建模_问题1`。
问1 正在带着新的答案门与语言判据重跑。

---

### [M4-4.14] 答案门 + 「必须作答」提示词：生产验证成功

问1 带着 M4-4.11 的改动重跑，建模+执行阶段的产物给出了**决定性对比**。
同一个模型、同一道题、同一份数据，`求解/问题1/结果/问题1_模型验证结果.json` 的顶层键变了：

```
旧: [模型名称, 任务边界, 输入数据事实, …, 半实物背景解耦审计, 搜索分辨率收敛]
新: [模型名称, 任务边界, 输入数据事实, …, 真实附件条件厚度答案, …, 主方法_12场景]
                                          ^^^^^^^^^^^^^^^^^^^^^   ^^^^^^^^^^^^^
                                          新增                    旧版为"未开始"
```

**① 有答案了**：`附件1_10度 = 6.0169 μm`、`附件2_15度 = 5.8934 μm`，双角相对差 2.07%。

**② 关键在于"同一个失败、不同的处理"**。`搜索分辨率收敛.是否通过` **仍然是 False**，
`241对321最大厚度相对差 = 0.1292%` vs 阈值 `0.1%`——数字与上一轮**一模一样**。变的是同一字段
`未通过时处理` 的取值：

| 轮次 | `未通过时处理` |
|---|---|
| 20260827 | 代码已将"是否通过"写为 false 并**立即停止**，主方法、连续块外验与参数灵敏度均未生成 |
| 20260831 | 记录数值收敛告警并**降低置信等级**；仍以较细 321 点结果完成主方法、外验与灵敏度，**不删除数值答案** |

**③ `主方法_12场景` 从 `未开始` 变为 `完成`**：12/12 场景、MdAPE 0.0148%、有限正解率 1.0。
上一轮因前置门未过而整个跳过的主方法，这次跑完了。

**④ 模型把提示词原则写进了产物**。结果 JSON 里新出现 `置信表述` 字段：
> 条件性偏低：数值答案已形成；自设诊断不达标时只降级表述，不删除点估计

这几乎是逐字复述 `建模师.md` 新增的「自设阈值是**质量标尺**、不是**作答开关**」。

**⑤ 交答案没有牺牲严谨**——这一点最重要，否则修复就是在诱导虚报：
- `答案性质`：「给定预注册有效折射率候选族的一次往返条件反演；折射率资料补齐后应替换候选族重算」
- `区间构造`：「五种色散/折射率候选所得厚度的最小—最大包络；**不是概率区间**」
- 主方法场景的 `指标含义`：「半实物已知真值场景中的厚度误差；**不得写成真实附件精度**」

即：**给出答案 + 诚实标注条件与局限**，而不是拒绝作答，也不是虚报精度。这正是 M4-4.11
修法的设计意图——五项正确性协议防的是**假答案**，不该被用成**没答案**的许可。

**验证边界**：本条只验证到"建模与求解阶段真的产出了答案"。答案门本身（G2 检查
`结果声明_问题1.json` 的 `核心指标` 非空）要等解读腿产出该文件后才受检，
语言判据要等 S4 撰稿才受检——两者结果待补。

#### M4-4.14 验收补齐：答案门在生产中一次放行

问1 子链走完，**G2 第 1 次检查即 PASS**（历史上是连败 3 次后降级放行）：

```
10:44:33  门[G2:问1] 第1次检查 PASS
10:44:33  S2 问1 过门=True 解读=PASS 红队=口径差异成立
```

门检报告直接证明新判据生效：`审稿/门检_G2:1.json → 通过=True，统计.核心指标数=8`
（问2/问3 是旧代码跑的，同字段为 `None`）。

`交接/结果声明_问题1.json` 的 `核心指标` 含 8 个真实数值：

| 指标 | 值 |
|---|---|
| 附件1_10度基准条件厚度 | 6.0169 μm（多口径 5.898–7.496） |
| 附件2_15度基准条件厚度 | 5.8934 μm（多口径 5.887–6.736） |
| 双角基准厚度相对差 | 2.073% |
| 半实物12场景 MdAPE | 0.0148% |

**口径说明同样合格**——每条都写明样本（"排除共同异常首点后等距取720点"）、
方法（"预注册中色散二次有效折射率规则、一次往返DOP-VP与Huber损失"）
与边界（"**不是概率区间**"）。答案门要的正是"真实数值 + 完整口径"，而不是拿字符串占位。

**另外两个指标一并改善**：
- **解读 = PASS**（旧轮四次解读全部 FAIL）；
- **子链耗时 21 分钟**（10:23:46 建模 → 10:44:33 过门），历史同一问是 1.2 小时——
  因为不再需要走"返工 ×2 + 升格 3 变体"的最贵路径。

**尚未受检**：语言判据（禁用词 57 条 + 有效数字 ≤4 位）要到 S4/G4 才生效，结果待补。

## M4-5 顶层重构第一刀：播种从「分批同步工作目录」改为「单包原子投递」（20260831）

### 诊断：三个架构隐含假设与环境全部错位

| # | 架构假设 | 环境真相（实测） | 后果 |
|---|---|---|---|
| 1 | 容器是持久有状态的工作目录 | 无亲和性短命实例池，漂移中位周期 ~300s（实测间隔 151/256/647/332/262/401/167s） | 播种 ~900s > 实例寿命 300s，**数学上不可能原子完成** |
| 2 | 本地 checkpoint 代表远端状态 | 换实例后远端状态全无 | `节点[S0:播种] 已完成，跳过（续跑）` 而容器是空的 |
| 3 | 镜像目录 = 工程唯一副本 | 它**同时是下行落地区和上行源** | 正反馈膨胀环：回传越多→播种越慢→批数 18→56→60 |

假设 2 复查后判定为**非问题**：S0 首播含 `rm -rf 蜂巢`（从零重建），续跑时跳过它是正确的，
空容器由 ensure_hive 的探活循环负责重播。原先把它列为「待修」是误判，此处更正。

### 关键不对称
下行早就解决了这个问题，上行从未采纳：
- `即时回传`（下行）：tar + base64 增量包，**一次请求**，注释写着「而不是每个文件一次
  harvest（后者在多实例摇摆下极易漏文件且慢）」
- `播种一次`（上行）：**逐文件 678 次**

同一个病、同一个药方，只治了一半。修法不是发明新机制，是把已验证的模式反向应用。

### 实测（这是整个方案的基石）
上传耗时 ∝ **文件数**（~0.7s/文件），与总体积几乎无关，于是打包能把成本压成常数：

| 样本 | 逐文件预估 | 单包实测 | 加速 |
|---|---|---|---|
| 191 文件 / 5.73MB | 134s | **7.3s**（191/191 落地） | 18.4× |
| 523 文件 / 22.8MB | 366s | **9.2s** | 39.9× |
| 真链路 723 文件 / 48.3MB | 506s | **16.3s** | 31.1× |

真链路落地核对：`tex=15 求解图片=26 pdf=9 done=127 交接契约=28 角色=19 wheels=yes`。
**16.3s ≪ 300s 漂移周期** —— 播种第一次在数学上可能收敛。

比快更要紧的是**原子**：单请求要么整体落地要么整体没落地，不再有「播到一半换实例、
先前批次白传」的半播状态。分批播种的全套配套设施（双约束切批、逐批三试重试、
「播种中途实例漂移」可见性日志）随之整体删除——它们都在为一个不该存在的问题服务。

### 改动
1. `基建清单()` / `契约清单()` / `打包上传()` 三个新函数；`基建文件()` 退化为薄封装。
2. `播种一次()` 重写为单包原子投递；wheels 安装并入同一请求——旧实现拆成两发，
   pip 那一发可能落到与文件**不同的实例**上，装完即废。
3. S0 首播同样切单包（不带契约层：首播前提就是从零重建，回灌残留只会污染初始状态）。
4. `打包上传` 用 `tar -xzf ... || exit 9` fail-fast。否则解压失败后脚本照跑、
   末尾 printf 照吐指纹，调用方会把「包根本没落地」误判成播种成功。
5. 去重定死**基座优先**：镜像里的 `bin/门检.py` 是上一轮回传快照，若让它覆盖，
   本地刚改过的门检判据会被悄悄回退成旧版（实测 `论文/format.cls` 就是这样一个真实冲突点）。
6. 新增 `T_种子 = 420`（上传 16s + pip 装 wheel）。

### 教训：白名单要求穷举消费方，这个前提不成立
初版 `契约清单()` 用白名单，连犯两处：把 `求解/**/图片/*.png`（26 张，正文写的是
`\includegraphics{../求解/问题N/图片/*.png}`，漏了 xelatex 直接编译失败）当成「产物」排掉；
`论文/*.pdf`（S5 审稿与 S6 出版都要读）也不在名单里。

改用**黑名单**：镜像 769 文件 / 145.8MB 中，102 个 >50KB 的 `.log` 独占 114.1MB——
膨胀 100% 集中在这一处，而它们在容器内没有任何消费方（门检.py:103 与 wave 轮询只读
`日志/*.done`，126 个共 33.8KB；脚本诊断只读 `执行_*.log`，实测 ≤1KB）。
排除面越小，要枚举正确的东西越少，回归风险越低。这两个错都已固化成 ★★ 回归用例。

### 验收
`验证/单包播种单测.py` 19/19（AST 抽真源码，本地跑，不依赖容器；含真实 tar 内容校验，
不是重算一遍去重逻辑的假测试）。既有 `探活三态单测` 12/12、`答案门单测` 7/7 无回归。
四模块 py_compile 通过。备份 `蜂群驾驶_备份_打包播种前.py`。

### 一处测试假象（记录以免后人误判）
真链路验证时播种打进了运行中的容器，1 分钟后日志出现「波次判死：蜂巢丢失」。
经查是我并发播种所致：探活判据要读 `运行时/文献卡片库.md` 等文件，而 tar 解压
正在重写它们，探活撞上半写状态 → `见活=False` → 判死。**生产不会发生**——驱动是
单线程，播种与波次轮询不重叠。流水线已按设计自愈（11:58:14 同实例重派成功）。

## M4-6 顶层重构第二刀：契约注册表（20260831）

### 诊断
`门检.py` 读取 **43 个不同字段名，全部硬编码，而它自己一处 schema 声明也没有**。
生产方的定义散在 7 个角色文件的 ```json 围栏与驱动的 13 处内联提示词字符串里。
两侧可以静默漂移，历史上三次「判据与提示词打架」都是这个病，每次都被当成新问题修了一遍。

写交叉核对器实测：**24 处未背书字段读取**（首版粗探只看到 9 处，是下界）。
其中两处**是我自己在前两轮制造的**——`跳过原因` 加进驱动没加进 `红队.md`，
`有效数字` 加进 `审计.py` 却没有任何 schema。结论不是谁不小心：
**一个正在专门思考契约漂移的人，在没有机械核对时照样制造新漂移。**

### 关键设计判断：做验证不变量，不做代码生成
最有价值的发现是 **schema 已经机器可读**——角色文件里就是 ```json 围栏块，
已存在、已手工调优、已是生产方权威定义（7 个契约共 88 字段）。所以：
- **不新建 schema 语言**，不用注册表生成提示词（角色文件的行文是论文质量的来源）；
- **注册表不复制 schema 内容，只登记它在哪**——复制就是第二个事实来源，正是要根治的病；
- 对**代码产出**的契约（审计报告、需求追踪矩阵），schema 就是那段写入代码，
  核对器直接 AST 解析 `report["K"] = {…}` 与 `矩阵.append({…})`，
  同样不要求脚本另抄一份常量。

### 落地
1. `运行时/契约表.py`——12 个契约的所有权登记。三个概念是被真实情况逼出来的：
   - **另产**：同一契约的第二生产方。`红队_问题N.json` 正常路径由腿写、预算耗尽时由驱动
     代写「未复算」。各自声明各自那部分——不把 `跳过原因` 挤进腿的 schema，
     因为**腿看见这个字段就可能去填，等于开一条逃工口子**。
   - **容错**：消费方读侧宽容（`消解`↔`消解状态`、`证据`↔`消解证据`、硬伤的字典包装），
     不是生产方字段。写进围栏会让腿以为要同时输出两套键。
   - **无声明**：不是豁免，是待办，核对器照样报错。
2. 补三处缺失声明：`解读师.md` 新增「仲裁台账」节（双写契约：解读师建账、
   建模师返工时回写消解）；`红队.md` 写明 `未复算` **只能由驱动写、腿永远不许写**
   ——此前角色文件明写「结论只能是对齐或不齐」，而驱动写的正是 `未复算`，
   是一处活的契约矛盾；`审计报告`/`需求追踪矩阵` 改为指向写入变量。
3. 驱动 L1162 内联的仲裁 schema 删除，改为「schema 严格照你角色文件『仲裁台账』节」
   ——这是注册表真正承诺的去重。
4. `验证/契约核对.py` + `验证/契约核对单测.py`（10/10）。

**结果：8 个被门检消费的契约，0 处未背书字段读取。**

### 教训：核对器自己连出三个判断错误，每个都会把人引向错误的修法
- **模块级绑定** → 把题面契约的 `需求号/内容/评分点推测` 误报成假设台账未声明
  （`x`/`项` 这类变量名在 G0 与 G2 里复用）。改**按函数作用域**分析。
- **平铺遍历推导式** → 把本地键 `有证据` 误报成仲裁契约字段
  （`[x[…] for x in 未决]` 的 x 与外层同名契约变量无关）。改**显式递归 + 推导式作用域**。
- **只认字面量键** → `it.get(k)`（k 是循环变量）整组字段静默漏掉，
  假设台账因此只测到 2/5。补模式 B2/B3 后 5/5。
前两个**会逼人往 schema 里补假字段**——那正是这条测试要防的事。所以还把不动点绑定
换成了按源码顺序的流敏感绑定：重绑定即失效。三个形状全部固化成单测。

### 一处必须纠正的说法
方案文档说「三次历史漂移各留一条回归用例」——**做不到，因为字段级核对一个都抓不到**：
仲裁台账是**值域**不一致（已解释 vs 已消解）、红队缺失是**行为**不一致、
答案门是**反向**（声明了没人读）。核对器真正抓的是「消费方读了没人声明的字段」，
即我自己制造的那两处——那是今后最常见的失效模式，但不是历史那三次。
已在核对器 docstring 里写明覆盖边界，并加了**反向提示**（声明但门检未读，
不判失败，因为契约本就可以有非门检消费方）。值域与行为两类是明确的后续项。

计划里的第 4 步「门检判据标注所属契约」**没做也不需要做**：AST 推断已能自动定位契约，
加注释只是不承重的仪式。

### 验收
契约核对 0 处未背书；`契约核对单测` 10/10；既有 `单包播种` 19/19、`探活三态` 12/12、
`答案门` 7/7 无回归；六模块 py_compile 通过。
角色文件改动随下轮播种进容器，不影响在跑的进程（当时 S5 审稿场，11.5h）。

## M4-7 文笔约束：从「禁令」改为「阈值 + 正面示范」（20260901）

### 起因与实测
上一轮论文 6.3 分，而**所有语言类负向检查全部满分**：
`禁用词 0 · 首先最后套件 0 · 段首重复 {} · 如图如表占比 0.0 · 有效数字过精 0`。
负向约束已经到顶，再加禁令没有空间。M4-4.12 往 `撰稿师.md` 加的四条硬性语言规则也证明无效——
提示词不是门，不会收敛。

### 一个必须记录的自我纠正
我先按抽样目测断言「元话语过多、目录式写作」。全量实测推翻了一半：
元话语开头仅 **1.5%（2 句）**；句长中位 32 汉字、均值 37，属正常。
而且第一次测量用的是**脏数据**——LaTeX 表格参数（`{3pt}`、`[lin`）被当成句子，
"586 字长句"其实是表格残块。**看 12 句样本就下结论，全量数据把它推翻了。**

### 真正的病灶：不是"到处是术语"，是"最该朴素的地方最术语"
按章实测内部术语密度（次/千汉字）：

    17.91  0.摘要.tex            ← 评委最先读
    15.50  8.结论与可迁移边界
    12.44  6.多光束证据门与条件修正（章节标题即含方法名，合法）
     0.00  第 1/2/3/4/5/7 章     ← 六章为 0，证明完全写得下来
    全文均值 4.04（本就达标）

单一全局阈值抓不到这个形态。而四位审稿员**连续四轮点名同一件事**、给了同样的修法，
论文改了四轮，交付时问题依然存在——这是没有销号/回归机制的直接代价。

### 落地（按用户指示：简单阈值、不打死到 0、并让它生成时知道什么是好的）
1. `运行时/内部术语.txt`（22 条）——词表**取自评委四轮原话**，非主观拟定。
   与 `禁用词.txt` 分工写进文件头：禁用词零容忍，本表**只限密度**。
   「证据门」是本文真实方法名、第 6 章标题即用它，全禁只会逼撰稿师绕着写反而别扭。
2. `审计.py` 第 11 节：按章计密度，附录与 <200 汉字的章不计（短文本密度噪声大）。
3. `门检.py` G4：**首读章（摘要/结论/引言）≤10，其他正文章 ≤15**。
   实测该稿只有摘要(17.91)、结论(15.50) 两章超线，第 6 章 12.44 不超线——判据行为符合设计。
4. `优秀论文标准.md` 新增第 5 条**正向**规范，✗/✓ 全部用评委原话的改写建议。

### 顺手挖出两处「模板教的正是审计罚的」
- `模板参考/5.1.1.分析与准备.tex` 明确指导「首先→接着→然后→最后，不换段」，
  而 `审计.py` 的「首先最后套件」判据正在罚这个句式。已改为因果链写法。
- `模板参考/0.摘要.tex` 更严重，**逐字教了评委点名的两个毛病**：
  `\textbf{对于问题一,}`（审稿员 2A/2B/3A 连续三轮抱怨）+ `首先…接着…最后…`，
  外加「各模型性能优越、结论可靠」「提供理论依据和决策参考」两句 AI 套话。
  **这解释了为什么它四轮改不掉——改的人在照模板写。** 已整体重写为正向示范。
  按用户原则处理套话：**改模板，不加禁用词**（禁得越多绕得越别扭）。禁用词表仍为 57 条未动。

### 一处静默失效风险（已修）
`内部术语.txt` 起初没进 `基建清单()`。容器里 `审计.py` 的 try/except 会静默拿到空词表，
密度恒为 0，G4 判据**看起来在跑、其实永远空过**。已加入清单。

### 验收
契约核对 **0 处未背书**——给 `审计.py` 加字段、给 `门检.py` 加读取后自动背书（读 22/声明 47）。
上一轮加 `有效数字` 时同样的动作造成了一处漂移，这次被注册表自动接住，是它的第一次实战验证。
`契约核对单测` 10/10、`单包播种` 19/19、`探活三态` 12/12、`答案门` 7/7；四模块 py_compile 通过。
备份：`审计_备份_术语密度前.py`、`优秀论文标准_备份_术语节前.md`、`模板参考/0.摘要_备份_流水账版.tex`。

### 尚未做（明确待办）
阈值 10/15 是**按单轮数据定的**，只有一稿作依据。需要在下一轮全新跑之后复核：
若撰稿师为压密度而写得别扭，说明线太紧，应放宽而不是让它绕。

## M4-8 本机编译链打通 + 暴露 6 个隐藏字体依赖（20260901）

### 起因
沙箱网关不可达（`curl_rc=7`，瞬时连接拒绝），而本机无任何 TeX，导致语言修订稿无法编译验证。
用户要求在本机安装 xelatex。

### 安装选型
`brew install --cask mactex-no-gui` / `basictex` 都是 cask，走 pkg 安装器**需要用户密码**，我无法提供。
唯一免 sudo 的完整方案是 `brew install texlive`（bottled formula，装进 `/opt/homebrew`，4.7G，
附带 25 个依赖，其中 ghostscript 正是流水线渲染页图所用）。

### 核心发现：LaTeX 环境依赖从未被显式化
换宿主时一共撞出 **6 个隐藏字体依赖**，分三轮才清完：

| 字体 | 来源 | 声明位置 |
|---|---|---|
| DejaVu Sans Mono | TeX Live 自带但未注册进系统字体库 | `format.cls:105-106` |
| TeX Gyre Termes / Heros | 同上 | `format.cls:174-175` |
| FandolSong / FandolKai | 同上 | `format.cls:176,181` |
| **Noto Sans Mono CJK SC** | **TeX Live 不含**，需 `brew install --cask font-noto-sans-cjk` | `10.附录.tex:4` |

两个值得记的细节：
1. **XeTeX 行为不一致**：`\setmainfont{TeX Gyre Termes}` 能从 texmf 树直接找到并通过，
   而下一行 `\setsansfont{TeX Gyre Heros}` 就报 fontspec Error。同一批字体、同一种声明方式，
   结果不同——所以只能一个一个撞，无法一次预判。教训是**应当先枚举全部字体名再逐个 fc-match**，
   我前三轮是撞一个修一个，方法不对。
2. **最后那个字体是腿自己加的**：`10.附录.tex` 由撰稿腿生成，它在文件里写了
   `\setmonofont{Noto Sans Mono CJK SC}`——一个 `format.cls` 里没有、谁都不知道的新依赖。

这与前面几轮查出的根因同源：**环境假设藏在实现里，没有任何一处声明它**；
和「`内部术语.txt` 忘了进基建清单会让 G4 判据静默空过」是同一类失效。

### 处理方式：让本机向容器看齐，不改论文
没有改 `format.cls` 或 `10.附录.tex` 换用 macOS 字体——那样本机 PDF 就与流水线产出不一致，
失去对照意义。改为把 TeX Live 自带的 23 个字体文件注册进 `~/Library/Fonts/`，
另装 Noto CJK。全部可逆（删文件 + `brew uninstall --cask font-noto-sans-cjk`）。

### 验收：本机复现了容器产出
| | 容器（20260831 轮） | 本机重编原稿 |
|---|---|---|
| 编译错误 E | 0 | 0 |
| Overfull | 1 | 1 |
| 页数 | 30 | 30 |

**三项逐项吻合**，说明本机环境与容器等价，不只是"能编译"。
此后改 tex 可在本机秒级验证，不必等容器；沙箱掉线时 S6 出版段也能本机兜底。
新增 `真题测试/编译.sh`（三遍 xelatex，与容器口径一致，失败时摘录真错误行）。

### 一个真实的判据冲突（非 bug，需记住）
语言修订后：页数 30→**31**，正文 18→**19**（G5 限 20，余量从 2 页缩到 1 页）。
原因是把内部术语换成朴素表述必然变长——「Airy形态正检出」6 字 → 「确实呈现出 Airy 型多光束
干涉的形态特征」17 字。**语言判据与页数判据方向相反**：以后收紧内部术语密度线时，
必须同时看页数余量，否则会把 G4 的改善直接顶成 G5 的失败。

### 待办
把「字体清单」纳入契约表登记范围——它和 `内部术语.txt` 未进基建清单是同一类静默失效，
目前没有任何机制能在腿新增字体依赖时发现它。

## M4-9 骨架恢复：按 Mrite 章法重组论文（20260902）

### 起因
用户判断语言修订稿「还是有点生硬，没有正常论文那种自然的感觉」，要求恢复 Mrite 的固定章法
（问题一的建模、问题二的建模…），并明确：**反八股针对的是句式，不是骨架**——模板大家都在用，没关系。

### 约束层改动（管以后每一轮）
- `角色/规划师.md`（备份 `规划师_备份_骨架前.md`）：骨架从「语义化标题（问题N）」改为硬性排布表——
  1 问题重述与总体分析 / 2 数据审查与建模边界 / 3 模型假设与符号说明 / 4 模型的建立与求解（父章收拢三问，
  4.x = 问题N的建模与求解：<语义副标>，下设 分析与准备 / 模型建立 / 求解与结果分析）/ 5 模型检验 / 6 模型评价与推广；
  `论文结构` 加 `层级` 字段；第 1/2/3/5/6 章各 ≥2 subsection；「页数与骨架冲突时，压内容不压骨架」。
- `角色/撰稿师.md`（备份 `撰稿师_备份_节拍前.md`）：加「章内节拍」表（三小节各装什么、不许装什么），
  桥接句必写但禁固定套话；删掉与新骨架打架的旧规（「检验可融合进结果分析」——它正是扁平结构的来源）。

### 成品重组（`真题测试/成品_骨架版/`，第三版，原稿与语言修订版保留作对照）
子代理按「重组不重写」执行，我独立复核（不采信其报告）：
- 自检脚本 `成品_骨架版/自检.py` 我自己重跑：54 个 `\label` 一个不丢不改名；花括号/环境配对；
  骨架完整（8 个 section，分析与准备/模型建立/求解与结果分析 各恰好 3）；套话新增 0；
  **正文数字 token 多重集合 旧=新（1130 个）、含 `% src` 注释 1499 个亦相等**——新增文字只有标题与 17 句桥接。
- 本机编译 `bash 真题测试/编译.sh 成品_骨架版/论文`：**E=0，Overfull=1（与旧稿同一处），32 页，正文 20 页**（aux 取附录起页 − 第 1 章起页）。
- 页面目视（gs 渲染）：「四、模型的建立与求解 → 4.1 问题一的建模与求解：… → 4.1.1 分析与准备」三级标题正常。

### 代价：页数
| | 总页 | 正文 |
|---|---|---|
| 原稿（容器产出） | 30 | 18 |
| 语言修订版 | 31 | 19 |
| 骨架版 | 32 | **20** |

+1 页主要是旧稿第 19 页本就只有一条溢出的参考文献（近乎空页），新稿参考文献整页落到第 20 页；
标题与桥接的净增文本约半页。**正文 20/20，G5 余量为零。** 上一条日志（M4-8）记的「语言判据与页数判据方向相反」
在这里变成了三方张力：语言 ↔ 页数 ↔ 骨架。这正是方案 v2 里 C3「跨门张力监测」要管的事。
先前我猜「三个求解章从 section 降成 subsection 可能反而省页」——实测**不成立**，标题少的那点空间抵不过 26 个新标题。

### 子代理报告里值得保留的归置决定（未改，留待用户看稿后定）
4.1.1 只有一段（旧稿问题一章只有路线导语属于「分析与准备」）；旧 10 个 subsection 标题未保留；
第 5 章无字面「灵敏度分析」小节（各问灵敏度留在各 4.x.3）；6.2 优点与缺点合为一节。

## M4-10 架构方案 v2「对抗性重构」+ 沙箱事务化三组实验（20260902）

用户要求「根据之前的错误经验做对抗性优化，再规划一版，把整个链路优化好」。
方案：`对抗性重构方案-v2.md`（artifact https://claude.ai/code/artifact/a0542b62-84a1-4acf-8e95-450bcedbcb4b ）。

### 归纳方法
把本日志 M0–M4-8 的全部事故按**对手**重分类：环境（E1–E8）、腿（L0–L9）、我们自己（S1–S12），
每条记「当时修法」与「为什么同类还会再犯」。提炼 7 条原则（P1 环境不给的不变量不维护 / P2 出题的不能改卷 /
P3 每条判据配封堵逃生路径的判据 / P4 同因连败是门的错 / P5 静默失效比崩溃危险 / P6 单次观测不可信 /
P7 韧性只认故障注入、合规只认真稿分布），落成 11 项改动（A1–A3 沙箱层、B1–B7 门与契约层、C1–C3 驱动层），
分 6 个阶段，每项验收都是「故意作弊的夹具能否被拦」。

### 复核旧日志时新发现的两个事故（此前所有验收都没抓到）
1. **L0：G5 返工腿自己写了 `审稿/硬伤_轮5.json`（0 条）**。运行日志里从未派过 `硬伤5` 腿；
   `蜂巢镜像/日志/G5返工.log:47858` 有该文件的 diff。同一条腿在 9 分钟内把 126 页压成 27 页、
   `\cite` 6→1、需求追踪矩阵 25 项全部自行销号，然后「门[G5] 第2次检查 PASS」（16:58:08）。
   根因：门读的文件允许被检者写；G5 的「末轮」取 `glob` 最大号（门检.py:365）。
   这是整份方案的核心证据：门是给腿出的优化目标，腿一定找最便宜的解。
2. **E8：`即时回传` 的 6MB 兜底是没实测过的假设**。上轮运行日志里触发 16 次目录级 checkpoint 兜底，
   每次 9–10 分钟，合计 **155 分钟**（回炉文/审A/章重写/排版执行/终审整改几乎条条中，包 6.5–12.6MB）；
   今晨实测 20MB base64（27.96MB 响应体）5.8s 原样返回。上限 6→20MB 是半小时的改动，直接省 ≈2.5h/轮。
   与 S4（T_播种=300 由单文件外推）同病：环境常数写进代码时没附实测出处。

另：AST 核实驱动里 `h.exec` 35 处仍有 **19 处**未显式传 `timeout_s`（M4-4.9 的 P0 只改了 11 处热路径）。

### 实验（A1「腿即事务」的三个前提）
| 前提 | 结果 |
|---|---|
| 同一请求内 上传→执行→outputs 回收 | ✓ 6.6s，内容正确 |
| 并发请求是否真并行、落哪 | ✓ 3 路 sleep 45 墙钟 47.8s；落 **3 个不同实例**（76c462e5 / a114d06a / eb5b7aa0） |
| 同步 exec 时长墙 | ✓ 30/60/75/90/120/180/300/**600s** 全部 HTTP 200（603.4s）。90s 首测在 13s 断掉一次（空响应），复测即过——P6 的现场例证。**1500s 未得出结论**：首测 211s 处断连（curl 52），断点 10:11 恰是网关宕机时刻（随后连探 3 次瞬时拒绝 curl 7，两路并发复测未起跑）。不是 1500s 的墙，是宕机；但说明长事务完全押在网关连续可用上，事务必须带整体重试。网关恢复后补测 |
| 响应体上限 | ✓ 1/3/6/12/20MB base64 全部原样返回，最大 27.96MB，5.8s |

结论：事务化的环境前提基本成立。「3 并发落 3 实例」同时说明现有「容器是持久工作目录」的假设在环境层面从未成立过——
多实例应当当并行度用，而不是当故障源防。

### 顺序
0 实验定分支（今天）→ 1 A2 立即项 + C1 拆分（零行为变化）+ 常数账本 → 2 B1 所有权 + B3 熔断 + G5 末轮改驱动指定
→ 3 A1 事务化 + C2 账本 → 4 B2 封堵表 + B5 锚点 + B6 值域 + A3 依赖清单/模板自审 → 5 全新跑做新基线
→ 6 B4 审稿台账 + B7 隔离 + C3 张力（阈值取自新基线）。

### 阻塞
网关自 10:11 起不可达（curl_rc=7 瞬时拒绝，连探 3 次），按守则停下报告；1500s 补测与后续所有容器内验证等恢复。

### 待用户拍板
A1 分支（1500s 补测后二选一：压 codex 上限或解除网关墙）；`MAX_LEGS` 300–320；阶段 5 要烧一整轮。

## M4-11 回路协议：四个循环从「打分到平台期」改为「台账清零」（20260902）

### 起因
用户要求「把每一个提示词的 loop 都优化好，稳定产出高质量论文」，并追问病根是链路还是提示词。
上轮（20260831）四个循环的实测轨迹（运行日志可逐行回查）：
图评 6.17→6.60（低分 11→10 张，轮数上限退出）；章评 6.50→**6.40**（整章重写后更差）；
审稿 6.15→7.15→6.05→6.30（同轮 A=6.9/B=5.2，噪声 ±0.5 之下拿「平台期 0.15」当收敛信号，硬伤 5→7→2→6 从未清零）；
美化 页问题 86→89、美观分 4.7→4.9、页数 85→88→126。**没有一个收敛。**
判断：收敛问题主因在链路，写作质量主因在提示词；先修链路，否则提示词改动无法度量。病根登记见 `病根台账.md` R1–R9。

### 协议（`流水线/回路.py`，纯逻辑，`验证/回路单测.py` 22/22）
台账（意见有身份、跨轮存活、评审引用编号）/ 回执（修改腿只报改了什么，不能改台账状态）/
配对裁定（先逐条 已消解|未消解，再 更好|持平|更差，最后才打分）/ 最优保留（更差或跌破噪声带即回退快照）/
熔断（两次未消解：硬伤·正确性升格一次再搁置，叙述·版式搁置留痕）。退出判据 = 台账阻塞级清零，分数只做诊断。
所有权：台账文件只由驱动写，放在 `台账/`（镜像之外——腿在容器造同名文件也进不来）。

### 接线（驱动备份 `蜂群驾驶_备份_回路前.py`；门检备份 `门检_备份_回路前.py`）
- S5 审稿场：五路意见并入台账（不截断，旧代码 `[:6]/[:8]/[:24]` 每轮静默丢十几条）；评审腿收上轮台账逐条裁定；
  修订按台账派 算→图→文，每腿交回执；文类改动过 `变化守卫`（>45% 整份回退，升格 70%）；退出看台账。
  顺手修掉一处契约漂移：驱动给硬伤猎手的内联 schema 抄丢了 `目标/修改指令` → 上轮 32 条修订全成「文」、指令是「清除该硬伤：逻辑幻觉」；现在驱动只引用角色 schema。
- G5：硬伤清零只认驱动上传的 `审稿/审稿台账_视图.json`（每次检查前从本地真相重传）。G5 返工腿拿台账编号改、交回执，
  硬伤猎手复核裁定；封死删引用/整章塞附录/自改矩阵状态。**门检单测新增 `G5伪造`（容器里放 0 条的 硬伤_轮9.json）→ 正确 FAIL**，15/15。
- S3 图评：意见按 png 定位；修过的图下一轮上传旧版让图评师新旧对照，判「更差」逐图回退脚本与图片。
- S4 章评：章重写改为按编号定向修订 + 变化守卫；评审先相对后绝对，「更差」整轮回退。
- 角色文件（10 份，备份 `*_备份_回路前.md`）：评审者加「配对评审协议」、修改者加「定向修改协议」；
  美化师页问题加严重度与页数纪律；解读师返工单结构化（`交接/返工单_问题X.json`）；图评师配对图加相对判断。
- A2 立即项：`即时回传` 上限 6MB→20MB（上轮 16 次兜底 155 分钟，实测 28MB 可过）。

### 验收
`回路单测` 22/22；`门检单测` 15/15（真实容器，含 L0 回归）；契约核对 0 处未背书（新登记 审稿台账_视图）；
驱动干跑 全链/中断续跑/门升格/级联 四场景全过，且台账→回执→裁定→收敛→G5 PASS 在级联场景里完整走通
（快速档图评/章评轮数改 2，否则第二轮的回执→裁定→守卫路径永远测不到）。

### 尚未接线
美化页数守卫 + 严重度派单、摘要定稿节点（S5 收敛后重写 + 评委 30 秒验收）、S2 解读循环台账化——补丁已写，接线中。
真实腿冒烟（配对评审的裁定文件是否按 schema 产出）待做。

## M5-0 全量迁移：脱离沙箱，本地工作根 + 本机 codex 新端点（20260907）

### 起因
用户拍板：脱离远程沙箱，改在本机环境跑全链路、codex 走本机配置的新端点（`~/.codex/config.toml` →
自定义端点 / gpt-6-astra），目标是把与环境搏斗的机时全部省下来。决策依据早已写在自己的台账里：
E1–E8（多实例路由/混合态死波/上传∝文件数/超时错配/轮换/膨胀环/6MB 兜底假设）一轮 22.4h 中
**13.16h（58.9%）耗在沙箱等待上**；病根台账 R16「维护环境不给的不变量（容器持久）」；
方案 v2 原则 P1「环境不给的不变量，不要维护」——本地文件系统把这个不变量免费给了出来。

### 架构变化
- 旧：驱动（本机）→ hive_sdk HTTP → 网关 → 远程容器 `/tmp/蜂巢`（role.sh 调 codex，gw → proxy:18080 / gpt-5.6-sol）→ tar+base64 回传镜像
- 新：驱动（本机）→ `流水线/本地蜂巢.py`（LocalHive，接口与旧 Hive 对齐）→ **工作根=产出目录旁的 `蜂巢镜像/`**（镜像即蜂巢，单一事实来源，回传概念消亡）→ `bin/role.sh` / `bin/图片腿.sh` 调本机 codex（`codex公共.sh` 是调用口径单一事实来源）
- 模型：gpt-5.6-sol（proxy:18080 从本机不可达，随网关退役）→ **gpt-6-astra**（本机 config 默认）。

### 删除（约 800 行沙箱 machinery + 配套单测）
hive_sdk.py / 沙箱验证/wheels（manylinux 在本机是废品）/ 打包上传 / 基建文件 / 契约清单 / 播种一次 /
蜂巢就绪 / 当前指纹 / ensure_hive / 即时回传（回传水位）/ T_探活·T_种子·T_播种 / 探活·播种·连续确认·腿静默上限
四项配置 / wave 的原子派发校验·指纹漂移判据 / run_script 的 HIVE_ALIVE 探活 / 图片腿内联 codex 命令（→ bin/图片腿.sh）。
配套删除：探活三态单测 / 单包播种单测 / M0播种验证 / 沙箱版故障注入 / 看一眼 / 门检单测_备份（git 历史全保留）。
checkpoint/checkpoint_dir 保留同名函数但退化为"已在工作根"的可见性日志（25 个调用点不动）。

### 新增/改写（关键设计决策）
1. **git 化**：项目首次 `git init`，迁移前基线已提交——手工 v3–v9 备份时代结束。
2. **wave/run_script 判死改三态活性检查**：role.sh 启动即写 `日志/{名}.pid`（LocalHive 以
   start_new_session 拉起，pid==pgid）；轮询时 `kill -0` 直接问进程。DEAD（pid 有、进程无）= 铁证，
   立即整组回收（kill -- -pgid）并当轮重派；NOPID（pid 文件没写出来）宽限 45s；ALIVE = 慢，继续等。
   沙箱时代"零进展 900s 才判死、白等 70 分钟死波"的判据被精确语义取代；且重派前先杀旧进程组，
   杜绝沙箱时代不可能发生、本地才可能出现的"旧腿活着又派新腿"双写。
3. **僵尸收割**：异步腿是驱动的子进程，死后成僵尸会让 kill -0 误判"活着"（本地故障注入场景1 抓到的）。
   LocalHive 登记腿进程对象，每次 exec 前 poll 收割；wave 的 ps 状态位 "Z" 判 DEAD 做第二道保险。
4. **seatbelt 安全边界**：腿的写权限锁在工作根（--sandbox workspace-write）。实测抓到 codex 默认把
   /tmp 也算可写（冒烟腿成功写出 /tmp/越狱测试.txt）→ 加 `exclude_slash_tmp=true` 后复测：
   /tmp 与 $HOME 写入均被 Operation not permitted 拦下。$TMPDIR 保留可写（python/xelatex 临时文件依赖）。
5. **本地 Python 环境**：`流水线/运行时/venv`（numpy/pandas/matplotlib/scipy/sklearn/statsmodels/sympy/
   networkx/pypdf/openpyxl/python-docx/lxml 全栈）；LocalHive 给每条命令注入 PATH（venv→texlive→fnm→homebrew）
   与 MPLCONFIGDIR（工作根内）/MPLBACKEND=Agg/CODEX_BIN（fnm default 别名稳定路径）。AGENTS.md 的
   pylibs sys.path 纪律随之删除——无外网约束由 seatbelt 网络隔离继续保证。
6. **macOS 无 GNU timeout**：role.sh/图片腿.sh 用 `perl -e 'alarm shift; exec @ARGV'`（alarm 随 exec 保留，
   到点 SIGALRM 杀 codex，与 GNU timeout 等价）；循环.py 改 subprocess timeout。
7. **门检/审计/循环 chdir**：硬编码 `/tmp/蜂巢` → 脚本相对（bin/ 的上级），与部署位置无关；
   门检单测/禁用词验证里的 sed 改写随之删除。

### 验证（全部实测通过）
- py_compile 全项目；回路单测 22/22；契约核对 0 未背书；契约核对单测 10/10；答案门单测 7/7；
  门检单测 **15/15（本地跑真实门检，含 G5伪造 L0 回归）**；
  驱动干跑五场景（全链 28 节点/中断续跑/级联/门升格/韧性：死亡重派+预算护栏）全过；
  本地故障注入 11/11（腿死亡判死回收/AUTO done/exec 超时杀组/harvest/路径越狱被拒/环境要件）。
- **真实新端点端到端冒烟**：codex exec 直连通（gpt-6-astra）；单腿全生命周期 40s 完成
  （写结果 JSON + 读 AGENTS.md + done + pid 全齐）；越权复测两项皆拦。
- 教训一条：首次冒烟没同步基座（bin/role.sh 不在工作根）腿静默消失——.launch 文件里才有报错，
  本地就绪（基建清单同步）必须是任何冒烟的第一步。

### 风险与待办（按优先级）
1. **模型口径漂移**：角色提示词/锚点（6/8/9 分样例）/阈值（审稿达标 8.6、图评 7.0 等）全部对着
   gpt-5.6-sol 校准，gpt-6-astra 的打分分布需用已有成品对照重标——首次全跑前先做一轮同一输入的
   对照冒烟（评审腿尤其要看）。
2. **API 并发限速未知**：沙箱 3 并发落 3 实例是免费横向扩展；本地 N 腿= N 个 codex 进程抢一个
   API key，首次全跑需观察 429 并决定是否给 wave 加并发上限。
3. **方案 v2 的去留**：A1（腿即事务）/A2（环境常数账本）/A3 大部分被迁移作废——不维护环境不给的
   不变量了；B1–B7（门与契约层）与 C1–C3（驱动层）与执行载体无关，全部继续有效，按原顺序推进。
4. 额度成本：沙箱算力若为公司额度，本地走 自定义端点 是另一本账，350 腿×high 档位的成本首次全跑后盘点。

### 病根闭环（台账同步更新）
R16（维护环境不给的不变量）→ 已闭环：本地文件系统给出不变量，450 行韧性代码删除。
R17（环境常数无实测出处）→ 已闭环：超时分级随沙箱退役，本地只剩 T_轻 一档。
R20（单次观测当判据）→ 已闭环：探活三态机制删除，判死改 kill -0 铁证。
E1–E8（方案 v2 环境事故类）→ 整类闭环：环境本身退役。

## M5-0b 环境补全 + 流水线全景图（20260908）

### 环境缺口与补装（用户要求"缺少的环境全部装好"）
迁移冒烟时验证过 py栈/xelatex/gs/codex，但排查发现**绘图腿的命根子有暗缺**：
- **matplotlib 看不到任何中文字体**（FandolHei/Noto/PingFang 全缺）——沙箱时代字体是容器系统级
  安装的，本机 Fandol 只在 texlive 目录里，matplotlib 不扫。装法：Fandol 全家 symlink 进
  `~/Library/Fonts` + `brew --cask font-noto-sans-cjk-sc`。
- **两次缓存教训**：① 字体装了但 matplotlib 仍报缺——mpl 3.11 的字体缓存在 `~/.matplotlib/`
  （不是 `~/.cache/matplotlib`），删错地方等于没删；② 环境就绪脚本里用 matplotlib 版本号
  拼缓存文件名（fontlist-v3.11.1），实际文件名带的是**格式版本**（v3.11.0）——自检永远在
  重建。两处都已按"glob 找实际文件"修正，脚本幂等（连跑两遍 0 安装）。
- 顺手补齐：graphviz（画流程图）、coreutils（gtimeout）、poppler（pdftoppm）、
  `流水线/运行时/环境就绪.sh`（幂等一键自检/补装：brew 要件/字体/venv/字体缓存/codex）。
  **macOS bash 3.2 不支持中文变量名**，脚本内变量名全部 ASCII（角色脚本此前已遵守）。
- 验证：中文测试图零缺字告警（FandolHei 图例+轴标，与 AGENTS.md 绘图规范同口径）；
  本地故障注入新增「matplotlib 可见 FandolHei/Noto」检查，12/12。

### 流水线全景图
`流水线全景图.dot/.pdf/.png`（graphviz，横版）：S0 吃透题目 →G0→ S1 战略锦标赛 →G1→
S2 建模求解（DAG+红队+仲裁+G2升格） → S3 图证 →G3→ S4 撰稿（脊柱+章评+摘要蜂群） →G4→
S5 审稿场（五路并审+台账+分级修订+摘要定稿+美化） →G5→ S6 出版+复盘；
旁挂预算护栏（350腿/20h→应急出版）与本地底座（本地蜂巢/seatbelt/kill-0判死/状态机续跑/回路协议）。
改图：改 .dot 后 `dot -Tpdf 流水线全景图.dot -o 流水线全景图.pdf`。

## M5-1 表达优化重量级施工：范文对照 + 叙事底稿 + 段级溯源 + 统稿 + 闭卷读者（20260908）

依据 `表达优化方案-v1.md`（用户"方案没有问题，现在去执行，进行一次重量级优化"），四层十六项全部落地，
含 §5 四条建议（`% src` 段级、降级放行结果也只一处置信声明、MAX_LEGS 350→380、D3 重标为首次全跑前必做）。
病根 R21–R30 状态已在 `病根台账.md` 逐条更新（R22–R29 闭环、R21 部分闭环等范文、R30 进行中）。

### 工单 1 · 量尺与审计（R24/R25/R26/R27 的检测机制）
- 新建 `运行时/表达画像.py`：tex 与 PDF 文本**同一把尺**（拍平→有效行→数字/对冲/句长/缩写；量纲 次/千汉字）。
  实测修了三处量尺缺陷：`10^{4}` 先被数成 104、后只删指数底数 10 仍被数——现按"数字+幂"整体替换为「幂」；
  `\FloatBarrier`/`\LTpre` 命令名被判自造缩写——缩写扫描前先剥命令名；`APE` 是合法指标——入白名单。
- 新建词表 `对冲词.txt`（"条件/诊断"太泛改成复合形）、`缩写白名单.txt`（155）、`流程词.txt`；`表达阈值.json`
  出处=先验（数字密度 摘要30/首读30/正文35，对冲 8/6/6，句均 40/45/48，摘要计数 12/3/0/40）。
  **段内数字取舍**：方案写"≤2"，落地为 软线 2（报告标密集段）+ 硬线 4（G4 阻塞）——骨架版第 4 章按 ≤2 判有 17 段
  超预算、按 4 判 37 段（全文），直接用 2 做阻塞线 G4 不可过；软线保留目标，靠章评/统稿/审稿场逐轮压。
- `审计.py`：第 7 节改**段级溯源**（一段内全部 `% src` 合成来源池，段内每个统计数字须在池中；键路径逐级取→
  包含匹配→任意深度回退）；新增 12 表达画像 / 13 自造缩写+标题违规 / 14 图题对冲 / 15 实验记录分流 / 16 问题重述五要素。
  **只审 论文.tex 真正 `\input` 的章**——工作根残留的旧版章（骨架版有 9 个 `_xxx.tex`/旧标题章）此前把禁用词、密度、
  缩写账全部污染（实测：过滤后自造缩写 96→27、键未命中 110→52、汉字 18173→9876）。
- 骨架版实测（过滤后）：摘要 数字 20/对冲 12/句均 42.6 三项超线；全文数字密度 41.1、对冲 6.8；超线 9、段首 3、
  超预算 37；自造缩写 27（CM/DN/DOP/EG/MB/VP）；标题违规 3（证据门）；图题对冲 4；实验记录 327 条全未分类。
- 新建 `验证/审计单测.py`（手造小论文跑真审计，15 项断言全过）。

### 工单 2 · 门检与契约
- `门检.py`：G4 加表达判据（阈值文件缺失=不放行、密度超线、摘要画像超线、段首数字段、超预算段、自造缩写+标题违规、
  图题对冲、问题重述缺要素；审计报告缺 表达画像 节=旧版审计不放行）；G2 加实验记录分流（本问 ≥1 科学尝试、
  未分类=0、科学尝试不含流程词）；新门 `叙事`（`交接/叙事底稿.md`：每问五段标记/≥150 汉字/零公式命令文件名缩写流程词内部术语）。
- `契约表.py` 登记 `交接/实验记录.json`（建模师围栏 + 解读师另产），两角色文件补围栏（`类别/问题/尝试/现象/决定/依据`）。
  `契约核对.py` 两处放宽：中转变量除 `.append` 外也认整份 dict 赋值（审计的 摘要画像 = {…}）；G2 的读取写法改成
  核对器认得的 for-循环+字面量 `.get`（此前 `记录 = 记录 if … else []` 的 IfExp 重绑把契约变量解绑，读取静默漏检）。
  结果：9 契约全部背书、0 未登记。
- `门检单测.py` 19/19（新增 G2记录反 / G4表达反 / 叙事正 / 叙事反，夹具自带最小词表与阈值）。

### 工单 3 · 角色与标准
- 撰稿师：输入加叙事底稿；军规 4 改段级 src；军规 6 只引 类别=科学尝试；新增「表达四律」（先讲清楚再钉数字 / 置信一处 /
  名字用人话 / 图题是陈述 + 1.1 给定要求 + 1.2 思路图 + 摘要首句白话）与「叙事底稿」任务规范。
- 解读师：结果声明加 `置信{等级,理由}`（全文唯一置信母本）；建模师/解读师：实验记录围栏与分类规则。
- 规划师：命名政策（≤12 字中文短语）+ 图表规划必含全文思路图；绘图师：图注素材加 `图题` 字段。
- 章评师/审稿员：语言维改**范文对照**（数字节律/置信口吻/术语命名/图题 各给 更像|一样|更远 + 理由，写 `范文对照` 字段）。
- 新角色 `读者.md`（闭卷评委：只读 tex，输出 复述/卡住/自造词/读者分；摘要复述 schema）、`统稿师.md`（只动语言不动事实）。
- `优秀论文标准.md` 新增 §6 表达四律；新建 `范文卡片库.md`（9 张范式卡，**标注 Claude 手写、非真实范文**，附替换指南）、
  `表达锚点.md`（六维 6/8/9 档 + 对照口径）。

### 工单 4 · 统稿守卫 + 驱动新节点
- 新建 `运行时/统稿守卫.py`：事实层逐文件比对（统计数字多重集合、label/ref/cite 集合、显示公式数、插图数、标题序列、
  `% src` 只增不减），任一差异整份回退；`验证/统稿守卫单测.py` 11/11（数字搬进表格放行、改一个数字拦）。
- `蜂群驾驶.py`：新节点 **S4:叙事底稿 → G叙事**（脊柱前，`调度器.门` 最多返工 1）、**S4:统稿**（章评循环后、摘要前：
  统稿师单腿 → 统稿守卫回退 → 45% 变化守卫）；章评循环每批加 **读者腿** `读者R{轮}_{k}`（卡住/自造词进台账 来源=读者，
  每章 ≤3 条）；章评师任务加范文对照；撰稿任务加叙事底稿/表达四律/段级 src，问题重述章加给定要求+思路图；
  S4:摘要改写（首句白话、数字 ≤12、置信 ≤1 处、五侧重改写；摘评融合后 **画像门 + 读者摘要复述**，最多 1 次定向修订）；
  S5 审稿场把表达机械量（超线/段首/超预算/自造缩写/图题）逐条进台账；S5 摘要定稿每轮加画像门；G4 返工文本补表达判据；
  基建清单加 表达画像.py/统稿守卫.py 与 6 个新词表/阈值/范文文件；**MAX_LEGS 350→380**；S6 终审补跑 叙事 门。
- `驱动干跑.py`：mock 门检_叙事/读者R/摘要复述/统稿回执；节点期望 28→**31**（全链实测 31 节点、66 腿、0 降级；
  韧性场景 A 判死重派后 31 节点、场景 B 预算刹车 PASS）。
- `角色冒烟.py`：资产列表补齐新文件；任务表加 叙事底稿/统稿师/读者；新增 `--根=路径` 以免真腿冒烟污染可续跑的蜂巢镜像。
- `验证/范文分布.py`（A3）：对 `运行时/范文/*.pdf` 跑同一画像，P90 写回阈值（篇数 <3 只打印）；空目录退出 0。
- `验证/评审重标.py`（D3）：骨架版复制到 `真题测试/重标镜像`，真腿 章评师×3 + 审稿员×3，offset=新均分−6.3，
  建议阈值截断 [6.5, 9.0]，写 `验证/重标结果.json`，**只给建议不改配置**。已在后台启动，其后串行跑
  叙事底稿/读者/统稿师三条真腿冒烟（`真题测试/冒烟镜像`，输出 `真题测试/冒烟_M5-1.out`）。

### 单测汇总（本工单收尾时）
契约核对 ✓ 0 未背书 · 门检单测 19/19 · 审计单测 全过 · 统稿守卫单测 11/11 · 回路单测 22/22 · 契约核对单测 10/10 ·
驱动干跑 全链 31 节点 / 韧性 A+B PASS。

### 未闭环 / 交给用户
- **范文 PDF**：本机 0 篇；已在 GitHub `zhanwen/MathModel` 找到历年国赛优秀论文 PDF（2004–2023，官方编号），
  O 奖（美赛）来源未找到。下载需用户逐项许可（文件名/来源/大小已列在会话末尾）。放入 `运行时/范文/` 后跑
  `python3 验证/范文分布.py` 覆盖先验阈值，并按卡片库替换指南换真稿摘录。
- 重标与冒烟结果落盘后再看：`验证/重标结果.json`、`真题测试/冒烟_M5-1.out`。
- 全新对照跑（阶段 5）：380 腿/20h，由用户决定启动时机。

### M5-1 补记（20260908 晚）：真腿暴露的两处链路病根（R31/R32）
- **现象**：`验证/评审重标.py` 骨架版真跑，章评师×3 成功（4/5/6 章 6.0/6.5/6.0，均分 6.17，offset −0.13，建议 章评阈值 6.9、
  审稿达标 8.5；范文对照几乎全「一样」，评审明确指出卡片库是脚手架而非真范文），审稿员×3 **全部失败**：A/C `AUTO(rc=142)`
  ——`role.sh` 里 `with_timeout 1200` 的 SIGALRM 把 >20 分钟的全文审稿腿杀了；B `rc=1`，日志末行 `429 Too Many Requests`（6 腿并发限流）。
- **修法（链路，不动提示词）**：腿上限改为按波次下发——`运行时/role.sh` 读 `LEG_TIMEOUT`（默认仍 1200）；`本地蜂巢.leg(...,
  leg_timeout=)` 把它放进子进程环境（`_spawn(env=)`）；`蜂群驾驶.wave()` 初派与重派都下发 `max(600, timeout−60)`（腿上限必须小于
  波次超时，否则重派时旧进程还活着会双写）。重活波次放宽：章评+读者 / 审稿场 / 统稿 1300→2700，脊柱 / 撰稿 / 章修订 / G4 返工
  1300→1800，摘评 900→1200，摘要定稿 900→1200。wave 失败分支新增两类识别：腿日志含 `429 Too Many Requests` 记「命中上游限流」，
  done 含 `rc=142` 记「超过腿上限」。`角色冒烟.py`/`评审重标.py` 真腿 leg_timeout=2700、等待 2700/3600；重标新增 `--补跑`
  （只补 done 缺失或 AUTO 的腿，不重建镜像、保留章评产物）。
- **验收**：六个文件 py_compile / bash -n 过；干跑全链 31 节点、韧性 A+B PASS（假 Hive.leg 接受 `**kw`）。病根台账登记 R31（已闭环，
  真腿验证待补跑）、R32（部分闭环：识别落地，并发自适应待对照跑数据）。
- **未闭环**：审稿员重标三腿待冒烟链结束后 `--补跑`（≤3 并发）；正在跑的冒烟镜像副本 `bin/role.sh` 临时 sed 成 2400，不入库。
- **追加发现（同晚）**：超时其实没杀死腿。`codex` 是 node 包装脚本，perl alarm 只杀了包装，原生 `codex-darwin-arm64` 子进程成孤儿
  （ppid 1）继续跑：重标审稿 C 在被判 rc=142 的 8 分钟后自己写出 done 与 25KB 审稿意见（总分 6.1，印象分 6）；A 孤儿 30 分钟后仍在写日志。
  这意味着判死→重派会与孤儿双写。修法：`codex公共.sh` 的 `with_timeout` 改成 fork+setsid 再 exec，到点对子进程组 TERM→10s→KILL，
  外部 TERM/INT/HUP 转发给子组（驱动 `kill -- -pgid` 不失效），退出码约定不变（超时 142 / 外部终止 143 / 正常透传）。
  单测：整组 `sleep` 全死且 rc=142；`exit 7` 透传；外部 TERM 无残留。
- **追加修复（同晚）**：`本地蜂巢.exec`/同步 `leg` 的解码改 `errors="replace"`——冒烟脚本用 `head -c 1800` 截断多字节 UTF-8，
  `communicate()` 抛 UnicodeDecodeError 把整条冒烟链打崩（叙事底稿腿之后的 读者/统稿师 未跑）。驱动里凡 exec 输出被截断都可能中这一枪。
- **真腿验证 ①**：撰稿师真腿写出的 `交接/叙事底稿.md`（450s）过 `门检.py 叙事`：3 问、869/776/933 汉字、明细 0 条；
  文本无公式/命令/文件名/英文，问 3 未把诊断厚度写成答案（与结果声明一致）。读者/统稿师两腿已重新启动（`真题测试/冒烟_M5-1b.out`）。

### M5-1 补记 ②（20260908 晚）：范文到位，阈值改认真稿
- **来源**：用户许可后从 GitHub `zhanwen/MathModel`「国赛论文/2021–2023 年优秀论文」下载 10 篇（A/B/C 题各若干，共 16.5 MB，
  `运行时/范文/*.pdf`，gitignore；pdftotext 全文 `运行时/范文/文本/*.txt` 入库并进三处工作根同步列表）。**注意**：这批是
  **研究生数模竞赛（华为杯）一等奖稿**（封面即"华为杯"），40–104 页，不是本科国赛（高教社杯）稿；同一仓库没有本科稿，
  美赛 O 奖原版论文集（2004–）有但是英文。表达层面作代理，篇幅类不搬。
- **量尺修正**：`验证/范文分布.py` 首版把目录页当正文（第一个"参考文献"在目录里），正文数字密度算出 300+/千汉字；
  改为散文过滤（空行分块；块内只留 ≥6 汉字、汉字+中文标点 ≥50%、无点线目录的行；块 ≥20 汉字成段）+ 最后一个"参考文献"
  截尾 + 摘要容忍封面把"摘 要"排成三行。校验：骨架版 tex 口径 41.1 ↔ PDF 口径 39.9，对得上。新增 `--对照=x.pdf`。
- **真稿常态（10 篇，中位/P90）**：正文数字密度 29.1/41.9（骨架版 39.9——在上沿，不是离群）；段内数字 2/10（骨架版 P90 12.6；
  真稿 31% 的段 >4 个数字，先验硬线 4 会把真稿本身拦下）；**正文对冲密度 0.0/0.1（骨架版 5.95——决定性差距）**；
  句均 49.7/55.1（骨架版 48.2；先验 40 字线比真稿紧）；摘要数字 15.5/31.1（骨架版 21；先验 ≤12 太紧）；摘要对冲 0/0（骨架版 12）。
  图题是 8–20 字短名词短语、零保留话，"示意图/流程图"是常态用词。
- **写回**（`表达阈值.json` 出处=范文分布）：数字密度 摘要 29.0 / 首读 42.4 / 正文 41.9；对冲密度 全部 1.0（=max(P90, 1.0)，
  给每问一处置信声明留余量）；句均 69.2 / 51.4 / 55.1；摘要画像 数字 31 / 对冲 1 / 缩写 0（不写回，依赖白名单）/ 句均 69.2；
  段内数字 硬线 10 / 软线 6；段首数字 0（不写回，PDF 分块把小节号并进段首量不准）。
- **骨架版在新阈值下**（散文口径审计）：超线 7 条（摘要 数字密度 39.1>29 与 对冲 23.5>1；4/5/6 章 对冲 1.06–1.34>1；4/6 章 数字密度 45.6/61.6）、
  超预算段 37→10、密集段 11、摘要超线只剩 对冲 12>1——门的火力集中到了"保留话"与两章的数字堆，与真稿差距的方向一致。
- **文档**：`表达锚点.md` 首表由阈值 JSON 生成（数字不手抄）、6/8/9 档改写（图题"陈述式"降为加分项，"示意图"不再算空词；摘要数字线 31、句均 69）；
  `范文卡片库.md` 九卡改为"真稿怎么做（篇号+小节+转述）/ 示范（本题改写，标示范）/ 为什么好 / 反例"，不贴原文长段；
  角色文件加一句"要看原句打开 运行时/范文/文本/<篇号>.txt"。`审计单测` 阈值夹具改内联（不随校准漂移）。
- **单测**：门检 19/19 · 审计单测 全过 · 统稿守卫 11/11 · 契约核对 0 未背书 · 干跑全链 31 节点 / 0 降级。病根 R21 闭环。

### M5-1 补记 ③（20260908 晚）：三条新角色真腿验证 + 重标收口
- **真腿验证 ②（统稿师，720s）**：在骨架版 24 个 tex 上改了 12 章（措辞与置信收口 4–13 处/章、术语统一、拆句、图题调整），
  `统稿守卫` 逐文件 **24/24 通过**——数字多重集合、引用键、公式数、插图数、标题、`% src` 全部未动；溯源核验 76/2/52 前后一致。
  名字表把 DOP--VP / CM--DN--EG / 零模型 / 诊断性区间 等 15 组自造名改成中文短语（色散光程—基线解耦变投影、退化可验证据门、算法扰动区间…）。
- **真腿验证 ③（统稿后画像，先验阈值口径）**：全文对冲密度 **6.78→2.03**（4 章 7.45→1.06、6 章 10.18→1.34、1 章 4.98→0）、
  段首数字段 3→0、自造缩写 27→1、图题对冲 4→0、超线 9→6；数字密度 40.3→40.4、超预算段 37→37（统稿师不动数字，符合设计；
  新硬线 10 下为 10 段）；摘要按设计未动（对冲 23.5 留给摘要节点）。副作用：句均 34→25，把说理句剁短了——统稿师规则已改为
  只拆超过阈值上限（55 字）的句子、目标句均 40–55（真稿 50）。
- **真腿验证 ④（读者，630s）**：闭卷读 14 章，读者分 5–7，卡住 36 条（每章 2–5），自造词逐章列出（CM--DN--EG、DOP--VP、主口径、
  噪声代理、趋势尺度…），每条卡住都指到原句并说"为什么读不懂"；摘要复述：三问都能复述、第一句白话=否、置信声明 6 次、读不懂的词 9 个，
  30 秒印象"六处句级保留声明和密集术语压过了主线"。产物 schema 与门检/驱动期望一致。
- **重标收口**（`验证/重标结果.json` 17:52）：章评 6.0/6.5/6.0 均 6.17（offset −0.13）；审稿 A 总分 6.5（印象 6.0） / B 总分 6.2（印象 6） / C 总分 6.1（印象 6），均 6.27；
  范文对照相对锚点几乎全「一样」（评审明确指出当时卡片库是脚手架）；建议 章评阈值 6.9、审稿达标 8.5（现行 7.0/8.6），
  **只给建议，改 配置 由维护者定**。范文校准后的锚点已换真稿，下次对照跑的评审分不再是同一把尺。
- **R31/R32 检测计数（本晚全部真腿：冒烟 3 + 重标补跑 1）**：`AUTO(rc=142)` 0、`429 Too Many Requests` 0（≤3 并发）、孤儿 codex 进程 0。



## M5-2 全新对照跑（20260909 14:38 启动）
- 用户指令：现在启动、codex 随便调用、用 Extra 档（= `model_reasoning_effort=xhigh`，探针 9s 返回正常）、把整条链路打通交付稳定高质量成品。
- 启动：`CODEX_EFFORT=xhigh HIVE_MAX_CONCURRENT=4 python3 流水线/后台启动.py 真题测试/驾驶_对照.out python3 流水线/蜂群驾驶.py 真题测试/输入 真题测试/成品_对照版`
  （pid 91597，运行ID 20260909-143830，档位=全开；旧 `真题测试/蜂巢镜像` 已由驱动轮换为 `蜂巢镜像.old`）。腿环境实测 `CODEX_EFFORT=xhigh`、`LEG_TIMEOUT=1240`。
- 启动前落地（提交 9124482）：`wave()` 并发闸——同一波次最多 4 条腿同时跑、其余排队补位，每腿时限从自己启动算，命中 429 的波次把上限降 1（下限 2）；
  MAX_LEGS 380→600（xhigh 更慢、重试更多，不让预算刹车误伤）；章评+读者 / 审稿场 / 统稿 波次 2700→3600、撰稿 1800→2700；
  `role.sh` 推理档改由 `CODEX_EFFORT` 下发（默认仍 high）。干跑：全链 31 节点/66 腿/0 降级，韧性 A+B PASS（干跑发现并修掉补位双计数：上限 4 实际只派 2）。
- 阈值：`配置` 的章评/审稿阈值保持 7.0/8.6（重标建议 6.9/8.5 未采用，用户未定；且锚点已换真稿，量尺本身变了）。
- 监视：会话监视器过滤 门检结果 / `!!` / Traceback / 降级 / 429 / rc=142 / 阶段完成 / 终态；结果随时补记在本节下。
- 17:01 停跑（问1 首轮 G2 FAIL、`G2返工_问1` 进行中）：发现 R33（仲裁返工后红队不重跑，独立复算永远 null）并顺藤核实 R34（升格先裁决后跑变体脚本 + 升格后无解读腿——骨架版三问降级放行的直接机制）。驱动/腿/codex 全部收干净（0 孤儿）；问1 的 S2 半成品归档到 `真题测试/蜂巢镜像/_归档_问1首跑_1701/`（交接/求解/数据/日志共 54 份日志副本），S1 产物与 done 标记保留，`状态.json` 清零 G2:问1 失败计数。
- 补丁（`流水线/蜂群驾驶.py`）：①`红队复算(编号, 标, 深度)`——仲裁返工→执行后派**复核轮**（红队重算+报告；不齐可再仲裁/再让建模返工，深度封顶 1 不递归），`解读_问题N_仲裁后` 只在复核后派一次；②G2 `_返工`（标 `_G2返工1/2`）与 `_升格`（标 `_升格后`）之后同样红队复核；③`升格蜂群(变体后=)`：三条变体脚本在裁决腿之前跑完；④升格裁决后新增 `解读_问题N_升格后` 腿；⑤红队 / 仲裁返工 / 建模任务文本加冻结合成输入包口径（`数据/问题N_冻结合成输入/` + `输入清单.json`，驱动探测到包时明示红队必须用它），`角色/红队.md` 只准读清单加该包、`角色/建模师.md` 加导出义务（提示词改动在链路修复之后，遵守病根纪律）。
- 干跑（`验证/驱动干跑.py`，新增统一事件序）：全链 31 节点/71 腿/0 降级；新增场景 `红队不齐` PASS；`门升格` 加先后序断言 PASS（111 腿，三问各两次返工+升格全走完）；中断续跑 PASS；韧性 A/B PASS。
- 17:16:32 续跑：`CODEX_EFFORT=xhigh HIVE_MAX_CONCURRENT=4 python3 流水线/后台启动.py 真题测试/驾驶_对照.out python3 流水线/蜂群驾驶.py 真题测试/输入 真题测试/成品_对照版 --resume`（pid 94786）。已完成 9 节点全部跳过、运行ID 不变、累计机时 8342s 续算，S2 第 1 层从 `建模_问题1` 重新开始；镜像内角色文件已同步为新版（`角色冒烟.py` 无参同步 + 驱动续跑分支 `本地就绪()`）。修复提交 066f19f。
- 17:16–18:07 二跑问1：建模师一开始就导出冻结合成输入包（新提示词起效）；解读轮1 FAIL（旧 `执行状态.json` 与成功日志矛盾、缺交叉证据）→ 修复 → 轮2 PASS；红队 8/8 有数、5 处不齐 → 仲裁 4 项判建模（合成噪声未按冻结协议、灵敏度未同步缩放色散斜率）/1 项判红队（中位数定义）/1 项口径差异（d·q 峰距 vs 周期投影）→ 仲裁返工 + 重跑 rc=0。复核轮暴露补丁两处缺口：①`结果声明_问题1.json` 是解读师产物，返工重跑后没人刷新，红队复核拿新脚本撞旧声明，6 处不齐全是假分歧；②复核轮红队腿两次 `AUTO(rc=0) 角色未写done标记`（任务文本"重新独立实现并比对"让它把比对当成未完成）。18:07 停跑（0 孤儿），问1 二跑产物归档 `蜂巢镜像/_归档_问1二跑_1807/`（49 项 + 81 份日志副本）。
- 补丁 v2：新增 `声明刷新(编号, 标, 缘由)` 解读师腿（只按最新 结果/ 逐键刷新声明与引用清单，不裁定），插在 仲裁返工/G2返工 的 执行 与 红队复核 之间；复核轮红队任务文本改为"只写不跑、比对另派、写完必须写 done"；`解读_仲裁后` 提示词说明复核轮内若建模再返工，红队报告比对的是那次返工前的声明。干跑：红队不齐 PASS（序含 声明刷新_问题1_仲裁后）、门升格 PASS（117 腿）、全链 31 节点/66 腿/0 降级。18:08 再次 `--resume` 续跑（问1 第三次从建模开始）。
- 18:08–18:45 三跑问1：建模 7 分钟、脚本一次 rc=0、解读轮1 PASS；红队 5 处不齐 → 仲裁只判 1 项建模 → 仲裁返工 → 声明刷新（1 分钟，d·q 刷成 19.5376）→ 复核轮红队（这次写了 done）→ 6 处不齐（复核轮仲裁进行中）。补丁 v2 的顺序在真跑里全对；但复核结果暴露契约级病根 R35：d·q 已对齐到 0.0%，其余 6 处全是方法自检误差（建模 0.0008% vs 红队 9.66%），两种方法的自检误差天然不可比。改契约：`结果声明` 新增 `自检指标`（红队不比对），`核心指标` 只放可独立复现的估计量/诊断量/区间；`角色/解读师.md`、`角色/红队.md` 热同步进镜像（腿派发时重读角色文件，驱动不必重启），驱动内联 schema 同步（下次启动生效）。契约核对通过。
- 18:46 问1 G2 第 1 次检查只剩一项 FAIL：实验记录 11 条科学尝试含流程词（红队/json/契约）——解读、红队/仲裁、结果声明各项已过。派 G2 返工（建模师改措辞）。顺手给 `_返工` 加求解脚本+结果目录指纹：返工前后指纹相同（只改台账/实验记录措辞）就跳过 声明刷新+红队复核（约 25 分钟）；干跑三场景仍全过。此优化在下次驱动启动时生效，本轮问1 返工仍走全路径（不为此重启，重启代价更大）。
- 18:58 问1 G2 第 2 次检查仍只剩 1 条科学尝试含流程词（JSON/目录）。为措辞问题走第三次失败→升格蜂群不成比例：`运行时/门检.py` 加 `清洗实验记录 <编号>` 模式（含流程词的科学尝试机械降级为流程事件、带 `清洗` 字段留痕），驱动 G2 `_返工`/`_升格` 在明细只剩该项时改调清洗、不派建模返工不升格。门检脚本已热同步进镜像（判据未变，只多一个模式）；驱动侧分流下次启动生效，本轮在解读腿之前手动清洗兜底。干跑门升格/全链仍过。
- 19:06 问1 过 G2（第 3 次检查 PASS；解读 PASS、红队 仲裁后修正→复核对齐）。趁 S2:问1 刚落盘、问2 建模腿刚起 2 分钟，19:09 停跑并 `--resume` 重启，让驱动侧三项改动（G2 返工指纹跳过、流程词项机械清洗分流、解读任务内联 schema 含 自检指标）对问2/问3 生效；损失仅问2 建模腿 2 分钟。
- 19:07–21:15 问2：解读两轮 FAIL（五折条件厚度 13.4/10.5/6.5/13.5/5.5 μm 跨切分不稳定，灵敏度合法窗 0.5–18.1 μm）；红队三轮独立复算 10.58 / 0.50 / 13.34 μm（核心指标已按 R35 只含厚度与区间），仲裁两次判口径差异；G2 两次返工头条数字未变（10.80 μm，区间 0.64–18.1）→ 20:54 升格蜂群（修好顺序后首次真用：三变体 5 分钟静态交付、脚本各修一次跑通、裁决前结果已落地）。21:14 裁决：主线胜（正式留段误差最低、五折不稳定性低于升格2），但五项协议仍只过三项，维持低置信条件发布。观察（留给复盘，不人工干预）：①四条方法族都过不了五折交叉印证与灵敏度，怀疑五折按波数窗切分对反射谱定厚本身不适定，协议可能需要按题型定制；②问1 的表观光学厚度乘积 19.54 μm 若按 q=n·cosθ、n≈2.6 折算约 7.5 μm，与问2 的 10.8 μm 存在张力，看 S5 硬伤猎手是否抓到跨问不一致。
- 21:29–23:06 问3：解读轮1 FAIL（灵敏度分支未用切窗样本重估）→ 轮2 PASS；红队 6 处不齐 → 仲裁 1 项建模错（"硅全量"实为问2 传下的 480 点抽样，红队抓到的真 bug）/1 项红队错/4 项口径差异 → 仲裁返工 → 复核轮口径差异成立 → G2 第 1 次只剩形式项（假设台账引用 灵敏度.csv 非结果键 + 2 条流程词）→ 返工改脚本写灵敏度 JSON 键（指纹变化，走完整复核路径）→ 第 2 次 PASS。**S2 收官（23:06）：问1 PASS、问2 降级放行（低置信条件发布）、问3 PASS**；全程 0 次 429、0 次 rc=142、0 孤儿腿。进入 S3 图证。
- 01:44–02:12 S4 章评轮1：均分 6.95、低分（<7.0）14 章、读者分 6.54、台账 184 条；6 条章修订腿回执全受理（各 7–10 条），但变化守卫把 6 章中的 5 章整份回退（1.1 61%、2 68%、3 78%、3.1 58%、3.2 47%）。查实是**度量错不是腿错**（病根 R36）：守卫按行比对，而 tex 一段一行——`1.1.问题重述.tex` 18 行里正文只有 4 行、`% src` 注释 9 行；评审点名 9 处、4 段各改一句再按意见重排 src 路径格式，行比例必然 >45%；回执证据显示腿改的正是点名行（3/7/8/9/11/12/13/15/16/18）。副作用：44 条意见被记成「修过 1 次仍未消解」，下轮渲染会催腿「换一种改法」。顺藤发现修订腿名额按台账字典序取前 6 章，章评 3 分的 4.1.2.模型建立 排第 9，两轮都轮不到，被修的反而是 6.5 分的前几章。
- 02:12 修法（链路级，`回路.修订守卫`）：单位改为正文**句子**（`%` 注释行不计）；评审意见里引号引出的原文片段视为点名处，点名句的改动不计；再配一把字符**丢失比**（匹配块字数/旧文字数，删也是推倒）；两把尺都超线才回退。在真实 1.1 上实测：4 段各改一句+src 重排 行尺 56%→未点名句 24%/字 17% 放行；整章推倒 94% 拦下；删半章 字 49% 拦下；评审点名删的半章 放行；统稿式术语替换 句 59%/字 3% 放行。驱动 `变化守卫回退` 改用新尺、被回退新稿留底 `审稿/回退稿/<标签>/`、日志打两把尺明细；统稿语言层守卫同尺；章修订选章按章评分从低到高，名额进配置 `章修订每轮章数`=8；`撰稿师.md` 定向修改协议措辞对齐新尺（热同步）；`MAX_HOURS` 20→30（用户口径算力不设限；R33–R36 四次链路修复各重做了一段，20h 会在 S5 审稿场中途被护栏刹停）。`回路单测` +5 用例（27/27）、干跑全链走到 S6 复盘。02:12 停跑（章评R2 波次进行中，作废），轮1/轮2 评审产物与台账归档 `真题测试/蜂巢镜像/_归档_S4章评轮1_行尺回退_0158/`，台账清空后 `--resume` 重做 S4:章评循环（2.2.可用信息 的已受理修订保留在镜像里）。
- 02:43–03:02 S4 章评轮1 重做：均分 7.09、低分 10 章、读者分 6.57、台账 164 条；选章按分从低到高（3.1.模型假设 3.0、4.1.2.模型建立 3.0 排最前——两章 3 分是章评师核对实验记录后指出正文过程叙述与记录相反，属编造过程感，意见全引原句）。8 条修订腿回执全受理；新尺下 6 章过（3%–24%，其中 4.1.2 句 77→120 仍只 3%）、2 章超线回退：`2.数据审查与建模边界` 88%（16 句→6 句）、`4.1.1.分析与准备` 50%。比对留底稿：前者是评审明确要求「把从A到B的三段压缩为一段、数值细节移出父章」，腿照做却被判重写——点名粒度只到句子，不认「从A到B」范围与「X起头的两段」段落指法；后者是评审要求「整段前移」重排，按序比对把搬家的句子当成丢失。
- 03:05 R36 补②（`回路.点名掩码`/`_存活`）：点名按评审指法分三级——句级（引号片段所在句）、范围级（从“A”到“B”之间全部句）、段级（片段后紧跟 起头/所在/整段/两段/三段，或意见含 压缩/合并/移出/前移/拆成 等结构动词时点到句所在整段）；未点名句的「改动」改为存活判定（新文里任一处有 ≥0.9 相似的句即算存活，不看位置）。两份留底稿在新尺下：2.数据审查 过 0%（15/16 句点名）、4.1.1 过 14%（21/28 句点名）；单测 +5（32/32）、干跑全链过。台账再派提示分流：被守卫回退的条目提示「只动点名句、别顺手润色合并前移」，被评审判未消解的才提示「换一种改法」（原先一律催换改法，会把回退项推向更大改动）。驱动进程已加载旧 `回路`，改动待 S4:章评循环 收口（轮2 评审 03:02 起）后在 S4:统稿 节点边界停跑续跑生效；轮2 的 2/4.1.1 两章若再被旧尺回退，留给 S5 回炉。
- 03:44 S4 章评轮2 收口：均分 7.09→7.53、低分 10→4 章、读者分 6.49、裁定 45 消解/5 未消解、相对判断=更好（轮数上限，轮2 只评不改，179 条留给 S5）。统稿腿刚起即停跑（节点边界，S4:章评循环 已落盘），`--resume` 续跑（pid 93849）：跳过 21 节点、S4:统稿 重派，补② 的三级指法/存活判定/再派提示自此生效（S4 统稿、S5 回炉文、G5 返工共用）。累计腿数 256，用时 12.7h/30h。
- 04:02 S4 统稿完成（34 章，事实层回退 0，语言层守卫 0 回退）；04:26/04:31 摘要画像门两检：机械画像不超线、第一句白话，但闭卷读者复述不出问3（摘要只给硅 2.58 μm 与碳化硅沿用 10.80 μm，没说多次反射把厚度改了多少、是否确认多光束干涉）——内容缺口，留给 S5a 摘要定稿。
- 04:43 G4 第 1 次 FAIL 六项：禁用词 2、有效数字过精 8、溯源存疑 20、段首开场词重复（'def re'×10、'ROOT ='…）、表达密度超线 4、问题重述缺要素（总体分析/思路图）。查审计报告：前四项里禁用词（`FAIL`、`不发布`）与过精浮点全在 `8.附录.tex` 的 lstlisting 代码块内、段首重复全是代码行开头、存疑 20 条全是 `% src:交接/建模笔记_问题N.md:节名`（审计只认 JSON，存在的 md 记「缺失或非JSON」）——审计器误报，返工腿会被派去改代码清单里的字符串/浮点常量（病根 R37）。
- 04:48 修 `运行时/审计.py`（链路级，热同步 `bin/审计.py`，驱动每次门检重新起进程即生效）：① `_遮代码` 把 lstlisting/verbatim/minted 内容按行数换成空行（行号不漂），禁用词/有效数字/段首统计不再数代码；② `_附录集`/`_是附录`：主控 `\appendix` 之后 `\input` 的章（8.1–8.3 源码章名字里没有「附录」）与名含附录/参考文献的文件统一按「附录不检」口径，段首重复也加此口径；③ 非 JSON 来源（.md 建模笔记、.py 求解脚本）按文本核对：节名/函数名命中即通过、不命中记「键未命中」只记账、数字池取全文数字。影子根（符号链接镜像输入）验证：禁用词 2→0、有效数字 8→0、段首重复 13 项→0、溯源存疑 20→0（核对通过 208→220+）。中途一次手滑：批量替换把 `_是附录` 自身体内的判据也换成了递归调用，影子审计栈溢出但旧报告仍在、同步条件只看了禁用词数——已在 30 秒内修正重同步（教训：影子验证前先删旧报告）。G4 第 2 次检查将用修后的审计器。
- 05:00 G4 第 2 次检查 PASS（修后审计器）。但读 `日志/G4返工回执.json` 与稿件发现返工腿越权（病根 R38）：① 为清零附录代码里的「禁用词/过精浮点」把三问完整源码清单整段删除（8.1/8.3 一个 `def` 都不剩，违反 `优秀论文标准` 第 15 条「附录必须含全部关键可运行源代码，缺代码可能取消评奖资格」）；② 把 1.1/1.2 清空并入首章文件、删掉主控两条 `\input`（驱动 `章文件`/章评台账/脊柱都按三文件结构，评审与回炉会读到空章）；③ 改 geometry 22mm/行距 1.10/公式间距压正文到 20 页；④ 六张核对图移进补充材料。原因：G4 返工是全链唯一没有任何守卫的返工腿（S4/S5/G5 都有 变化守卫回退），任务文本又直接写「正文≤20页、禁用词清零」。05:03 停跑（S5 审稿场 3 腿刚起 3 分钟）。
- 05:05 稿件修复：合并稿按 `\subsection` 边界拆回 1.x（3 行）/1.1（18 行）/1.2（31 行，含思路图），主控恢复两条 `\input`；三问求解脚本全文回填到 8.1/8.2/8.3 章末 `\subsubsection{完整源码}`（634/951/793 行，`def` 37/28/34）；xelatex 两遍 0 错误、摘要 1 页、总 137 页（正文 20 + 附录）。版式改动（22mm/1.10）暂留——没有改前快照可比，且 S5 美化会再定版式。
- 05:06 链路修复（R38）：驱动新增 `结构守卫回退(快照, 路径们, 上限, 标签)`——主控 `\input` 集变了回退主控、章被清空回退、附录 lstlisting 数量减少回退、其余按 `回路.修订守卫`（无点名、升格线 0.7）；G4 返工改成「改前快照 → 腿 → 结构守卫」，任务文本加封死条款（不许删/缩短源码清单、代码字面量不算禁用词与过精、不许合并清空章或改主控 `\input`、不许改版式压页数）；G5 返工同接结构守卫且快照含主控。干跑全链/门升格过。
- 05:13 S5 审1前编译 FAIL（400 s 超时）却照常下发三路文本腿：论文.pdf 残缺、页图为空、评委模拟1 被静默跳过（驱动读到的是 05:02 旧驱动实例留下的评委模拟_轮1.json，评的是修复前的 77 页稿）。追根：模板 format-sandbox.cls 按家族名加载字体，本机 CoreText 找不到 texmf 的 Fandol、查 DejaVu Sans Mono 直接挂死；04:38 编修编0 腿修过一次，05:06 续跑基座同步把 `论文/format.cls` 打回模板（病根 R39）。
- 05:40 链路修复（R39）：模板改全文件名加载（与回炉文_轮1_1 腿 05:34 二次自修的版本一致）；驱动 `本地就绪()` 对 论文/ 只播种不覆盖；S5 审前编译失败接 `_编译修复循环`。镜像重编三遍 E=0 P=138、页图 138，损坏稿留底 `日志/审1前重编/`。干跑 全链/中断续跑 过；驱动改动待下次 --resume 生效（本轮靠镜像 format.cls 已修好，不必重启）。S5 轮1 面板均分 6.65（A 6.95/B 6.35）、硬伤 1、评委第一印象 7.2、台账新增 20（叙述 13/正确性 6/硬伤 1）→ 回炉文 20 条。
- 05:48 回炉文_轮1_1 回执 12/12（18 分钟，未改 0）。细读发现分路失灵（病根 R40）：审稿员/评委 schema 无 `目标` 字段，20/20 条全落「文」，撰稿师自己补跑网格搜索、写 `搜索补查.json`、重绘 7 图，红队/G2 门/图评全绕过；审稿员 B 的键名（直接执行/文件与位置/验收依据）未被识别，5 条指令定位为空。
- 05:55 链路修复（R40）：驱动 `推断目标()` 关键词分路（算 > 图 > 文）+ `意见条目` 键别名；`收回执` 识别「需转算/需转图」改路退回待改；S5 轮1 台账非空即渲染给评审并收裁定（为 --resume 从轮1重进铺路，轮1 无快照不回退）。提示词：撰稿师第 5 条「算与图不归你」、审稿员 schema 加 `目标` 统一键名，热同步。计划：等「编译#修1」出现后停驱动 --resume，S5 从轮1 重进（老台账 20 条待复核由评审裁定，算/图类按新路派）。
- 06:01 实盘验证：R36 补② 在 S5 轮1 回炉后 17 章变化守卫全过、0 回退（比例 0–44%，2.数据审查 44% 贴线过；多数章「点名 0」是因为审稿员 B 的定位键名未被识别——R40 的键别名已补）；R39 验证：编译#修10 E=0 P=130、编译#审2前 E=0（驱动自己的编译，不再 FAIL/挂死）。回炉文_轮1_2 回执 8/8。
- 06:02 在轮2 评审起腿前停驱动（腿 0、xelatex 0）并 `--resume`（pid 67373，腿数 277）：S5 从轮1 重进，`本地基座同步` 未覆盖 论文/；编译#审1前 E=0 P=130、页图 130；审1A/审1B/硬伤1 任务文本带上一轮台账 20 条与裁定文件，评委模拟1 按新页图重跑（替换 05:02 那份旧稿评委意见）。R40 待验：本轮「待改 → 算a/图b/文c」不再 a=b=0。
- 06:22 S5 轮1（重进）：面板均分 7.17（A 7.33/B 7.02，上一版 6.65）、硬伤 0、评委第一印象 7.6、相对判断=更好；老台账 20 条评审裁定 已消解 5/未消解 15，新增 10。R40 实盘验证：待改 25 条 → 算1/图6/文18（算=问3 求解脚本 refit_sensitivity 零基准扰动分支，定位到问题3 正确；图 6 条含 4 条审稿员指到绘图脚本、2 条评委页级）；驱动派 回炉算_轮1_问3 走 建模师→跑脚本→解读→红队复算→G2 门。
- 06:22–07:45 S5 轮1 算路实盘：回炉算腿 8 分钟回执；跑脚本 rc=0；解读；红队独立复算「不齐」（8 项复算值全 null：结果声明未公开折分/固定源行/条件模型口径）→ 仲裁裁定「8 项均为同口径复算输入未交付，由建模补齐，数值保留」→ 仲裁返工 → 红队复核 对齐（最大相对差 0.0045%）→ 解读却判 FAIL（正文/附录/图未同步，见 R42）+ 门检 7 条未分类（见 R41）→ G2:问3 第 1 次 FAIL 派返工。
- 07:48 链路修复：R41 门检类别词表（热同步后 G2:3 手工复检 未分类=0，只剩『解读未 PASS』）；R42 驱动四处解读任务加 `_回炉边界句()`（S5 阶段生效）+ 解读师.md 同款条款（热同步，G2 返工后的解读腿即用）；AGENTS.md 固定实验记录类别词表。py_compile 过、干跑 全链/门升格 过。
- 08:22–08:59 问3 G2 门：返工 1/2 后红队复核均对齐，解读两次仍 FAIL——理由已按 R42 排除「文图待同步」，剩下的是真科学缺口：硅两束模型公平对照的留出误差比训练均值劣化 165%/20%，协议 2 基线、3 交叉印证不过（审-1-06）；第 3 次检查 FAIL → 升格蜂群 3 变体腿（08:59）。其间一条解读腿 codex 6 分钟无输出 rc=1，波次自动重试成功。
- 09:05 链路修复（R43）：S5 轮级断点（每轮修订后落盘 `S5已完成轮`，续跑从下一轮起）+ MAX_HOURS 40。计划在轮1「checkpoint[S5-轮1修订后]」出现后停驱动 --resume，让 R42 驱动侧、R43 与新预算一起生效，轮2 起不再重跑。
- 09:49 问3 升格裁决选升格3（硅全量往返 2.17 μm、折2 却 16 μm；碳化硅 10.80 μm 不变），红队复核对齐，解读仍 FAIL（硅最坏折相对朴素基线风险，非 R42 类误判）→ G2:问3 降级放行，级联无下游。回炉图_轮1 回执 6/6；回炉文_轮1_1 回执 12 条、转路 3（审-1-03→算、审-1-06→算、审-1-09→图）——R40 回执改路首次真腿验证。
- 10:20 链路修复（R44）：已升格并降级放行过的问，S5 回炉算后 G2 只复检一次，不再每轮重烧 返工×2+升格（否则轮2 起问2/问3 各 1.5–2 h/轮）。干跑 门升格 场景加断言。与 R42/R43 一起在轮1 落盘后由切换脚本 --resume 生效。
- 10:26 轮1 落盘（编译#修10 E=0 P=132，checkpoint[S5-轮1修订后]）→ 切换脚本 2 秒内停旧驱动 67373、状态.json 写 S5已完成轮=1、--resume 起新驱动 87763（腿数 313）；新段打出「S5 续跑：前 1 轮已落盘，从轮2 继续」，未重跑轮1 任何腿——R43 真跑验证。编译#审2前首次 E=97 是旧驱动被停时正在起的 审2前 编译留下半截 aux，修复循环第 0 步重编即 E=0，无腿参与。10:27:49 审2A/审2B/硬伤2 起腿。切换脚本两处瑕疵已改：macOS pgrep 无 -c（改 wc -l）；日志/*.pid 会过期且 pid 被系统复用（建模_问题2.pid 指向的 69828 已是别的进程，非组长故 kill 组未生效），改为先验命令行再杀。
- 10:29–10:32 事故：切换脚本仍在 `sleep 150` 时我原地改写了它（修 pgrep/pid 守卫），bash 按字节偏移续读被改长的文件，醒来后从 --resume 那一行重新执行 → 10:29:14 起了第二个驱动（pid 90817）。两驱动并行 ~2 min：第二驱动重编 审2前、`rm` 掉四条腿的 pid/done 并重派同名腿（91xxx），第一驱动（87763）随后按 pid 文件判自己的 审2A/审2B/硬伤2「已死」再重派（94xxx），原腿（88xxx）成孤儿。处置：杀第二驱动及其 91xxx 腿组；杀孤儿 88502/88511/88519 及其 codex 组（保留驱动正在跟踪的 94xxx 腿）；评委模拟2 的 done 是第二驱动腿被杀时写的 AUTO 假标记，删之并把 pid 文件改回第一驱动仍在跑的图片腿 88497。损失：三条评审腿重跑 3 min；腿数计数多 4。**纪律：正在运行的 bash 脚本不许原地改写（要改就先杀再起，或写新文件）；停跑/切换一律先杀干净再动文件。**
- 10:50 S5 轮2 面板 6.50（A 6.7/B 6.3）硬伤 4 评委 7.7，相对判断=更差（B：问3 权威结果已是硅 2.17 μm/两折 1.73–16.04，论文摘要—正文—附录—图仍是 2.58/2.70/3.83，成套错版），续跑无轮1 快照故不回退。裁定 已消解 8/未消解 17；熔断搁置 5、升格 4——其中 审-1-01/-10/-20 三路已消解仅评委模拟「未核实」→ 暴露 R45；算 3 条只派问3 → 暴露 R46（问2 的 审-1-03 无腿接手）。待改 27 → 算3/图7/文17，回炉算_轮2_问3 起腿（G2 将走 R44 只复检）。
- 10:55 链路修复 R45（评委模拟弃权票不否决）+ R46（受影响问认章号）落地，单测 37/37、干跑四场景过；轮2 落盘后切换生效，切换时按新规则复核轮2 裁定、纠正误搁置条目。
- 11:24 问3 回炉算后 G2 走 R44「只复检一次」→ 降级放行（累计 4），算路 34 min（轮1 为 2.8 h）——R44 真跑验证。回炉图_轮2 回执 7/7、成图 rc=0（5 张问3 图 11:34 重绘）；回炉文_轮2 回执 12+5、转路 3（两条撰稿腿误以为 PNG 未更新转图，轮3 图路再过一遍）。
- 12:02 变化守卫：24 章过，8.3.问题三源码.tex 换成升格3 源码被判 90% 重写回退 → 暴露 R47。编译#修20 E=0 P=139，checkpoint[S5-轮2修订后]，S5已完成轮=2。切换脚本 12:03:24 触发停驱动后被我按停（要先上 R47），驱动停在轮2 边界、无腿在飞。
- 12:08 R47（源码章不设变化守卫）落地；停机窗口内：按 R45 复核轮2 裁定（弃权票 6）把 审-1-01 改回已消解；8.3 回填留底新稿（旧版另存 审稿/回退稿/S5轮2/8.3.问题三源码.旧版.tex），对应条目改回待复核；随后 --resume 从轮3 起。
- 15:10 skill 骨架 + 配置级整合（用户指令「先搭 skill 骨架和配置级整合，这炉照常收尾；skill 适配 Claude Code 与 Codex 两端安装运行」）：
  ① `.claude/skills/paper-foundry/`：SKILL.md（操盘手册：接手必读/开炉续跑/监督/插手四步/收尾/铁律/两端差异/安装）+ `scripts/`（启动·双驱动保护、停跑·验命令行后整组回收、状态、看守·分段盯日志、验证·py_compile+单测+干跑、切换·自复制后等标记停净→钩子→验证→续跑、安装、跑后指标.py）+ `references/`（铁律 14 条、监督清单、流程与档位、交付报告模板、病根登记）+ `agents/openai.yaml`（Codex UI 元数据）。一份源双端：`安装.sh` 符号链接到 `~/.claude/skills/paper-foundry` 与 `~/.codex/skills/paper-foundry`（已装）；Codex 沙箱内起不了腿（腿要联网、要 kill 进程组），SKILL.md 写明启动/切换/停跑须沙箱外执行。
  ② 配置级整合（不改阈值/门/守卫/提示词）：`--档位=深度|标准|快速`（标准 = 摘要变体 5→3、审稿 4→3 轮、MAX_HOURS 30、角色分档；`--快速` 旧写法仍认）、`--角色分档`、`蜂群驾驶.角色分档表`（读题/规划/建模/红队/解读/预测 xhigh；撰稿/审稿/猎手/章评/统稿/绘图 high；图评/美化/评委/读者/复盘 medium）→ `本地蜂巢.leg/leg_img(effort=)` → 环境变量 `LEG_EFFORT` → `role.sh` `${LEG_EFFORT:-${CODEX_EFFORT:-high}}`、`图片腿.sh` 同理；不下发时口径与 20260909 对照跑完全一致。角色分档表按角色文件名，图片腿的仲裁/裁决类腿仍走全局值。
  ③ 验收：py_compile 39 文件；单测 回路 38/38、统稿守卫 11/11、门检 19/19、审计、契约核对 10/10；干跑 全链/S5续跑/门升格 过；`DRYRUN_OPTS="--档位=标准" python3 流水线/验证/驱动干跑.py 全链` 打印「角色分档下发：xhigh 19 / high 26 / medium 20 / 全局 1」（干跑假腿记 effort=）。skill 脚本在运行中的对照跑上实测：状态/看守/跑后指标正常；`启动.sh ... --resume` 对同父目录在跑的驱动正确拒绝（exit 3）；通过 ~/.codex 与 ~/.claude 的链接、从 /tmp 运行也能回溯到项目根。
  ④ 踩坑：macOS 自带 bash 3.2 变量名必须 ASCII，`$var` 紧跟中文要写 `${var}`（否则中文字节并进变量名报「未绑定的变量」）；macOS `ps` 把非 ASCII 命令行转义成 `M-x`，`ps | grep 中文` 永远不中——进程判定一律 `pgrep -f`。已知未修：`验证/答案门单测.py` 源码级抽取漏 `读词表`，0/7（列入整合清单 i）。
  ⑤ 本炉照常收尾：14:31 S5 四轮到上限退出（面板 7.17→6.50→7.35→7.43，评委 7.6/7.7/7.6/7.8）；14:39 摘要定稿门过（复述=能，第一印象 7.9）；美化轮1 6.8 分/113 页 → 排版执行按美化师意见删附录重复刊载的问一/问二完整源码（lst:full-q1 634 行、lst:full-q2 951 行）→ 68 页（正文各章大小未变，核过）；美化轮2 7.1 分。
- 15:20 R48（G5 门检附录代码量只数内联 lstlisting）：15:08 G5 第1次检查 FAIL 两条明细，其一「附录 lstlisting 代码过少或缺失（107 字符）」是假阳性——排版执行1 按美化师意见删掉 8.附录.tex 里重复内联刊载的问一/问二完整源码后，源码全在 8.1/8.2/8.3 的 `\lstinputlisting` 外链里（8 处 93113 字符）。为免 G5返工1 为凑字符把 45 页重复代码塞回去，15:09 用 skill 的 `停跑.sh` 首次实战停净（驱动/腿/xelatex 0 残留；G5返工1 未写任何 tex）。修 `运行时/门检.py`（附录范围含 源码/补充材料 章；外链按 firstline/lastline/linerange 计量；缺文件单列）+ 门检单测 2 例（G5外链 过 / G5外链缺文件 拦）→ 21/21；`验证.sh --干跑=全链` 过；停机镜像实跑 G5 门检只剩台账阻塞级一条明细；15:2x `启动.sh --resume`（G5 节点重跑：终编→审计→门检→返工）。教训：门检的机械规则要跟着排版规范走——源码外链刊载是正解，门不能只认内联。

- 15:30 R49（G5 返工没有图路）：R48 修后 G5 第1次检查只剩台账一条明细——8 条阻塞级里 4 条 目标=图、1 条 算，而 `_G5返工` 只派撰稿师，图条永远消不了 → 必然三败降级、错图出版（审-2-06 图13 仍标 2.58 μm，正式结果 2.165/1.732 μm）。修：`_G5返工` 先走与 S5 回炉图同构的图路（绘图师 `G5返工图{n}` 改脚本 + `求解/成图回炉G5_{n}.sh` 重跑受影响图 + checkpoint 求解），再派撰稿师改剩下的文/算条，猎手复核一并裁定；干跑新场景 `G5图路`（末轮留一条图硬伤：事件序 G5返工图1→成图回炉G5_1→G5返工1→硬伤G5返工1，第 2 次过门不降级）。验证.sh 全过（39 文件、单测 5 组、干跑 7 场景）。切换.sh 布防在 `门[G5] 第2次检查 FAIL` 边界（当前 G5返工1 撰稿师腿让它跑完并复核，切换后 G5 从头：检查→图路返工→复核）。算路不进 G5（列为 流程与档位 §5 k，出版前重算风险大）。
- 15:41 R49 实战切换：15:40:54 G5 第2次检查 FAIL（剩 审-2-04 算、审-2-06 图 + 两条搁置图条）→ 切换.sh 4 秒内停净（TERM 驱动 56819 与刚起的 G5返工2；一个被 node 复用的旧 pid 被守卫跳过）→ 验证 10 秒全过 → 15:41:13 --resume（pid 2934）→ 15:42:03 G5 重检 FAIL → 先启 ['G5返工图1']（绘图师图路首次真跑）。
- 15:50 R50（G5 对算条空转）：撰稿师 G5返工1 回执 8 条转路 4，猎手复核 4 条文类全消解，剩下 审-2-04 是算条——G5 无算路，再派撰稿师只会「需转算」退回、每轮白烧 0.5–1 h 后照样降级。修：`_G5返工` 图路之后把算条记搁置（理由「G5 无算路」）、无文可改不派撰稿师、无图条不派复核；干跑新场景 `G5算条`（验证.sh 8 场景全过）。再次布防切换于续跑段的「门[G5] 第2次检查 FAIL」。

### 2026-09-10 15:55 全量日志复盘 → 链路调优分析（用户要求：跑太久 + 要更好效果）
- 数据：`真题测试/驾驶_对照.out` 12 段全量（09-09 14:38 → 09-10 15:48）、三本台账、镜像 351 腿落盘时刻；脚本 `scratchpad/分析/时间线2.py`（波次以「波次起→下一条非补位日志」计，紧随脚本按腿 `.done` 切开）。
- 产出：`真题测试/链路调优分析_对照版.md`（时间账 / 质量账 / 分级方案 A1–A5、B、C / 叠加预期 / 与整合清单 a–k 的对应）。`流程与档位.md` §5 已链接。
- 主要结论：在线 24.4 h；S5 算路 5.45 h（22%）且是换版不同步硬伤主源（审稿台账 14 条、轮 2 面板跌 0.67）；S2 红队↔仲裁循环 4.1 h，7 次仲裁 2 次有效；单腿波次 15.45 h（72%），同层问 2/问 3 串行 1.6 h；一次性重做+停机 4.5 h；S4 章评 221 条只消解 19%，179 条到上限后无环节接手。
- 估算：干净跑 ≈20 h → 标准档 ≈15 h → +A1 算路收口 ≈12 h → +A2 并行 ≈10 h → +A3/A5/C ≈9 h。方案由用户定，未动链路。

### 2026-09-10 16:25 R51 + R52（G5 返工：复核前编译 / 页数守卫）
- 现象：R49 图路 15:52 已把图 13 重绘成 2.17 μm，猎手 16:10 仍按旧 PDF 判「仍标 2.58」熔断搁置（R51）；15:12→15:40 全稿 68→112 页，8.3 被整份塞进 求解_问题3.py + 复算.py，源码章守卫豁免、门检不查总页数（R52）。
- 修法：`_G5返工` 复核前 `_编译修复循环("G5返工N复核前")`；新增 `页数守卫_G5`（基线 `状态.美化后页数`，美化阶段每次编译记录）；撰稿师任务文本加页数纪律。干跑新场景 `G5页数`（撰稿师塞源码 → 40 页 → 回退代码章留底 → 过门）；干跑 exec mock 不再用空串盖掉 f_local 回退文件。
- 本炉处置：切换钩子 `scratchpad/钩子R52.py`（8.3 回退到排版执行后版本并留底、状态记 美化后页数=68、审稿台账并入 正确性/文 条目要求不塞源码地重写 8.3 入口说明），布防在「门[G5] 第2次检查 FAIL」。
- 验证：py_compile 39、单测 5 组、干跑 9 场景全过。

### 2026-09-10 16:50 提示词收紧 v1（用户要求：优化每步提示词、skill 约束更紧、降低回流）
- 方案 `提示词收紧方案-v1.md`；病根台账新增 P 系列 P1–P10（证据/归属/修法/检测）。
- 改动：角色文件 11 个（红队/解读师/建模师/撰稿师/绘图师/硬伤猎手/审稿员/评委模拟/章评师/美化师/复盘官，全部追加不删）；驱动任务文本（红队、回炉三路、G5 返工、章评、章修订）+ 美化阶段图条直派绘图师；skill 铁律 15/16、监督清单回流预算、新参考 `提示词与回流.md`。
- 生效：角色文件在下次 --resume/新炉同步；本炉剩余腿不受影响。验证与冒烟见下一条。
- 验证（17:12）：py_compile 39；单测 回路 38/38、统稿守卫 11/11、门检 21/21、审计、契约核对 10/10；干跑 9 场景全过（全链 新增断言 P6 美化图路：美化师 目标=图 → 绘图师 美化图1 + 成图美化1，文条 → 排版执行1）。角色冒烟待本炉终态后在副本根做（活炉期间不对着活镜像冒烟）。
- 驱动侧同步落地：红队两条腿任务句（两套口径/只按声明口径判）、章评任务句（≤5 条、[严重度N]、轮 2 只评改动段）+ 章评条目记 严重度、章修订按严重度 1→3 排、S5 回炉·重算/改图/改文 与 解读回炉 与 G5 改图/改文 任务句（换版清单/成图脚本纪律/体量纪律）、`_S5美化` 图条直派绘图师（`美化图N` + `求解/成图美化N.sh`）。
- 真跑旁证（17:01）：`编译#G5返工1复核前0: E=0 O=0 P=68`（R51 生效）；页数守卫未触发（撰稿师本轮未塞源码）；结构守卫按「源码清单减少 2→1」回退 8.3——指令「≤80 行入口片段」与守卫尺「清单数不减」打架，记入经验沉淀 §6 / 故障手册 E 节，尺子不擅改。

### 2026-09-10 17:20 paper-foundry skill 交付版（用户：事无巨细、按任务经验沉淀、先交付 skill）
- SKILL.md 重写为操盘手册（三件事分清、接手必读、动作总览表、开炉/监督/插手/收尾、铁律 19 条一句话版、两端差异、安装、参考索引、量级参考）。
- 参考 11 份：铁律（+15 提示词改动纪律、16 回流预算、17 豁免配体量尺、18 指令最小改动、19 磁盘）、监督清单（+6 行线索 + 回流预算表）、流程与档位（S5b/G5 行、§6）、交付报告模板（收尾 SOP、§4b 回流账）、病根登记（P 系列）、新：提示词与回流、故障手册（A–H 八类 40+ 条）、切换手册（含三次实战记录与钩子）、角色与契约（17 角色表 + 契约所有权）、经验沉淀（15 条经验、用户决定清单、下一炉配方）、安装与环境。
- 脚本 11 个：新 体检.sh（不读密钥，只看 config.toml 存在；磁盘/链接/驱动/锁/git）、钩子样板.py（恢复/留底/状态/并入，--演练，驱动在跑拒绝真跑；副本根实测 审-9-01）；跑后指标.py 加 回流账 + P 检测；公共.sh WATCH_RE 加页数守卫/复核前/换版清单/修脚本/rc≠0/美化图/转路；安装.sh --检查；openai.yaml。
- 实测：体检.sh 硬失败 0 提醒 5（磁盘 15 GB、驱动在跑、断点、git 34 处）；跑后指标 回流账 在对照跑全量日志上：仲裁 16、口径差异 8、转路 28、脚本 rc≠0 16、修脚本腿 15——正是 P1/P6/P8 的证据。

### 2026-09-10 18:05 对照跑终态 + 收尾
- 17:56:01 `终态：E=0 Overfull=0 页数=66 总腿数=411 用时1596分钟`；18:02 复盘报告（瓶颈 12、规则库建议 12、不建议改 7）。门史：问 1 PASS、问 2 降级、问 3 三次降级记录、G5 第 3 次 FAIL 降级放行（审-2-04 算条、审-7-01）。S6 终审 14 条必改，整改后 68→66 页。
- R51/R52 真跑验证写入台账；复盘官实证 R53（终审整改清单按 5000 字符截断，14 条只发 10 条）：驱动改为传文件路径与条数，干跑 全链 加断言；S6 图路/复核留给用户定。
- 交付报告 `真题测试/交付报告_对照版.md` 回填全部「待回填」；跑后指标落盘 `真题测试/跑后指标_对照版.md`（含回流账：仲裁 16、口径差异裁定 8、转路 28、脚本 rc≠0 16、修脚本腿 15）。
- 角色冒烟（P 系列）：副本根 `scratchpad/冒烟根`（rsync 镜像去日志/页图，528 MB），`角色冒烟.py 红队 硬伤猎手 章评师 解读师 审稿员 --根=副本`（任务表新增章评师/解读师/审稿员三条），结果见下一条。

### 2026-09-10 18:29 角色冒烟结果（P 系列）
- 副本根 `scratchpad/冒烟根`，五腿顺序单跑 18:03–18:28，全部 done：红队 6 min、硬伤猎手 6 min、章评师 5 min、解读师 2 min、审稿员 6 min。核对脚本 `scratchpad/冒烟核对.py` 0 项不符。
- 逐角色：红队报告 `口径说明` 有、结论「不齐」且分歧明细如实写空（脚本只写不跑）；猎手 `硬伤_轮0.json=[]`，done 写明依据 PDF 17:55:54/66 页、无换版清单时不臆造旧值；章评师两章各 3 条、`[严重度N]` 3/3、范文对照 有；解读师 口径说明四段 3/3、返工单带 `层级=实现`；审稿员 5 条五键齐全、定位到 文件:句 引原句、无整章重写。
- 台账 P1/P2/P4/P7/P9 → 已落地·冒烟过；P5/P8 冒烟部分（样本无版式项/脚本未执行）；P3/P6/P10 仍是干跑或待真跑。冒烟根不进仓库。

### 2026-09-10 19:30 skill 双版本（Claude 版 / Codex 版）+ claude 腿引擎 + 参考论文入库 + 推送准备
- **腿引擎开关**（用户：Skill 做两个版本，一个 Claude 驱动可开子代理，一个 Codex 驱动，链路一样）：新增 `流水线/运行时/claude公共.sh`（`claude -p` 口径：系统提示 = AGENTS.md + 角色文件留档 `日志/<腿>.system.md`，任务作用户消息，`--effort` 直吃分档表，`--permission-mode acceptEdits` + 工具白名单 + 禁 WebFetch/WebSearch，`--no-session-persistence`，stdin 关闭，子 shell 里 unset CLAUDECODE）；`role.sh`/`图片腿.sh` 按 `LEG_ENGINE` 分发（claude 看图腿把图路径列进任务由腿 Read）；`本地蜂巢.py` `LocalHive(engine=)` + `找claude()` + 同步腿 stdin=DEVNULL；`蜂群驾驶.py` `--引擎=codex|claude`（默认 LEG_ENGINE→codex）记进 `状态.json`，续跑换引擎打「!! 腿引擎切换」。角色文件、编排、门、守卫、台账零改动。
- **验证**：新单测 `流水线/验证/腿引擎分发.sh`（桩 codex/claude，16 项：flag/推理档/系统提示/附图/stdin/done 兜底/引擎名校验）；干跑新场景 `引擎`（--引擎=claude → 配置 → Hive → 状态.json）；`验证.sh` 全过（py_compile 39、五组单测、10 场景干跑、打包核对）。**真腿未跑**：本机 `claude -p` 带用户 settings 时指向退役端点约 3 分钟后 401 且 `--model` 被 env 覆盖；跳过用户 settings 时 `Not logged in`——用户侧配置，密钥纪律不动。首次实测还发现 `claude -p` 会等 stdin EOF（未关就永远挂），已进 故障手册 I。
- **skill 脚本**：启动/切换/状态/体检/安装/公共 加引擎（启动.sh 续跑默认沿用状态里的引擎；体检 `--引擎=claude --探针` ≤120 s 只转述 CLI 原话，本机实况 = 120 s 无响应；安装 `--宿主=`；公共 `是腿/腿数` 认 `claude -p`，`_find_proj` 加 `发布/<版本>` 层级）。新增 `打包.sh`：从核心生成 `发布/paper-foundry-claude`（默认引擎 claude、无 agents/）与 `发布/paper-foundry-codex`（默认引擎 codex、openai.yaml 改名），`--核对` 进验证门防漂移。两处 bash 3.2 `$v（`/`$rc：` 变量名吞中文的老坑又踩了一遍，已改 `${v}`。
- **文档**：SKILL §2.1/§4（4.1 宿主差异、4.2 引擎差异表）/§5；铁律 20（引擎只在边界显式换）；安装与环境 §7 两种引擎鉴权口径与本机实测；角色与契约 §引擎与腿契约；故障手册 I；经验沉淀 17；流程与档位 §引擎与档位正交。
- **参考论文入库**：`参考论文/2025国赛B_对照版/`（论文 PDF/tex、求解、交接、审稿，去候选/页图/缓存/>5 MB，41 MB 603 文件）+ README（成品位置、量级、裁剪说明、题目附件不入库）。密钥/端点扫描 0 命中。
- **推送准备**：仓库无 remote；历史里有沙箱时代的一个 `HIVE_KEY` 默认密钥字面量与端点主机名（工作树 3 处已改为「自定义端点」）——不推历史，用当前树造孤儿提交推到新建私有仓库（见下一条）。

### 2026-09-10 19:55 已推送 github.com/qybaihe/paper-foundry（私有）
- 仓库原无 remote，用户 gh 已登录（qybaihe），105 个仓库里没有本项目 → `gh repo create paper-foundry --private` 新建；`origin` 已加。
- 推的是当前树的**孤儿单提交**（分支 `github-main` → 远端 `main`，提交 79b4410，977 个文件，含 发布/ 两版 skill 与 参考论文/）；本地 `main`（53+3 个提交，含沙箱时代 `HIVE_KEY` 默认密钥字面量与端点主机名）不推、也没设 upstream。导出树扫描：密钥模式 0 命中，主机名 0 命中，`HIVE_KEY` 只作为名字出现在两处文档。
- 第三方内容提醒：`参考项目/MM-Agent关键源码`（usail-hkust/LLM-MM-Agent，CC BY-NC 4.0，README 有署名）随树进了私有仓库；转公开前请用户自行判断是否保留。
- 以后再推快照：`git commit-tree HEAD^{tree} -p origin/main -m "<说明>"` 得到提交号 → `git branch -f github-main <号>` → `git -c credential.helper='!gh auth git-credential' push origin github-main:main`（不要 `git push origin main`，那会把本地全历史推出去）。

### 2026-09-10 20:15 仓库改名 MM-Final-Skill、转公开、根 README
- `gh repo rename MM-Final-Skill` + `--visibility public`（用户明示），本地 origin 已改为 github.com/qybaihe/MM-Final-Skill；描述同步。
- 根 `README.md`（用户要求「细皮实」）：§0 推荐 Astra（gpt-6-astra）+ `api.timoz.me` 官转渠道放最前（**端点主机名进 README 是用户本次明示的例外；密钥仍只在 config.toml，示例用 env_key 占位**）；§1 阶段/门/角色表 + 全景图；§2 真跑成绩与诚实声明；§3 两版本两引擎；§4 安装；§5 开炉到交付 + 档位；§6 仓库结构；§7 P1–P10；§8 边界；§9 许可与致谢（MM-Agent CC BY-NC 4.0）。
- 推送仍走孤儿快照：`commit-tree HEAD^{tree} -p origin/main` → `github-main` → `origin main`。

### 2026-09-11 准备 2026 国赛 A 题一炉（用户：解压 CUMCM2026Problems.zip，做 A 题，干净工作目录 + 交接提示词）
- 解压：zip 内文件名是 GBK，`unzip` 直接解会把 `A题/A题.pdf` 丢掉（Illegal byte sequence）；用 Python zipfile 按 cp437→gbk 解名。A 题 = 「药材的烘干问题」4 问，附件1（烘房温度/水分 242 行）、附件2（半径随时间 146 行）、附件3 四个结果模板 result1–4.xlsx；`format2026.doc` 用 textutil 转 txt（2026 规范：摘要 1 页、正文 ≤30 页、附录含全部可运行代码）。
- 工作目录：`国赛2026A/输入/`（A题.pdf、论文格式规范2026.txt、附件1/2.xlsx、附件3/result1–4.xlsx），产出 `国赛2026A/成品_A题`；.gitignore 加 `国赛2026A/{输入,成品*,蜂巢镜像*,台账}/` 与 `驾驶_*.out`（题目附件不入库）。
- R54：播种不递归 → `基建清单()` 改 rglob（数据保相对路径，题目层 .txt/.md 进 题目/）；播种清单自测 8 项；验证.sh 全过。自测时误把驱动顶层段 exec 了一遍，在 国赛2026A 生成了一个空 状态.json，已删（体检确认「无断点」）。
- 新增 `流水线/验证/核对结果模板.py <成品> <模板目录>`：核对 result1–4.xlsx 工作表名、0…2/0.1 距离网格（21 列，result4 末列药材表面）、A 列时间步（1 s / 60 s）、数值非空、四位小数；空模板自测 ✗×4、合格样例 ✓×8。收尾 SOP 加这一步（不合格派建模师，不手填）。
- 体检 `国赛2026A/成品_A题`：硬失败 0（磁盘 21 GB）。交接提示词 `交接提示词-2026A.md`（本次任务、开炉命令、监督、已知坑、收尾、待用户决定：正文 20/30 页、档位、引擎、入库）。未开炉——由下一线程按交接执行。


### 2026-09-11 11:04 开炉 2026 国赛 A 题（按 交接提示词-2026A.md 执行）
- 开炉前：git 干净；`体检.sh 国赛2026A/成品_A题` 硬失败 0、提醒 2（另有 claude 引擎 / shell python3 无 numpy，均正常）；`验证.sh --干跑=全链,中断续跑,级联,门升格,韧性,S5续跑,G5图路,G5算条,G5页数,引擎` 全过（py_compile 40、单测五组、干跑 10 场景）；磁盘 21 GB。
- §七 决定项用户未答，按默认：正文上限 20 页、档位深度、引擎 codex、成品不入库、阈值/参数沿用。
- 命令：`启动.sh 国赛2026A/输入 国赛2026A/成品_A题 --档位=深度 --并发=4 --effort=xhigh --引擎=codex` → 驱动 pid 10323，运行ID 20260911-110414，日志 `国赛2026A/驾驶_A题.out`。
- R54 真跑证据：镜像 `数据/附件3/result1–4.xlsx` 与 `题目/论文格式规范2026.txt` 均播进（11:04 基座同步 89 文件）；数据体检师 11:08 done 识别「结果模板数 4」并标「缩略模板/变半径表面列」风险。
- 监督：Monitor 盯 `驾驶_A题.out`（WATCH_RE + 驱动退出检测）。已备 scratchpad 工具：`契约核对_A题.py`（只读核对契约是否覆盖 result1–4/网格/时间步/格式约束/附件覆盖）、`补腿.py`（驱动停净后对镜像派一次性腿，驱动在跑时拒绝）。
- 11:15 S0 完成（读题 638 s）：`契约核对_A题.py` 核对——问题数 4；需求 24 条（6/6/5/7）连续编号；每问 可交付物/需求条目 含 resultN.xlsx、0.1 cm 网格、时间步（1 s / 60 s）、工作表名、result4 药材表面列；硬约束 44 条（C10 四位小数、C21 模板同名同表、C30 摘要一页、C31 正文 ≤30、C32 附录全部可运行源程序、C33 无身份信息）；附录 2/3/4 公式按 PDF 渲染图核对（C5–C7，指数 -0.89/C、-3850/T 等无误）；歧义裁定 17 条；附件清单覆盖 数据/ 全部 6 文件。**不需补契约。**
- 11:22 工具侧改动（非门判据）：契约裁定 result4 收缩后「越界格留空、表面列填当时半径处」，而 `流水线/验证/核对结果模板.py` 原要求全部单元格非空。已把 result4 的非空判据改成「表面列每行非空 + 每行空格只许是尾部连续越界段」，其余三文件仍全非空；不为迁就脚本让建模师填 0。自测：合成 result4（半径逐步收缩、尾部留空）✓，中间挖空 ✗，result1 全网格 ✓；`验证.sh` py_compile 40 + 单测五组通过。驱动未动。

### 2026-09-11 12:16 R55 腿内 python3 看不见 venv（登录 shell path_helper 重排 PATH）→ 规划师把全炉锁成纯标准库路线
- 现象：G1 PASS 后核对 `计划.json`，交付与执行.环境 写「numpy、scipy、pandas、matplotlib、openpyxl 均不可发现…数值与 xlsx 读写延续标准库路线…自实现小型线性代数…matplotlib 示意图缺库是待环境维护解决的前置条件」；回查 `日志/体检.log:1012`、读题官、`日志/规划.log:2677-2681`，三条腿探针（`/bin/bash -lc` 内 `python3`）全报 ModuleNotFoundError/不可发现；数据档案 风险 D07 同源。
- 根因：codex 腿在 seatbelt 里用 `/bin/bash -lc` 跑命令，macOS 登录 shell 的 path_helper 把 /opt/homebrew/bin 排到 LocalHive 放在首位的 venv 之前，腿内 `python3` 落到 homebrew 裸 3.14；本机模拟 `PATH=<venv>/bin:$PATH bash -lc 'command -v python3'` → /opt/homebrew/bin/python3 复现。AGENTS.md 第 12 行「已装齐」与腿实测矛盾，规划师据实测降级。2025 B 炉建模师直接写 numpy 未被探针吓退，故未暴露。
- 修法（链路，不动提示词/蓝图）：`本地蜂巢.py` LocalHive.__init__ 给腿环境加 `PYTHONPATH=<venv>/lib/python3.*/site-packages`（venv home 即同一 homebrew 3.14）；`体检.sh` 新增硬失败项「腿内登录 shell 探针」（LocalHive.env + `bash -lc`），`打包.sh` 同步两发布版。
- 插手四步：12:16 `停跑.sh` 停净（S2 开跑 4 min，只 TERM 建模_问题1 一腿）→ 改 → `验证.sh --干跑=全链,引擎` 通过、体检探针 ✓ → 12:18 `补腿.py 规划师.md 计划修订`（任务：先探针、只改因缺库而降级的口径）1.5 min 完成：探针原话 `python3.14 numpy 2.5.3 scipy 1.18.1 pandas 3.0.5 matplotlib 3.11.1 openpyxl 3.1.5`，只改 交付与执行.环境/绘图规范 与 求解计划.md 两段，顶层 14 键/4 问/45 结构/20 图/19.5 页不变，实验记录 66→67 → 12:20 `启动.sh --resume` 新驱动 pid 13122，S2 第 1 层重开。
- 病根台账 R55（已修·待真跑验证）；检测：真跑后 `grep -l "No module named" 蜂巢镜像/日志/*.log` 与 `grep -l 不可发现` 应为空。

### 2026-09-11 12:55 R56 红队「口径行」误触发仲裁（P1 驱动侧漏改）
- 现象：问1 红队报告 结论=对齐、四项按声明口径最大相对差 5.8e-5，但 分歧明细 里一条 类型=口径 的「1800秒表面含水率」相对差 4.81%（表面半格采样带体积均值 vs 表面点值），驱动 12:55:00 判「红队不齐（1 处超容差）→ 派仲裁腿」；仲裁 3.5 min 后裁「口径差异成立、应改方 无」，12:58:31 `门[G2:问1] PASS`。本次代价小，但每问都会重演，且仲裁腿裁错就会走返工链（对照跑同类回流 4.1 h）。
- 根因：`蜂群驾驶.红队复算()` 的分歧过滤只看 相对差 > 红队容差，不看 P1 引入的 `类型` 字段；红队按 P1 把口径差异写进 分歧明细（类型=口径）是设计内行为。链路问题，提示词没错。
- 修法：过滤排除 类型=口径 行并在日志记数；`驱动干跑.py` 红队 mock 加 4.8% 口径行 + 全链断言「无 仲裁_问题* 腿 且日志含『口径差异 1 条只作交叉印证』」。铁律 6：旧驱动下断言先失败（伪仲裁 问1/2/3 三次），新驱动 10 场景全过。
- 上线：`切换.sh --标记 "先启 \['建模_问题2'\]"`（标记已出现 → 立即停/验/续，只损失刚开 4 min 的 建模_问题2）。
- 问1 结果文件门：`核对结果模板.py` 对镜像 `求解/问题1/结果/result1.xlsx` ✓（温度/水分浓度两表、1800 行 × 21 列、1 s 步、四位小数、全非空）——R55 修复后建模师用 openpyxl 直接写出，无需补写。R55 检测：续跑后 建模/执行/解读/红队 腿日志 `No module named` 0 命中。

### 2026-09-11 13:06 R57 切换.sh 副本缺 打包.sh → R56 上线的自动切换在「验证」步失败
- 现象：12:59:03 `切换.sh --标记 "先启 \['建模_问题2'\]"` 命中标记、`停跑.sh` 停净后，副本目录跑 `验证.sh` 报 `…/qiehuan.Ycm0k9/打包.sh: No such file or directory` → `!! 验证未通过，不要续跑`（exit 6），驱动留在停净状态。12:59:52 人工 `启动.sh … --resume` 续上（pid 60832，日志「S2 第1层：问[1] 已完成跳过（续跑）… 先启 ['建模_问题2']」），停机约 1 min；新驱动已载入 R56 代码。
- 根因：`切换.sh` 自复制清单只有 切换/公共/停跑/启动/验证 五个脚本；2026-09-10 双版本打包时给 `验证.sh` 加了同目录 `打包.sh --核对`，清单没同步。工具问题，非驱动/提示词。
- 修法：清单加 `打包.sh`（它经 公共.sh 用 `$PROJ` 解析路径，复制后照常工作）；`验证.sh` 加静态核对「切换.sh 复制清单闭合」（清单内脚本引用的 `dirname "$0"` 同目录脚本必须都在清单里）。铁律 6：临时去掉 打包.sh 让核对先失败一次（`✗ … 验证.sh→打包.sh`），恢复后 ✓「6 个脚本，同目录引用全在清单内」。途中踩了 bash 3.2 的 `$f→` 多字节变量名坑（`未绑定的变量`），改 `${f}→${ref}`。
- 验证：模拟切换副本（六脚本拷临时目录跑 验证.sh）→ 发布版一致、验证通过；`打包.sh` 重打；`验证.sh --干跑=全链` 通过（R53/R56 断言均 PASS）。病根台账 R57（已修·待真切换验证）。R56 行状态不变（等问2–4 红队日志）。
- 炉况：13:06 状态.sh —— 阶段 S2:第1层，活腿 建模_问题2（≈7 min），本段 429/rc=142/Traceback/!! 均 0，问题门 {1: PASS}。
- 13:25 问2 过门：`红队结论=对齐（口径差异 6 条只作交叉印证，不计入不齐）`→ 无仲裁腿 → `门[G2:问2] PASS`（建模 24 min、红队 1.5 min）。R56 真跑验证，台账状态闭环。`核对结果模板.py` 对镜像 `求解/问题2/结果/result2.xlsx` ✓×8（温度/水分浓度、10800 行 × 21 列、1 s 步、四位小数、全非空；末行中心 49.8492 ℃ / 1.7663 kg/kg 与红队复算一致）。
- 工具侧：`核对结果模板.py` 加 A1 角格软提示（△，不计不合格）——result1.xlsx 角格写成「时间/s；距离/cm」而模板是「时间\\到药材中心的距离」，契约未规定角格文字，记入交付报告「需人工关注」，不为此回炉。

### 2026-09-11 14:12 R58 结果模板文件不在门与任务文本视野内：问3 result3.xlsx 未舍入存值
- 现象：14:12:50 `红队结论=对齐（口径差异 4 条…）`→ `门[G2:问3] PASS`（建模 47 min）。`核对结果模板.py` 对镜像：result3.xlsx 网格/步长/非空 ✓，但 `✗ 存在超过四位小数的值`——3433 行全是未舍入浮点（t=600 行 2.549999999999922），末行实际结束时刻 205927.3300770656 s 也未舍入；角格 A1 写成「时间」。`求解_问题3.py` 导出段只给 WriteOnlyCell 设 number_format="0.0000"。契约硬约束「所有结果保留四位小数」、裁定「非 60 s 规则终点补完整末行」（末行本身合规）。红队 3 项数值分歧 ≤5.6e-4、4 条口径行 ≤4.0e-4，结束时长 57.2020 h。
- 根因：G2 只看解读/红队/门检产物；`跑一问` 的建模任务文本一个字没提 resultN.xlsx；`建模师.md` 也无小数位/模板条款——问1/2 自觉 round 了，问3 没有。链路 + 任务文本两处漏。
- 修法（不改门判据）：驱动加 `结果模板()`/`结果文件核对()`，过门后跑 `核对结果模板.py --只=resultN.xlsx` 只记日志（`!!` 前缀进监督正则）；`核对结果模板.py` 加 `--只=` 过滤；P11：建模任务追加条款 ⑥（模板存在时）。`验证.sh --干跑=全链` 通过。是否把结果文件核对升为 G2 判据 → 交付报告里问用户。
- 本炉处置：`切换.sh 国赛2026A/输入 国赛2026A/成品_A题 --标记 "先启 \['绘图" --钩子 国赛2026A/钩子_结果文件回炉.py --干跑=全链 --最长 14400`（后台）。标记选 S3 绘图波次首行——它在 `状态.标记完成("S2:问4")` 之后打印，停在这里不会让问4 重跑；只损失刚起的绘图腿。钩子：驱动停净后跑核对 → 对每个 ✗ 文件派 建模师 `回炉_结果文件_问题N` 腿（任务：只修写出层、所有存入数值先 round(…,4)、角格沿用模板、从 分钟数表_未舍入.json 等重建、改回求解脚本导出函数并另写 重建结果文件.py、openpyxl 读回自检写 导出核验.json、实验记录/建模笔记追加、不改结果声明数值）→ 复核仍 ✗ 则退出 1 不续跑。工具副本入库：`国赛2026A/钩子_结果文件回炉.py`、`国赛2026A/补腿.py`。
- 台账：R58（已修·待边界回炉验证）、P11（已落地·待冒烟）。

### 2026-09-11 14:43–14:52 S2→S3 边界切换：结果文件回炉（R58 处置）+ R57 真跑验证
- 14:43:20 `门[G2:问4] 第1次检查 PASS`（问4 红队对齐，口径行 4 条；四问 G2 全部一次过，0 仲裁 0 返工 0 升格）。14:43:23 切换命中 `先启 ['绘图1','绘图2','绘图3']`，14:43:26 停净（TERM 绘图 3 腿）。
- 钩子 `国赛2026A/钩子_结果文件回炉.py`：首核 result3/result4 ✗ 四位小数 → 派 建模师 `回炉_结果文件_问题3`（256 s，PASS：75547 个数值实际四位舍入、末时刻 205927.3301 s、角格对）、`回炉_结果文件_问题4`（168 s，PASS：3054 行 × 23 列、末时刻 183218.4375 s、越界留空/表面列合规、未重算）→ 复核 `结果模板核对：全部合格`（仅剩 result1 角格 △ 提示）。独立复核：数值只是舍入（result3 48 h 中心 0.1615、6 h 表面 0.5335 与回炉前一致），结果声明_问题3/4 mtime 未动，实验记录 155→157，建模笔记各追加「结果文件回炉」，求解脚本导出函数已改并各带 重建结果文件.py。
- 副本 验证.sh（含 打包.sh --核对、R57 清单闭合）全过 → 14:50:45 `--resume` → pid 1386，S3 绘图波次重启 → 14:52:05 `切换完成`。停机 7.4 min。R57 首次真切换走通，R58 边界回炉闭环，P11 同款措辞冒烟过；三行状态已改。
- 监视器重挂到 pid 1386（正则加 `结果文件核对`）；清掉上一监视器泄漏的 tail。回炉任务文本留档 `国赛2026A/任务_回炉_结果文件_问题3.md`、`…问题4.md`。

### 2026-09-11 19:56 R59 审计只看第一个「问题重述」文件 → G4 两次 FAIL 的「缺要素」是假阴性；P12 返工腿不改稿
- 进度：S3 图证 15:02 成图 20 张 → 图评两轮 7.22→7.60（0 张低分）→ G3 首检 FAIL（图型分布：散点 17/折线 10/热力 4、示意 2）→ 返工重绘 17 张 → 15:55 G3 PASS。S4：叙事底稿门 PASS（16:00）、脊柱/主控 45 章（16:15）、45 章撰稿 85 min、章评两轮 8.49→8.53（读者分 6.89→6.95，121 条意见留给 S5）、统稿第一次腿因端点流断连（Reconnecting 5/5）rc=1 无 done，第二次成功（43 章、0 回退）、摘要 5 变体画像门过（19:36）。编译#编0：E=0、372 页（附录 lstinputlisting 全部源码）、摘要 1 页。
- G4：19:37 首检 FAIL 三条（摘要/2.2 对冲密度、自造缩写 KC、问题重述缺要素 ['有总体分析','有思路图']）；返工只改了摘要（KC 与摘要密度消掉）；19:52 二检 FAIL 剩两条不变。查证：`论文/1.2.总体分析.tex` 明明有 `\subsection{总体分析：…}` 和思路图 `\includegraphics`；`运行时/审计.py` 第 16) 节只取第一个文件名含「问题重述」的文件（1.1.问题重述.tex）就 break，拆成三文件的第 1 章永远判缺。返工腿自己也复现了这一点并在日志里写「意见 2」，对 2.2 的 1.71 密度（唯一命中：小节标题里的「不作」）写「意见 1」认为不是置信声明，两处都不改稿。第三次检查必降级放行（G4 无升格 fn）。
- 处置：19:56:00 `停跑.sh` 停净（TERM G4返工 腿一条，损失 4 min）。R59：审计第 1 章组合并判定（文件名/标题含 问题重述/总体分析/分析思路/总体思路 的章文件合并；判据不变）；`审计单测.py` 加拆分三文件两例，旧代码下 ✗×2 后再修；修完误伤单文件夹具（\section 标题后的空段被当成重述段）→ 改成取所有「问题重述」标题后的段落拼接 → 全部通过；对活镜像副本试跑五要素全 True。P12：`_G4返工` 任务文本追加「门判据不由腿裁决，超线必改写，异议写 审稿/审计异议.md」。`验证.sh --干跑=全链,门升格` 通过。20:0x `--resume`，G4 节点重跑（预期：首检只剩 2.2 密度 → 返工改标题措辞 → 二检 PASS）。
- 待用户决定（写进交付报告）：附录 372 页——8.3–8.6 用 lstinputlisting 收录四问全部源码（含各 3 条原型路线、复算、核验脚本），格式规范要求附录含可运行源码，契约裁定「全部参与计算核验的源码」；是否只保留主求解脚本+公共模块、原型/复算脚本改为清单，属蓝图级取舍。

### 2026-09-11 20:10 G4 过门（R59+P12 上线后 1 轮返工）；进入 S5
- 20:00:00 `启动.sh --resume` 新驱动 pid 71068；S4 三节点跳过（续跑）；20:00:53 编译 E=0 P=372 摘要 1 页。G4 首检 FAIL 只剩一条（2.2 对冲密度 1.71>1.0），R59 假阴性「缺总体分析/思路图」消失，审计修复在真跑中验证。
- P12 冒烟：返工腿把 2.2 副标「早期变化不作坏点」改为「早期变化原样保留」（层级/前缀/正文不动，汉字数 586 不变），并按新条款写了 `审稿/审计异议.md`；20:07:20 G4 二检 PASS。台账 P12 → 已落地·冒烟过。
- 审计异议.md 里的第二条（`bin/审计.py` 点号拆键遇数组根 `/38/尝试` 转整数失败被记为「键未命中」，只记账不阻塞）先记为 R60 候选，等 S5 过后再核证据、不在炉中改审计器。
- G4 终态统计（`审稿/门检_G4.json`）：禁用词 0、过精 0、表达超线 0、自造缩写 0、溯源覆盖率 1.0、溯源存疑 0、段首数字段 0、摘要 1 页、总页 372（正文按书签 ≈18 页，附录自第 22 页起）。
- 20:08 进入 S5 轮 1：审1A/审1B/硬伤1 三腿并发。继续以 Monitor（bsmkcg689）盯 `checkpoint[S5-轮N修订后]`/美化/G5。

### 2026-09-11 22:50 S5 轮 1 回炉 S2 全过；发现超时腿不死、双副本并行（R60 已修待上线，R61 待查）
- 回炉：问 2（20:38 G2 PASS，result2 未变）、问 3（21:03 G2 PASS，result3 21:46 重导出仍四位合格，换版清单空）、问 4（22:34 仲裁后 G2 PASS；换版清单 1 条：零秒表面接口 2.5226→2.55，题定时长 50.894 h 不变）。三份 result 文件均用 核对结果模板.py 只读复核合格。
- 事故：解读_回炉_轮1_问4 两次「到时限仍无 done（1100s）」被判最终失败，但两副本都没被杀，分别跑了 65/50 min 才各自写 PASS；并行期间 22:26 改写 结果声明_问题4.json，正撞上红队复算窗口 → 红队「不齐（0 处超容差）」→ 仲裁 → 放行。图修1（15:08）同样两副本 rc=0。全炉 0 条 rc=142。
- R60（已修，验证.sh --干跑=韧性 通过）：wave() 判「到时限仍无 done」后先 `回收超时腿`（收集 role.sh 后代进程组，TERM→KILL，僵尸不误报），再重派/判失败。补丁只在源码，运行中驱动不受影响，计划在 checkpoint[S5-轮1修订后] 用 切换.sh 上线。
- R61（待查）：with_timeout 离线七种复现全部按时 rc=142（含真 codex exec、两种 perl、蜂巢 env、忽略 TERM 子进程），唯独驱动拉起的真腿不死。挂了每分钟采样器看在跑腿 34787（$t=1240）22:55 是否到点被杀，另起 1040 s 长闹钟对照。
- 操作纪律：只读复核 + 源码补丁 + 台账/日志，没有停炉、没碰镜像；期间误判「孤儿」一次是因为腿命令行含提示词全文（内含 grep 字样）被 `grep -v grep` 过滤掉，已改采样口径。

### 2026-09-11 22:53 切换在 checkpoint[S5-轮1修订后] 命中，停净判定被我自己的诊断进程搅浑；手工清残留后续跑
- 22:49:43 挂 `切换.sh --标记 'checkpoint\[S5-轮1修订后\]' --干跑=韧性`，22:49:52 标记即出现（编译#修10 E=0 P=381），22:49:53 TERM 驱动组 71068。
- 22:51:29 停跑判「未停净（驱动=1 codex=2）→ 不续跑」。三个残留全是我的：驱动=1 是一个滞留的工具 shell（命令行文本含「蜂群驾驶」被 pgrep -f 算作驱动），codex=2 是我 22:50:47 起的 R61 试验 8 真腿。炉子本身停得干净（TERM 腿组 0）。
- 处置：杀掉自己的试验树/滞留 shell/采样器 → 停跑.sh 复核「驱动=0 codex=0 xelatex=0 已停净」→ 22:53:00 `启动.sh --resume` 新驱动 pid 50250，「S5 续跑：前 1 轮已落盘，从轮2 继续」。R60 补丁随本次续跑上线（验证.sh --干跑=韧性 22:4x 已通过，源码此后未改）。Monitor 重挂到 50250。
- 教训登记 R62 候选：停跑/状态的进程计数用 `pgrep -f 'codex exec|蜂群驾驶'` 一类子串匹配，会把操盘手命令行里含这些字样的 shell、诊断进程算进去；以后诊断实验不在切换窗口内做，并把腿判定收紧到 argv 前缀。
- R61 新证据：试验 8（后台启动.py 祖先链 + 真 codex exec + LEG_TIMEOUT=60）60 s 准时 rc=142；被杀腿的沙箱内工具子进程 `sleep 400` 逃出组成孤儿（另记）。1040 s 长闹钟对照 22:57:46 到点，结果待记。

### 2026-09-11 22:58 R62 已修（驱动判定锚定 argv 前缀）；R63 登记（沙箱工具子进程逃出组）
- R62：`公共.sh` 四个驱动判定函数改用 `DRV_RE`（python 解释器 + 第一参数 …蜂群驾驶.py）。先用 `bash -c 'sleep 60; echo 流水线/蜂群驾驶.py'` 证明旧子串匹配误报、新匹配不误报且真驱动 50250 仍认；`打包.sh` 重新生成两份发布版，`验证.sh` 通过。只改操盘脚本，不碰运行中的驱动。
- R63：试验 8 里被 with_timeout 杀掉的真 codex 腿留下了沙箱内工具子进程 `sleep 400`（自成进程组），记为资源泄漏病根，修法按会话回收，待写失败用例后在下一切换点上线。
- 炉况：S5 轮 2 审稿场 22:54 起（审2A/审2B/硬伤2 并发），编译#审2前 E=0 P=381 摘要 1 页。Monitor b03jlbjti 盯 pid 50250。
- 23:00 R61 补记：1040 s 长闹钟对照 rc=142 准时（1042 s）。至此七种离线复现（短闹钟、长闹钟、两种 perl、蜂巢 env、忽略 TERM 子进程、真 codex、后台启动祖先链）全部按时杀腿，只有炉内三条真腿逃过；不再离线猜，改为活捉：采样器已重启，出现 etime>$t 仍活的腿时先 `kill -ALRM` 取证。R60 已上线兜底双写。
- 23:05 R63 已修（待上线）：`运行时/codex公共.sh` with_timeout 的 reap 先按 ppid 树收齐 $pid 全部后代再 TERM→KILL（组 + 后代）。先写会失败的用例（孙进程 os.setpgrp 后 sleep 300：旧 reap rc=142 但孙进程存活），新 reap 全清；回归四例通过；验证.sh 通过。bin/ 随下次续跑同步，不为它单独停炉。

### 2026-09-12 00:40 S5 轮 2：面板 7.50、硬伤 2（皆为文字与数据不同步）、回炉 S2 三问全过
- 轮 2 面板均分 7.50（A/B 各 7.5），评委第一印象 7.8，相对判断「更好」；待改 14（算 4/图 1/文 9）。两条硬伤：附录表 32 网格收敛差值仍是回炉前旧数；8.1 支撑材料清单五个指针指向问 3 导出核验.json 的「回炉自检」节点，而该文件已被 23:45 的求解重导出覆盖（R58 钩子腿把自检写进了会被生成器重写的文件——下一炉把自检产物另存或由导出层自己写，记入交付报告）。
- 回炉：问 2 23:38 G2 PASS（result2 未变；换版 8 条全是 128→256 加密校验量）；问 3 00:05 G2 PASS（result3 23:45 重导出、模板核对合格、换版空）；问 4 00:38 仲裁后 G2 PASS（result4 00:14 重导出合格；换版 8 条正是表 32 的新差值）。红队第二次「不齐」是其独立复算脚本 int64 初温数组原位加 float64 抛错，仲裁逐条定责「红队错、不发建模返工单」。同一问两轮都因红队自身实现问题进仲裁（轮 1 缺键、轮 2 类型错），各耗 6–15 min，记 R64 候选：红队复算脚本失败应先自修重跑一次再判不齐。
- 续跑后（R60 上线）至今无「到时限仍无 done」、无回收事件；采样器无超上限存活腿。

### 2026-09-12 02:56 S5 轮 3/4 与轮数上限退出；美化边界切换上线 R65（看图腿按并发上限分块）；R63 随本次续跑生效；登记 R64
- S5 轮 3（01:14）面板 7.82（A 7.8/B 7.85）、硬伤 1、评委 7.8、相对判断「更好」；熔断升格 审-1-01/02/03/06、熔断搁置 审-1-09（版式）。回炉 S2 三问 01:32/01:52/02:14 G2 一次过（问 4 本轮直接对齐、未仲裁）；result3 01:38、result4 01:58 重导出后模板核对合格，题定时长 57.20 h / 50.894 h 不变。编译#修30 E=0 O=1 P=327 摘要 1 页。
- S5 轮 4（02:41）面板 7.93（A 7.95/B 7.9）、硬伤 0、评委 7.8、「更好」；到轮数上限退出，4 条阻塞级留台账视图交 G5/复盘：审-1-01（硬伤·算，尝试 3、升格过，评委模拟以「所给页面未覆盖」判不可核验）、审-1-02（正确性·算，尝试 3、升格过，最新指令已自述为「非阻塞的语言整理」）、审-2-02（正确性·文，源码摘录/行号/SHA 同版，评委模拟判不可核验）、审-1-05（正确性·文，待改）。四轮里每轮都把问 2/3/4 送回 S2（≈1.5 h/轮），其中多条是「不重算」的文字同步项——路由效率问题写进交付报告，不在炉中改判据。
- 02:41 摘要定稿轮 1 一次过：能复述、四要素缺失 0、画像超线 0、第一印象 8.2、待改 0（编译#摘1 E=0 O=1 P=327）。
- 发现 R65：`图片腿()` 直接 spawn、不走 wave() 的并发闸，`_S5美化`/`_S6终审` 按 8 页一腿整份铺开——对照跑 66 页只有 9 腿，本炉 327 页（附录源码 ≈305 页）会一次起 41 条 codex 腿，10 倍于实测稳定并发 4（6 并发曾整晚 429）。属编排链路缺陷，按「先修链路」处置，不动阈值/蓝图。
- 02:51:40 挂 `切换.sh --标记 'S5 摘要定稿轮1: 能否复述=能.*待改0|S5 摘要定稿轮2:' --钩子 钩子_R65就位.sh`（钩子：源码含 `图片腿群` + py_compile 过 + 全链干跑通过标记新于源码，否则不续跑）。02:51:49 标记出现、02:51:51 TERM 驱动组 50250、02:52:45「驱动=0 codex=0 xelatex=0 已停净」，钩子按预期拒续（补丁尚未写入），炉停在美化边界；已完成节点 27（摘要定稿已落盘）。
- 补丁：`蜂群驾驶.py` 新增 `图片腿群(规格们, 单腿超时, 等待)`——按 `并发上限` 分块起腿、每块 `等腿`，美化与终审两处改为组规格后调用（腿名、任务文本、timeout 800/等待 1000 不变）。失败用例：`驱动干跑.py` 全链场景假页 19→40（美化/终审各 5 腿），假 hive 在 `leg_img` 计「美化/终审同时在跑峰值」、`等腿` 轮询归零；旧驱动峰值 {美化:5, 终审:5} > 4 → AssertionError（已复现），新驱动通过。`验证.sh --干跑=全链` 通过（40 文件 py_compile、发布版一致、9 组单测）。
- 02:56:02 `启动.sh --resume` 新驱动 pid 8652，S0–S5:摘要定稿 全部「已完成，跳过」，进美化（编译#美1）。停机 4 分钟。R63（with_timeout reap 收齐后代）随本次续跑同步进 bin/，状态改「已上线·待真跑验证」。R60 自 22:53 上线至今未再出现「到时限仍无 done」，保持「已修·待真跑验证」。Monitor 换新 pid。
- 登记 R64（待查）：红队复算脚本自身失败（问 4 轮 1 结果声明缺键、轮 2 int64 dtype）即判「不齐」→ 派仲裁，仲裁两次都裁「对齐」；脚本错误与数值分歧应分开处理。
- 采样器（R61 活捉）自 22:53 起无一例超 $t 存活腿；摘要定稿1 $t=1140 在 8 min 内正常结束。

### 2026-09-12 06:25 美化两轮 → G5 三检降级放行 → S6 终审/出版；终态 E=0 O=0 292 页 358 腿 1122 min；发现 R66（成品未收割 result*.xlsx）
- 美化：轮 1（03:25）41 腿 28 min，页问题 134（124 条在附录源码页）、美观 7.0、327 页；派 15 条：`美化图1` 受理 9 + `成图美化1.sh` rc=0、`排版执行1` 受理 6，编译 E=0 O=1 P=324。轮 2（04:39）41 腿 55 min，页问题 174（合并 161/新增 13）、美观 7.1；`美化图2` 受理 8、`排版执行2` 受理 7，编译 E=0 O=1 P=294——降页来自源码清单改单倍行距（8.5/10.2pt）、`emptylines=*1` 压空行、清单间距减半，各源码章 `lstinputlisting` 数不变（15/6/8/6/9），8.2 公共源码 190→163 页；另按美化意见删问 1 配置小表并入正文。页数守卫全程 0 次。R65 分块真跑：每块 4 腿、活腿 ≤4、429=0。
- G5：04:59 一检 FAIL（4 条阻塞级未消解）→ 返工 1：审-1-01/审-1-02（目标=算）按 R50 搁置；撰稿师 `G5返工1` 受理 4 转路 1；猎手 `硬伤G5返工1` 按实物独立核对裁 审-2-02 已消解（20 条 code-src 散列、39 个行号选择器一致），4 条叙述级熔断搁置。05:26 二检 FAIL（审-1-05 待改 + 2 搁置）→ 返工 2：审-1-05 转「需转图（仅排版）」走 R49 图路 `G5返工图2`（删重复 figure、改浮动体），无文条不派撰稿师，猎手裁已消解。05:46 三检 FAIL 只剩 2 条搁置 → 降级放行（状态.json 降级放行 1 条）。复核前编译 E=0 O=2 P=292。
- S6：终审 37 腿 18 min，10 条必改 → `终审整改` → 编译#终整改 E=0 O=0 P=292；06:18 出版收割 1299 文件；终态「E=0 Overfull=0 页数=292 总腿数=358 用时1122分钟」；复盘官腿 06:18 起。
- R66：对成品跑 `核对结果模板.py` → 4 项「未找到交付文件」——出版收割列举没有 `*.xlsx`，result1–4.xlsx 留在镜像 `求解/问题N/结果/`。修法：列举补 `-o -name '*.xlsx'`；干跑桩记下列举脚本原文、全链断言含 `'*.xlsx'`（旧驱动先红、新驱动后绿），`验证.sh --干跑=全链` 通过。处置计划：等驱动自然结束（复盘落盘）且停净 → 钩子 `钩子_重收割.py` 把 S6:出版 从 已完成节点 摘掉（备份 状态.json）→ `启动.sh --resume` 只重跑出版节点 → 再对成品核对。不手工搬文件。

### 2026-09-12 06:35 收尾：R66 重收割真跑验证、成品核对全过、跑后指标/交付报告落盘、采样器停
- 06:27:40 复盘落盘（瓶颈 14、规则库建议 13）→ 06:28「驾驶结束」；复盘腿进程 06:28:39 自然结束（$t=900 内），停净 驱动=0 codex=0 xelatex=0。
- 06:29:01 钩子 `钩子_重收割.py`：备份 状态.json → 摘除 S6:出版（复盘保持完成）；06:29:02 `启动.sh --resume` 只重跑出版节点：编译#出版 E=0 O=0 P=292 → 06:30:18「S6 终稿收割 1307 文件」→ 终态「E=0 Overfull=0 页数=292 总腿数=359 用时1133分钟」→ 复盘跳过 → 驾驶结束。成品多出 result1–4.xlsx（+ result4_尾窗均值.xlsx）；`核对结果模板.py 国赛2026A/成品_A题 国赛2026A/输入/附件3` 全部合格（仅 result1 角格 A1 提示）。R66 → 已修·真跑验证。
- 终态成品：`国赛2026A/成品_A题/论文/论文.pdf` 292 页（摘要 1、正文 p2–21、参考文献 p21–22、附录 p22–292）、E=0 O=0；状态 阶段=完成、腿数 359、已完成节点 32、累计机时 18.88 h；降级放行 1（G5，两条搁置算条 审-1-01/审-1-02）。
- 出版节点重跑的门检留档：G4 复检 FAIL（摘要数字密度 40.56>29；过精 3 处 0.04986/0.01963/2.53037）、G5 FAIL（同两条搁置）、G0/叙事 PASS——记入交付报告 §8 交用户决定，不手工改稿。
- 采样器（R61）22:41–06:29 共 464 次采样，超 $t 存活腿 0；已 `pkill -f scratchpad/sampler.sh`。R61 保持待查（现象未复现），R60 保持待真跑验证（回收分支未触发）。
- 落盘：`国赛2026A/跑后指标_A题.md`（跑后指标.py）、`国赛2026A/交付报告_A题.md`（按模板；占位全部回填）；台账 R64（待查）/R65（已修·真跑验证）/R66（已修·真跑验证）/R67（待修）；记忆文件更新。本条之后：提交本地 main、按 commit-tree 口径推 GitHub 快照、向用户汇报并请拍板（附录体量、终审 3 张图、G4 复检两项、参考论文归档、复盘 13 条建议）。
