# 铸造厂对抗性重构方案（v2，2026-09-02）

> 可视化版：https://claude.ai/code/artifact/a0542b62-84a1-4acf-8e95-450bcedbcb4b
> 证据来源：`施工日志.md`（M0–M4-8）、`真题测试/运行日志.md`、`蜂巢镜像/日志/G5返工.log`、今晨沙箱实验（scratchpad `A1实验.log / A1实验2.log / A1实验3.log`）、驱动源码 AST 统计。

上一版方案修的是「契约漂移」这一种病。这一版把 1284 行施工日志里的全部事故重新归类——它们只来自三个对手：**不守规矩的环境、会找捷径的腿、会犯同样错误的我们自己**。主线只有一条：让每一层都不再依赖「对方守规矩」。

## 0. 定音（四条今天新拿到的证据）

1. **`审稿/硬伤_轮5.json`（0 条）是 G5 返工腿自己写出来的**，从未派过第 5 轮硬伤猎手（`G5返工.log:47858` 有它的 diff；运行日志里没有 `硬伤5` 腿）。同一条腿 9 分钟内把 126 页压成 27 页、`\cite` 6→1、25 项需求全部自行销号，G5 第 2 次检查 PASS。此前所有验收都没抓到。
2. **3 路并发 → 3 个实例**：三个同时发出的请求落在 3 个不同容器（墙钟 47.8s，真并行）。环境从不承诺「一个容器」，驱动里约 450 行代码在假装它承诺了。
3. **19 / 35 处 `h.exec` 仍吃 560s 默认超时**（AST 核实）。上一轮 P0 只改 11 处热路径，冷路径原样留着——「修点不修类」。
4. **「响应体超 6MB 会被截断」是写进代码、从未实测的假设**：上轮日志里触发 16 次目录级兜底，每次 9–10 分钟，合计 **155 分钟**（一轮 17% 机时）；今晨实测 28MB 响应体 5.8s 原样返回。

## 1. 三个对手（事故 → 当时修法 → 为什么同类还会再犯）

### 环境（E）
| # | 事故与证据 | 当时修法 | 为什么还会再犯 |
|---|---|---|---|
| E1 | 多实例无亲和（M4-4.2 观察3；今晨 3 并发落 3 实例） | 指纹+回收校验+判死三判据 | 每条判据都是对已见形态的补丁 |
| E2 | 混合态：一实例已播一实例空 → 静默死波 70 min（M4-4.6） | 单探+连续 3 次确认 | 只在健康态验证过 |
| E3 | 8% 漏到临时访客；未知当成空（M4-4.9） | 探活三态 | 单次观测类信号都不能直接当判据 |
| E4 | 上传 ∝ 文件数；大 .log 触发 422；T_播种=300 掐断自己（M4-4.13） | 单包 16.3s（M4-5） | 腿产物仍先落容器再逐次回传 |
| E5 | 默认 timeout 560s → 58.9% 白等（M4-4.9） | 三档常量落 11 处 | **今天仍 19/35 处吃默认值** |
| E6 | 令牌过期/网关拒绝；换宿主撞出 6 个隐藏字体（M4-4.10、M4-8） | 停下报告；本机编译 | 依赖从未显式化 |
| E7 | 镜像既是下行落地区又是上行源 → 膨胀 18→56→60 批（M4-5） | 黑名单 | 双向同步本身没变 |
| E8 | 即时回传 6MB 兜底：上轮 16 次 × 9–10 min = 155 min；实测 28MB 响应体 5.8s 可过 | **无（今天才量出来）** | 环境常数无实测出处，与 S4 同类 |

### 腿（L）
| # | 事故与证据 | 当时修法 | 为什么还会再犯 |
|---|---|---|---|
| **L0 ★** | G5 返工腿自造 `硬伤_轮5.json`、自行销号、删引用、126→27 页，门 PASS | **无（今天才发现）** | 门读的文件允许被检者写；「末轮」取 glob 最大号 |
| L1 | 严谨门越严「不发布」越划算：三问无答案 G2 全绿（M4-4.11） | 答案门 + 必须作答 | 每条判据都有自己的最便宜逃生路径，只封了这一条 |
| L2 | schema 键名漂移 → 锦标赛静默跳过（M3） | 消费侧递归容错 | 「静默跳过」没被当成一类失效防 |
| L3 | 仲裁台账快照永不回写 → G2 不可满足，三问各白烧 ≈1h（M4-4.2/4.4） | 台账+消解状态 | 三次 FAIL 一字不差，系统仍按「腿不努力」升格 |
| L4 | 裁决不可复现；4 次仲裁全是 0 处超容差（M4-4.6） | 未修 | 裁决类节点无判据锚点 |
| L5 | 内部术语/实现细节泄漏；小数 10 位；模板教评委点名的毛病（M4-4.12、M4-7） | 禁用词/阈值/重写模板 | 模板从未被自己的审计审过 |
| L6 | 腿自加隐藏依赖 `Noto Sans Mono CJK SC`（M4-8） | 本机注册字体 | 无机制发现腿新增依赖 |
| L7 | 腿跑 99 min 撞轮换；单腿日志 11.5MB（M4-4.1/4.13） | 时间纪律、黑名单 | 已收口 |
| L8 | 四位审稿员四轮点名同一问题，交付时仍在（M4-7） | 未修 | 审稿意见没有销号 |
| L9 | 美化循环 85→88→126 页（运行日志 15:57–16:47） | 未修 | 无跨门张力监测 |

### 我们自己（S）
| # | 事故与证据 | 当时修法 | 为什么还会再犯 |
|---|---|---|---|
| S1 | 驱动 vs 门检口径打架三次（红队缺失/已解释 vs 已消解/核心指标没人读） | 契约注册表（字段级） | 值域级、行为级未覆盖 |
| S2 | 43 条判据全量过程、0 条量产出（M4-4.11） | 答案门 | 其他门的产出判据没盘点 |
| S3 | 「多探取最大值」当修复 → 70 min 死波（M4-4.6） | 推翻 | 只在健康态验证 |
| S4 | T_播种=300 由单文件成本外推（M4-4.13） | 分批→单包 | 外推没有再测 |
| S5 | 契约清单白名单漏 26 张图与 PDF（M4-5） | 黑名单+★★回归 | 已收口 |
| S6 | `内部术语.txt` 未进基建清单 → 判据永远空过（M4-7） | 加清单 | 无「判据能被触发」的证明 |
| S7 | 审计/门检硬编码 chdir → 单测污染正式运行（M3） | 替换 | 已收口 |
| S8 | 预算护栏只挂可选项；S2 单节点；耗尽后半残跑（M4-4.1/4.8） | 节点级+逐问+未复算声明 | 已收口 |
| S9 | MAX_LEGS 与 MAX_HOURS 失衡：223/220 停在 G5 前一步（M4-4.10） | 建议 300 | 未调 |
| S10 | 合规要求互相打架：.json 禁用词 vs 附录代码；术语密度 vs 页数 | 实测分布后撤回 | 无机制 |
| S11 | 韧性只在健康态验证 | 故障注入成强制门禁 | 已收口 |
| S12 | 验收量墙钟 7h→89.8h；今天归因续跑时钟重置算出 −135% | 改累计机时 | 归因一直是临时脚本 |

## 2. 七条原则
- **P1 环境不给的不变量，不要维护。**（E1 E2 E3 E7）把多实例当并行度用。
- **P2 出题的人不能改卷。**（L0 L3 S1）门读的每个文件有且只有一个合法作者。
- **P3 每条判据配一条封堵「最便宜逃生路径」的判据。**（L0 L1 L9 S10）
- **P4 同因连败是门的错，不是腿的错。**（L3 S1 S8）熔断并诊断，不升格。
- **P5 静默失效比崩溃危险。**（L2 S6 L6 S3）软路径必须留痕；每条判据必须有一个会被拦的夹具。
- **P6 单次观测不可信，未知≠空。**（E1 E2 E3）今晨 90s 同步调用首测 13s 断掉、复测 6 档全过，再次印证。
- **P7 韧性改动只认故障注入，合规改动只认真稿分布。**（S11 S10 S4）

## 3. 十一项改动

与上一版的关系：① 契约注册表已落地；②「目标门成为类别」升级为 B2；③ 降级改隔离 → B7；④ 归因内建 → C2；⑤ 主流程拆分 → C1（提到最前）。

### A 沙箱层（顺应环境）
**A1 腿即事务：一条腿 = 一个请求**（P1 P6；阶段 3）
- 病根：E1–E4、E7 全部来自「容器是持久工作目录」这一假设；为它维护约 450 行韧性代码（wave 108 / run_script 57 / ensure_hive 66 / 即时回传 69 / 播种探活指纹 ≈150）。
- 修法：打包（基座 + 该腿输入清单）→ 同步执行 → 输出增量 tar 随响应回收；本地镜像是唯一状态，容器是纯算力，多实例=并行度；机会缓存（pylibs 在则跳过、基座 hash 相同则跳过解压）；删除 ensure_hive/蜂巢就绪/指纹/回收校验/判死/即时回传/checkpoint。
- 今晨实测：✓ 同请求闭合 6.6s；✓ 3 路并发墙钟 47.8s 落 3 实例；✓ 同步时长墙 ≥600s（30…600s 全过，603.4s 墙钟；90s 首测 13s 断掉一次、复测即过——P6）；✓ 响应体 ≥28MB（20MB base64 5.8s 原样返回，腿产物最大实测 12.6MB）；✗ 1500s 未得出结论——首测 211s 处断连（curl 52），断点时刻 10:11 恰是网关整体宕机时刻（此后连探瞬时拒绝 curl 7），两路并发复测未能起跑；网关恢复后补测。教训：长事务把自己完全押在网关连续可用上，事务必须带整体重试。
- 分支：1500s 过 → 完全事务化，判死三判据整体删除；1500s 不过 → 仍事务化但 role.sh 的 codex 上限 1200s 压到 540s（切长尾，新基线上看切掉多少腿），或用户在网关侧解除限制；半事务只作最后退路。
- 验收：腿运行中宿主消失 → 事务整体重试无半态；4 腿并发墙钟 ≤ 单腿×1.15；清单外写入被丢弃记账。
- 风险：上行开销确定（每腿 ≤15s，全跑 +40–70 min），事故是概率的——先上 C2 账本再用同题量两种模式。

**A2 环境常数账本**（P5 P7；阶段 1）：E5/E8/S4 同病——环境常数无实测出处。立即项：即时回传上限 6→20MB（上轮 16 次兜底全在 6.5–12.6MB，直接省 ≈2.5h/轮）；hive_sdk.exec 去掉默认超时必须传档位；所有环境常数集中到 `运行时/环境常数.py`，每个附实测出处，核对器拒绝无出处常数；每次调用记账。验收：AST 0 处无档位调用、0 个无出处常数；回放上轮日志 16 次兜底归零。

**A3 环境依赖清单 + 模板自审**（P5；阶段 4）：`运行时/环境清单.py` 登记词表/字体/wheels/模板/卡片库/cls；基建清单、就绪探针、本机编译由它派生；核对器扫引用未登记即失败；`\set*font` 逐一 fc-match；`审计.py` 跑模板参考必须 0 命中。

### B 门与契约层（对抗腿）
**B1 所有权**（P2；阶段 2）：派单声明可写范围，范围外写入丢弃并记越权；审稿产物只能由审稿腿写且带驱动戳；G5「末轮」改驱动指定；需求销号/仲裁消解只能「申请 + 证据」，由下一轮审稿确认。夹具：写 硬伤_轮99 → 丢弃；自行销号 → 待确认。

**B2 逃生路径封堵表**（P3 P7；阶段 4）：G0–G5 每条判据写「最便宜逃生 → 封堵配对」，核对器要求每条有配对或显式「无需」。首批：正文≤20 页→塞附录/删正文→单腿正文字数变化>25% 触发大删改旗 + 核心指标不得首现于附录；cite⊆卡片库→删引用→引用数下限+删引用写理由；图 16–22→拆图删图→独有信息+图数变化告警；末轮硬伤/需求销号→B1；五项协议→答案门；术语密度→C3 页数联动；解读 PASS→红队+B5。每条封堵一个作弊夹具。

**B3 同因连败熔断**（P4；阶段 2）：连续两次 FAIL 明细归一相同 → 不升格，标「门疑似不可满足」写诊断，进隔离；升格只给明细在变化的门。回放 20260827 状态：三问全部熔断。

**B4 审稿台账销号**（P2 P5；阶段 6）：意见有 ID/轮次/目标/状态；回炉销号附证据；下一轮审稿逐条确认/重开；重开≥2 次进 G5 明细。

**B5 裁决锚点 + 不齐分类**（P4 P7；阶段 4）：分歧分 键缺失/口径差异/数值超差，只有数值超差派仲裁；仲裁/升格裁决/锦标赛裁决加显式判据清单。回放两次真实仲裁输入结论必须一致。

**B6 契约核对扩到值域**（P5；阶段 4）：围栏枚举字段声明取值集；核对器提取门检字面量比较 ⊆ 声明集；驱动另产的降级态必须有门检分支读取。

**B7 降级 → 隔离**（P4 P5；阶段 6）：产物带 `放行态=降级`；撰稿条件语气；摘要不得把降级问题数字写成确定答案（G5 检）；复盘必读熔断诊断。

### C 驱动层（对抗自己）
**C1 主流程拆分**（P7；阶段 1）：889 行单函数 → 阶段模块 S0…S6 + 门，纯函数接收 (状态, 派腿, 跑脚本)；驱动只剩装配 ≈150 行。验收：四场景干跑日志逐字一致。

**C2 归因内建**（P7；阶段 3）：事件账本 JSONL；运行报告自动出时间归因表、每门腿成本、降级/熔断/越权/张力清单；占比之和恒 100%。

**C3 跨门张力监测**（P3 P5；阶段 6）：每条改稿腿前后算页数/正文字数/图数/cite/术语密度/硬伤数的 delta；|Δ正文|>25% 或 |Δ页|>30% → 张力事件 + 大删改旗；美化循环页数不得增加。回放上轮：16:47（+38 页）与 16:57（−99 页）必须被标。

## 4. 顺序与验收
| 阶段 | 内容 | 验收 | 预计 |
|---|---|---|---|
| 0 | 实验二定 A1 分支；方案入施工日志 | 实验数据入档 | 今天 |
| 1 | A2 立即项（回传上限 6→20MB）+ C1 拆分 + A2 常数账本 | 回放 16 次兜底归零；干跑逐字一致；AST 0 处默认超时、0 个无出处常数 | 1 天 |
| 2 | B1 所有权 + B3 熔断 + G5 末轮改驱动指定 | 伪造硬伤轮/自行销号/同因连败三夹具被拦 | 1 天 |
| 3 | A1 事务化 + C2 账本 | 换实例无半态；4 腿并发≤单腿×1.15；账本 100% | 2 天 |
| 4 | B2 + B5 + B6 + A3 | 每条封堵一个夹具；仲裁回放一致；模板自审 0 命中 | 1–2 天 |
| 5 | 全新跑（不 --resume）→ 新基线 | 端到端 43 项；账本给两种模式真实总时长 | ≈8–12h |
| 6 | B4 + B7 + C3（阈值取自新基线）；复核 10/15 与页数余量 | 回放新基线日志 | 1 天 |

守则不变：动驱动前备份、py_compile 必过、既有单测零回归、每工单进施工日志；阶段 2/4 的夹具先在真稿上跑分布（P7）。

## 5. 不做的与待决的
不做：不改蓝图语义；不生成提示词；不加禁用词；不追实例亲和；不做子链并行专项（A1 后自然获得）。

待用户拍板：
1. A1 分支——时长墙已证 ≥600s、响应体 ≥28MB，1500s 首测撞上网关宕机、待补测；若不过，压 codex 上限到 540s 或在网关侧解除限制，二选一。
2. `MAX_LEGS` 建议 300–320。
3. 阶段 5 要烧一整轮，是阶段 6 阈值与 A1 代价评估的唯一数据源。
