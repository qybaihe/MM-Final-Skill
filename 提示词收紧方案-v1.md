# 提示词收紧方案 v1（2026-09-10，依据对照跑 24.4 h 全量日志）

目标：**降低回流重做次数**，同时不放松任何门判据。链路级的大改（`真题测试/链路调优分析_对照版.md` A1–A5）另议；本方案只动提示词、任务文本和 skill 约束，全部**增量**、全部**带证据**、全部**可检测**。

## 0 原则
1. 只加约束、只加可选字段，不改既有 schema 键名（改了必过 `流水线/验证/契约核对.py`）。
2. 每条改动对应 `病根台账.md` P 系列编号（证据在先，改动在后）。
3. 角色文件只在 `--resume`/新炉时同步进工作根：跑炉中改 `流水线/角色/` 不影响当前腿；下次续跑会带上。
4. 改后 `验证.sh` + `角色冒烟.py`（改动最大的角色先冒烟）。

## 1 回流证据 → 提示词层原因 → 收紧
| # | 回流 | 本炉计数 / 机时 | 提示词层原因 | 收紧（落点） | 预期 |
|---|---|---|---|---|---|
| P1 | 红队「不齐」→ 仲裁 → 返工 → 复核 | 仲裁 7 次仅 2 次有效；4 次复核判「口径差异成立」；问 2 复核轮连续 4 次不齐；≈2.5 h | 红队只按自己的口径算，声明的 `口径说明` 不够机器化，口径不同的两个数被当数值分歧 | 红队：**两套口径**（按声明口径 + 独立口径），判对齐只看前者；解读师：口径说明固定四段；仲裁：**先对口径再定责**（`红队.md`/`解读师.md`） | 伪仲裁归零，每问省 0.5–1 h |
| P2 | 解读 FAIL → 返工 → 再 FAIL → 升格 → 降级 | 问 2：解读两轮 FAIL、返工 ×2、升格 3 变体、仍降级；≈1.5 h 答案未变 | 返工单不区分「实现级/方法级」，方法级问题被当参数微调反复返工 | 返工单加 `层级: 实现|方法`；方法级必须换方法族/换验证设计或按「最佳估计 + 区间 + 低置信」收口（`解读师.md`/`建模师.md`） | 少 1–2 轮无效返工 |
| P3 | 换版后成套错版 → 回炉文/图 → 猎手再抓 | 审稿台账 14 条「换版不同步」，轮 2 面板 7.17→6.50，图 13 到出版门前仍是旧数 | 重算腿不产出「旧值→新值→出现处」清单，改文/改图腿各自猜 | 建模师回炉必写 `交接/换版清单_问题X.json`；撰稿师/绘图师按清单逐处同步；猎手加第 6 项「换版残留」（三个角色文件 + 回炉任务文本） | 成套错版类意见 14 → ≤2 |
| P4 | 评审指令被落实成整份塞源码 / 整章重写 | 审-2-01「补入现有求解入口的引用」→ 68→112 页（R52）；变化守卫回退 2 文件 88%/50% | 审稿员指令没有「最小改动」纪律；撰稿师没有体量纪律 | 审稿员：**指令写法纪律**（定位到句、最小改动、源码引用=路径+散列+片段、版式标 `版式`）；撰稿师：**体量纪律**（`审稿员.md`/`撰稿师.md`） | 附录回胀归零，配合 R52 守卫 |
| P5 | 版式意见占用回炉批次 | 57 条里 30 条版式；评委模拟 21 条中 16 条版式，全按阻塞级派回炉 | 评委/审稿员不标级别，驱动默认「正确性/叙述」 | 评委模拟/审稿员：版式类 `级别=版式`（不进出版门，交美化） | 回炉文批次省 30–50% |
| P6 | 美化意见撰稿师退回「需转图」 | 排版执行 30 条里 14 条转路，各多绕一轮 | 美化师不标 `目标` | 美化师加 `目标: 图|文`；驱动美化阶段图条直接派绘图师（`美化师.md` + 驱动） | 美化轮少 1 次空转 |
| P7 | 章评意见淹没、消解率低 | 221 条消解 19%；轮 2 又出 57 条 | 章评不限量、不分级、轮 2 对未改段落重复提 | 章评师：每章 ≤5 条、`严重度` 1–3、指令引原句；轮 ≥2 只对本轮改过的段落提新意见（`章评师.md` + 章评/章修订任务文本） | 消解率 > 60%，轮 2 新意见减半 |
| P8 | 脚本首跑 rc≠0 → 修复腿 | 12 次 rc≠0、13 条修复腿 ≈0.8 h：断言致死 2、解释器无 numpy 2、批脚本自设预算/字体缓存 2 | 脚本把自检写成致命断言；腿内用了 PATH 外的 python；成图批脚本覆盖 MPLCONFIGDIR 并按用时判失败 | 建模师/红队：**自检不致命**、只用 `python3`；绘图师：**成图脚本纪律**（不覆盖 MPLCONFIGDIR、单图失败不让批脚本非零退出、失败只看 PNG） | 修复腿 13 → ≤4 |
| P9 | 猎手按旧 PDF 判已修好的图 | 16:10 图 13 已重绘仍判「仍标 2.58」→ 熔断搁置（R51 链路已修） | 猎手不写明依据的版本 | 猎手：裁定写明 PDF 修改时间与页码，PDF 早于源文件时按源判（`硬伤猎手.md`） | 误判可见、可追 |
| P10 | 复盘报告不量回流 | 复盘官只算总账 | 提示词没要求回流账 | 复盘官：加「回流账」（每类回流次数/耗时 → 对应条款） | 下炉调优有数据 |

## 2 逐角色改动（全部为追加，不删旧条）
- `红队.md`：工作流 2 加「两套口径」「自检不致命」；产出 schema 加可选 `口径对照`、分歧明细加 `类型`；判据改为「按声明口径 ≤1% → 对齐」。
- `解读师.md`：结果声明 `口径说明` 四段模板；返工单加 `层级`；仲裁台账加「先对口径再定责」；回炉边界加换版清单核对。
- `建模师.md`：脚本「自检不致命 / 只用 python3」；定向修改协议加 5「回炉必写换版清单」、6「方法级返工必须换法」。
- `撰稿师.md`：定向修改协议加 6「体量纪律」、7「换版同步」。
- `绘图师.md`：加「成图脚本纪律」；定向修改协议加 5「换版同步」。
- `硬伤猎手.md`：猎杀清单加 6「换版残留」；纪律加「写明依据版本」。
- `审稿员.md`：产出加「指令写法纪律」。
- `评委模拟.md`：修订建议带 `级别`；只对看到的页下结论。
- `章评师.md`：≤5 条、严重度、引原句、轮 ≥2 只评改动段。
- `美化师.md`：页问题带 `目标`。
- `复盘官.md`：分析动作加「回流账」。
- 不动：读者、统稿师、规划师、图评师、答卷预测官、读题官（本炉没有由它们引起的回流）。

## 3 驱动任务文本（提示词的另一半）
- 红队腿：指向「两套口径」。
- S5 回炉·重算 / 改图 / 改文、G5 返工：换版清单的产出与消费。
- 章评腿：≤5 条 + 严重度；章修订腿：按严重度 1→3 全部处理。
- 美化阶段：读 `目标`，图条直接派绘图师 + `求解/成图美化{轮}.sh`（小链路改动，干跑 全链 覆盖）。

## 4 skill 约束（paper-foundry）
- 铁律 15「提示词改动纪律」、16「回流预算」；监督清单加回流预算行；新参考 `references/提示词与回流.md`（P 系列 ↔ 证据 ↔ 检测）。

## 5 验证与生效
- `验证.sh`（py_compile + 单测 + 契约核对 + 干跑 9 场景）；`角色冒烟.py 红队 解读师 审稿员 章评师 撰稿师 硬伤猎手`。
- 生效：本炉不再续跑则下一炉生效；若再切换，新提示词随 `--resume` 同步（铁律 15）。

## 6 不改的
红队信息隔离与首轮独立复算、解读五项协议、两把守卫尺、门判据与阈值、配对评审协议。
