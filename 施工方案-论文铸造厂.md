# 施工方案：论文铸造厂 · 蓝图 v1 → 沙箱全量落地

> 2026-08-26 编制。本文是**执行级施工文档**：目标架构、现状映射、全部交接文件契约、逐条工单（含验收标准）、端到端验收、作业环境铁则。执行者按本文自主施工，无需回溯历史对话。
> 流程蓝图（先读）：`理想蓝图-论文铸造厂.md`（可视化版 https://claude.ai/code/artifact/23f19965-2e46-4bcc-85a9-31dd02dd7492 ）。
> **纪律**：蓝图定义"做什么"，本文定义"怎么做+怎么验收"。两者冲突以蓝图精神为准并在施工日志记录裁定。

---

## 〇、使命与完成定义

**使命**：把现行 v3 流水线（已能全自主跑通一篇论文）升级为蓝图 v1「论文铸造厂」：证据总线、六道一票否决质量门、依赖 DAG 调度与级联重算、战略锦标赛、红队复算、五路审稿场、断点续跑、进化外环——全部在远程沙箱容器内执行，本地只有驱动进程。

**完成定义（三条全满足才算完）**：
1. 全部工单（§六）验收通过，记录在 `施工日志.md`；
2. 用 `真题测试/输入`（题目B.pdf + 附件1-4.xlsx）完成一次**全开配置的自主运行**，端到端验收清单（§七）全绿；
3. 复盘官产出 `复盘报告.json`，其规则库修改建议已择要落实或明确记录不采纳理由。

**算力原则**：不吝啬任何算力，目标是最好的论文。预算护栏只防失控（死循环），不防花钱。

---

## 一、现状盘点（每个文件是什么、能干什么）

项目根：`/Users/bytedance/数模自动化`

### 1.1 可运行主链（已实证端到端出过论文）

| 文件 | 作用 | 关键内容 |
|---|---|---|
| `流水线/hive_sdk.py` | 沙箱 SDK | `Hive(account)` 类：`exec(script, files, outputs, timeout_s)`（curl POST `{BASE}/v1/exec?account=`，自动 cd `/tmp/蜂巢`）、`f_text/f_local`（文本/二进制 contentB64 上传构造器）、`harvest(outputs, dest)`（base64 拉回本地）、`leg(role,task,name,sync)`（写任务文件→`bash bin/role.sh`，异步=nohup）、`legs_parallel`。**BASE/KEY 硬编码在顶部（可用环境变量 HIVE_BASE/HIVE_KEY 覆盖）——不要改动、不要外传、不要提交到任何远端** |
| `流水线/蜂群驾驶.py` | 自主驾驶主脚本（约 476 行） | 现行 P0-P6 全流程。核心函数：`log`（追加 运行日志.md）、`budget_ok`（MAX_LEGS=140/MAX_HOURS=6）、`wave(legs)`（异步波次+done 轮询+失败重试一次，先 `ensure_hive()`）、`get_json`（收割+解析+一次修复腿）、`run_script`（nohup 跑脚本+轮询 rc+修复腿循环）、`compile_paper`（xelatex×2+grep E/O/P/摘要页）、`checkpoint/checkpoint_dir`（收割到镜像）、`ensure_hive`（探活 `[ -f AGENTS.md ]`，容器轮换则从本地镜像全量重播种） |
| `流水线/AGENTS.md` | 容器内公约（播种到蜂巢根） | 目录布局、`sys.path.insert(0,'/tmp/蜂巢/pylibs')`、GBK/日期事实、绘图规范（FandolHei、无 set_title、grid alpha 0.3、despine、dpi=200）、`交接/实验记录.json` 纪律（只记真实尝试） |
| `流水线/运行时/role.sh` | codex 角色腿运行器 | `role.sh <角色文件> <任务文件> <日志名>`：拼 prompt→`timeout 1200 codex exec` 带全套网关 `-c` 覆盖→日志落 `日志/{NAME}.log`→保证写 done 标记（异常兜底 AUTO） |
| `流水线/角色/*.md` | 11 个角色提示词 | 读题官/数据体检师/规划师/建模师/解读师/绘图师/图评师/撰稿师/章评师/审稿员/美化师。已含叙述军规 v2、五项正确性协议、图表配额、文献军规、区间协议等 |
| `流水线/运行时/审计.py` | 机械审计 | 禁用词逐行、图型分布配额、`% src` 溯源标注计数、段首重复、句式占比>40% 判违、**溯源核验**（逐条打开 `% src:文件:键` 指向的 JSON 比对数值，含缩放容差，输出存疑明细）→ `审稿/审计报告.json` |
| `流水线/运行时/禁用词.txt` | 35 条硬禁词 | 综上所述/值得注意的是/得到如下/如下表/不难看出/接下来我们…… |
| `流水线/运行时/优秀论文标准.md` | 评委视角标准 | 5-10 分钟初评、摘要四要素、正文≤20 页无目录、附录必须可运行代码、扣分清单 |
| `流水线/运行时/文献卡片库.md` | 27 条已核实文献 | GB/T 7714 成串 + cite 键；撰稿只准从库选 |
| `流水线/运行时/方法卡片库.md` | 8 张现代方法卡 | 共形区间/CQR、MinT、HistGBDT 分位损失、分组感知验证等，每卡含检验义务；与 HMML 冲突以此为准 |
| `流水线/运行时/循环.py` | 通用进化循环引擎 | 生成→批评→修订、平台期/达标退出。**已建未接线**，可复用于逐图/逐章循环 |
| `沙箱验证/format-sandbox.cls` | 国赛模板沙箱适配版 | 已验证 xelatex×2 零错 |
| `沙箱验证/wheels/` | 8 个 cp314 manylinux 离线轮 | scikit_learn 1.9.0、openpyxl、python_docx、lxml、joblib、threadpoolctl、et_xmlfile 等 |
| `真题测试/输入/` | 验收用真题 | 题目B.pdf + 附件1-4.xlsx |
| `真题测试/蜂巢镜像/` | 上次运行的增量镜像 | ensure_hive 重播种的来源；上次运行进行到 P3 问题2 时本地驱动进程被会话重启杀死（教训→工单 1.1） |

### 1.2 已知欠账（施工要修）

1. **scratchpad 依赖**：`蜂群驾驶.py` 第 154/206/208/210 行与 `ensure_hive` 引用 `/private/tmp/claude-501/.../scratchpad` 下的 wheels、Mrite/模板、HMML.md——该目录是上一会话的临时目录，随时可能被清。→ 工单 0.1。
2. **无断点续跑**：驱动死掉只能从 P0 重来。→ 工单 1.1。
3. **问题严格串行**：P3 按问题清单顺序跑，无 DAG 并行、无过门校验、无级联重算。→ 工单 1.2。
4. **蓝图新节点全部缺位**：锦标赛（现在的"蜂群变体"是在 P3 做全量实现，成本高且晚）、红队、答卷预测、叙事设计、硬伤猎手、评委模拟、复盘官、需求追踪矩阵、假设台账、分级修订单与回炉路由、合规终检、连败升格。→ 工单 M2/M3。
5. `get_json` 修复腿统一叫"修json"，并行时会撞名。→ 工单 1.4 顺手修。

---

## 二、沙箱作业环境铁则（全部为实测结论，违反必翻车）

1. **端点**：本地无限时端点，配置在 `hive_sdk.py` 顶部（BASE=`http://127.0.0.1:3789`，鉴权头 `authorization: Bearer <KEY>`）。无 60s/240s 时间墙。`?account=a1/a2` 钉两个独立容器（a1 主线，a2 可作对抗泳道）。**KEY 只准留在本机文件里。**
2. **容器随时可能轮换**（hostname 变、/tmp/蜂巢 蒸发）。对策已内建：每波前 `ensure_hive()` 探活重播种 + 关键产物即时 `checkpoint` 到本地镜像。**任何新节点产出重要交接文件后必须立刻 checkpoint**——这是韧性的生命线。
3. **容器内 codex 调用**必须带网关覆盖（裸调会走 api.openai.com 被 403）：
   `codex exec --skip-git-repo-check --dangerously-bypass-approvals-and-sandbox -c model_provider="gw" -c 'model_providers.gw.name="gw"' -c 'model_providers.gw.base_url="https://proxy:18080/v1"' -c 'model_providers.gw.env_key="OPENAI_API_KEY"' -c 'model_providers.gw.wire_api="responses"' -c model_reasoning_effort="high" -m gpt-5.6-sol "PROMPT"`
   统一走 `bin/role.sh`，不要在驱动里手拼（图腿例外，驾驶脚本 P3.5/P5 已有可用的内联模板，照抄改参即可）。
4. **看图**：`codex exec … -i 图1.png -i 图2.png -- "提示词"`（`--` 必须有，否则提示词被当图路径）。**一批 ≤7-8 张图**，多了截断。
5. **网关是 Responses API**：直连时 payload 用 `{"model":"gpt-5.6-sol","input":"..."}`；发 chat/completions 的 `messages` 会 400。
6. **数据编码**：xlsx 一律 openpyxl；csv 很可能 GBK；二进制上传必须 contentB64（`f_local` 已封装）。
7. **Python 库**：容器有 statsmodels/sympy/networkx/pypdf/matplotlib 等；sklearn/openpyxl 等用离线轮：`pip3 install --no-index --no-deps --no-compile --target=/tmp/蜂巢/pylibs 运行时/wheels/*.whl`（约 1 秒）；脚本里 `sys.path.insert(0,'/tmp/蜂巢/pylibs')`。
8. **LaTeX**：完整 TeX Live 2025，`xelatex -interaction=nonstopmode` 跑两遍（约 16s/遍）；模板 `format.cls`=format-sandbox.cls；中文字体 Fandol 系。页渲染：`gs -dNOPAUSE -dBATCH -sDEVICE=png16m -r70 -sOutputFile=页/p%02d.png 论文.pdf`。
9. **异步腿模式**：长任务一律 nohup + done 标记 + 轮询（`wave`/`run_script` 已封装）。role.sh 的 timeout=1200s 且兜底写 AUTO done；wave 对 AUTO/空 done 重试一次。
10. **偶发噪声**：网关偶发 500、`/home/sandbox/.codex/skills/xq_*.sh: No such file` 幻影报错——腿通常照样完成，wave 的重试已吸收，**不要因此改架构**。
11. **本地驱动进程必须与编排会话解耦**：上次教训——驱动作为 Claude Code 后台任务跑，会话重启即被杀。正式运行用 `nohup python3 蜂群驾驶.py <输入> <产出> > 真题测试/驾驶.out 2>&1 &` 直接落系统后台，进度看 `真题测试/运行日志.md`。
12. **payload**：20MB 级上传实测可过；镜像重播种时 >8MB 单文件跳过（现行逻辑，保留）。
13. 本地 shell 里不要用长 `sleep` 等待（驱动内部用 `time.sleep` 没问题）。

---

## 三、目标架构速览（细节见蓝图文档，此处为接线视角）

```
S0 吃透题目 ──G0──▶ S1 战略锦标赛 ──G1──▶ S2 建模求解(依赖DAG) ──G2/问──▶ S3 图证 ──G3──▶
S4 撰稿 ──G4──▶ S5 审稿场(五路并审→分级修订→定向回炉) ──G5──▶ S6 出版+复盘 ─进化外环→ 规则库
左轨=证据总线(交接/目录)：契约·档案·假设台账·实验记录·结果包·引用清单·需求追踪矩阵
右轨=对抗：答卷预测(S0)·红队复算(S2)·评委画像(S5)
铁律：门一票否决；任一门连败两次→该环节升格蜂群重做；上游数字变更→下游按依赖图级联重算
```

**蓝图 → 实现映射总表**（"改"=改现有文件，"新"=新建）：

| 蓝图节点 | 现状 | 处置 | 工单 |
|---|---|---|---|
| S0 读题/体检 | 读题官+数据体检师 ✅ | 契约 schema 升级（改） | 2.1 |
| S0 答卷预测 | 无 | 新角色 | 2.2 |
| S0 需求追踪矩阵 | 无 | 驱动机械生成（新） | 2.1 |
| G0 契约门 | 无 | 门检脚本（新） | 3.3 |
| S1 锦标赛 | P3 内全量变体（晚且贵） | 前移+轻量原型（改驱动+改规划师） | 2.3 |
| S2 依赖 DAG+级联 | 严格串行 | 调度器（新模块） | 1.2 |
| S2 修错/解读返工循环 | ✅ `run_script`+解读轮 | 保留 | — |
| S2 红队+仲裁 | 无 | 新角色+驱动仲裁 | 2.4 |
| S2 假设台账 | 无 | 改建模师+审计 | 2.5 |
| G2 正确性门/问 | 解读 PASS（弱） | +红队对齐+溯源（改） | 1.2/2.4 |
| S3 逐图循环 | 批量图评+一轮修 ✅ | 阈值收紧为逐图 ≤2 轮（改） | 3.1 |
| S4 叙事设计 | 无 | 新腿（撰稿师前置任务） | 2.6 |
| S4 章评循环 | 章评师角色在、未接线 | 接线（改驱动） | 3.1 |
| S4 摘要蜂群 | ✅ 3 变体+评委融合 | 保留（全开时 5 变体） | — |
| S5 五路并审 | 双评委 A/B ✅ | +评委模拟+硬伤猎手+版式并轨（改） | 2.7/3.2 |
| S5 分级修订单+回炉路由 | top3 指令拼接给撰稿师 | 结构化修订单+按目标路由（改） | 3.2 |
| S5 收敛循环 | ≥8.6 或 Δ<0.15 ✅ | +硬伤清零硬条件（改） | 3.2 |
| S6 合规终检 | 部分散落 | 门检脚本收拢（新） | 3.3 |
| S6 逐页终审 | 美化师批页 ✅ | 保留+终轮全页 | — |
| S6 复盘官/进化外环 | 无 | 新角色+落地流程 | 2.8 |
| 断点续跑 | 无 | 状态机+--resume（新） | 1.1 |
| 连败升格蜂群 | 无 | 门框架（新） | 1.3 |

---

## 四、交接文件契约（JSON Schema，全部键名中文）

所有交接文件在容器 `/tmp/蜂巢/交接/`，产出即 checkpoint。以下 schema 是**驱动校验与角色提示词的共同依据**——实现时把 schema 原文贴进对应角色提示词。

### 4.1 `交接/题面契约.json`（读题官，工单 2.1）
```json
{
  "赛题": "B",
  "标题": "...",
  "问题": [
    {"编号": 1, "原文摘录": "...", "解读": "...", "可交付物": ["模型", "表格X", "预测值"],
     "需求条目": [{"需求号": "1-1", "内容": "...", "评分点推测": "..."}]}
  ],
  "硬约束清单": [{"约束号": "C1", "内容": "...", "出处": "题面第X段"}],
  "歧义裁定": [{"条目": "...", "候选解释": ["A", "B"], "裁定": "A", "理由": "...", "影响范围": "问2"}],
  "附件清单": [{"文件": "附件1.xlsx", "内容概述": "...", "关联问题": [1, 2]}]
}
```

### 4.2 `交接/计划.json` v2（规划师，工单 2.3；在现行 schema 上**增量**）
现行四键保留：`问题清单 / 论文结构 / 图表规划 / 页数预算`。问题清单每项新增/强化：
```json
{"编号": 2, "标题": "...", "主方法": "...", "备选方法": "...",
 "方法理由": "引用数据档案具体数字", "本题定制改造": {"名称": "XX修正", "为什么": "..."},
 "验证方案": {"基线": "...", "交叉印证": "...", "防泄漏": "...", "灵敏度": "..."},
 "依赖问题": [1],
 "锦标赛": {"参赛路线": ["机理路线", "数据驱动路线"], "优胜": "数据驱动路线", "依据": "见 锦标赛_问题2.json"},
 "蜂群变体": []}
```
新增顶层键：`"叙事主线"`（一句话全文故事线）、`"偏离点"`（对照典型答卷预测刻意差异化的 3-5 条）。
`依赖问题` 字段是 DAG 调度的唯一依据；规划师必须显式给出（无依赖=空数组）。

### 4.3 `交接/锦标赛_问题X.json`（裁决腿，工单 2.3）
```json
{"问题": 2, "路线": [
  {"名称": "机理路线", "原型脚本": "求解/问题2/原型_机理.py", "实测指标": {"MAPE": 0.31}, "用时秒": 95,
   "论文价值": "可解释性强但拟合差", "败因/胜因": "..."}
 ], "优胜": "数据驱动路线", "裁决理由": "指标+论文价值双维", "败者用途": "论文算法对比节素材"}
```

### 4.4 `交接/红队_问题X.json`（红队，工单 2.4）
```json
{"问题": 1, "复算方式": "独立实现，未读建模师代码",
 "复算指标": {"总需求量": 12345.6, "峰值月": "2024-07"},
 "口径说明": {"总需求量": "附件1全行求和，单位吨"},
 "结论": "对齐|不齐", "分歧明细": [{"指标": "...", "建模值": 1.0, "复算值": 1.3, "相对差": 0.3}]}
```
驱动仲裁规则：逐键相对误差 ≤1%（或红队声明的口径差异成立）→ PASS；否则派仲裁腿（解读师，可读双方代码定责）→ 按裁定返工一方。

### 4.5 `交接/假设台账_问题X.json`（建模师，工单 2.5）
```json
[{"假设号": "A1", "假设": "...", "依据": "数据档案:...", "灵敏度义务": "扰动±20%看指标变化",
  "检验结果": "已检验，指标变化<3%（求解结果:灵敏度键）"}]
```

### 4.6 `交接/论点脊柱.json`（叙事设计腿，工单 2.6）
```json
{"章": [{"文件名": "3.问题1求解.tex", "论点链": [
   {"发现": "带具体数字的一句话", "证据": ["图X", "表Y", "结果键Z"], "含义": "...", "衔接下段": "..."}
 ], "开场策略": "...", "禁止": "模板化开场"}]}
```

### 4.7 `审稿/修订单_轮N.json`（驱动合并五路意见生成，工单 3.2）
```json
[{"序号": 1, "级别": "硬伤|正确性|叙述|版式", "目标": "算|图|文",
  "问题": "问题2峰值数字与图7不符", "定位": "5.问题2.tex:第3段 / 求解/问题2/结果/xx.json",
  "指令": "可执行的一句话", "验收": "如何确认已修好", "来源": "硬伤猎手"}]
```
路由：`算`→建模师修+run_script+解读+红队复算+**级联**；`图`→绘图师+成图+图评；`文`→撰稿师。同轮内先算后图后文。

### 4.8 `交接/需求追踪矩阵.json`（驱动从契约生成；销号腿更新，工单 2.1/3.3）
```json
[{"需求号": "1-1", "内容": "...", "状态": "未落位|已落位", "落位": {"章节": "3.问题1求解.tex", "图表": ["图3"], "关键数字": "MAPE=12.3%"}}]
```

### 4.9 `审稿/复盘报告.json`（复盘官，工单 2.8）
```json
{"总评": "...", "瓶颈环节": [{"环节": "S5轮2", "现象": "...", "耗时/返工数": "..."}],
 "规则库修改建议": [{"文件": "运行时/禁用词.txt", "修改": "增加'XX'", "理由": "本次出现4处"}],
 "下次运行参数建议": {"审稿轮数": 4}}
```

### 4.10 本地 `真题测试/状态.json`（驱动状态机，工单 1.1）
```json
{"运行ID": "20260826-1", "阶段": "S2", "已完成节点": ["P0", "S0", "G0", "S1", "G1", "S2:问1", "S2:问3"],
 "问题门状态": {"1": "PASS", "2": "PENDING", "3": "PASS"}, "腿数": 42, "起始时间": 1756..., 
 "门失败计数": {"G2:问2": 1}, "级联待重算": []}
```

---

## 五、驱动架构改造总则

1. **入口不变**：`python3 蜂群驾驶.py <输入目录> <产出目录> [--resume]`。动工前先备份 `cp 蜂群驾驶.py 蜂群驾驶_v3备份.py`。
2. **允许拆模块**（同目录）：`调度器.py`（DAG/级联/门框架/状态机）、`门检.py`（G0-G5 机械检查，容器内跑也行）。`hive_sdk.py` 只准加方法不准改已有签名。
3. **每个节点原子化**：节点=（名称, 依赖节点, 执行函数, 门检函数）。执行完即写 `状态.json` + checkpoint。`--resume` 时跳过已完成节点（问题级粒度：`S2:问X`）。
4. **门框架**：`gate(名称, 检查fn, 返工fn, 最多返工=2)`；连败 2 次→`升格蜂群(节点)`：对该节点派 3 条变体腿（不同切入提示）+1 裁决腿，取优者过门；再不过→记录降级放行并在 运行日志 高亮（不许死循环）。
5. **预算**：`MAX_LEGS=220`、`MAX_HOURS=14`；全开参数集中在文件头一个 `配置` dict（审稿轮数上限、摘要变体数、每问路线数、图评阈值……），方便一处调。
6. **每次改完** `python3 -m py_compile 蜂群驾驶.py 调度器.py` 必须过；新增角色先用 `Hive().leg()` 单腿冒烟看真实输出，再接进驱动。

---

## 六、施工工单

> 顺序：M0 → M1 → M2 → M3 → M4。M2 内部工单可并行推进。每单完成在 `施工日志.md` 记一条：`[工单号] 改动文件 / 验收结果 / 遗留`。

### M0 资产就位（先做，约 0.5h）

**工单 0.1 消灭 scratchpad 依赖**
- 资产已于交接前抢救进项目（2026-08-26 完成，无需再拷）：wheels 在 `沙箱验证/wheels/`（7 个轮，与 scratchpad 逐一比对一致）；Mrite 模板 15 个 tex 已拷入 `流水线/运行时/模板参考/`；HMML 已拷为 `流水线/运行时/HMML.md`。
- 动作：只改引用——`蜂群驾驶.py` 的 P0 播种段与 `ensure_hive` 中，wheels 路径改指 `沙箱验证/wheels/`、模板参考改指 `流水线/运行时/模板参考/`、HMML 改指 `流水线/运行时/HMML.md`；删除 SCRATCH 常量及所有 `/private/tmp/claude-501` 引用。
- 验收：`grep -rn "claude-501" 流水线/` 零命中；`python3 -m py_compile` 过；单独执行 P0 播种段成功，容器内 `ls 运行时/wheels 运行时/模板参考 运行时/HMML.md` 齐全。

### M1 骨架：状态机、DAG、门、预算（约 3-4h）

**工单 1.1 状态机与断点续跑**
- 规格：按 §4.10 维护 `状态.json`（产出目录同级）。每个节点/每问过门后立即落盘。`--resume`：读状态→`ensure_hive()`（自动从镜像重播种容器）→跳过已完成节点继续。`运行ID` 不变；日志追加写。
- 验收：**演练**——启动一次真跑，在 S2 中段 `kill` 驱动进程，`--resume` 重启，日志证明未重做 P0/S0/S1、直接续跑；最终正常到达后续阶段（演练可用低配参数）。

**工单 1.2 依赖 DAG 调度器 + 级联重算**
- 规格：读 `计划.json` 各问 `依赖问题` 构建 DAG（自环/环检测→报错回炉规划师）。按拓扑分层执行：同层各问的建模腿合入一个 `wave` 并行；每问独立走"建模→执行→解读→红队"子链并单独过 G2；下游问建模任务文本自动注入各上游的 `结果解读_问题X.md` 摘要与结果目录路径。`级联重算(问X)`：把依赖问X 的全部下游在 `状态.json` 标记 `级联待重算`，其 G2 状态清空，按拓扑序重跑子链（撰稿若已发生，把受影响章列入下一轮修订单）。
- 验收：用真题计划（或手造 1→2、3 独立的最小计划）跑 S2：日志出现"问1∥问3 并行、问2 等待问1 过门"；手工调用 `级联重算(1)` 后问2 自动重跑并重过门。

**工单 1.3 门框架与连败升格**
- 规格：见 §五.4。至少接线 G2（解读 FAIL 计数）与 G4（编译/审计失败计数）。升格蜂群的 3 条变体腿提示差异：①换主方法族 ②换数据口径/特征 ③保方法改激进修复；裁决腿用解读师同口径评。
- 验收：单元模拟（把门检 fn 换成恒 FAIL）触发升格分支、产生 3+1 腿、降级放行路径日志正确；恢复真实门检后正常。

**工单 1.4 预算与杂修**
- 规格：MAX_LEGS=220 / MAX_HOURS=14 / `配置` dict 集中化；`get_json` 修复腿名改 `修json_{path 的 basename 去扩展名}`；`wave` 的 timeout 默认保持 1300（role.sh 1200 + 余量）。
- 验收：py_compile 过；一次小冒烟 wave 正常。

### M2 角色扩编（约 4-5h，可并行）

> 新角色文件一律放 `流水线/角色/`，风格对齐现有角色（简短、硬性条款、产出清单、done 标记要求）。**把 §四对应 schema 原文贴进提示词**。每个新角色先单腿冒烟再接线。

**工单 2.1 读题官 v2 + 需求追踪矩阵**
- 改 `角色/读题官.md`：产出改为 `交接/题面契约.json`（§4.1，兼容保留旧 题面.json 可以两个都写）；歧义必须裁定不许含糊。
- 驱动：契约落地后机械生成 `交接/需求追踪矩阵.json`（全部"未落位"）并上传回容器。
- 验收：真题 P1 出契约，schema 校验器（驱动内写 10 行校验函数）通过；矩阵条目数=需求条目数。

**工单 2.2 答卷预测官（新角色）**
- `角色/答卷预测官.md`：读题面契约+数据档案，输出 `交接/典型答卷预测.md`：平庸 AI 卷会用的方法/结构/图表/摘要套路各 3-5 条 + "我们必须偏离的清单"。
- 接线：S0 与读题/体检同波；规划师、绘图师、撰稿师提示词各加一句"读 交接/典型答卷预测.md，刻意偏离其中套路"。
- 验收：文件生成；计划.json 出现 `偏离点` 键且非空。

**工单 2.3 战略锦标赛（S1 重构，本里程碑核心）**
- 流程：规划师任务拆两步。**步1 路线侦察**：规划师输出 `交接/路线侦察.json`（每问 2-4 条真正不同路线，各带"原型实验设计"：小样数据、单一指标、≤3 分钟运行）。**步2 原型**：每路线一条建模师腿写 `求解/问题X/原型_<路线>.py`（只写不跑，小样！），驱动 `run_script` 并行执行（budget=400，repairs=1）。**步3 裁决**：每问一条解读师腿产出 `锦标赛_问题X.json`（§4.3）。**步4 定稿**：规划师读全部锦标赛结果，产出 计划.json v2（§4.2）。
- 现行 P3 的"蜂群变体全量实现"逻辑保留代码但默认不触发（`蜂群变体` 通常为空数组），转为 G2 连败升格的备用武器。
- 单问路线数、是否全问开锦标赛由 `配置` 控制（默认：每问都开、每问 3 路线；快速档：仅分值最高问开）。
- 验收：真题跑 S1：路线侦察→N 条原型腿→原型全部 rc=0（或修复后）→每问锦标赛 JSON 齐→计划 v2 含 `锦标赛`/`依赖问题`/`偏离点`/`叙事主线`；P3 只实现优胜路线。

**工单 2.4 红队与仲裁（新角色）**
- `角色/红队.md`：铁律——**只准读** 题面契约/数据档案/数据文件/该问"结果声明"（驱动生成：核心指标键值+口径一句话），**禁止读**建模师代码与笔记；独立写 `求解/问题X/复算.py`（只写不跑）输出 `求解/问题X/红队结果/`；再产出 `交接/红队_问题X.json`（§4.4）。
- 驱动：G2 子链尾部（解读 PASS 后）：红队腿→run_script→读 JSON 仲裁（§4.4 规则）→不齐派仲裁腿（解读师）→按裁定开返工单给一方→重跑该问子链尾部。红队腿可与解读腿同波并行（都在执行成功之后）。
- 验收：真题问1 红队"对齐"记录在案；注入测试——手工把某结果 JSON 数值×1.3 再跑仲裁，确认走"不齐→仲裁"分支。
- 可选强化（时间富余再做）：红队用 `Hive(account="a2")` 独立容器（需给 a2 做一个最小 ensure_hive：AGENTS+role.sh+角色/红队+数据+契约）。

**工单 2.5 假设台账**
- 改 `角色/建模师.md`：新增产出 `交接/假设台账_问题X.json`（§4.5），灵敏度义务必须对应求解脚本里真实存在的检验。
- 改 `运行时/审计.py`：新增检查——每份台账每条 `检验结果` 非空且引用的结果键存在。
- 验收：真题一问的台账生成且审计过。

**工单 2.6 叙事设计腿**
- 不新建角色文件，S4 首腿用撰稿师角色+专用任务：读计划/结果解读/图注素材/叙事主线，产出 `交接/论点脊柱.json`（§4.6）。撰稿波次任务文本注入本章脊柱。
- 验收：脊柱文件生成；抽查 2 章 tex 开头与脊柱"发现"一致（人眼/编排端核对记录进施工日志）。

**工单 2.7 评委模拟 + 硬伤猎手（新角色×2）**
- `角色/评委模拟.md`：**只看**渲染页图（-i 批次：摘要页+目录性首页+各问首页，≤8 张）+摘要 tex；模拟 5-10 分钟初评：第一印象分(0-10)、30 秒能否复述四要素、三个"评委会卡住的地方"；输出 `审稿/评委模拟_轮N.json`。
- `角色/硬伤猎手.md`：只干五件事——逻辑幻觉（抽 10 个关键数字令其溯源）、推理跳步（抽 3 处推导逐步核）、创新不足（定制改造是否真的改了）、同质化（对照典型答卷预测）、说不清道理（抽 5 个选择要理由链）；输出 `审稿/硬伤_轮N.json`：`[{类别, 定位, 证据, 级别:硬伤}]`，**空数组=清零**。
- 验收：单腿冒烟各出合法 JSON；接入 S5 后修订单里出现"来源:硬伤猎手"条目。

**工单 2.8 复盘官（新角色）+ 进化外环落地**
- `角色/复盘官.md`：读 运行日志.md（驱动上传）+全部审稿/审计 JSON+终稿页图（抽样 -i）；产出 `审稿/复盘报告.json`（§4.9）。
- 落地流程：驱动收割报告→编排端（或执行者本人）逐条评审"规则库修改建议"→采纳的直接改 `运行时/` 与 `角色/` 文件并记入施工日志；**不自动改**（防军规漂移）。
- 验收：端到端运行后报告存在、建议≥3 条且可执行。

### M3 循环完备（约 3-4h）

**工单 3.1 逐图/逐章循环收紧**
- 图：现行"图评→低分修一轮"改为循环 ≤2 轮（评→修→重跑成图修.sh→再评低分子集），阈值 7 分保持；机理示意图必须≥3 张（审计.py 已查，确保门检引用）。
- 章：P4 每波撰写完，派章评师批量腿（每腿 ≤4 章）按"AI 味/跳步/溯源"三项打分出改单；<7 分章重写腿；≤2 轮。
- 验收：日志出现图/章两级循环轮次与分数；故意在一章塞禁用词，确认被章评或审计逮住并修掉。

**工单 3.2 审稿场五路化 + 分级修订单 + 回炉路由（本里程碑核心）**
- 每轮五路并行：①审稿员A（全面十维）②审稿员B（专挑硬伤侧重数字一致/跳步/图文相符——现有）③评委模拟（页图）④硬伤猎手 ⑤机械审计（审计.py+门检.py，不占腿）。美化师页图审并入轮次⑤后置或保留现行独立美化循环（保留，置于收敛后）。
- 驱动合并五路输出→`修订单_轮N.json`（§4.7，级别排序硬伤>正确性>叙述>版式，同级内先算后图后文）→按`目标`路由执行（见 §4.7）；`算`修订完成后**必须**重跑解读+红队并触发级联，受影响章进下一轮修订。
- 收敛条件：`(均分≥8.6 或 Δ<0.15) 且 硬伤清单为空`；上限轮数进 `配置`（默认 4）。
- 验收：真题运行中至少一轮修订单含多级别条目且路由日志清晰；构造一个"算"级修订（可在冒烟中注入）验证"重算→红队→级联→章列入下轮"全链。
- 需求矩阵销号：每轮审稿员A 任务附带"更新 交接/需求追踪矩阵.json 落位字段"；门检 G5 核对 100% 已落位，未销号项自动生成修订单条目。

**工单 3.3 门检脚本 `运行时/门检.py`（G0-G5 机械项收拢）**
- `python3 bin/门检.py G0|G2|G4|G5`，输出 `审稿/门检_GX.json {通过:bool, 明细:[]}`：
  - G0：契约 schema 齐、需求号连续、歧义裁定每条有"裁定"、附件清单覆盖 数据/ 全部文件；
  - G2（每问）：结果目录非空、解读 PASS、红队 JSON 结论=对齐、假设台账检验结果全非空；
  - G4：E=0、禁用词=0、`% src` 溯源核验存疑=0（或明细逐条带"已解释"）、摘要 aux=1 页、句式占比≤40%；
  - G5：需求矩阵全销号、硬伤清单空、图配额过、正文页数≤20、无 `\tableofcontents`、附录含 `lstlisting` 且非空、全部 `\cite` 键 ∈ 文献卡片库。
- 驱动在对应门调用并依结果放行/返工/升格。
- 验收：每个门单测一正一反（人为制造违规→FAIL 明细指到点位）。

### M4 端到端验收与进化（一次全跑 6-12h + 复盘）

**工单 4.1 低配冒烟全链路**
- `配置` 快速档（审稿 1 轮、每问 2 路线、摘要 3 变体、图评 1 轮）跑真题，允许中途发现问题→修→`--resume` 续跑。目的：所有新节点至少各执行一次。
- 验收：到达 S6，状态.json 节点全勾，施工日志记录发现与修复。

**工单 4.2 全开自主运行（正式验收跑）**
- `配置` 全开档（审稿≤4 轮、每问 3 路线、摘要 5 变体、图 18-22 张、逐图逐章循环开）。启动方式见 §二.11（nohup 解耦）。**运行期间不修代码不干预**；只看 `运行日志.md`。
- 验收：§七 清单全绿。
- 若中途容器轮换/驱动崩：`--resume` 续跑，事件记入施工日志（这本身就是韧性验收）。

**工单 4.3（可选）a2 对抗泳道**
- 红队与评委模拟迁到 `Hive(account="a2")`；最小播种；与主线并行。
- 验收：红队腿日志 hostname 与主线不同。

**工单 4.4 复盘与规则库前进一版**
- 复盘官报告 → 逐条评审 → 采纳项落盘（军规/词表/方法卡/配置默认值）→ 施工日志收尾总结（含与上次运行的量化对比：腿数/用时/终稿页数/均分/审计违规数）。

---

## 七、端到端验收清单（工单 4.2 的放行标准）

产物齐备（`真题测试/成品/`）：
- [ ] 论文.pdf + 全部 tex + 页渲染 png
- [ ] 求解/ 全部脚本、结果 JSON、图 png、红队结果
- [ ] 交接/ 契约、计划 v2、锦标赛、假设台账、实验记录、论点脊柱、需求追踪矩阵（全销号）
- [ ] 审稿/ 各轮五路 JSON、修订单、审计报告、门检报告、复盘报告

硬指标：
- [ ] 编译 E=0；摘要恰 1 页；正文 ≤20 页；无目录；附录 lstlisting 含各问核心源码
- [ ] 禁用词命中=0；"如图/如表"句式占比 ≤40%；段首开场词重复合规
- [ ] 图 16-22 张、同型 ≤3、柱折合计 ≤半、机理手绘图 ≥3
- [ ] 溯源核验存疑=0（或逐条附解释）；每问红队结论=对齐（或仲裁裁定在案）
- [ ] 双评均分 ≥8.6 或平台期，且末轮硬伤清单为空；美观分 ≥8.5
- [ ] 每问 G2 过门记录 + 依赖问的执行顺序符合 DAG（日志可查）
- [ ] 状态.json 完整可复盘；总腿数 ≤220；总时长 ≤14h

质量抽查（编排端/人工，写进施工日志）：
- [ ] 随机抽 3 个正文数字沿 `% src` 注释追到结果 JSON 一致
- [ ] 抽 2 处"过程感"叙述在 实验记录.json 有对应真实条目
- [ ] 摘要 30 秒读完能复述四要素

---

## 八、执行方式要求（对执行模型）

1. **全程中文**（代码注释、日志、文档、提示词）。文件/角色命名沿用现有中文风格。
2. **增量演进**，不重写：动 `蜂群驾驶.py` 前先备份；保持既有函数签名与日志风格；新逻辑优先进新模块。
3. **每工单闭环**：实现→验收→写 `施工日志.md` 一条→再下一单。验收不过不许进下一单（同一单可返工）。
4. **实测优先**：任何"沙箱应该支持X"的假设，先花一条 tiny `h.exec` 验证再依赖它。
5. **自主推进**：按本文自裁一切次级决策并在施工日志记录理由；只在（a）端点/网关不可用（curl 拒绝、连续 401/403）（b）需要花钱或触碰本机之外的系统（c）真题输入文件缺失——三种情况停下来向用户报告。
6. **长跑解耦**：正式运行用 nohup 落系统后台（§二.11）；你的会话可以随时结束/重启，靠 `状态.json`+`--resume` 接管。
7. **密钥纪律**：KEY 不出现在任何新文件、日志、外部请求中（hive_sdk.py 现状除外）。本项目不是 git 仓库，也不要 git init 后提交密钥。
8. **不越权改蓝图**：流程节点/门/军规内容如需变更，只能因实测不可行，且在施工日志写明"蓝图偏离：原因+替代"。

---

## 九、风险与应对

| 风险 | 概率 | 应对 |
|---|---|---|
| scratchpad 资产丢失 | 已消除 | 交接前已全部拷入项目（工单 0.1 只剩改引用） |
| 容器轮换打断全开跑 | 中 | ensure_hive+checkpoint 已内建；--resume 是工单 1.1 的第一优先级 |
| 网关偶发 500/幻影 skill 报错 | 高（无害） | wave 重试吸收；连续 10+ 次同错才升级为停下报告 |
| 锦标赛原型跑超时拖慢 S1 | 中 | 原型脚本硬性小样+3 分钟预算；超时按失败路线处理不阻塞裁决 |
| 红队与建模"殊途同归地一起错" | 低 | 红队禁读代码+口径独立声明已最大化独立性；残余风险由溯源核验+审稿场兜底 |
| 修订循环震荡不收敛 | 中 | 轮数上限+平台期判据+硬伤清零硬条件；升格蜂群是最后手段，之后降级放行并高亮 |
| 腿数爆炸 | 低 | MAX_LEGS=220 硬闸 + 每循环显式轮数上限 |

---

*附：上次真实运行（被会话重启中断于 P3 问题2）的镜像在 `真题测试/蜂巢镜像/`、日志在 `真题测试/运行日志.md`——可作为各交接文件真实样例参考，但正式验收跑必须从 P0 全新开始。*
