# 问题2升格裁决返工单

## 裁决结论

本轮判定 **FAIL**，失败于第0项产物真实性硬门，未进入同口径核心指标比较与五项正确性裁决，也未产生优胜方。

## 缺失产物

- `求解/问题2/升格1/结果/` 不存在，JSON 数量为 0。
- `求解/问题2/升格2/结果/` 不存在，JSON 数量为 0。
- `求解/问题2/升格3/结果/` 虽存在，但目录为空，JSON 数量为 0。
- 未找到 `日志/执行_问题2_升格*.log` 或 `日志/跑升格_问2_*.log`；现有 `日志/升格_G2问2_1.done`、`_2.done`、`_3.done` 均明确记载脚本“只写不跑/未运行求解”，不能充当正常执行日志。

## 无法逐键核验的同口径结果键

由于三个变体均无结果 JSON，以下主线同口径核心键在每个变体中均无可核验实体：`共享厚度_um`、`附件1_10度独立厚度_um`、`附件2_15度独立厚度_um`、`盲块双向峰谷MdAE_cm-1`、`盲块指标口径ID`、`半合成经验覆盖率`、`自动验收门/总体通过`、`实际用时秒`。基线改善幅度、交叉印证、防泄漏审计及关键参数灵敏度也均无结果 JSON 可核验。

## 返工要求

1. 分别运行 `求解/问题2/升格1/求解.py`、`升格2/求解.py`、`升格3/求解.py`，将真实输出写入各自 `结果/`，并保存对应正常结束执行日志及 `rc=0` 标记。
2. 每个变体至少提供一个合法 JSON；推荐统一提供 `汇总结果.json`，完整包含上述同口径核心键，以及 naive 基线改善幅度、备选方法或不同切分、时序防泄漏审计、关键参数扰动结果。
3. 三个变体的 `盲块指标口径ID`、样本范围、单位和聚合方式必须与主线一致；若方法不适用同一指标，应先显式给出可比映射，不能静默换口径。
4. 结果齐备后重新执行升格裁决；在此之前不得改写 `求解/问题2/结果/`、`交接/结果解读_问题2.md`、`交接/结果声明_问题2.json` 或 `交接/假设台账_问题2.json` 为任何变体的优胜版本。
