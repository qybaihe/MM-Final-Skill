# 问题3升格裁决返工单

## 裁定

**FAIL。** 第 0 项产物真实性硬门未通过，立即终止升格裁决；本轮不比较数值、不指定优胜者，也不更新论文引用清单或头条数字声明。

## 缟门缺口

1. `求解/问题3/升格1/结果/` 不存在，因而没有任何可核对的结果 JSON。
2. `求解/问题3/升格2/结果/` 不存在，因而没有任何可核对的结果 JSON。
3. `求解/问题3/升格3/结果/` 不存在，因而没有任何可核对的结果 JSON。
4. `日志/升格_G2问3_1.log` 与 `日志/升格_G2问3_1.done` 明确记载“未运行主求解”“仅完成静态审计”，不能作为脚本正常执行结束的证据。
5. `日志/升格_G2问3_2.log` 与 `日志/升格_G2问3_2.done` 明确记载“脚本按要求未运行”“仅做静态检查”，不能作为脚本正常执行结束的证据。
6. `日志/升格_G2问3_3.log` 与 `日志/升格_G2问3_3.done` 明确记载“仅做语法、导入和 JSON 静态检查，未运行求解主流程”，不能作为脚本正常执行结束的证据。

## 返工要求

- 依次实际运行 `求解/问题3/升格1/求解.py`、`求解/问题3/升格2/求解.py`、`求解/问题3/升格3/求解.py`。
- 每个变体必须在自己的 `结果/` 目录产出至少一个合法 JSON，并保留包含运行状态、核心指标、基线比较、交叉印证、灵敏度和自动验收门的结果键。
- 为每个变体生成独立执行日志；日志必须显示脚本正常结束，而非仅记录脚本编写或静态检查完成。
- 三个变体完成后再按与主线一致的样本范围、切分和指标口径重启裁决；论文引用的每个数值必须逐键追溯到对应结果 JSON。

## 本轮文件处置

- 不将任何升格脚本中的预期键名或方法承诺当作真实结果。
- 不改写 `求解/问题3/结果/`，因为尚无合格优胜结果可整理。
- 不更新 `交接/结果解读_问题3.md`、`交接/结果声明_问题3.json`、`交接/假设台账_问题3.json` 与 `交接/升格裁决_问题3.json`，避免下游误读为已经完成裁决。
