# 问题1升格裁决返工单

## 判定

**FAIL。** 第 0 项产物真实性硬门未通过，本轮立即停止，不进入主线与 3 条升格变体的同口径指标比较及五项正确性裁决；不产生优胜方，不改写主线结果、结果解读、结果声明或假设台账。

## 缺失项

- 主线 `求解/问题1/结果/` 虽含 JSON，但当前 `日志/` 中不存在协议要求的 `日志/执行_问题1*.log`，无法证明本轮可核验脚本正常结束。
- `求解/问题1/升格1/结果/` 目录为空，缺少实际运行产生的结果 JSON；`日志/升格_G2问1_1.done` 明确记载“只写不跑”。
- `求解/问题1/升格2/结果/` 目录为空，缺少实际运行产生的结果 JSON；`日志/升格_G2问1_2.done` 明确记载“未运行求解”。
- `求解/问题1/升格3/结果/` 目录不存在，因而没有结果 JSON；`日志/升格_G2问1_3.done` 明确记载“仅做静态语法与 JSON 合法性检查”。
- 三份 `日志/升格_G2问1_*.log` 均为建模/写脚本过程日志，不是求解脚本执行日志，不能证明零退出和正常结束。
- 因升格结果 JSON 不存在，候选核心指标无法逐键核对，禁止生成论文引用数字或宣布任何变体胜出。

## 修改建议

1. 分别实际运行主线及 `升格1/求解.py`、`升格2/求解.py`、`升格3/求解.py`，保存各自独立执行日志，并确认零退出、正常完成摘要与实际用时。
2. 确保三个升格目录各自存在 `结果/`，且至少含一个合法 JSON；所有待比较核心指标须采用相同单位、样本范围、聚合方式与异常处理口径。
3. 重新发起裁决时，先逐键核对论文候选数字，再依次审查量纲、naive 基线胜幅、独立交叉印证、连续波段防泄漏和关键参数灵敏度。
