# 交接提示词 · 数模自动化「论文铸造厂」

> 给下一个接手的执行模型。读完这一份 + `施工日志.md` 就能接着干。
> 写于 2026-08-28，交接时后台正在跑第二次全开真题（S2 第2层 问3）。

---

## 一、你接手的是什么

一套**全自主数学建模论文生产流水线**。给定赛题 PDF + 附件数据，端到端产出一篇可提交的国赛风格论文（PDF + tex + 图 + 全套过程交接文件）。

- **项目根**：`/Users/bytedance/数模自动化`（**已是 git 仓库**，M5-0 起；改动直接提交，不再靠手工备份）
- **本机**：macOS。**没有 `setsid`、没有 `timeout`**（Linux 习惯会踩空；角色脚本用 perl alarm 代替 timeout）
- **算力载体**：**本机工作根**（产出目录旁的 `蜂巢镜像/`），通过 `流水线/本地蜂巢.py` 直接驱动。
  M5-0（20260907）已从远程沙箱容器全量迁出：播种/探活/指纹/回传整套机制随容器退役，
  沙箱 SDK（hive_sdk.py）与 manylinux wheels 已删除（git 历史可查）
- **角色腿**：工作根内跑本机 `codex exec`（端点/模型由 `~/.codex/config.toml` 决定，
  当前 gpt-6-astra；seatbelt workspace-write 把腿的写权限锁在工作根内），
  每条"腿"是一个带独立提示词的智能体（读题官/建模师/解读师/红队/审稿员等 11 个角色）

### 三份必读（按顺序）
1. `施工方案-论文铸造厂.md` —— 施工圣经：现状盘点、13 条环境铁则、逐条工单与验收标准
2. `理想蓝图-论文铸造厂.md` —— 目标流程：S0-S6 / G0-G5 / 双轨 / 依赖DAG / 级联 / 升格规则
3. `施工日志.md` —— **21+ 条施工记录**（M5-0 本地迁移是分水岭），每条含"证据链 + 根因 + 修法 + 验收"。本文档是它的索引和摘要

### 主要文件
| 文件 | 作用 |
|---|---|
| `流水线/蜂群驾驶.py` | 自主驾驶主脚本（S0-S6 全流程）。**动它之前先提交一个干净的 git 基线** |
| `流水线/本地蜂巢.py` | 本地执行 SDK（exec/腿/harvest，接口与旧 hive_sdk 对齐） |
| `流水线/调度器.py` | 状态机 / 门 / DAG 拓扑 / 断点续跑 |
| `流水线/运行时/门检.py` | G0-G5 机械门检（工作根内运行，chdir 取脚本上级） |
| `流水线/运行时/审计.py` | 禁用词 / 图型配额 / 溯源核验 |
| `流水线/运行时/codex公共.sh` | codex 调用口径单一事实来源（seatbelt 参数在这） |
| `流水线/角色/*.md` | 11 个角色提示词 |
| `流水线/验证/*.py` | 验证脚本（驱动干跑/门检单测/回路单测/本地故障注入等） |
| `流水线/运行时/venv/` | 本地 Python 环境（numpy/pandas/matplotlib/sklearn 等全栈，已入 PATH） |

---

## 二、整条链路长什么样

```
S0 播种 → 读题体检 → 预测与矩阵 ─[G0 契约门]─→
S1 路线侦察 → 3路线×3问原型 → 锦标赛裁决 → 定稿 ─[G1]─→
S2 按依赖DAG分层，逐问：
     建模 → 跑脚本 → 解读循环(FAIL则修复重跑) → 红队独立复算
     → 红队不齐则仲裁 → 仲裁判建模错则返工 ─[G2 正确性门/问]─→
     G2 连败2次 → 升格蜂群(3变体+裁决) → 仍不过 → 降级放行(高亮记录，不阻塞)
S3 绘图 → 图评循环 ─[G3 图证门]─→
S4 撰稿 → 章评循环 ─[G4 成稿门]─→
S5 五路审稿 → 修订单 → 定向回炉 → 硬伤清零 ─[G5 出版门]─→
S6 美化循环 → 终审 → 出版
```

**关键机制**
- **依赖DAG**：本题 `{1:[], 2:[], 3:[2]}` → 拓扑分层 `[[1,2],[3]]`，问3 必须等问2 过门
- **级联重算**：上游数字变更 → 全部下游作废重跑
- **红队信息隔离**：红队禁读建模代码，独立复算后比对，超容差则派仲裁
- **断点续跑**：`--resume` 读 `状态.json` 跳过已完成节点
- **预算护栏**：`MAX_LEGS=220 / MAX_HOURS=14`，超了抛 `预算耗尽` → 走 `应急出版()` 兜底

---

## 三、我发现的卡点与对应修法（**这部分最重要**）

按重要性排序。每条都是真题跑出来的，不是推测。

### 卡点1 ★★★ G2 仲裁死锁：三个问全部撞死，每问白烧约 1 小时
**现象**：三问 G2 失败明细**一字不差**都是 `仲裁裁定建模需修正但未消解`，`门失败计数={G2:问1:3, G2:问2:3, G2:问3:3}`，全部走完"2次返工+3变体升格"最贵路径后降级放行。

**根因**：`门检.py` 把 `仲裁_问题X.json` 当**一次性快照**读，只要存在 `应改方=建模` 就判未消解；而返工腿修完代码后**没有任何环节回写这个文件**——这条记录永久留存，**G2 在数学上不可能通过**。真实文件结构证实：`逐项[]` 只有 `指标/裁定/理由/应改方`，根本没有表示"已处理"的字段。升格、降级这些兜底反而掩盖了它（流水线没死，只是每问白烧 1 小时）。

**修法**：把快照改成**有状态的待办台账**（与需求追踪矩阵的销号机制对齐）
- 门检：`已消解/已修正/已解决/已落实` + **有证据** → 出清；`已解释/裁定不成立` + 有证据 → 放行但计入 `统计.仲裁已解释项`（软路径，留痕供人工复核）；**只声明不给证据一律不放过**
- 驱动三处派单（`仲裁返工`/`G2返工`/`升格裁决`）都要求回写台账，证据必须具体到文件/位置/新数值

**验收**：判据单测 9/9；容器内真实门检双向验证（旧格式 `通过=False`，补证据后 `通过=True`）。
**成效**：第二次跑问1 `门失败计数=0`、`问题门={'1':'PASS'}`——**首次真正过门**，不再降级。

> ⚠️ 设计时我第一版自相矛盾：提示词让建模师写"已解释"，但门检只认"已消解"族，那会造出新死锁。所以才有了"已解释+证据"的软路径。改判据时务必检查**提示词与门检的词表是否对齐**。

### 卡点2 ★★★ 静默死波：白等 70 分钟，且是我自己引入的回归
**现象**：`波次中途蜂巢丢失（3 次探活均无，已等 4184s）`——白等 4184 秒才动作。更要命的是全程 `播种完成`/`缺蜂巢` 日志计数**都是 0**，说明 `ensure_hive` 每次都走"一切正常"早退分支，从未真正播种。

**根因（我的设计错误）**：为解决"多实例误判"，我把判据改成"多探几次，**只要有一个实例 alive 就算活**"。稳态下没问题，但**混合态**（一个已播种 + 一个空）下最危险：
1. `ensure_hive` 总能探到活的那个 → 判就绪、不播种
2. 腿却可能被派到**空实例** → 永远写不出 done
3. `wave` 取"最大 done 数"=0，但总有一次探到 alive → `not 见活` 永远为假 → **死波检测永不触发**

**修法**：
- `蜂巢就绪()` 改**单次**探测、不聚合，且同时校验 `AGENTS.md` **和** `bin/role.sh`（只查前者不够，role.sh 缺了腿一样跑不起来）
- `ensure_hive` 改为"探到 missing 就立刻播种，直到**连续3次** alive"
- `wave` 新增**零进展判据**（done 数 900s 不涨即判死）——不再依赖会被摇摆路由污染的"蜂巢可见"信号
- `run_script` 加 `pgrep` 进程探测，"蜂巢在但进程不在且无 rc"超 90 秒判死

**验收**：生产实测 `判死：蜂巢丢失（已等 30s）→ 重播种后重跑` → `rc=0`。**30 秒 vs 4184 秒**。

### 卡点3 ★★★ 状态只存在容器里 → 架构级脆弱（已改造）
**问题**：所有中间产物只在容器 `/tmp/蜂巢`，checkpoint 是**阶段性**的，腿级产物有丢失窗口。实测容器 4 小时内换了 4 个实例。

**修法：腿级即时回传（write-through），容器降级为纯计算资源**
- `find -newermt '@水位戳'` 取增量 + `tar --null -T -` + `base64 -w0` **一次 exec 拉回全部增量**（逐文件 harvest 在多实例摇摆下极易漏）
- 水位前移留 5s 重叠防漏；**首次戳=0 自动退化为全量**，天然适配续跑/重播种对齐
- 超 6MB 降级走目录级 checkpoint；全程 try/except，**回传失败绝不阻塞流水线**
- 挂载点：`wave()` 腿落地后、`run_script()` rc=0 后

**成效**：当前已回传 **482 文件**，本地镜像 185 文件 / 17MB。验证时还意外把上次停跑遗留在容器里的 34 个真实产物抢救回本地——这些在旧机制下会随下次轮换永久丢失。

### 卡点4 ★★★ 派发与就绪判定不原子 → 腿被派进空容器（已改造）
**问题**：`ensure_hive()` 与 `h.leg()` 是两次独立请求，中间被网关重路由就把腿派进空容器。这正是卡点2 的根源。

**修法：实例指纹**
- 指纹 = `hostname@boot_id`。实测 `/proc/sys/kernel/random/boot_id` 可读，**比 hostname 可靠**（hostname 若复用，boot_id 仍能识别换了实例）
- **派发后回收校验**：派完腿立刻回读「任务文件是否都落地 + 指纹是否还是派发时那个」，不符就重播种重派（最多3次）
- 轮询**新增第3条判死判据：指纹漂移**——指纹一变即判死，腿的宿主已消失
- 波次日志带 `@指纹`，事后可查某条腿跑在哪个实例

**成效**：生产日志现在长这样 `波次启动 1 腿(第1次)@f0f434ce94fb@a26d150b: ['建模_问题3']`。

### 卡点5 ★★ 预算护栏在 S2 内部完全失效（刚修，未经生产验证）
**现象**：本轮实测 **20.4h vs 14h 预算，超支 6.4 小时**，护栏毫无察觉。

**根因**：`节点()` 只在**进入阶段前**查预算，而 S2 是**一个节点**里跑完 3 个问 ×（建模+执行+解读循环+红队+仲裁+G2门+升格）。一旦进了 S2 就完全不受约束。

**修法**：
- S2 逐问循环开头加 `budget_ok()` 检查，超了抛 `预算耗尽` 转收尾（已完成的问保留）
- 顺带修**续跑时钟**：原来按"首次启动至今的墙钟"算，把我停跑分析/改代码的间隔也计入了。改为累计**实际在跑的机时**（`累计机时` 随 log 落盘，续跑时 `T_START = now - 累计机时`）

**状态**：干跑 4 场景全过，但**正在跑的进程仍是旧代码**，下次重启才生效。

### 卡点6 ★★ macOS 上 `nohup &` 不足以解耦后台任务
**现象**：按方案用 `nohup python3 … &` 起跑，驱动**只活了几十秒**就随包裹 shell 一起消失。

**根因**：`nohup` 防的是 SIGHUP，但工具 shell 退出时整个进程组被回收。且 **macOS 没有 `setsid`**，`timeout` 同样没有。

**修法（已实测）**：
```python
subprocess.Popen([...], stdout=f, stderr=STDOUT, stdin=DEVNULL, start_new_session=True)
```
`start_new_session=True` 等价 setsid，进程 reparent 到 init（**PPID=1**）。**起跑后必须确认 PPID=1**。

### 卡点7 ★ 网关多实例路由，且不提供亲和控制
**实测结论**（省下你重新踩坑的时间）：
- 同账号请求会被分发到**多个容器实例**（一次运行里见过 6 个 hostname）
- **`account` 参数不控制路由**（a1 与 a2 落到同一容器）
- 网关**无亲和/会话接口**（`/v1/accounts`、`/v1/status` 均 404；可用路由只有 `/v1/exec`、`/v1/tasks`、`/v1/env` 等）
- 所谓"幻影 ENOENT"（旧铁则 §二.10）**大概率就是落到了另一个实例**，不是幻影
- 上传能力没问题：单个 9.1MB wheel 15 秒，7 个共 15.2MB 一次性 25 秒全部落地

→ 拿不到亲和控制，只能让驱动自身对多实例免疫（卡点2、4 的修法）。

---

## 四、当前后台运行到什么程度

**进程**：PID `59787`，PPID=1（已正确解耦），已运行 14h24m，**健康推进中**

| 指标 | 值 |
|---|---|
| 阶段 | S2 第2层（问3） |
| 腿数 | 94 / 220 |
| 完成节点 | 11：S0全部 + G0 + S1全部 + G1 + S2:问1 + S2:问2 |
| 问题门 | 问1 **PASS**、问2 **降级放行**、问3 进行中（G2 已失败1次，正在返工） |
| 门失败计数 | `{G2:问1: 0, G2:问2: 3, G2:问3: 1}` |
| 即时回传 | 累计 **482 文件**；本地镜像 185 文件 / 17MB |
| 墙钟 | 20.4h（含我停跑分析的间隔；真实机时远低于此，见卡点5） |

**问1 是修复成效的最好证据**：`门失败计数=0`，第一次真正 PASS。对比第一次跑（G2失败3次→升格3变体→降级放行，2.8 小时）。

**问2 仍降级放行**：失败明细已从 `仲裁未消解` 变成 `解读未 PASS：FAIL`——**说明卡点1 确实修好了**（不再是死锁），现在是真实的建模质量问题（解读师认为结果不达标）。这属于内容质量范畴，不是流水线缺陷。

**下一步**：问3 过门后进 S3（绘图）→ S4（撰稿）→ S5（审稿）→ S6（出版）。

### 接手后立刻做什么
```bash
cd /Users/bytedance/数模自动化
ps -p $(cat 真题测试/驾驶.pid) -o pid,ppid,etime   # 确认存活且 PPID=1
tail -20 真题测试/运行日志.md                        # 看最新进展
python3 -c "import json;d=json.load(open('真题测试/状态.json'));print(d['阶段'],d['腿数'],d['问题门状态'])"
```

**如果它还在跑**：别动代码（守则：运行期间不干预）。卡点5 的护栏修复要等这轮结束或下次重启才生效。
**如果它已退出**：先 `python3 流水线/验证/端到端验收.py` 看差多少，再决定 `--resume` 续跑还是补跑缺失阶段。

---

## 五、验证工具（改动前后都要跑）

| 脚本 | 用途 | 现状 |
|---|---|---|
| `验证/驱动干跑.py [全链\|中断续跑\|门升格\|级联]` | 流程编排回归，不碰容器、秒级 | 4 场景全过 |
| `验证/故障注入.py` | **对真实容器注入故障**验证韧性 | 9/9 过 |
| `验证/门检单测.py` | G0-G5 判据正反用例 | 10/10 过 |
| `验证/端到端验收.py [成品目录]` | 施工方案 §七 全部 41 项机械核对，每项输出**实测值** | 已备好 |
| `验证/角色冒烟.py`、`禁用词拦截验证.py`、`M0播种验证.py`、`看一眼.py` | 单腿冒烟 / 禁用词 / 播种 / 快速查看 | 可用 |

### ⚠️ 最重要的一条纪律：韧性改动必须故障注入验证
我犯过的最大错误：修完多实例问题后，**只在"两实例都健康"的稳态下验证**（harvest 6/6 vs 6/6，看不出任何差异），就认为修好了。结果混合态在生产里白烧 70 分钟。

**健康态下所有实现看起来都是对的。** `故障注入.py` 现在是韧性改动的**强制门禁**，它能直接复现旧实现的误判（只补 `AGENTS.md` 不补 `role.sh` 时，旧判据报 alive、新判据报 missing）。

---

## 六、硬性守则（沿用施工方案 §八）

1. **全程中文**（代码注释、日志、文档、提示词），沿用现有中文命名风格
2. **增量不重写**：动 `蜂群驾驶.py` 前先备份；保持函数签名与日志风格；新逻辑优先进新模块。每次改完 `python3 -m py_compile` 必须过
3. **实测优先**：任何"沙箱应该支持X"的假设，先用 tiny `h.exec` 验证再依赖
4. **新角色先冒烟**：新提示词先用 `Hive().leg()` 单腿试跑看真实输出，再接进驱动
5. **长跑解耦**：必须 `start_new_session=True`，起跑后确认 PPID=1
6. **密钥纪律**：`hive_sdk.py` 的端点与 KEY 不改动、不外传、不写进任何新文件或日志
7. **不越权改蓝图**：流程/门/军规只能因实测不可行才偏离，且在施工日志写明"蓝图偏离：原因+替代"
8. **每工单闭环**：实现→验收→写 `施工日志.md` 一条→再下一单
9. **只在三种情况停下报告**：端点/网关连续不可用、需要花钱或触碰本机之外的系统、真题输入缺失

---

## 七、还没做完的事

1. **卡点5 的护栏修复未经生产验证**——代码已改、干跑已过，但当前进程是旧代码
2. **端到端验收尚未全绿**：运行还没到 S6，`端到端验收.py` 现在跑会报大量"缺失"（正常）
3. **复盘报告未产出**：`复盘报告.json` 要等 S6 完成后由复盘官生成，其规则库修改建议需择要落实
4. **问2 降级放行需人工复核**：不是流水线缺陷，是解读师判定结果质量不达标。终稿前应确认该问结论是否可用
5. **我提过但未实施的两条优化**（用户当时只要求做前两条）：
   - **门的判据应分层**：现在"真错"（数值不符）与"口径差异"（q 与 d 非同一物理量）都算 FAIL，逼流水线走最贵路径。应区分**阻塞性缺陷**与**需说明的差异**，后者留痕放行即可。仲裁台账的"已解释"软路径就是这个思路，值得推广到所有门
   - **`真题测试/` 目录治理**：已堆积多轮状态/日志/pid/归档，靠命名约定维持，建议按 `运行ID` 分目录隔离

---

## 八、给你的三句话

1. **先读 `施工日志.md` 的 20 条记录**，尤其 M4-4.4 / 4.6 / 4.7 三条——那是三次"修完又发现修错了"的完整推理链，能让你避开我踩过的坑
2. **这套系统的兜底做得比较厚**（升格→降级放行→应急出版），好处是不会死锁，**坏处是会掩盖真实缺陷**。看到"降级放行"不要当成正常，要去查为什么
3. **对沙箱的任何假设都先用一条 tiny `h.exec` 验证**。我这几轮最有价值的进展全部来自实测（boot_id 可读、account 不控路由、find 支持 -newermt、tar+base64 能一次拉回增量），而最大的失误来自"我以为这样改就对了"

---

## 附：操盘 skill（20260910 起）

开炉/续跑/监督/停跑/切换/收尾一律走 `.claude/skills/paper-foundry/`（Claude Code 内 `/paper-foundry`，Codex 内 `$paper-foundry`；`scripts/安装.sh` 把它符号链接到 `~/.claude/skills` 与 `~/.codex/skills`）。
铁律、监督清单、档位与角色分档、交付报告模板、病根登记格式都在它的 `references/`；脚本用法见 `SKILL.md`。本文档与 `施工日志.md`、`病根台账.md` 仍是接手必读，skill 只是把「怎么操作」固化成脚本。

