# 数模论文全沙箱流水线 · 架构规划 v3「驻留式蜂巢」

> 2026-08-25 · 三轮沙箱实测 + 双参考项目（Mrite / MM-Agent NeurIPS 2025）
> v3 核心升级：从"每条腿一次 HTTP 调用"的接力（v2），升级为**驻留式蜂巢**——整个多智能体工作流作为守护进程在沙箱容器内连续运转，容器内自主派生 codex 角色进程与直连模型网关，外部只做播种/轮询/收割与多模态终审。用户已确认可解除 CF 层时间限制（纳入事实）。

---

## 〇、结论速览

1. **驻留模式全部实证**：后台作业跨 60s/240s 墙运行 260 秒完成；钉账号容器 10+ 分钟零漂移；**容器内直连模型网关（Responses API）200 OK**；**容器内自主驱动 codex CLI 成功**（`-c model_providers` 指向网关，完整 agentic 循环）。
2. **蜂巢架构**：一次播种（输入+运行时包+daemon），容器内 daemon 按 DAG 派生角色（codex 进程 = 重思考岗，Responses 直连 = 轻推理岗，subprocess = 计算/编译），无 HTTP 往返、无重复上传、5 CPU 真并行、状态全程驻留。
3. **质量军备**（比 v2 更强的根本）：方案锦标赛（每问 N 方案并行→评审择优融合）、对抗验证（反驳者推翻不了才进论文）、审稿收敛循环（10 维打分到平台期）、Claude 编排端多模态终审。
4. **韧性双保险**：daemon 持续写 checkpoint + 外部增量收割 → 容器死亡从最近 checkpoint 重播种续跑；CF 限制解除后长调用亦可，v2 HTTP 接力降级为 fallback 层。
5. 继承不变：Mrite 论文规范与 LaTeX 模板（已做沙箱适配版）、MM-Agent 的 HMML 方法库 / LLM 任务 DAG / MMBench 8 维打分卡 / 111 道真题回测集。

---

## 一、三轮实测证据链（全部亲测）

### 环境层（第一轮）
- **完整 TeX Live 2025**：国赛模板双遍 xelatex 16s、0 错误；Fandol 中文字体齐全；**Ghostscript** 可 PDF→PNG（视觉审稿）
- Python 3.14 另有 statsmodels/sympy/networkx/pypdf/chardet/yaml（文档未列）；sklearn/openpyxl/python-docx 用 cp314 manylinux wheel 离线安装，`--no-index --no-deps --no-compile --target` **仅 1 秒**
- matplotlib 中文完美；网络全封仅模型网关；20MB payload 可过；适配模板 `沙箱验证/format-sandbox.cls` 已验证

### 契约层（第二轮）
- 当前 CF 层：exec 墙 ≈60s、tasks 墙 ≈240s、`async=1` 无效、文档超时上限是虚的 → **用户已确认可在 CF 侧解除时间限制**（纳入事实；SDK 仍内置墙探测自适应，作保险）
- `?account=a1/a2` 钉账号（双泳道）；容器复用是机会缓存
- 三棒 HTTP 接力实证：数据体检师(194s)→建模师-设计(62s)→执行器(12s)，交接继承有效，RF MAPE 21.1% 打败基线 26.2%，出版级中文图

### 驻留层（第三轮，v3 关键）
| 实验 | 结果 |
|---|---|
| nohup 后台作业 260s（跨两堵墙） | ✅ 心跳连续、正常完成，同容器 |
| 钉账号容器稳定性 | ✅ a2 容器跨 10+ 次调用、10+ 分钟 hostname 不变 |
| 容器内直连模型网关 | ✅ `$OPENAI_BASE_URL/responses`（Responses API 格式）200，正常应答 |
| 容器内自主驱动 codex | ✅ `codex exec -c model_provider=gw -c model_providers.gw.base_url="https://proxy:18080/v1" -c model_providers.gw.env_key="OPENAI_API_KEY" -c model_providers.gw.wire_api="responses" -m gpt-5.6-sol --skip-git-repo-check --dangerously-bypass-approvals-and-sandbox`——完整 agentic 循环成功（写文件/diff/汇报，12.9k tokens） |
| 注意 | codex 裸调用会走 wss://api.openai.com 被代理 403，必须带上述 `-c` 覆盖 |

---

## 二、v3 架构：驻留式蜂巢（Resident Hive）

```
┌───────────────────────────────────────────────────────────────┐
│ 编排端（Claude Code / 未来 App）——蜂巢之外，只做四件事           │
│  ① 播种 seed：输入+运行时包+daemon.py+角色提示词 → nohup 启动    │
│  ② 轮询 poll：tiny exec 读 status.json，增量收割 checkpoint 产物 │
│  ③ 终审 review：阶段边界多模态审核（渲染页图/数据图），           │
│     写 编辑指令.json 注入容器，daemon 消费后继续                 │
│  ④ 复活 resume：容器死亡→从最近 checkpoint 重播种续跑            │
├───────────────────────────────────────────────────────────────┤
│ 蜂巢内部（钉住的容器，a1 主线）                                  │
│  daemon.py：确定性 Python 状态机，执行任务 DAG，全程驻留          │
│   ├─ 重思考岗 = codex exec 子进程（角色提示词注入，可并行多开）    │
│   │   规划师/方法军师/建模师/撰稿师/审稿员……                     │
│   ├─ 轻推理岗 = Responses API 直连（打分/校验/润色等短调用）      │
│   ├─ 计算/编译 = subprocess：python 求解、xelatex、gs 渲染        │
│   └─ checkpoint：每步产物+状态落盘，随时可收割可复活              │
│  优势：无 HTTP 往返、无重复上传、5 CPU 真并行、上下文以文件驻留    │
├───────────────────────────────────────────────────────────────┤
│ 对抗泳道（a2，第二容器）                                        │
│  独立复算校验 / 审稿团 / 或并行第二篇；与主线互为红蓝对抗          │
└───────────────────────────────────────────────────────────────┘
```

### 三条铁律（v2 继承，蜂巢内同样成立）
1. **设计与执行分离**：codex 角色只思考写码/写文，重计算与编译由 daemon 用 subprocess 确定性执行（可观测、可重试）。
2. **文件接力即上下文**：`交接/` 目录是血液；每角色输入=上游交接文件，输出=下游依赖+checkpoint。
3. **正确性协议一票否决**：量纲/基线/交叉印证/防泄漏/灵敏度五项，不过即打回，不许带病进论文。

### 专职智能体名册（蜂巢内派生）
| 角色 | 实现 | 职责 |
|---|---|---|
| 读题官 | gs 渲染 + 编排端视觉（终审时）/ codex 多模态 | 题目 PDF → 结构化题面 |
| 数据体检师 | codex | 全量体检 → 数据档案.json（已实证角色化有效） |
| Planner 总规划师 | codex | 求解计划 + 任务 DAG + 页数预算；每阶段可修订（DAG 解析失败降级串行，MM-Agent 同款） |
| 方法军师 | codex + HMML | 逐问方法检索选型，供论文"算法选择"节真实依据 |
| 建模师-设计 ×N | codex 并行 | 公式推导（含 actor-critic 自批评）+ 求解脚本（只写不跑） |
| 执行器 | daemon subprocess | 跑脚本+检查点；失败 stderr 回传建模师修复（debugger 循环 ≤5 轮） |
| 解读师 | codex | 结果解读 + 正确性协议核验 |
| 绘图师 | codex 设计 + subprocess 执行 | 统一视觉系统、补图成图、图质自检 |
| 撰稿师 ×N | codex 章节并行 | 按 Mrite 规范写 tex，注入真实数值与真实文献 |
| 排版师 | daemon subprocess | xelatex×2 + 日志结构化 + gs 全页渲染 |
| 审稿团 | codex/Responses 并行 ×10 维 | MMBench 8 维 + 版式视觉 + 摘要质量，`<reason>/<score>` |
| 反驳者（对抗泳道） | codex@a2 | 试图推翻关键结果：复算、边界、泄漏、量纲 |
| 总编 | 编排端 Claude | 汇总裁决、编辑指令、终审出版 |

### 质量军备四件套（v3 新增，"更牛逼论文"的根本）
1. **方案锦标赛**：每问由 3 个建模师并行出独立方案（HMML 不同方法族切入）→ 审稿团按 MMBench 维度打分 → 择优为主、融合亮点；论文的"算法对比"节直接引用锦标赛真实评分与实测指标（彻底告别拍脑袋"优良中差"）。
2. **对抗验证**：a2 泳道反驳者对每个关键数值结论发起攻击（独立复算/换切分/边界压力），未被推翻才准进论文；攻击记录转化为论文"模型检验"节素材。
3. **审稿收敛循环**：10 维打分 → 总编转定向修订指令 → 修订重编译 → 再评，直到连续两轮无显著提升（分数平台期）才出版，替代 v2 的固定 2 轮。
4. **Claude 终审**：编排端多模态逐页看渲染效果，亲自复核摘要与核心公式推导——编排端模型的质量优势花在刀刃上。

---

## 三、六阶段流水线（蜂巢版）

| 阶段 | 蜂巢内动作 | 编排端动作 | 验收 |
|---|---|---|---|
| P0 播种 | daemon 自检：能力快照/wheels/编译冒烟 | seed 一次上传全部 | 三绿 |
| P1 读题 | 读题官+数据体检师 | — | 题面结构化、陷阱清单 |
| P2 规划 | Planner 出 DAG；方法军师选型 | 审 DAG checkpoint | DAG 合法、主备方法+验证方案 |
| P3 求解 | 按 DAG 并行锦标赛：设计×3→执行→解读→择优；绘图师补图 | 收割中期结果 | 正确性协议全过 |
| P4 撰稿 | 撰稿师并行 → 排版师首编 | — | 编译 0 错误 |
| P5 审稿 | 渲染全页 → 审稿团 10 维 + a2 对抗 → 修订循环至收敛 | **多模态终审 + 编辑指令注入** | 各维 ≥7 且平台期、对抗未破防 |
| P6 出版 | 摘要 1 页 aux 验证、页数预算、cite 对应、Overfull=0、切思源字体终榨 | 收割终稿 | 全项通过 |

预计端到端 40-70 分钟（蜂巢内并行 + 无 HTTP 往返开销）。

---

## 四、里程碑（v3）

| 里程碑 | 内容 | 工作量 |
|---|---|---|
| M1 蜂巢骨架 | `daemon.py` 状态机（DAG 执行/角色派生/checkpoint）+ `hive_sdk.py`（seed/poll/harvest/resume/墙探测保险）+ 10 角色提示词 + HMML 打包 + codex 网关配置封装 | 1.5 天 |
| M2 单题端到端 | 零售题 P0-P6，第一篇完整论文 PDF | 1 天 |
| M3 质量军备 | 锦标赛 + 对抗泳道 + 审稿收敛 + Claude 终审协议 + 去模板化 | 1-2 天 |
| M4 韧性 | checkpoint 复活续跑、双泳道调度、状态面板（artifact 实时监控页，可选） | 1 天 |
| M5 回测 | MMBench 选 3-5 道真题回测，按打分卡出对比报告 | 1-2 天 |

## 五、风险与对策

| 风险 | 对策 |
|---|---|
| 容器中途被回收 | checkpoint 持续落盘 + 轮询增量收割 → 重播种续跑（设计为常态而非事故） |
| CF 限制解除前的墙 | 蜂巢天然免疫（后台进程不依赖请求生命周期）；SDK 保留墙探测 |
| codex 网关配置失效/版本变化 | 封装成单一配置函数；P0 冒烟含一次最小 codex 调用 |
| 沙箱 LLM 质量波动 | 锦标赛+对抗多路对冲；关键产物 Claude 复核 |
| 交接 JSON 崩 | schema 校验+重试+编排端降级修复 |
| HMML 许可（CC BY-NC 4.0） | 训练/教学注明出处；商业化自建中文方法库 |
| 同质化/AI 检测 | 去模板化+变体注入（流程图自由绘制、摘要句式多套、对比表用实测数据） |

## 六、合规提醒

各赛事 AI 政策差异大（美赛要求 AI 使用报告页；国赛限制代写）。定位训练、教学与模拟赛工具；正式参赛前对照当年规则自查。

---

## 附一：参考项目结论（保留备查）

**Mrite**：论文规范九类约束全盘继承给撰稿师/排版师（14 章结构、摘要 ≤900 字且 aux 验证 1 页、longtable 列宽公式 1.04−0.04N、公式密度、编译修复循环）；其九短板（本地绑定/单遍无闭环/结果零验证/模板化/不看产物/算法库空/串行/上下文粗放/文献可编造）由 v3 逐一化解。

**MM-Agent（NeurIPS 2025，2025 美赛 Finalist 前 2%）**：搬运 HMML（12 域 98 方法节点，162KB）、分层方法检索（父 0.5+子 0.5 加权 top-6）、actor-critic 建模循环、coding debugger 循环（≤5 次）、LLM 任务 DAG+降级串行、MMBench 8 维打分卡与 111 题回测集。其短板（无 LaTeX 闭环、不看图不看版面、单模型串行、无正确性协议）正是 v3 的超越点。

## 附二：验证材料清单

- `沙箱验证/format-sandbox.cls`、`沙箱编译_论文.pdf`、`中文测试图.png`、`wheels/`（8 个 cp314 离线轮）
- `沙箱验证/接力实验/`（三棒接力全产物：数据档案/建模方案/脚本/结果/预测对比图）
- `参考项目/`（MM-Agent 关键源码摘录 + HMML方法库.md）
- codex 网关调用配置（见本文第一章表格，已验证可用）

---

## 增补 · v3.1 审查与强化（2026-08-26，蜂群首次自主运行期间完成）

### MM-Agent（2025）过时点审查

| 借鉴项 | 2026 视角判定 | 处置 |
|---|---|---|
| HMML 静态方法库+分层检索 | **过时为主**：为 gpt-4o 补知识的拐杖，对前沿模型反而窄化选型；98 节点无 2024-26 实践 | 降级为"覆盖面清单"；新建 `方法卡片库.md`（共形区间/CQR、MinT 层级调和、HistGBDT 分位损失、分组感知验证、变点检查、离线求解器要点、"何时不用深度学习"），冲突以卡片库为准 |
| actor-critic 公式自批评 | **弱**：同模型自审有自我同意偏置 | 已被执行落地的五项正确性协议+独立解读师取代；保留轻量自批评 |
| coding debugger 循环 | 仍有效 | 已实现（run_script 修复循环） |
| LLM 任务 DAG + 降级串行 | 仍有效 | Planner 依赖字段实现；后续可加独立问题并行 |
| MMBench 单评委 8 维打分 | **过时**：单评委单次打分噪声大、无锚定，轮间"平台期"判断不可靠 | 升级为**双评委独立面板**（A 全面 + B 专挑硬伤），均分裁决、指令合并；后续加范文锚定 |

### 本流水线自查发现的短板（均已修）

1. **溯源只查标注不查真伪** → 审计.py 新增"溯源核验"：逐行打开 `% src` 指向的 JSON 比对数值（含百分比/万元缩放容差），存疑清单直接进审稿输入——逻辑幻觉的硬闸门。
2. **容器轮换会杀死运行中的蜂群** → 驾驶脚本新增 `ensure_hive()`：每波前探活，丢失即从本地镜像自动重播种（基建+输入+全部状态）；P3/P3.5 checkpoint 加密到结果 JSON、脚本与图片。
3. **文献防编造靠嘱咐** → 新建 `文献卡片库.md`：27 条逐一核对的经典（含 GB/T 7714 成串），撰稿只准从库选。
4. **区间语义曾出错（零售题审稿抓到）** → 立"区间协议"：任何区间必须声明构造方法（共形/经验分位）+ 经验覆盖率验证，写进规划师与建模师军规。

### 统筹路线图

- **R1（已完成）**：上述四修 + 双评委面板 + 方法/文献卡片库。
- **R2（真题跑完后）**：总编复盘找问题 → 修订流水线 → 对抗泳道(a2)接入驾驶（独立复算头条数字）→ 同题重跑 A/B 对比验证提升。
- **R3**：范文锚定——取 2-3 篇公开国奖论文提炼特征（不复制文本），作为评委面板的 9-10 分锚与撰稿风格参照。
- **R4**：多题回测台（MMBench 题库 + 我们的 10 维评分 + 轮间成对比较），量化流水线版本迭代收益。
