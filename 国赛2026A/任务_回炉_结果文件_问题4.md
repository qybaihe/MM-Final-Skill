你负责问题4的【结果文件回炉】——不是重新建模，模型、口径、结果声明与所有已定数值一律不变，只修 求解/问题4/结果/result4.xlsx 的写出层。
契约要求把完整结果按 数据/附件3/result4.xlsx 模板写出，题面硬约束「所有结果保留四位小数」。核对脚本对当前文件的结论（✗ 为不合格）：
    ✓ result4.xlsx[Sheet1]：首行距离网格 0…2/0.1（21 列）对；药材表面列 有
    ✓ result4.xlsx[Sheet1]：A 列时间 3054 行，步长应为 60 s：对（首格 '时间(s)'）
    ✓ result4.xlsx[Sheet1]：数值 44119/67188 非空；表面列空格 0 行；空格非尾部越界段的行 0（契约裁定：越界格留空、表面列必填）
    ✗ result4.xlsx[Sheet1]：存在超过四位小数的值
    △ result4.xlsx[Sheet1]：角格 A1='时间(s)' 与模板 '时间\\到药材中心的距离' 不同（提示，不计不合格）
契约相关裁定摘录：
  - 药材会因水分流失发生尺寸变化。请根据附件2，确定药材的烘干时长（相关经验公式见附录4）。在论文中按表6的格式给出每隔6 h、到药材中心距离每隔0.5 cm的水分浓度，并将药材内部水分浓度每隔60 s时间、0.1 cm距离的完整结果保存到文件result4.xlsx中（模板文件见附件3）。
  - result4.xlsx：Sheet1水分浓度完整结果，含药材表面列
  - 在result4.xlsx的Sheet1中保存从60 s起至结束每隔60 s、从中心起每隔0.1 cm且位于当时药材内的水分浓度。
  - 在result4.xlsx中保留“药材表面”列，逐个输出时刻填写当时实际半径位置的水分浓度。
  - 在result4.xlsx中保存实际烘干结束时刻的完整水分浓度行，包含有效0.1 cm间隔位置及表面；非60 s规则时刻补行，重合时不重复。
  - result4.xlsx的Sheet1从60 s起每60 s报告水分浓度；普通列为实际距离cm、间隔0.1 cm，覆盖当时有效位置，另保留“药材表面”列。越界固定距离格裁定留空而非填0；表面与规则点重合时两处值一致。非规则结束时刻补完整行，重合不重复。
修法要求：
1. 所有存入单元格的数值——含 A 列时间（包括契约要求补的实际结束行的时刻）与全部含水率/温度——先 round(值, 4) 再写入单元格；只设 number_format="0.0000" 不算保留四位小数。
2. 角格 A1 沿用模板文字「时间\到药材中心的距离」；工作表名、首行距离网格（0…2 每 0.1 cm，result4 末列「药材表面」）、A 列时间步、行数、末行（实际结束时刻）、越界格留空规则全部保持契约裁定不变。
3. 优先从已保存的未舍入数表（如 求解/问题4/结果/分钟数表_未舍入.json、检查点文件）重建工作簿；若必须重跑求解脚本，运行控制在 12 分钟内。
4. 改动必须落在 求解/问题4/求解_问题4.py 的导出函数里（附录要收录可复现源码），并另写一个最小重建脚本 求解/问题4/重建结果文件.py 供本次直接运行；本次就运行它把 result4.xlsx 覆盖为合格版。
5. 写完用 openpyxl 读回自检：行数、列数、A 列步长、末行时刻、每个数值 round(v,4)==v、角格文字，把自检结果追加进 求解/问题4/结果/导出核验.json 的键「回炉自检」。
6. 交接/实验记录.json 追加一条（类别 流程事件）；交接/建模笔记_问题4.md 末尾追加一节「结果文件回炉」说明改了什么、数值未变。
7. 不许手填/手改任何数值，不许改 交接/结果声明_问题4.json 的数值。
完成写 日志/回炉_结果文件_问题4.done（首行 PASS 或 FAIL，其后一行写自检摘要）。