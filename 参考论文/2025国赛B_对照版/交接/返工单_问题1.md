# 问题1返工单

1. **真实附件表观光学厚度乘积扫描边界错误（正确性）**
   - 定位：`求解/问题1/求解_问题1.py` 的 `apparent_dq`。
   - 证据：当前扫描周期为 `1.5—80.0 cm^-1`，附件2选出的周期正好为 `80.0` 边界；红队结果的两个 `q` 值对应约 `254.65—256.62 cm^-1` 周期，说明当前上限截断了主峰。
   - 必须修改：将周期或 (q) 的搜索范围改为有依据且覆盖真实谱主周期的范围；增加边界命中检查，边界命中时不得直接发布；对去趋势与正弦/余弦投影采用明确一致的残差化口径。
   - 验收：重新运行后核对 `求解/问题1/结果/模型验证.json` 的 `真实附件诊断.周期_每厘米` 不再撞搜索边界，并给出两谱周期、`表观光学厚度乘积_dq_微米` 及搜索范围；结果应与独立实现的约19.56微米量级一致或解释差异。

