# 审-1-22 图版交接

当前仅完成定向改脚本和静态核验；未运行绘图、未统一重编PDF、未作整页图评。旧PNG和旧PDF不是本轮产物。

1. 执行 `bash 求解/成图回炉G5_2.sh`，仅重绘图5、10、11、12、13；总预算750秒。图8不动，图11—13不改变曲线、残差符号、散点与统计口径。
2. 批次在首次实际绘制前绑定数值文件字体和数学字体，检查ASCII负号与U+2212字形；缺字或替代日志、文字越界/重叠、框内压线、数据不同版均按失败处理。失败状态不能用旧PNG、更新时间或进程退出码替代。
3. 五图全部成功后才生成 `交接/G5返工图2_编译交接.json`，由编译腿统一重编 `论文/论文.pdf`；本绘图批次不修改正文、版式或编译PDF。
4. 编译后执行 `G5_FIGURE_BATCH=2 python3 求解/核验G5图版嵌入.py`，核对本批五图的PNG散列与PDF内嵌像素；不带批次变量的旧命令仍对应第1批。
5. 图评须按最终PDF整页核对图5的对象、单位、三路线72点与24/16/10例分组，图10的每次乘u、相位差arg(u)与求和后取强度，以及文字/图例不遮挡、正文页数≤20。静态核验和像素匹配均不替代这一步。

图5由独立右侧统计栏改为分组标签内置均值和均值短线，扩大左侧留白；图10由底部两折表改为侧边证据列，并将级数展开与闭式求和上下对齐。两图仍为8.0×5.74英寸，不通过加大图幅或改正文挤压版面。
