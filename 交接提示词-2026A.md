# 交接提示词 · 2026 国赛 A 题「药材的烘干问题」整炉跑通

> 给下一个线程（Claude Code 操盘 + codex 角色腿）。目标只有一个：用 paper-foundry 流水线把 A 题从开炉跑到终态，交出论文与全部交付物。写于 2026-09-11，输入目录与环境已备好，**尚未开炉**。
> 读完这一份 + `.claude/skills/paper-foundry/SKILL.md` 就能开工；遇到具体事再翻 `references/`。

---

## 一、你接手的是什么（一分钟）
- **项目根** `/Users/bytedance/数模自动化`（git 仓库，本地 `main` 全历史；GitHub 公开镜像 `qybaihe/MM-Final-Skill` 只收孤儿快照，见 §六.6）。每条命令都显式 `cd /Users/bytedance/数模自动化`——工具的工作目录会漂。
- **流水线**：`流水线/蜂群驾驶.py` 在本机跑 S0 读题 → S1 锦标赛 → S2 建模/红队 → S3 图证 → S4 撰稿 → S5 审稿场/美化 → G5 出版门 → S6 终审出版复盘；17 条角色腿走本机 `codex exec`（模型 gpt-6-astra，端点/密钥只在 `~/.codex/config.toml`，**不读不改不打印**）。
- **操盘 skill** `/paper-foundry`（核心 `.claude/skills/paper-foundry`，发布版 `发布/paper-foundry-{claude,codex}`）。项目根 `.agents/skills/paper-foundry/` 是一份旧的本地副本（已 .gitignore，不是权威版本，别按它改）。开工前通读 `SKILL.md`，再读 `references/铁律.md`（20 条）、`监督清单.md`、`交付报告模板.md`。
- **上一炉可对照**：2025 B 题对照版 `真题测试/交付报告_对照版.md`（26.7 h / 411 腿 / 66 页，章评 7.53 / 面板 7.43）。别动 `真题测试/`（1.5 GB 镜像仍在，用户未说归档）。
- **纪律**（用户定的，不商量）：全程中文；增量不重写；改 `流水线/*.py` 必过 `python3 -m py_compile` + `验证.sh`；不替腿写论文、不手工解题、不手填结果文件；不越权改蓝图/阈值/门判据/守卫尺（用户决定）；每张工单闭环写 `施工日志.md` 一条；病根进 `病根台账.md`（证据/归属/修法/检测/状态，先修链路再改提示词，不闭环不删）；正在跑的 bash 脚本不能原地编辑；kill 前确认 pid 是腿（`公共.sh` 的 `是腿`）；停干净再碰 `状态.json`/台账/镜像。

## 二、本次任务
**题目**：2026 高教社杯 A 题「药材的烘干问题」（`国赛2026A/输入/A题.pdf`，4 页）。圆柱药材长 25 cm、半径 2 cm，初温 28 ℃、初始水分 2.55 kg/kg；热风烘干分预热平衡与恒温干燥两阶段。四问：
| 问 | 要求 | 论文里的表 | 结果文件（附件3 模板） |
|---|---|---|---|
| 1 | 预热平衡阶段的温度与水分浓度模型（烘房温湿边界见附件1，参数见附录2） | 表1/表2：100…1800 s × 到中心 0/0.5/1/1.5/2 cm | `result1.xlsx`：1800 s 内每 1 s × 每 0.1 cm，工作表「温度」「水分浓度」 |
| 2 | 整个烘干过程模型（经验公式统一用附录3） | 表3/表4：0.5…3.0 h × 同上距离 | `result2.xlsx`：3 h 内每 1 s × 每 0.1 cm，两个工作表 |
| 3 | 各处水分 <0.15 kg/kg 所需烘干时间（h） | 表5：每 6 h 直到烘干结束 | `result3.xlsx`：每 60 s × 每 0.1 cm |
| 4 | 考虑失水收缩（半径随时间见附件2，公式见附录4）的烘干时长 | 表6：每 6 h，末列「药材表面」 | `result4.xlsx`：每 60 s × 每 0.1 cm + 药材表面 |
所有结果保留四位小数。附件1 = 242 行「时间/温度/水分浓度」；附件2 = 146 行「时间/半径」；模板 A 列为时间（s），首行为到中心距离（cm）。
**格式**（`输入/论文格式规范2026.txt`，已随题目播进 `题目/`）：摘要独立 1 页；正文不超过 30 页、不要目录；附录必须含全部可运行源代码；任何处不得出现身份信息。

**输入**：`国赛2026A/输入/`（A题.pdf、论文格式规范2026.txt、附件1.xlsx、附件2.xlsx、附件3/result1–4.xlsx）。驱动会播成 `题目/A题.pdf`、`题目/论文格式规范2026.txt`、`数据/附件1.xlsx`、`数据/附件2.xlsx`、`数据/附件3/result*.xlsx`（R54 已支持子目录）。整个 `国赛2026A/输入/` 与成品/镜像/台账都在 `.gitignore` 里。
**产出**：`国赛2026A/成品_A题`；父目录 `国赛2026A/` 里会出现 `状态.json`、`蜂巢镜像/`、`台账/`、`驾驶_A题.out`。
**交付物**：论文 PDF + 全套 tex + 图 + 求解脚本 + **填好的 result1–4.xlsx** + 交付报告 + 跑后指标 + 病根/施工日志闭环 + GitHub 快照。

## 三、开炉前（按顺序，都做）
1. `git status` 干净；不干净先提交基线。
2. `bash .claude/skills/paper-foundry/scripts/体检.sh 国赛2026A/成品_A题` → 硬失败 0。（2026-09-11 已跑：硬失败 0；提醒「另有 claude 引擎」「shell python3 无 numpy」都正常。）
3. `bash .claude/skills/paper-foundry/scripts/验证.sh --干跑=全链,中断续跑,级联,门升格,韧性,S5续跑,G5图路,G5算条,G5页数,引擎` → 验证通过。（09-11 已过。）
4. §七 的决定项向用户问一次；没回音按默认。
5. 磁盘 ≥ 5 GB（现在 21 GB）。

## 四、开炉与监督
```bash
cd /Users/bytedance/数模自动化
bash .claude/skills/paper-foundry/scripts/启动.sh 国赛2026A/输入 国赛2026A/成品_A题 --档位=深度 --并发=4 --effort=xhigh --引擎=codex
bash .claude/skills/paper-foundry/scripts/状态.sh 国赛2026A/成品_A题          # 20 秒后一次，之后每 15–30 分钟
bash .claude/skills/paper-foundry/scripts/看守.sh 国赛2026A/成品_A题 --秒 900   # 或 Monitor 盯 WATCH_RE（公共.sh）
```
- 深度档 = 与对照跑同口径（摘要变体 5、审稿 4 轮、全员 xhigh），对照跑实测 26.7 h（含 4 次链路修复后的重做）；`--档位=标准`（变体 3、审稿 3 轮、角色分档）会明显更短，但没有实测数。并发只用 4（6 整晚 429）。
- **S0 结束立刻核对 `蜂巢镜像/交接/题面契约.json`**：问题数 4；依赖 DAG 合理（问 3 依赖问 2 的模型，问 4 在问 3 之上加收缩）；交付物里有 result1–4.xlsx 及其网格（每 1 s/60 s × 每 0.1 cm、四位小数）；格式约束进了契约（摘要 1 页、附录含代码）。缺了 → 停跑，给读题官补契约（任务文本点名缺项），再 `--resume`。这是本题与上一炉最大的不同：**结果文件是评奖硬件**。
- S2 每问过门后看 `求解/问题N/结果/`：建模师应把 result 文件按模板写出（同名、同工作表名）；缺就在 G2 返工单里点名，不许放到收尾再补。
- 日志出现 `!!`、`rc=142`、`孤儿`、`Traceback`、`腿死亡`、`降级放行`、`熔断`、`结构守卫`、`页数守卫`、`转路`：按 `references/监督清单.md` 处置。插手四步：停（`停跑.sh`）→ 改 → 验（`验证.sh`）→ 续（`启动.sh … --resume`）；要等某个边界再上线用 `切换.sh --标记 … --钩子 …`（钩子样板 `scripts/钩子样板.py --父目录 国赛2026A --演练`）。
- 每次插手：施工日志一条；换个地方还会再犯的原因进病根台账。

## 五、这条链路最近一炉踩过的坑（提前知道）
- **429**：并发闸自动降 1，别手动重启。
- **成图脚本首跑 rc=1**：脚本覆盖 MPLCONFIGDIR / 预算 750 s / 旧图冒充（P8 已收紧）；修脚本腿会接，看 `修脚本_` 行。
- **G5 结构守卫「源码清单减少」**与「≤80 行入口片段」指令打架 → 撰稿师改动被回退两次（故障手册 E）。守卫尺是用户的，不改。
- **S6 终审整改没有图路和复核**（R53 只修了截断）：终审必改里有图类条目时，回执写「需转图」——要人工派绘图师或钩子，不然直接收割。
- **换版**：重算腿必须产出 `交接/换版清单_问题N.json`（P3），猎手按清单 grep 旧值；看到「换版清单 缺」就派。
- **续跑**：`--resume` 读 `状态.json`；S5 有轮级断点；角色文件只在开炉/续跑时同步进镜像（改了角色文件要停→续才生效）。
- **Claude 引擎**：本机 `claude -p` 未登录/端点 401，探针不通——本炉用 codex，不要切。
- **磁盘**：一炉镜像 1.5 GB + 日志 1 GB。

## 六、收尾（终态「E=0 Overfull=0 页数=… 总腿数=…」之后）
1. `状态.sh` 确认 `阶段=完成`、驱动已退出、活腿 0。
2. **结果文件门**：`流水线/运行时/venv/bin/python3 流水线/验证/核对结果模板.py 国赛2026A/成品_A题 国赛2026A/输入/附件3` → 四个文件全部 ✓（工作表名、0…2/0.1 网格 21 列、result4 末列药材表面、时间步 1 s/60 s、非空、四位小数）。不合格 → 给建模师派「回炉·重算」口径的任务补写，**不许手填**；补完重跑核对。
3. `python3 .claude/skills/paper-foundry/scripts/跑后指标.py 国赛2026A/成品_A题 --markdown > 国赛2026A/跑后指标_A题.md`（回流账 + P 检测；日志默认取 `国赛2026A/驾驶_A题.out`）。
4. 交付报告 `国赛2026A/交付报告_A题.md`（按 `references/交付报告模板.md`；门史、章评/面板/美观、页数、回流账、台账终态、搁置与降级放行逐条、需人工关注、复盘官建议摘录；对照 2025 B 的 7.53 / 7.43 / 66 页）。
5. 病根台账状态闭环；施工日志收尾条；memory 更新（`~/.claude/projects/-Users-bytedance------/memory/shumo-automation-project.md`）。
6. 提交本地 `main`（不含 输入/成品/镜像/台账）；推 GitHub 快照：`git fetch origin && CID=$(git commit-tree 'HEAD^{tree}' -p origin/main -m "<说明>") && git branch -f github-main "$CID" && git -c credential.helper='!gh auth git-credential' push origin github-main:main`。**不要 `git push origin main`**（本地历史里有沙箱时代的密钥字面量）。成品要不要裁剪进 `参考论文/2026国赛A_首炉/`（同 2025 版做法：去候选/页图/>5 MB），问用户。
7. 给用户报告：成品路径、指标、门史、result 核对结果、需人工关注、决定项。

## 七、待用户决定（开炉前问一次；不答按默认）
| # | 事项 | 默认 | 备注 |
|---|---|---|---|
| 1 | 正文页数上限 | **保持 20 页**（2026 规范 ≤30，20 合规且更紧） | 想放宽到 30：`流水线/运行时/门检.py:537`、`流水线/蜂群驾驶.py:1821/1826`、`流水线/角色/规划师.md:43/55` 三处 + 验证；属阈值改动，需用户点头 |
| 2 | 档位 | 深度 | 标准省 6–10 h，质量略降 |
| 3 | 引擎 | codex | claude 未就绪 |
| 4 | 跑完成品是否入库 | 不入 | 入则裁剪进 `参考论文/` 并推快照 |
| 5 | 阈值 6.9/8.5、A1→A4→A5→A3→A2 参数、P 条款 | 沿用 | 见 `真题测试/交付报告_对照版.md` §决定 |

## 八、三句话
1. 你是操盘手不是作者：论文和 result 文件都由腿写，你只保证链路把它们写出来、验出来。
2. 停干净再改，改完必验，验过再续；每次插手留一条日志、一条病根。
3. 终态不是结束——result 文件门、跑后指标、交付报告、快照推送做完才算交付。
